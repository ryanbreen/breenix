
[unstable]
bindeps = true

# The custom AArch64 soft-float target causes build-std's `core` to register a
# future-incompatibility report for its internally allowed NEON declarations.
# Do not print Cargo's aggregate end-of-build reminder; compiler diagnostics
# remain unchanged and visible to warning scans.
[future-incompat-report]
frequency = "never"
