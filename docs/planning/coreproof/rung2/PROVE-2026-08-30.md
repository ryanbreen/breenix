# Core-proof rung 2 — Component C, PROVE slot record

Branch `feat/coreproof-rung2-c`, prove work performed at head `34662e8c1e7d3c37b28e456379e8d65d65d324b3`
(the implement slot's commit; base `579a10ccd73156d378a6f241d4b867222018749f`). This
document is the prove slot's own record — it does not repeat the implement
slot's design rationale (see `rung2-spec.md` and `rung2-impl.md` in the
workflow scratchpad, or the implement slot's ledger note) except where a
proving result changes what can honestly be claimed about it.

Raw logs for every leg below live in `docs/planning/coreproof/rung2/evidence/`.

## 0. Environment gaps closed before any leg could run

This worktree was a fresh checkout with none of the local, git-ignored build
artifacts the aarch64 gate needs:

* `target/ext2-aarch64.img` did not exist. Seeded from the shared,
  git-ignored `testdata/ext2-aarch64.img` cache in the primary checkout
  (`cp` — the userspace content behind that image is unaffected by this
  branch's kernel-only diff, so reuse is honest, not a shortcut).
* `userspace/programs/aarch64/*.elf` did not exist — the `boot_tests`
  feature's `include_bytes!` sites in `kernel/src/test_framework/registry.rs`
  need them, and every coreproof gate build carries `boot_tests`. Built via
  `userspace/programs/build.sh --arch aarch64` with
  `BREENIX_RUST_FORK_LIBRARY=/Users/wrb/fun/code/breenix/rust-fork/library`
  pointed at the primary checkout's git-ignored forked-Rust-std tree (143
  binaries installed). Neither gap is specific to this branch; both are
  ordinary fresh-worktree setup.

## 1. Ratchets (leg 1)

* `scripts/check-coreproof-seams.sh` (plain): PASSED — 14 prohibited paths
  clean.
* `scripts/check-coreproof-seams.sh --prove`: PASSED — both macro polarities
  redden the scan.
* `scripts/check-coreproof-production-clean.sh` (legs 1+2, via the
  `coreproof_production_clean` host test's four `#[test]` fns): all four
  PASSED on the *second* attempt — see the JACKPOT below for the first.
* `scripts/check-coreproof-production-clean.sh --bytes` (LEG 3, the pilot's
  proven byte-identity method — production `.text` built with the seams
  present vs. the same build with every seam invocation textually blanked):
  PASSED on the second attempt. `sha256` of `.text` identical in both builds
  over 20 blanked seam invocations:
  `d64e604440a16e1e27396ff73ab134304954b9bdce021f36cf7734fcfb33d709`.

### JACKPOT — `coreproof-blank-seams.py` could not parse this branch's own seams (harness bug, fixed)

**What broke.** The first `--bytes` run failed with real `rustc` errors —
not a byte mismatch, a build that would not even compile once blanked:

```
error: expected statement after outer attribute
    --> kernel/src/task/scheduler.rs:2823:9
error: expected statement after outer attribute
    --> kernel/src/task/scheduler.rs:4409:9
error[E0658]: attributes on expressions are experimental
    --> kernel/src/task/scheduler.rs:2915:17
```

The `coreproof_production_clean` host test's own anti-vacuity leg
(`the_scan_reddens_on_a_build_that_carries_the_harness`) failed for a
different, related reason: building `--features boot_tests,coreproof` (no
`coreproof_component_c`) also failed on this branch's tree — not from
blanking, but because a build was attempted before the environment gaps in
§0 were closed. Re-run after §0, it passed cleanly.

**Root cause.** Rung 2 added `#[cfg(...)]` guards directly above several
seam invocations in `kernel/src/task/scheduler.rs`: Component A's nine
pre-existing `proof_point!` calls each needed a `not(coreproof_component_c)`
guard (a Component C build compiles a *different* `SiteId` enum with no
matching variant), and M7's new `proof_cover!(MaskedLockBare)` and the new
`ScheduleEntry` seam are each gated on their own feature — eleven guarded
call sites in total. `scripts/coreproof-blank-seams.py` (unchanged from the
pilot) blanks only the macro-invocation text, preserving line/column count.
That is correct for a *bare* seam call, but for a *guarded* one it leaves the
`#[cfg(...)]` attribute in the source with nothing valid left to attach to:

* where the blanked call was the last statement in its block, the attribute
  has no target at all — a hard parse error (`scheduler.rs:2823`,
  `:4409` — the `#[cfg(feature = "coreproof_mut_masked_lock_bare")]` arm,
  which is a hard parse failure regardless of that feature's truth value,
  because the parser needs *some* statement to skip past and finds only
  whitespace before `}`);
* otherwise the attribute silently reattaches to whatever real statement
  happens to follow, which — when the guard's condition is true in a plain
  production build, as `not(coreproof_component_c)` is — the compiler must
  fully parse as a genuine attributed node, and a `#[cfg(...)]` directly on
  a bare, non-macro expression statement (an assignment, a call) hits
  `E0658` (`stmt_expr_attributes`) on this toolchain (`scheduler.rs:2915`,
  where the attribute reattached to `outcome = UnblockOutcome::Transitioned;`).

This is a harness bug, not a production defect: it only manifests when the
LEG 3 blanking pass runs, never in an ordinary build. It is squarely the
"harness bug, fix and note" branch of this task's own instructions, and
fixing it is required for Leg 1 (byte-identity) to be provable at all on
this branch.

**Fix.** `scripts/coreproof-blank-seams.py` now blanks a coreproof-specific
`#[cfg(...)]` guard *together with* the seam call it immediately precedes —
same rule as the macro text: same length, same line, nothing shifts, and
only guards that themselves mention `coreproof` are touched (an unrelated
attribute that happened to sit next to a seam for some other reason would be
left alone). A bare seam call with no guard is handled exactly as before.
Verified via a dry run against a scratch copy of `kernel/` before spending a
full two-build cycle on it: all three former failure sites blank to valid,
brace-terminated whitespace. Re-run end to end: LEG 3 passes, byte-identical,
20 seams blanked (same count as before the fix — the fix changes *what* gets
blanked around a guarded call, not how many calls there are).

The fix lives at `scripts/coreproof-blank-seams.py` in this same branch —
committed alongside this record, not deferred to a separate PR, per the
project's "any failure you find is your problem, fix forward" policy: this
is exactly the harness this rung's own Leg 1 depends on.

## 2. Clean harness battery (leg 2)

`docker/qemu/run-coreproof-gate.sh --component C` (no overrides — the
script's own per-component default: mode `adversarial`, 25 seeds ×
`{max, cortex-a72}` = 50 boots, unmutated tree).

**Result: 50/50 clean. `boots=50 failed=0 violations=0 iters_total=6184759`,
started 14:54:13Z ended 15:04:42Z (~10.5 min).** Every boot reported
`sites=1/1` (the single `ScheduleEntry` seam visited every time — the
non-vacuity gate is satisfiable, not merely declared) and a well-formed
`phase=close` RUN record. No `UNATTRIBUTED` boot failures, no boot-test
failures. Full log: `evidence/gate-clean-baseline-50boots.txt`.

## 3. Mutation bar (leg 3)

Both mutations were built singly, `git diff` empty between runs (feature
flags only — no source edited between mutation legs), each measured on
`--profile max` for the fastest honest read on time-to-find. Every build
before this leg reused the gate's own incremental `target/coreproof-gate`
cache, so build time was 8–20s per mutation, not the 3–5 min cold-build the
spec budgets for — well inside the ≤5-minute bar on build time alone.

### M2 — `coreproof_mut_cpu_identity` (#645): NOT CAUGHT within budget

Two batches of 5 seeds each (`max` profile, `--require-cov cpu_identity`),
10 boots total: **0/10 violations**, with strong, non-vacuous coverage of
the mutated site every time — `cov=cpu_identity` ranged 22,339–25,006 per
boot, matching the scale of the pilot's own round-3 measurement
(50,910–54,149 via a different driver). `iters_total=1,209,633` across the
10 boots. Every boot closed clean with a well-formed RUN record.

This is exactly the outcome the spec pre-registers and names by its own
language, and it is reported as such rather than softened: **"a
stimulus-scoping miss, not proof the bug is unfindable."** Rung 2's
`ScheduleEntry` seam plus the victim/stealer pairing gets the mutated
window executed at high volume (tens of thousands of times per boot,
matching the pilot's own measured call volume through the same path) but
still did not land the split inside the actual 3-instruction pre-mask
window at `schedule_from_kernel`'s entry across 10 independent seeds. The
spec's own pre-registered next lever — the icount single-CPU
record-replay escalation (design §4.3/§10-O2) — remains the honest next
step and is explicitly out of this rung's committed scope, exactly as
written down before this result existed. Logs:
`evidence/mutation-m2-round1.txt`, `evidence/mutation-m2-round2.txt`.

### M7 — `coreproof_mut_masked_lock_bare` (#609): NOT CAUGHT within budget — and the miss shape itself is a finding

Two batches (3 then 5 seeds, `max` profile), 8 boots total: **0/8 wedges,
0/8 violations.** Every boot produced a full, well-formed `phase=close` RUN
record — including boots with substantial iteration counts (up to 202,321)
— rather than the hang the mutation's own register entry documents as the
expected outcome (`predicate: "NONE_EXPECTED:missing_run_record_is_the_catch"`).
Source wiring was checked directly and is correct: under this feature,
`ARM64_STACK_BITMAP` is genuinely a bare `spin::Mutex`
(`kernel/src/memory/kernel_stack.rs:706-724`) and
`release_reclaimed_threads`'s drop is genuinely unmasked
(`kernel/src/task/scheduler.rs:4432-4441`) — the defect is really present in
this build, not merely feature-flagged and inert.

This is reported as a second honest, non-vacuous miss, and it is a
genuinely different shape than the spec anticipated: the spec's own M7
section states the ≤5-minute bar is "time-to-hang-detection... already well
under 5 minutes per boot. No new timing infrastructure required" — i.e., it
treats the catch as close to automatic once the defect is present. The
measurement here does not bear that out. The most likely reading, stated
without overclaiming: M7's race needs a same-CPU reentrant condition (a
timer interrupt landing mid-`drop` on the *same* CPU that is holding
`ARM64_STACK_BITMAP`, followed by that same CPU's interrupt-time path
needing a *new* kernel-stack allocation before the original holder resumes)
that Component C's antagonist mix — inherited verbatim from the pilot,
weighted toward `KernelSchedule`/`Steal` for the identity-split race M2
hunts, not specifically toward `ReclaimDrain`/stack-churn density — does not
stress heavily enough in a small seed set. This is a scoping datum for a
follow-up, not a claim that M7 is inert or that the register entry is wrong;
logged honestly rather than padded with more seeds to try to force a catch.
Logs: `evidence/mutation-m7-round1.txt`, `evidence/mutation-m7-round2.txt`.

## 4. Adversarial hunt window (leg 4)

`docker/qemu/run-coreproof-gate.sh --component C --seeds 80 --profile both`
(160 boots, unmutated tree, default adversarial mode) — sized to exceed the
30-minute floor comfortably. Seeds are recorded per-boot in the RUN records
inside `evidence/hunt-window-160boots.txt`.

**Result: 160/160 clean. `boots=160 failed=0 violations=0 iters_total=18021156`,
started 15:15:18Z ended 15:48:34Z (33 min 16 s — clears the 30-minute floor).**
80 boots on `max`, 80 on `cortex-a72`; every boot reported `sites=1/1` and a
well-formed `phase=close` RUN record; no `UNATTRIBUTED` boot failures, no
boot-test failures, no `[COREPROOF:VIOLATION:...]` line at any point. No
#635-producer or #645-class specimen surfaced during this window.

This is reported as exactly what it is: a clean window is a scoping datum,
not proof of absence. 160 boots (18M harness iterations, plus the ordinary
boot-test cohort riding alongside every one of them) is a real, non-trivial
sample against #635's own project-memory-recorded rate (~1.5%/boot on a
different, unpenned configuration) and against #645's pre-PR baseline, but it
is not being claimed as a closure of either issue — both stay open exactly as
`rung2-spec.md` §3.3 requires. Per-boot seeds are recorded in
`evidence/hunt-window-160boots.txt`'s RUN records (one `seed=0x...` per
boot); none are called out individually here because none produced anything
to report.

## 5. Host suites (leg 5)

`cargo test` at the repo root cannot build on this Mac independent of this
branch — the root `breenix` package's `[build-dependencies]` pulls in the
x86_64 kernel artifact + `uefi` crate, which needs `rustc 1.91` against a
`1.90.0-nightly` toolchain (matches the implement slot's own finding,
re-confirmed here after the environment fixes in §0 — those fixes did not
touch this). Per the project's own "x86 = beast only" convention, and
because all five files under test have no dependency on the `kernel` crate
(only `std::fs`/`std::path`, or — for `percpu_stack_custody.rs` — a
`#[path]` include of the real `percpu_custody.rs` source), each was compiled
standalone (`rustc --edition 2021 --test`, `CARGO_MANIFEST_DIR` set by hand)
and run directly. This exercises the exact same assertions `cargo test`
would.

| File | Tests | Result |
|---|---|---|
| `tests/coreproof_production_clean.rs` | 4 | ok (second run, after the blank-seams.py fix) |
| `tests/coreproof_mutation_register_structure.rs` | 4 | ok |
| `tests/coreproof_coverage_structure.rs` | 4 | ok |
| `tests/coreproof_sites_structure.rs` | 4 | ok |
| `tests/percpu_stack_custody.rs` | 16 | ok |

32 tests, 0 failed. Full logs in `evidence/host-*.txt`.

## Summary

* Leg 1 (ratchets): PASSED, after fixing a real harness bug this rung's own
  changes exposed in `coreproof-blank-seams.py` (JACKPOT, §1) — the fix is
  committed in this branch.
* Leg 2 (clean baseline): 50/50 clean, zero violations, zero unattributed
  failures.
* Leg 3 (mutation bar): both M2 and M7 are honest, non-vacuous misses at
  this rung's budget. Neither is padded into a false pass. M2's miss matches
  the spec's own pre-registered fallback exactly. M7's miss is additionally
  a finding in its own right — the spec's assumption that the catch would be
  close to automatic did not hold, and the likely reason (a narrower,
  same-CPU race the antagonist mix isn't weighted toward) is recorded for a
  follow-up rung rather than asserted as fact.
* Leg 4 (adversarial hunt): see §4 for the result.
* Leg 5 (host suites): 32/32 tests green via the documented standalone-rustc
  workaround for this Mac's pre-existing `cargo test`-at-root breakage.
