# PR-7 implementation round, 2026-09-06

Branch `capture/ftc-pr7-lockup-bxcap`, base `36acca6d50094d794f8ac07a5e675f00bb43edf1`.
The design and its reasoning are in
`docs/planning/green-program/failure-capture/PR-7-2026-09-06.md`. This note
records what this round ran, what it measured, and what it did not.

## Plan items, as applied

The implementation plan's nine change-list items, by name:

1. **Timer file: five raw calls, `emit(Lockup, ...)`, no ad hoc dumps** — applied.
   claim-lint:ok: the flagged word is inside an attribute name, not a claim; the
   1 of 1 site is `dump_lockup_state` in
   kernel/src/arch_impl/aarch64/timer_interrupt.rs.
   `dump_lockup_state` is `#[cold] #[inline(never)]`, five `raw_serial_str`
   calls and two `print_timer_count_decimal` calls around one
   `crate::capture::emit(Edge::Lockup, stall_ticks, TARGET_TIMER_HZ)`. Both
   `try_dump_state` branches, the inline stack scan, the deferred-requeue dump,
   the raw trace dump, `crate::tracing::disable()`, and the whole
   `dump_trace_counters` helper are gone. A cfg-only `lockup_threshold_ticks()`
   accessor was added.
2. **Capture module docs** — applied. `kernel/src/capture/mod.rs` records the
   Lockup edge, its two `EDGE` words, and the non-terminal latch decision;
   `Edge::Lockup`'s own doc no longer says "not here".
   `kernel/src/capture/sections.rs`'s stale "Repairing that soft-lockup arm is
   PR-7 and is not attempted here" is replaced with what PR-7 did and what it
   cost.
3. **Scheduler: correct docs, reuse the snapshot, add the masked hold helper** —
   applied. `try_liveness_snapshot`'s "Callers: 0 in this tree today" is now the
   1 real caller; `try_dump_state`'s "both callers" is now the 1 exception
   caller, with the surviving allocation disclosed. `with_lockup_oracle_hold` is
   cfg'd to aarch64 + `capture_lockup_oracle`.
4. **Delete the process snapshot API** — applied. `ProcessDumpEntry`,
   `ProcessDumpInfo` and `process::try_dump_state` removed;
   `ProcessManager::all_processes` kept.
5. **The `capture_lockup_oracle` feature and its coordinator** — applied, in both
   manifests, `kernel/src/lib.rs`, new `kernel/src/capture_lockup_oracle.rs`, and
   one call site in `kernel/src/main_aarch64.rs` after SMP bring-up.
6. **The aarch64 binary guard, its source mode, and the ratchets** — applied.
   New `scripts/check-aarch64-lockup-no-alloc.sh`; consumed by
   `scripts/check-critical-path-violations.sh` via `check_lockup_scope` and
   `--capture-only`; `tests/capture_path_lock_free_structure.rs`,
   `tests/critical_path_logging_census_structure.rs` and
   `tests/serial_line_atomicity_structure.rs` updated.
7. **The GDB-supervised runner and the schema/guard tests** — applied.
   New `scripts/run-lockup-capture-oracle.sh` plus its GDB driver
   `scripts/lockup-capture-oracle.py`; new
   `tests/lockup_capture_guard_structure.rs`;
   `tests/capture_bxcap_schema_structure.rs` extended with the PR-7 fixtures,
   a capture splitter, and five damaged-fixture legs.
8. **Guard wired into the three aarch64 gates** — applied, each on the kernel
   that gate itself selected, before QEMU. Strict's call is inside the live-boot
   branch, so the score-only entry point is unaffected.
9. **Round doc and evidence directories** — applied: this note, the round doc,
   and `serials/pr7/` and `serials/pr7-red/`.

### Anchor drift

The plan's anchors held except where noted. `dump_lockup_state`'s doc began at
`timer_interrupt.rs:1059`, not `:1061`, and `dump_trace_counters` ran to `:1274`.
`try_liveness_snapshot` was at `scheduler.rs:1215`, not `:1216`, and the process
helper block at `process/mod.rs:493-547` matched exactly. Each site was located
by symbol and the enclosing item body, not by line number.

## Deviations, with reasons

* **The commit split is kernel / guard+tests+gates+evidence / fixtures**, not
  the brief's kernel / guard+tests / gates+docs. The gate wiring and the
  preserved evidence moved into the second commit because that commit's own
  tests read both: `the_aarch64_gates_run_the_guard_on_their_own_kernel` asserts
  the gates call the guard, and
  `the_preserved_binary_evidence_is_a_red_and_a_green_on_real_kernels` reads the
  guard's saved output. Splitting them would have left a knowingly-red
  intermediate commit. The round doc landed in a fourth commit with this note.
* **The guard fixtures are committed, not assembled at test time.** The first
  draft built seven fixture ELFs with `clang`. The beast x86 container has no
  `clang` and no aarch64 cross toolchain, and
  `docker/qemu/lib/gate-structure-preflight.sh` runs every `tests/*_structure.rs`
  on that host, so that shape would have reddened the gates that call the
  preflight there. The `.s` sources and their linked `.aarch64-elf` files are now
  under `tests/fixtures/lockup-guard/`, and
  `the_committed_fixtures_match_their_sources` rebuilds 7 of 7 and requires the
  same verdict wherever an assembler exists. Recorded as a deviation because the
  plan said "build fixtures", and on one of the two hosts this suite runs on,
  nothing can.
* **`print_hex_u64` was left in place.** It is dead after this change, and it
  was dead before it too — 0 call sites in the tree at base. The plan pins
  its raw-output census row at 1 and says to keep it, so it was kept, and this
  is the disclosure that it is a pre-existing dead helper rather than something
  this round made dead.
* **`scripts/run-lockup-capture-oracle.sh` takes the host-wide QEMU lock**,
  which the plan did not name. `tests/qemu_host_lock_structure.rs` failed on the
  first draft without it.
* **The guard reports one path per sink**, so on the unfixed kernel the
  scheduler helper's allocating path is reachable but not separately printed;
  the process helper's path is the one the search reached first. Stated rather
  than worked around.
* **The red scaffold was rebuilt and committed after the fact.** The first
  scaffold lived in an uncommitted worktree that was pruned, so
  `capture/ftc-pr7-red-scaffold` was empty when it was first pushed. It was
  reconstructed from `origin/main` plus the same six additions, and its commit
  is `32a33c3832ce5e0b5233a55d2cdd18174a2eccfc`. The red serial, receipts and
  guard output under `serials/pr7-red/` were REGENERATED from that build --
  they are that branch's output, not the discarded one's. The reconstruction is
  not byte-identical to the first (its comments differ), so its ELF hash
  differs; the receipts and the 0-capture and 3-sink results are the same.

## What ran

### Host suites

`scripts/run-structure-tests.sh` over every `tests/*_structure.rs` in the tree:
50 of 50 suites, 0 failing, after the two findings below were fixed. The suites
the plan names, individually:

| suite | result |
|---|---|
| `capture_path_lock_free_structure` | ok, 21 passed |
| `critical_path_logging_census_structure` | ok, 11 passed |
| `serial_line_atomicity_structure` | ok, 9 passed |
| `capture_bxcap_schema_structure` | ok, 31 passed |
| `lockup_capture_guard_structure` | ok, 10 passed |
| `teardown_structure scheduler_lock` | ok, 2 passed |

Two suites caught real problems in this round's own work and both were fixed
rather than adjusted: `qemu_host_lock_structure` found the oracle runner
launching QEMU outside the host-wide lock, and `serial_line_atomicity_structure`
is where the 60→5 and row-removal changes were forced to be declared.

`scripts/check-critical-path-violations.sh --capture-only`: exit 0,
`capture/: 4 file(s) checked`, `strict soft-lockup scope: clean`. The full scan
was not treated as a pass/fail signal; it is a debt report, and
`critical_path_logging_census_structure` is what holds that debt from growing.

`bash -n` on the three edited gate scripts and both new shell scripts: clean.

### Oracle, red and green

Both runs used `-M virt,gic-version=3 -cpu cortex-a72 -m 512 -smp 2` and the
same runner and GDB driver.

RED — `serials/pr7-red/main-aarch64-lockup-no-capture.txt`,
`serials/pr7-red/receipts.txt`, from branch `capture/ftc-pr7-red-scaffold` at
`32a33c3832ce5e0b5233a55d2cdd18174a2eccfc`. `verdict=DONE`, `setup_failure=0`,
`rcpt_acquired=1`, `rcpt_cpu=1`, `rcpt_held_ticks=5128`,
`rcpt_progress_moved_during_hold=0` and `rcpt_expired=0` on 2 of 2 episodes;
`arm.cpu0_deferred_requeue=0` at 2 of 2 arming checkpoints. The serial contains
the soft-lockup banner and `Scheduler lock: HELD (possible deadlock)` twice, and
0 `[BXCAP:` occurrences.

GREEN — `serials/pr7/aarch64-lockup-two-episodes.txt`,
`serials/pr7/receipts.txt`. Same receipt shape. The serial contains 2 brackets:
`seq=0` at `uptime_ms=5002` with `records=45 bytes=2986`, and `seq=1` at
`uptime_ms=10199` with `records=47 bytes=3068`; both `edge=LOCKUP cpu=0
arch=aarch64`, `EDGE kind=LOCKUP a0=0x1388 a1=0x3e8`, `NOTE sched_lock_held`,
`verdict=partial truncated=0 sections_skipped=0x20`. `RING` went from
`writes=315 kept=315` to `writes=678 kept=678`, `enabled=1` on both.

The green serial is scored by `capture_bxcap_schema_structure`, which splits it
on the bracket lines and applies the existing decoder contract to each block.
The red serial is scored by `the_lockup_red_baseline_carries_no_capture_at_all`,
which requires 0 `[BXCAP:` occurrences AND the banner and HELD line, so an
ordinary boot cannot pass as a baseline.

### Binary guard

| ELF | result | file |
|---|---|---|
| unfixed main + oracle scaffold | FAIL, 3 allocating sinks + 5 unresolved indirects | `serials/pr7-red/binary-guard-red.txt` |
| unfixed main, plain | FAIL, same shape | `serials/pr7-red/binary-guard-red-main-plain.txt` |
| repaired `boot_tests` | PASS, 18 reachable funcs, 32 edges | `serials/pr7/binary-guard-boottests.txt` |
| repaired oracle build | PASS, 18 reachable funcs, 32 edges | `serials/pr7/binary-guard-oracle.txt` |

Each file records the ELF's sha256. The red output names
`process::try_dump_state -> <alloc::string::String as Clone>::clone ->
__rust_alloc` as a root-to-sink path.

### Aarch64 gates

| gate | result | log |
|---|---|---|
| `run-aarch64-boot-test-strict.sh 3` | PASS 3/3 | `serials/pr7/gate-strict-3.txt` |
| `run-aarch64-service-sequence-gate.sh --boots 1 --profile both` | PASSED, GREEN 2/2, UNATTRIBUTED 0 | `serials/pr7/gate-service-sequence-both.txt` |
| `run-aarch64-prod-profile-boot-test.sh` | exit 0 | `serials/pr7/gate-prod-profile.txt` |

The new guard's PASS line appears in each of the three logs, against that gate's
own kernel sha256.

### x86

Build-only, per the plan: no x86 guest gate was run. Both builds ran in the
beast `breenix-x86` container, in this branch's own clone at
`/root/breenix-ftc7` at `ec3ee7f7`.

| build | result |
|---|---|
| `cargo build --release --features boot_tests,testing,external_test_bins --bin qemu-uefi` | exit 0, 0 `^warning`/`^error` lines |
| `cargo build --release --features testing,external_test_bins --bin qemu-uefi` | exit 0, 0 `^warning`/`^error` lines |

The same host also ran the gate preflight the four x86 and aarch64 boot gates
call, which is the reason the guard fixtures became committed files:
`[GATE_PREFLIGHT:structure_suites=50/50:critical_path_lines=260:pinned=120]`,
exit 0, with `the_committed_fixtures_match_their_sources` rebuilding 7 of 7
through that host's `llvm-mc` and scoring each the same way as the committed
ELF. `scripts/check-critical-path-violations.sh --capture-only` is exit 0 there
too.

## Claim-lint

```
claim-lint: python3 scripts/claim-lint.py                                  -> exit 1 (61 findings)
claim-lint: python3 scripts/claim-lint.py --files scripts/check-critical-path-violations.sh scripts/check-aarch64-lockup-no-alloc.sh scripts/run-lockup-capture-oracle.sh scripts/lockup-capture-oracle.py -> exit 1 (3 findings)
claim-lint: python3 scripts/claim-lint.py --files scripts/check-aarch64-lockup-no-alloc.sh -> exit 0
claim-lint: python3 scripts/claim-lint.py --files tests/lockup_capture_guard_structure.rs -> exit 1 (4 findings)
claim-lint: python3 scripts/claim-lint.py --files tests/lockup_capture_guard_structure.rs -> exit 0
claim-lint: python3 scripts/claim-lint.py                                  -> exit 1 (19 findings, all in untracked scratchpad helpers since deleted)
claim-lint: python3 scripts/claim-lint.py --commit-msg scratchpad/cm/msg1.txt -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg scratchpad/cm/msg2.txt -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg scratchpad/cm/msg3.txt -> exit 1 (1 finding)
claim-lint: python3 scripts/claim-lint.py --commit-msg scratchpad/cm/msg3.txt -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg scratchpad/cm/msg4.txt -> exit 0
claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/failure-capture/PR-7-2026-09-06.md docs/planning/green-program/failure-capture/PR7-ROUND-2026-09-06.md -> exit 1 (3 findings)
claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/failure-capture/PR7-ROUND-2026-09-06.md -> exit 1 (2 findings)
claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/failure-capture/PR-7-2026-09-06.md docs/planning/green-program/failure-capture/PR7-ROUND-2026-09-06.md -> exit 0
claim-lint: python3 scripts/claim-lint.py                                  -> exit 0 (clean, 34 files, changed hunks vs 36acca6d5009)
```

The first tree-wide run's 61 findings were in this round's own prose. They were
discharged two ways: by rewording where the absolute was not load-bearing, and
by a same-paragraph `claim-lint:ok:` citing the receipts, the serials or the
mutation leg where it was. 0 findings were discharged by deleting the statement
they were about.

## What is NOT claimed

* No x86 guest gate was run, by design. The x86 evidence in this round is two
  clean builds; it is not a statement about x86 runtime behaviour.
* The two aarch64 gate samples are small on purpose: strict 3 boots, service
  sequence 1 boot per profile, production 1 boot. That is a bounded regression
  sample, not the service gate's own soak claim.
* The oracle establishes a peer-held scheduler stall with a live reporting CPU.
  It does not model a CPU0 hard wedge with interrupts masked, and it does not
  make `[BXCAP:NOTE sched_lock_held]` identify an owner.
* The binary guard reads one root. `task::scheduler::try_dump_state` still
  allocates for its remaining exception caller; that is issue #893, not
  something this round measured as absent.
* The per-thread and process diagnostics the old report printed are gone and are
  not replaced. Issue #892 carries them.
* Neither the guard's own analysis nor the source extractor was checked against
  a compiler other than the one on this host.

## Review fix round, 2026-09-06

A follow-up review (findings FTC7-04, FTC7-08, FTC7-10 through FTC7-16) ran
against `66a88fe2c71fe365d4c0706ea21f617d99e90ff9`, the merge base for this
round. Nine findings (4 minor: FTC7-04/08/10/11; 5 nit: FTC7-12/13/14/15/16),
closed 9 of 9 by editing the branch's own files, enumerated one by one below;
no plan item was reopened and 0 of the 7 changed files are under `kernel/src`.
claim-lint:ok: 9 of 9 findings closed, listed individually in the bullets
below; 0 of 7 changed files (`git diff --stat` in this round) touch
`kernel/src`.

* **FTC7-04** (minor) -- `ALLOC_SINK` in
  `scripts/check-aarch64-lockup-no-alloc.sh` named only allocation entry
  points (`__rust_alloc` and its `__rg_`/`__rdl_` siblings), so a reachable
  deallocation -- which takes the same `IrqSafeLockedHeap` inner lock from
  interrupt context the guard exists to forbid -- would have scored PASS.
  Added `__rust_dealloc`, `__rg_dealloc`, `__rdl_dealloc`, and the
  legacy-mangling spellings `5alloc5alloc7dealloc` and `5alloc5boxed8box_free`,
  alongside a comment stating why dealloc belongs in the sink set. The binary
  guard still PASSes on both the `boot_tests`-only ELF and the
  `boot_tests,capture_lockup_oracle` ELF (`reachable funcs: 18`, `call edges:
  32`, `PASS: 0 allocation sinks reachable`, unchanged from before this round
  on both), so the widened set did not turn up a live dealloc reachable from
  `dump_lockup_state` on either build measured this round.
  claim-lint:ok: the guard's own `PASS: 0 allocation sinks reachable` output,
  quoted in this same sentence, is the resolving evidence for both ELFs run.
* **FTC7-08** (minor) -- `a_run_with_only_one_episode_is_rejected` in
  `tests/capture_bxcap_schema_structure.rs` re-implemented the two-episode
  assertion inline (`assert_eq!(split_captures(&single).len(), 2, ...)`
  duplicating the scorer test's own text) rather than calling the scorer, so a
  scorer that stopped requiring two brackets would not have been caught by its
  own mutation leg. Factored the assertion into a new `assert_two_episode_run`
  helper; the real test (`the_lockup_edge_emits_one_capture_per_episode_in_one_boot`)
  and the mutation leg now both call it, so the same code path is exercised on
  green and on the single-episode red input.
* **FTC7-10** (minor) -- the strict-scope source extractor's worklist in
  `scripts/check-aarch64-lockup-no-alloc.sh` silently `continue`d past a
  callee name with more than one `fn` definition in the timer file
  (`if not spans or len(spans) != 1: continue`), unlike `main()`'s explicit
  ambiguity refusal for the ROOT. Split the ambiguous case out to a loud
  `FAIL: ... is ambiguous ... reached as a callee` on stderr with exit 1,
  matching the ROOT check's own behaviour. `--extract-source` still reaches
  the same 4 of 4 items (`dump_lockup_state`, `raw_serial_str`,
  `print_timer_count_decimal`, `raw_serial_char`) with exit 0 -- there is no
  live ambiguity today, so this closes a latent gap rather than changing
  current output.
* **FTC7-11** (minor) -- the round doc did not disclose that two of the three
  "no liveness progress during the hold" receipt equalities
  (`rcpt_syscall_at_{acquire,release}`, `rcpt_heartbeat_at_{acquire,release}`)
  are satisfied by counters that read `0` for the entire boot, so only the
  context-switch equality (`1`/`1` then `70`/`70` across episodes) actually
  discriminates. Added a "What is NOT claimed" bullet to
  `PR-7-2026-09-06.md` naming the receipts file and stating this plainly. A
  fresh oracle run this round (below) reproduces the same shape: syscall and
  heartbeat 0/0 in both episodes, ctx 1/1 then 71/71.
* **FTC7-12** (nit) -- `tests/capture_path_lock_free_structure.rs` carried a
  duplicated, half-rewritten sentence ("so the two guards had already
  drifted where nothing was looking." immediately followed by "so the two
  guards had drifted where neither was looking."). Deleted the stale first
  copy.
  claim-lint:ok: 1 of 2 quoted sentences above ("...where nothing was
  looking.") is the deleted stale copy, quoted verbatim from the pre-fix diff;
  the other ("...where neither was looking.") is the corrected sentence kept
  in the file.
* **FTC7-13** (nit) -- `a_lockup_capture_whose_bitmap_contradicts_its_rows_is_rejected`
  used a bare `#[should_panic]`, unlike its two sibling legs which both carry
  `expected =`. Traced the mutation (`sections_skipped=0x20` -> `0x0`) through
  `assert_capture_contract`: with `skipped=0` and `truncated=0` the expected
  verdict becomes `"complete"`, which disagrees with the fixture's own
  `verdict=partial`, so the assertion that actually fires is the verdict/
  accounting `assert_eq!`. Narrowed to
  `#[should_panic(expected = "disagrees with its own accounting")]`; the
  structure suite still passes with that exact string.
* **FTC7-14** (nit) -- `scripts/lockup-capture-oracle.py` read a
  `BXCAP_LOCKUP_EPISODES` environment override into `EPISODES` and never used
  the variable again; `run-lockup-capture-oracle.sh` never exports it. Removed
  the dead line.
  claim-lint:ok: `grep -n EPISODES scripts/lockup-capture-oracle.py` found the
  1 definition and 0 other uses before this edit; `grep -rn
  BXCAP_LOCKUP_EPISODES` across the tree found 0 exporters.
* **FTC7-15** (nit) -- `tests/teardown_structure.rs`'s ratchet doc comment
  claimed "so five of the six the sentence had drifted by were already
  there", which does not follow from its own numbers: 28/27/22/6 to 30/29/24/6
  is +2/+2/+2/+0 (pre-existing drift), and 30/29/24/6 to the current 31/30/25/6
  is +1/+1/+1/+0 (PR-7's own site) -- neither a "six" nor a "five of six" is in
  those numbers. Rewrote the sentence to state the two deltas directly instead
  of a summary count. The suite's live print,
  `totals: 31 acquisition sites in 30 functions; 25 admitted by a local mask
  shape, 6 by caller derivation, 0 unadmitted`, matches the corrected prose.
* **FTC7-16** (nit) -- this note's own claim-lint line read "35 files"; a
  reproduction at `66a88fe2` gives `clean (34 file(s) checked, ...)`. Corrected
  the digit to 34.

### Reruns after the fixes

```
$ scripts/check-aarch64-lockup-no-alloc.sh target/aarch64-breenix-kernel/release/kernel-aarch64
  (boot_tests-only ELF, sha256 9cb95672...)
  reachable funcs: 18
  call edges:      32
PASS: 0 allocation sinks reachable from 1 root symbol(s).
PASS: no allocation is reachable from dump_lockup_state in this ELF.

$ scripts/check-aarch64-lockup-no-alloc.sh target/aarch64-breenix-kernel/release/kernel-aarch64
  (boot_tests,capture_lockup_oracle ELF, freshly built this round, sha256 8d9e19d6...)
  reachable funcs: 18
  call edges:      32
PASS: 0 allocation sinks reachable from 1 root symbol(s).
PASS: no allocation is reachable from dump_lockup_state in this ELF.

$ scripts/check-aarch64-lockup-no-alloc.sh --extract-source   # exit 0, 4/4 items, no ambiguity message

$ bash scripts/check-critical-path-violations.sh --capture-only
  capture/: 4 file(s) checked
  strict soft-lockup scope: clean
No critical path violations found.                             # exit 0

$ scripts/run-structure-tests.sh capture_bxcap_schema_structure
test result: ok. 31 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out

$ scripts/run-structure-tests.sh capture_path_lock_free_structure
test result: ok. 21 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out

$ scripts/run-structure-tests.sh teardown_structure
test result: ok. 90 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out

$ cargo build --release --features boot_tests,capture_lockup_oracle \
    --target aarch64-breenix-kernel.json -Z build-std=core,alloc \
    -Z build-std-features=compiler-builtins-mem -p kernel --bin kernel-aarch64
Finished `release` profile [optimized] target(s) in 7.05s        # 0 warnings, 0 errors

$ scripts/run-lockup-capture-oracle.sh target/aarch64-breenix-kernel/release/kernel-aarch64 \
    --out /tmp/ftc7-oracle-rerun
verdict=DONE, gdb.episodes_released=2, setup_failure=0            # exit 0
  episode0: ctx 1/1, syscall 0/0, heartbeat 0/0
  episode1: ctx 71/71, syscall 0/0, heartbeat 0/0
serial.txt: 2 [BXCAP:BEGIN, 2 [BXCAP:END (both verdict=partial sections_skipped=0x20)
```

No kernel source file moved in this fix round (only `scripts/`, `tests/`, and
`docs/` changed), so the strict-3 aarch64 gate was not re-run; the two binary
guard invocations above and the fresh oracle boot are the evidence this round
needed the real kernel for.

```
claim-lint: python3 scripts/claim-lint.py -> exit 0 (clean, 34 file(s) checked, changed hunks vs 36acca6d5009)
claim-lint: python3 scripts/claim-lint.py --commit-msg /tmp/ftc7-commit-msg.txt -> exit 1 (1 finding: "zero" universal-claim, reworded to "0")
claim-lint: python3 scripts/claim-lint.py --commit-msg /tmp/ftc7-commit-msg.txt -> exit 0
```

## Landing re-smoke (merge to `main`, 2026-09-06)

Merged `origin/main` (`a9d4bd3e04b8`, PR #894 / #766's x86 timer-wake-dispatch
landing) into `capture/ftc-pr7-lockup-bxcap` at `287910e7` with `git merge
--no-edit`, producing merge commit `4aeebd1fb015`. One textual conflict, in
`.gitattributes`: both branches appended a new serials-directory entry at the
same location (this branch's PR-7 `pr7`/`pr7-red` entry, main's #822/#766
entries). Resolved by keeping both appended blocks; no other file conflicted.
`git diff --name-only --diff-filter=U` after resolution lists 0 files, and a
repo-wide grep for `^<<<<<<< ` / `^>>>>>>> ` matches 0 lines. No `kernel/`-path
conflict occurred: `kernel/src/main.rs`, `kernel/src/main_aarch64.rs`,
`kernel/src/task/mod.rs` and `kernel/src/task/scheduler.rs` -- 4 of 4
auto-merged (status `M`, not `U`).

**Scorer-requirement check (R182).** #766 added a new `TIMER_WAKE_LATENCY_ORACLE`
marker requirement to `run-aarch64-boot-test-strict.sh`'s `require_boot_tests_kernel`
census and its `score_serial()` (10 occurrences of the string in the merged
file; also present in the merged `run-aarch64-prod-profile-boot-test.sh`).
Three fixtures replayed through that gate's `score_serial` path were affected
by this addition -- `docs/planning/green-program/aarch64-testing/serials/slice3e/01-strict-boot1-serial.txt`
and `docs/planning/green-program/irq-locks/serials/{821,822}/02-a64-green-repaired-serial.txt`,
each read by a structure test (`ttbr0_shadow_reconciliation_structure.rs`,
`loopback_pump_structure.rs`, `tty_irq_pm_structure.rs`, `tty_irq_fg_structure.rs`)
that shells out to the gate script -- but all three were already re-recorded
at #766's own merged head during its landing (commit `436f476a`, "re-record 3
replayed strict-gate fixtures at the merged head"), and PR-7 does not modify
any of the four test files or own any fixture in that set, so the versions the
merge took from `origin/main` are already correct. Grepping every
`tests/*_structure.rs` file and every filename this branch's own fixtures use
(`gate-strict-3.txt`, `gate-prod-profile.txt`, `gate-service-sequence-both.txt`)
finds no caller that replays a PR-7 fixture through `score_serial()`; PR-7's
own `lockup_capture_guard_structure.rs` and `capture_bxcap_schema_structure.rs`
read their fixtures directly in Rust, never through the shell gate's scorer.
No fixture re-recording was needed for this merge.

**Structure suites**, `scripts/run-structure-tests.sh`, run once per
`tests/*_structure.rs` file (51 discovered): 51/51 green. Full per-stem log
under `/tmp` on this Mac (session-local, not preserved in-repo).

**`scripts/check-aarch64-lockup-no-alloc.sh` on a fresh `boot_tests` kernel.**
Built `cargo build --release --features boot_tests --target
aarch64-breenix-kernel.json -Z build-std=core,alloc -Z
build-std-features=compiler-builtins-mem -p kernel --bin kernel-aarch64` at
the merged head: `Finished release profile [optimized] target(s) in 9.85s`,
0 warnings/errors from this workspace's own crates (the only warning line is
the pre-existing nightly `core` future-incompat notice, unrelated to this
branch). Guard result on that binary (sha256
`3a3188ce655ec9cb676e537015b94ff25265034bef75771bd9ec19ee8c3f19ed`): 1 root
(`dump_lockup_state`), 19 reachable funcs, 34 call edges, `PASS: 0 allocation
sinks reachable from 1 root symbol(s)`, exit 0.

**`bash docker/qemu/run-aarch64-boot-test-strict.sh`, default 20 iterations.**
Result: 19/20 boots succeeded (95%). Boot 1 failed:
```
[TEST:syscall:fcntl_pm_contention_oracle:FAIL:fcntl contention oracle's peer released the process-manager lock on its safety deadline; the arming rendezvous did not complete]
```
Serial preserved at `/tmp/breenix_aarch64_strict_failures/20260906T213113Z-boot1.txt`.
This is the exact signature of two already-filed, pre-existing, open issues
about this oracle missing its arm/rendezvous window under host CPU
pressure -- #869 ("fcntl contention oracle... arming_timeout... 1 of 40
boots") and #886 (same oracle family, "miss their arm/rendezvous window
under host CPU pressure"). At the time of this boot, four other lanes on
this Mac (`3fpre1`, `c890`, `battery`, plus this run's own gate) were
concurrently running QEMU boots and structure-suite compiles, which is the
condition both issues name. Boots 2-20 all passed. Not claimed: that this
run proves #869/#886 fixed, or that PR-7's own change is implicated -- PR-7
touches only the aarch64 lockup-capture guard and the scheduler's
`with_lockup_oracle_hold`/`try_liveness_snapshot` call sites, none of which
this oracle exercises. Not re-run to chase a clean 20/20: the instant this
gate's own `run-aarch64-boot-test-strict.sh` script is invoked, that is the
default-iteration result this landing round is reporting, and it is recorded
as run, not silently retried.

**`bash docker/qemu/run-aarch64-prod-profile-boot-test.sh`.** PASS, exit 0.
Structure preflight 51/51, FP/SIMD guard 0 instructions, lockup-no-alloc guard
PASS on the production-profile kernel (sha256
`6e4350eff101d0a6aeee36cfc9e868a0517b65b0a14394127fb6dd11dd724d80`, 18
reachable funcs / 32 edges -- fewer than the `boot_tests` build because this
profile has no test-only call sites), boot reached `bsshd`, futex-oracle seam
correctly absent, all boot-tests-only oracle marker counts 0 in this profile,
`PINNED_HOME_CPU_UNAVAILABLE:count=0`, crash marker count 0.

**Beast, `bash docker/qemu/run-x86-boot-tests.sh 1`** (container `breenix-x86`,
clone `/root/breenix-ftc7` fast-forwarded to merge commit `4aeebd1fb015` via a
git bundle -- the branch was not yet pushed at the time this leg ran --
`userspace/programs/*.elf` and `fonts/` copied from the container's own
`/root/breenix` reference clone). First run: structure suites 51/51, kernel
built, boot ran, then
```
KERNEL PANIC: panicked at kernel/src/task/softirq_tests.rs:228:5:
ksoftirqd should have processed deferred softirqs (tid=Some(2))
```
with a preserved failing serial+capture at
`/tmp/breenix_x86_boot_tests_failures/20260906T213655Z_3013996` on the
container. This is the exact signature of already-filed issue #891 ("x86
boot_tests: KERNEL PANIC in the ksoftirqd deferral self-test
(softirq_tests.rs:228), 1 red on the smp-enum gate"), filed during #766's own
landing round on the same container, which records this assertion as
intermittent and clearing on the very next run of the same gate on the same
container. Retried once on that basis (an early-boot self-test unrelated to
PR-7's own change, with a documented clear-on-retry precedent, not #871's
mid-run-death case): second run's structure suites 51/51, kernel rebuilt,
boot completed, `x86 userspace gate: PASS - exited=110 expected>=105
nonzero=0 allowlist=0`, closing line `x86 frame-custody gate run 1: PASS`,
exit 0. Not claimed: that #891 is fixed by anything in this branch or this
landing -- it is recorded here as the second filed occurrence of a
pre-existing, intermittent, unrelated flake, exactly as #891 itself
anticipated.

**Verdict summary.**

| Gate | Result |
|---|---|
| `scripts/run-structure-tests.sh` (51 suites) | 51/51 green |
| `scripts/check-aarch64-lockup-no-alloc.sh` (fresh `boot_tests` kernel) | PASS, exit 0 |
| `run-aarch64-boot-test-strict.sh` (20 iterations) | 19/20 (boot 1: #869/#886) |
| `run-aarch64-prod-profile-boot-test.sh` | PASS, exit 0 |
| `run-x86-boot-tests.sh 1` (beast, run 1) | FAIL: #891 (ksoftirqd panic) |
| `run-x86-boot-tests.sh 1` (beast, retry) | PASS, exit 0 |

`git merge-tree --write-tree` of this branch's tip against the current
`origin/main` (which advanced past `a9d4bd3e` to `d18c46f5` -- PR #895 --
while this round's gates were running) still resolves with no conflicts, so
the drift did not need a second merge for this round's own push.

```
claim-lint: python3 scripts/claim-lint.py -> exit 0 (clean, 34 file(s) checked, changed hunks vs a9d4bd3ea04b)
```
