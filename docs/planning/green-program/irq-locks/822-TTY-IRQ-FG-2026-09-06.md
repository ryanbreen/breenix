# #822 — the TTY interrupt path stops touching the console's `foreground_pgrp`

Branch `fix/822-tty-irq-no-fg-block`, off `origin/main` at `783a6a53`
(the #821 merge). R157 small-PR mode: one defect, one repair, one oracle, one
ratchet file.

## The lock, and who holds it unmasked

The console `TtyDevice`'s `foreground_pgrp: Mutex<Option<u64>>`
(`kernel/src/tty/driver.rs`) is a plain `spin::Mutex`. It performs no mask
operation of its own, so each of the 5 acquisitions of it in `driver.rs` runs
with interrupts unmasked, on both architectures. The thread-side reachers:

| reached from | through |
|---|---|
| `tcsetpgrp` / TIOCSPGRP (`tty/ioctl.rs`) | `set_foreground_pgrp` |
| TIOCGPGRP (`tty/ioctl.rs`) | `get_foreground_pgrp` |
| process creation (`process/creation.rs`, 2 sites) | `set_foreground_pgrp` |
| `sys_read`'s stdin arm (#821's consumer) | `adopt_deferred_foreground_pgrp` |
| the blocking input entry `input_char` | `send_signal_to_foreground` |

## The defect

At `origin/main`, 2 interrupt-side paths reached that mutex:

| site | shape | what a busy lock did |
|---|---|---|
| `input_char_nonblock` | `foreground_pgrp.try_lock().map(..).unwrap_or(false)` | answered "a foreground pgrp is set" whether or not one was, so the adoption #821 defers was skipped |
| `send_signal_to_foreground_nonblock` | `foreground_pgrp.try_lock()` with a returning arm on a busy lock | DROPPED the SIGINT/SIGQUIT/SIGTSTP it had just resolved, and announced the drop through `serial_println!` on x86 — itself a lock, taken from interrupt context |

Neither waited, so neither could wedge a CPU. That is why this is the narrowest
of the census's five findings and why the census called it "the same defect
shape one line below F3". What these two did instead was lose the keystroke's
meaning exactly when a `tcsetpgrp` was in flight — the moment a shell hands the
terminal to a new foreground job is precisely when its user is most likely to
be typing.

**One thing the census recorded is no longer true at this head, and is stated
rather than claimed.** #822's issue text names `driver.rs:304`
(`set_foreground_pgrp`, a blocking acquisition) reached from
`input_char_nonblock:191`. PR #878 (#821) removed that call from the interrupt
entry, so at `783a6a53` no BLOCKING acquisition of this mutex is reachable from
a TTY interrupt entry; #821's own document disclosed that and left #822 open on
its own terms. What this round repairs is what was left: the 2 `try_lock` sites
above.

## The repair

Both interrupt-side readers now read `foreground_pgrp_snapshot`, an `AtomicU64`
on the same device.

**Ordering.** Both writes of the field go through one private writer,
`store_foreground_pgrp`, which:

1. takes the mutex,
2. writes the field,
3. stores the snapshot `Release`,
4. releases the mutex.

Writers are therefore serialised by the mutex, so the cell's value sequence is
the field's own value sequence. It lags the field by at most the remainder of
one critical section and does not lead it. An interrupt reader loads it
`Acquire` and gets a value the field held at some instant — during a
`tcsetpgrp` window, the outgoing pgrp. That is a strictly better answer than
the `try_lock` gave: a busy lock gave it no value at all.

**The sentinel.** `u64::MAX` encodes "unset". 0 of the 3 production writers can
produce it: TIOCSPGRP returns `EINVAL` for a negative pgrp (so `pgrp as u64` is
in `[0, i32::MAX]`), and `process/creation.rs`'s 2 sites publish
`pid.as_u64()`. Rule 5 of the ratchet pins the TIOCSPGRP refusal for that
reason — it is load-bearing for the encoding, not decoration.

**Behaviour, uncontended.** The snapshot answers with the value the mutex would
have given. Both readers do the same thing with it they did before.

**Behaviour, contended.** The input entry defers the adoption when the field is
unset (rather than skipping it), and the signal path delivers to the last
published pgrp (rather than dropping the signal). The x86 `serial_println!` on
that degrade arm is gone with the arm.

### Why the snapshot rather than try-and-defer

The brief allowed either. The snapshot was taken because it removes the mutex
from the interrupt path outright rather than leaving a non-waiting touch on it,
and because the signal path has no sensible thing to defer: a Ctrl+C deferred
to the next `sys_read` is a Ctrl+C the reader will not see as a signal. The
brief's `TTY_IRQ_FG_DEFERRED` counter belongs to the try-and-defer design and
has no subject here; #821's `TTY_IRQ_PM_DEFERRED` already counts the one
deferral this entry makes, and this round leaves it counting the same thing.
What replaced it is named below.

## The detector, in production

`crate::process::in_no_blocking_process_manager_scope()` reports whether #821's
per-CPU `NoBlockingProcessManagerScope` is open on this CPU. That scope is
entered at exactly one place — `input_char_nonblock` — so a reading of true
means this CPU is inside a TTY interrupt entry. This round extends it to a
second lock rather than adding a second scope, which is what the brief
preferred.

`note_foreground_pgrp_acquisition(blocking)` runs immediately before each of the
5 acquisitions in `driver.rs` and, when the scope is open, bumps
`TTY_IRQ_FG_LOCK_TOUCHES` — plus `TTY_IRQ_FG_BLOCKING_ACQUIRES` for the blocking
subset. The note is taken BEFORE the acquisition, so a call that waits on a lock
its own CPU owns is counted rather than lost.

`try_lock` IS counted here, unlike `try_manager()` in #821. The reason is the
defect: a `try_lock` on this mutex cannot wedge, and it is still wrong, because
its degrade arm loses a signal. The two counters are kept separate so the x86
arm can key its refusal on the blocking one alone.

The other 4 counters: `TTY_IRQ_FG_SNAPSHOT_READS` (interrupt-side reads
answered from the snapshot) and `TTY_IRQ_FG_SIGNAL_CALLS` /
`TTY_IRQ_FG_SIGNAL_LAST_PID` / `TTY_IRQ_FG_SIGNAL_LAST_SIG`, recorded by
`send_signal_to_process_nonblock` at the delivery hop. The signal three are 3
relaxed cells and not one transaction: a reader sampling them while 2 CPUs
dispatch can pair a count with the other CPU's pid. The oracle reads them after
a single injection with preemption disabled, where that cannot happen.

## The touch census, at `783a6a53` and at this head

`kernel/src/tty/driver.rs`, the 5 acquisition sites of the field, plus the 2
interrupt-side reads that used to be among them:

| site | context at `783a6a53` | at this head |
|---|---|---|
| `input_char_nonblock` | IRQ, `try_lock` + degrade arm | reads the snapshot; 0 acquisitions |
| `send_signal_to_foreground_nonblock` | IRQ, `try_lock` + degrade arm that returned | reads the snapshot; 0 acquisitions |
| `set_foreground_pgrp` | thread, blocking | routed through `store_foreground_pgrp`, blocking, counted |
| `get_foreground_pgrp` | thread, blocking | unchanged, counted |
| `set_foreground_pgrp_raw_for_test` (boot_tests) | thread, blocking | routed through `store_foreground_pgrp`, counted |
| `send_signal_to_foreground` | thread, blocking | unchanged, counted |
| `foreground_pgrp_busy_for_test` (boot_tests, new) | thread, `try_lock` | counted |
| `hold_foreground_pgrp_for_test` (boot_tests, new) | thread, blocking | counted |

Not this lock, and named so the census is not read as covering it: the PTY's own
`foreground_pgid` (`kernel/src/tty/pty/pair.rs`), acquired blocking in
`PtyPair::send_signal_to_foreground`. Its only reacher is `master_write`, a
`write()` syscall path, so it has no interrupt-side caller in this tree. It was
read, not surveyed for other properties.

## The oracle

`tty_irq_fg_oracle`, in `kernel/src/test_framework/registry.rs`, boot_tests
only. Registered in the aarch64 registry at `TestStage::ProcessContext` beside
#796's, #812's and #821's windows — tests within one subsystem run sequentially
on that subsystem's kthread, so its console borrow cannot overlap #821's — and
called from `main.rs`'s driver-test window on x86, where the boot thread runs
with IF=1 and a thread-context holder of this lock with interrupts live is the
exposed shape.

It borrows the console: saves the termios and foreground pgrp, clears ECHO so
its injected bytes (the INTR byte's `^C` included) cannot land in a serial log a
gate is scoring, installs pgrp 822, and puts both fields back afterwards
(`restored=1`, which checks the snapshot as well as the field).

Three legs.

1. **The detector, with the mutex free.** One ordinary byte (`s`) through
   `tty::push_char_nonblock`, with preemption disabled so the per-CPU scope is
   released on the slot it was taken on.
2. **The held-lock injection.** A thread holds `foreground_pgrp` for 20 ms,
   UNMASKED — aarch64 on a peer CPU through a kthread, x86 on this CPU with
   IF=1 — and the INTR byte (0x03) goes through the same entry under it.
   Unmasked is the point: `tcsetpgrp` holds this lock with interrupts live, so
   that is the shape an input interrupt has to survive.
3. **The thread side.** The field is cleared and set through the ordinary
   accessors, and the snapshot is read back both times (`snapshot_agrees`).

Verdict lines:

```
[TTY_IRQ_FG_ORACLE:aarch64:fg_known=822:target_absent=1:fg_lock_touches=0:fg_blocking_acquires=0:snapshot_reads=3:processed=2:buffered=1:irqs_enabled_before=1:holder_cpu=1:fg_busy_probe=1:hold_us=20002:entry_us=116:joined=1:sig_calls=1:sig_pid=822:sig_num=2:snapshot_agrees=1:restored=1:PASS:peer_hold]
```

`fg_lock_touches=0` and `fg_blocking_acquires=0` are the property.
`sig_calls=1:sig_pid=822:sig_num=2` is the injected Ctrl+C still reaching the
signal path with the right pgrp under the hold. `fg_busy_probe=1` is the
independent reading that the mutex really was owned at the instant of the
injection; `hold_us` is the holder's own measurement of its window. `entry_us`
is what the aarch64 arm measures: an entry that acquired the mutex waits the
peer's whole window out.

### Deviations from the brief, and why

* **The brief's red-on-main reading is `entry_us ~20000`; the unmutated
  `783a6a53` entry does not block.** Both interrupt-side sites there are
  `try_lock`, so the red the tree really has is `sig_calls=0` — the dropped
  Ctrl+C — not a 20 ms wait. Both reds are driven: leg M3 below is `main`'s
  exact text and reddens on `sig_calls=0`; leg M2 restores a blocking
  acquisition and reddens at `entry_us=20048`, which is the census's own F4
  shape measured rather than argued.
* **The byte is driven through `tty::push_char_nonblock`, not a hardware input
  event.** Neither a PS/2 scancode nor a virtio-input event can be injected from
  inside the guest. This is the real entry, one frame below the handler — the
  same deviation #821 records.
* **The aarch64 holder runs on a kthread, not in interrupt context.** What it
  measures is how long the entry takes while another CPU owns the lock, which is
  the same wait an interrupt would take; it does not claim the measurement was
  taken from an exception frame.
* **The x86 leg 2 is gated on leg 1**, and reports `arm=handler_would_block`
  when the entry still reaches a blocking acquisition. That machine boots
  `-smp 1`; the injection would take the boot with it.
* **`entry_us` is pinned tightly on aarch64 and loosely on x86**, for the reason
  #821 gives: the x86 profile's monotonic clock is TSC-backed only when the TSC
  is calibrated. The x86 boot recorded `entry_us=1125` against the aarch64
  gate's 116..154; that arm carries the completed-under-hold reading, not a
  timed one, and its gate pattern accepts any `[0-9]+`.
* **No live process's signal mask is touched.** See "What is NOT claimed".

## Red and green

| leg | build | reading | serial |
|---|---|---|---|
| aarch64 red M1 | this branch with both interrupt-side reads restored to instrumented `try_lock` | `fg_lock_touches=3:fg_blocking_acquires=0:snapshot_reads=0:entry_us=70:sig_calls=0:sig_pid=0:FAIL:peer_hold`; gate exit 1 | `serials/822/01-a64-red-try-lock-instrumented-serial.txt`, gate log `01b` |
| aarch64 red M2 | this branch with a BLOCKING acquisition in the signal path | `fg_lock_touches=1:fg_blocking_acquires=1:entry_us=20048:FAIL:peer_hold`; gate exit 1 | `serials/822/03-a64-red-blocking-lock-serial.txt`, gate log `03b` |
| aarch64 red M3 | `main`'s exact uninstrumented `try_lock` text in both readers | `fg_lock_touches=0:fg_blocking_acquires=0:snapshot_reads=0:entry_us=64:sig_calls=0:FAIL:peer_hold`; gate exit 1 | `serials/822/04-a64-red-main-shape-try-lock-serial.txt`, gate log `04b` |
| aarch64 green | the branch | `fg_lock_touches=0:fg_blocking_acquires=0:entry_us=116:sig_calls=1:sig_pid=822:PASS:peer_hold`; gate PASS | `serials/822/02-a64-green-repaired-serial.txt` |
| x86 red M3 (fix round) | `main`'s exact uninstrumented `try_lock` text in both readers | `fg_lock_touches=0:fg_blocking_acquires=0:snapshot_reads=0:entry_us=1294:sig_calls=0:sig_pid=0:sig_num=0:FAIL:local_hold`; gate FAIL, exit 1 | `serials/822/18-x86-red-main-shape-excerpt.txt`, mutation `18b` |
| x86 green (fix round) | this branch | `fg_lock_touches=0:fg_blocking_acquires=0:entry_us=1146:sig_calls=1:sig_pid=822:sig_num=2:PASS:local_hold`; gate PASS, exit 0 | `serials/822/17-x86-green-gate-excerpt.txt` |

M3 is the leg that matters for what the property rests on: it carries 0 of this
round's instrumentation, so `fg_lock_touches` reads 0 on it, and it still
reddens — through `sig_calls=0`. The property is not counter-mediated.

The 2 x86 rows are the fix round's answer to review finding F4: before it, the
x86 `local_hold` arm had been driven against the repaired tree only, so "red on
main" was an inference from the shared code rather than a reading. It is a
reading now, and it carries a second fact — `main`'s own busy-lock announcement,
`TTY0: Cannot send signal 2 - foreground_pgrp lock busy`, appears exactly once
in that boot's COM1 log. That is a `serial_println!` reached from the interrupt
entry, executing, on the shape `origin/main` ships.

## Mutations

### Kernel source, one at a time, reverted from a pristine copy kept outside the tree

| mutation | command | exit | assertion |
|---|---|---|---|
| both interrupt-side reads back to instrumented `try_lock` | `docker/qemu/run-aarch64-boot-test-strict.sh 1` | 1 | `fg_lock_touches=3`, `sig_calls=0`, `FAIL:peer_hold`; runner rejected the boot on `[TEST:syscall:tty_irq_fg_oracle:FAIL:...]` |
| a blocking `.lock()` in `send_signal_to_foreground_nonblock` | `docker/qemu/run-aarch64-boot-test-strict.sh 1` | 1 | `fg_blocking_acquires=1`, `entry_us=20048` |
| `main`'s exact `try_lock` text, uninstrumented | `docker/qemu/run-aarch64-boot-test-strict.sh 1` | 1 | `sig_calls=0` with `fg_lock_touches=0` |
| `main`'s exact `try_lock` text, uninstrumented, on x86 (fix round, beast) | `docker/qemu/run-x86-boot-tests.sh 1` | 1 | `sig_calls=0:sig_pid=0:sig_num=0` with `fg_lock_touches=0` and `snapshot_reads=0`, `FAIL:local_hold`; the gate's own abort is `test "$passed" = true` at `run-x86-boot-tests.sh:930` — its wait chain requires this oracle's PASS line and this boot printed the FAIL variant instead |

### Gate-side, over mutated copies of the green serial

`serials/822/05-a64-gate-mutations.txt`: 1 green control and 19 reds. Each `sed`
is anchored to the `TTY_IRQ_FG_ORACLE` line, so a leg cannot redden the gate
through another oracle's identically-named field, and each mutated file is
diffed against the control before it is scored — the first pass of this table
produced 17 "reds" from a quoting bug that wrote no mutation at all, and every
one of them said "Userspace not detected".

| mutation | gate verdict |
|---|---|
| control (unmutated green serial) | `SCORE: PASS`, exit 0 |
| oracle line deleted | `SCORE: FAIL - TTY input IRQ foreground-pgrp oracle marker missing or failed` |
| `PASS` -> `FAIL` | `SCORE: FAIL - ... oracle reported failure` |
| `fg_lock_touches=0` -> `=1` | `SCORE: FAIL - ... marker missing or failed` |
| `fg_blocking_acquires=0` -> `=1` | same |
| `entry_us` -> `=20022` | same |
| `sig_calls=1` -> `=0` | same |
| `sig_pid=822` -> `=0` | same |
| `sig_num=2` -> `=0` | same |
| `fg_busy_probe=1` -> `=0` | same |
| `hold_us` -> `=3` | same |
| `target_absent=1` -> `=0` | same |
| `snapshot_agrees=1` -> `=0` | same |
| `snapshot_reads` -> `=0` | same |
| `restored=1` -> `=0` | same |
| `fg_known=822` -> `=0` | same |
| `processed=2` -> `=1` | same |
| `buffered` -> `=0` | same |
| `joined=1` -> `=0` | same |
| `:PASS:peer_hold` -> `:PASS:unarmed` | same |

### Ratchet legs

`tests/tty_irq_fg_structure.rs` carries 7 rules, a green control and 13
in-memory mutations, each asserting its own mutation applied before asserting
the rule reddened with the expected message: a blocking acquisition restored in
the interrupt-side signal path, a `try_lock` restored in the input entry, an
acquisition that does not count itself, the publication moved outside the
critical section, a setter that skips the publisher, a reader that stops reading
the snapshot, TIOCSPGRP accepting a negative pgrp, the detector no longer
consulting the scope, the named entry renamed out from under the census, the
publication moved AHEAD of the field write inside the same critical section, the
census's own count shrinking, a `serial_println!` restored at the signal hop,
and a `log::info!` restored in the input entry. The last 4 are the fix round's;
see its section below. 7 further legs run the aarch64 strict gate for real, in
scoring-only mode, over committed serials.

## Gate pins

| gate | pin |
|---|---|
| `run-aarch64-boot-test-strict.sh` | `TTY_IRQ_FG_ORACLE_PATTERN` in `score_serial` plus a FAIL scan; `'[TTY_IRQ_FG_ORACLE:'` added to the `require_boot_tests_kernel` marker census; a `TTY_IRQ_FG_SELFCHECK` block that checks, in the gate's own matcher, that the pattern rejects `entry_us=20022` and accepts `entry_us=116` before the pattern scores any boot |
| `run-aarch64-prod-profile-boot-test.sh` | `TTY_IRQ_FG_ORACLE_LITERAL` asserted at count 0, and the count printed |
| `run-x86-boot-tests.sh` | the x86 pattern in the wait chain, and the matched line echoed back in the tail summary |
| `run-x86-prod-profile-boot-test.sh` | `'[TTY_IRQ_FG_ORACLE:'` added to the forbidden test-only marker census |

## Fixture re-record (the R182 standing step)

Adding a check to the strict gate's `score_serial` means each committed serial
another suite scores green through that gate has to carry the new marker. 2 of
them do:

* `docs/planning/green-program/aarch64-testing/serials/slice3e/01-strict-boot1-serial.txt`,
  scored green by 2 legs in `loopback_pump_structure` and 1 in
  `ttbr0_shadow_reconciliation_structure`;
* `docs/planning/green-program/irq-locks/serials/821/02-a64-green-repaired-serial.txt`,
  #821's own green control.

Both are replaced with strict boot 1 of this branch's 20-boot run, which carries
`PIN_GUARD_ORACLE`, `RING_SPAN`, `TTY_IRQ_PM_ORACLE` and `TTY_IRQ_FG_ORACLE`
together; the same bytes are kept as this round's own
`serials/822/02-a64-green-repaired-serial.txt`. #821's evidence table cites that
file for `entry_us=2` on its own oracle, and the replacement carries
`[TTY_IRQ_PM_ORACLE:aarch64:...:entry_us=2:...:PASS:peer_hold]`, so the citation
still reads true. The production-profile fixtures are untouched, for the reason
#821 records: those scorers require the new marker ABSENT, which serials that
predate it already satisfy.

## The unlocked-serial census

`tests/serial_line_atomicity_structure.rs` pins the unlocked multi-byte serial
writers by anchor. Removing the degrade arm removed one:
`impl TtyDevice::fn send_signal_to_foreground_nonblock`, 1 unlocked
`raw_serial_str` call announcing that the mutex was busy and the signal was
being dropped. The write is gone because its subject is gone. The anchor count
moves 69 -> 68 and the census comment records why.

The fix round moves the REMAINING anchor at that site,
`impl TtyDevice::fn send_signal_to_process_nonblock`, from 1 to 2: F1's repair
replaced that function's x86_64 `serial_println!` — the locked writer, which
this census does not count — with the same single unlocked `raw_serial_str`
write the aarch64 arm makes, so the site now carries 2 of them and the census
counts both.

## Gates run

| gate | result | evidence |
|---|---|---|
| aarch64 build, `--features boot_tests`, then `scripts/check-kernel-no-neon.sh` | exit 0, 0 code warnings; `PASS: 0 FP/SIMD load/store instructions` | — |
| `run-aarch64-boot-test-strict.sh` (default 20) | **20/20**, `PASS: 20/20 boots succeeded` | `serials/822/10-a64-strict-x20.txt` |
| the oracle across those 20 boots | see the correction below: the cited file carries the gate's own stdout and 0 oracle lines | `serials/822/10-a64-strict-x20.txt` |
| `run-aarch64-prod-profile-boot-test.sh` | PASS, `Observed TTY foreground-pgrp oracle marker count: 0` | `serials/822/11-a64-prod-gate.txt` |
| beast x86 build, `--features boot_tests,testing,external_test_bins --bin qemu-uefi` | exit 0, 0 `^(warning\|error)` lines | `serials/822/07-x86-build-boot-tests.txt` |
| `run-x86-boot-tests.sh 1` on beast | `x86 frame-custody gate run 1: PASS`, `GATE_EXIT=0`; the boot's own `TEST_TALLY: exited=110 nonzero=0 failed=[]` and `TEST RUNNER: All tests passed` | `serials/822/06-x86-boot-tests-gate.txt`, excerpt `06b` |
| the oracle on that boot | `[TTY_IRQ_FG_ORACLE:x86:...:fg_lock_touches=0:fg_blocking_acquires=0:...:fg_busy_probe=1:entry_us=1125:sig_calls=1:sig_pid=822:sig_num=2:snapshot_agrees=1:restored=1:PASS:local_hold]` | `serials/822/06b-x86-boot-tests-serial-excerpt.txt` |
| `run-x86-prod-profile-boot-test.sh` on beast | `PASS: x86 production profile reached steady state with the teardown census at rest`, `GATE_EXIT=0`, `test-only marker '[TTY_IRQ_FG_ORACLE:': 0` | `serials/822/08-x86-prod-profile-gate.txt` |
| `scripts/check-x86-dispatch-no-alloc.sh` on beast | exit 0, `PASS: 0 allocating call targets in 3 in-scope symbol(s), 19 edge(s) checked` | `serials/822/09-x86-dispatch-no-alloc.txt`; re-run in the fix round with the same reading, `serials/822/19-x86-dispatch-no-alloc-fix-round.txt` |
| every `tests/*_structure.rs` suite | 43 of 43 green, 689 cases (690 after the fix round's 7th rule) | — |
| `scripts/test_claim_lint.py` | exit 0 | — |

This table's x86 evidence was taken on beast at `7f2d85a2`, the head after this
round's kernel and oracle commits, and at that time `git diff 7f2d85a2..HEAD --
kernel/ docker/qemu/run-x86-*` was empty. **The fix round's kernel commit
changed that**: at this branch's head that diff is 16 insertions and 14
deletions in `kernel/src/tty/driver.rs`, so this table's x86 rows are readings
from a build 1 commit behind the tree. The fix round re-ran the x86 build and
both x86 gates at the current head; its own table below carries those readings,
and they are the ones to read. The aarch64 strict gate WAS re-run after that selfcheck change: 1/1 PASS
(`serials/822/12-a64-strict-x1-final-head.txt`). That file carries the gate's
verdict and 0 oracle lines; see the correction below for the reading this
sentence used to attribute to it.

UNATTRIBUTED failures: 0.

## Fix round 1 (review round 1's findings)

The round's review returned 1 blocking finding, 3 major and 1 minor. 5 of the 5
are answered here; the tables above were edited in place where they were wrong, and
this section records what moved.

| finding | what it said | what this round did |
|---|---|---|
| F1, blocking | `send_signal_to_process_nonblock`'s x86_64 arm still called `serial_println!` — `SERIAL1`, blocking, plus a string format — from the keyboard IRQ chain, with the `PROCESS_MANAGER` guard 2 lines above still held. Byte-identical on `origin/main`; this round neither introduced nor disclosed it | that arm is 1 unlocked `raw_serial_str` write now, the same write the aarch64 arm makes. Rule 7 of the ratchet refuses the class |
| F2, major | rule 3's field-write offset matched `guard = ` — which is also the ACQUISITION line `let mut guard = self.foreground_pgrp.lock();`, the earliest such offset in that body on each of the 2 orderings — so a body that published BEFORE it wrote passed the ordering half of the rule | the offset has to be a dereference, `*guard = `. Leg K mutates exactly that shape |
| F3, major | 2 rows cited `10-a64-strict-x20.txt` and `12-a64-strict-x1-final-head.txt` for per-boot oracle readings (`entry_us` spans, a 20-of-20 tally) that those files do not contain — they are the gate's stdout and carry 0 oracle lines | both rows corrected. This round's own 20-boot runs ship a per-boot oracle line PLUS a computed tally as committed files |
| F4, major | 1 of the 2 architectures had a red-on-`main` leg; the x86 `local_hold` arm had been driven against the repaired tree only, so "red on main" was an inference on x86 | the x86 red leg was run on beast against `main`'s exact text: `sig_calls=0`, gate exit 1 |
| F6, minor | rule 1's `_nonblock` census failed only below 5 while written against 6 — 1 entry could vanish silently | the threshold is 6. Leg J keeps the count at 6 and measures the NAMED entry; leg L is new and measures the count |

### The correction F3 names

`serials/822/10-a64-strict-x20.txt` and `serials/822/12-a64-strict-x1-final-head.txt`
are the strict gate's own stdout: per-boot `[OK]`/`[FAIL]` lines,
`GATE_BOOT_FACTS`, and the run's verdict. `grep -a 'TTY_IRQ_FG_ORACLE' ` returns
0 lines from either. The per-boot oracle readings the round attributed to them
(`entry_us` 116..154 over 20 boots; `entry_us=118` on the 1-boot re-run) were
read from the gate's per-boot serial logs under its temp directory during those
runs, and that directory is not committed. Those 2 sentences were therefore not
checkable from the repository, whatever they were when they were written.

Rather than soften them, this round re-ran the gate and committed the missing
layer:

* `serials/822/15-a64-strict-x20-run2-gate.txt` — the gate's stdout, 20/20;
* `serials/822/15b-a64-strict-x20-run2-oracle-lines.txt` — that run's 20 oracle
  lines, 1 per boot, each beside its own `GATE_BOOT_FACTS`, with the tally
  computed at the bottom of the file from the lines above it. `entry_us` is read
  out of THIS oracle's marker: 3 other oracles in the same serial spell
  `entry_us=` the same way, and a naive `grep -o 'entry_us=[0-9]*' | head -1`
  over the serial returns #821's `entry_us=2`, not this one's;
* `serials/822/15c-a64-strict-run2-boot1-serial.txt` — 1 raw per-boot serial
  from that run, so the extraction above can be checked against bytes the gate
  itself wrote.

Readings, from those files: 20 of 20 boots printed `PASS:peer_hold`, 0 printed
FAIL, 20 of 20 read `fg_lock_touches=0:fg_blocking_acquires=0`, 20 of 20 read
`sig_calls=1:sig_pid=822:sig_num=2`, and `entry_us` spans 122..296 us against a
20 ms hold.

### The strict gate's other 20-boot run, and its 2 rejected boots

Run 1 of the same gate, at the same head, came back **18/20**. Both rejected
boots are attributed, from the gate's own per-boot facts:

| boot | what the gate said | the facts on that boot |
|---|---|---|
| 10 | `Exec smoke did not complete (788 lines)` | `ended_by=poll_exhausted`, `guest_uptime_ms=18743` against ~11000 typical, `load_at_start=4.28` → `load_at_end=8.98`. No panic and no fault marker in its serial: it ends mid-PTY-test. Its own oracle line printed `PASS` |
| 20 | the oracle's FAIL | `entry_us=4120` against the 1000 us ceiling, with `fg_lock_touches=0`, `fg_blocking_acquires=0`, `hold_us=20000` and `sig_calls=1:sig_pid=822:sig_num=2` — the property held and the CEILING did not. `qemu_cpu_s=26.02` against ~19 typical, `ended_by=poll_exhausted` |

The environment those 2 boots ran in is in the same log: boots 17-20 each print
`QEMU HOST LOCK: host qemu-system-aarch64 count before acquire: 1`, and boot 19
waited past 60s for that lock — another `qemu-system-aarch64` was on this host
for the tail of the run. This is the shape `docker/qemu/lib/qemu-host-lock.sh`
documents in its own header (R181: concurrent QEMU on this host ran the guest
clock at 37-53% of wall-clock and falsely reds the ~18s poll ceiling, #826).
Run 2 was taken after that process left: each of its 20 boots reads `count
before acquire: 0`, and it is 20/20.

Both runs are committed, red one included —
`serials/822/14-a64-strict-x20-run1-contended-gate.txt` and its per-boot oracle
file `14b`. Across the 40 boots of the 2 runs, `fg_lock_touches=0`,
`fg_blocking_acquires=0` and `sig_calls=1:sig_pid=822:sig_num=2` read on 40 of
40; the 1 oracle FAIL is a timing ceiling on a starved host, not a touched lock.

UNATTRIBUTED failures in this round: 0.

### F1's repair, and what it is not

The x86_64 arm now writes `[TTY] sig sent to PID\n` through
`crate::tracing::output::raw_serial_str` — direct port I/O, 0 locks, 0
formatting — where it used to `serial_println!` the signal name, the process
name and the pid. The pid and the signal number are not lost: this round's own
census stores publish both to atomics at the top of the same function, before
the lookup, and `tty_irq_fg_signal_census()` reads them back from thread
context. That is where the oracle's `sig_pid`/`sig_num` come from.

* **NOT claimed: that a hang was observed, or that one is reachable today.** 3
  of the 4 `SERIAL1` acquisition sites in `kernel/src/serial.rs` — `:88`,
  `:118`, `:145` — mask interrupts across their critical section, and the 4th,
  `:27`, runs in `init()` before the IDT is loaded; the x86 machine also boots
  `-smp 1`. What is repaired is a blocking lock and a string format taken from
  an interrupt entry, against the rule that function's own doc comment states
  and the rule `task/scheduler.rs:21` states, on a path SMP is a stated target
  for. claim-lint:ok: kernel/src/serial.rs is the counted file.
* **NOT claimed: that the repaired write executed in either gate boot.** The
  oracle installs pgrp 822 only after checking the process manager holds no row
  for it (`target_absent=1`), so `get_process_mut` finds no row and this
  diagnostic — which sits inside that `Some` arm — is not reached. `grep -c
  '\[TTY\] sig sent to PID'` is 0 on both gate boots. What the gates show is
  that the branch builds warning-free and boots green on both architectures with
  the change in place; rule 7 and the unlocked-write census carry the rest.
* **NOT claimed: that the unlocked write is free of the interleaving #847
  measured.** A byte-level interleave on a shared UART is the accepted
  trade-off the other 67 anchors in `tests/serial_line_atomicity_structure.rs`'s
  census make; this site now makes it on both architectures rather than one, and that
  census's anchor moves 1 -> 2 with the reason recorded beside it.
* **What rule 7 does not reach.** It reads macro CALLS in the interrupt-
  reachable bodies. It does not read `crate::serial::write_byte`, which
  `output_char_nonblock` calls on x86_64 to put an echoed character on the
  user's console: that call takes `SERIAL1` too, deliberately, with interrupts
  masked across the critical section, and it is the echo itself rather than an
  announcement about it. This round changes 0 lines of it and rule 7 neither
  licenses nor measures it.

### F2's repair, probed both ways

Against leg K's mutated text — the `.store(..)` moved ahead of `*guard = pgrp;`,
both still inside the critical section — the OLD predicate returns `Ok(())` and
the new one returns the ordering error. Both readings were taken from a
throwaway copy of the suite before the change was committed. Leg E, which the
rule already had, moves the publication past `drop(guard)` and is caught by the
rule's other half (`publication < release`); it constrains 0 of what this
shape changes.

### Gates re-run in this round

| gate | result | evidence |
|---|---|---|
| aarch64 build, `--features boot_tests`, then `scripts/check-kernel-no-neon.sh` | exit 0, 0 code warnings, `PASS: 0 FP/SIMD load/store instructions` | — |
| `run-aarch64-boot-test-strict.sh 20`, run 1 | 18/20, both rejected boots attributed above | `serials/822/14-...`, `14b-...` |
| `run-aarch64-boot-test-strict.sh 20`, run 2 | **20/20**, oracle 20 of 20 `PASS:peer_hold` | `serials/822/15-...`, `15b-...`, `15c-...` |
| `run-aarch64-prod-profile-boot-test.sh` | PASS, `Observed TTY foreground-pgrp oracle marker count: 0` | `serials/822/16-a64-prod-gate-fix-round.txt` |
| beast x86 build, `--features boot_tests,testing,external_test_bins --bin qemu-uefi` | exit 0, 0 `^(warning\|error)` lines | — |
| `run-x86-boot-tests.sh 1` on beast, this branch | PASS, `GATE_EXIT=0`, `TEST_TALLY: exited=110 nonzero=0 failed=[]`, oracle `PASS:local_hold` | `serials/822/17-x86-green-gate-excerpt.txt` |
| `run-x86-boot-tests.sh 1` on beast, `main`'s reader text | FAIL, `GATE_EXIT=1`, oracle `sig_calls=0 ... FAIL:local_hold` | `serials/822/18-...`, mutation `18b` |
| every `tests/*_structure.rs` suite | 43 of 43 green, 690 cases | — |
| `scripts/claim-lint.py`, tree and each commit message | exit 0 | — |
| `scripts/check-x86-dispatch-no-alloc.sh` on beast | exit 0, same reading as the round's own row | `serials/822/19-x86-dispatch-no-alloc-fix-round.txt` |
| `scripts/check-critical-path-violations.sh`, both hosts | exit 1 with 274 lines of output, byte-identical to the `origin/main` baseline this round committed | `serials/822/13-critical-path-checker-main.txt` |

The x86 legs were run in a clone on the beast container at this branch's
`446d246a` plus the fix commits, in that clone's own `BREENIX_GATE_TMP`. The
beast host had 2 other x86 gate lanes queued on the same host lock while they
ran; both of this round's legs held the lock for their own boot.

## Critical-path checkers

`scripts/check-critical-path-violations.sh` is red on `origin/main` and stays
red for exactly the same reasons:

| | lines of output | `VIOLATION in <file>` headers | exit |
|---|---|---|---|
| `origin/main` `783a6a53` | 274 | 9 | 1 |
| this branch | 274 | 9 | 1 |

`diff` of the two outputs is empty. This branch adds 0 lines to that checker and
touches 0 of the files it names.

## What is NOT claimed

* **No live process's signal mask is touched, and "reached the right process"
  is measured one hop short of that.** The oracle installs pgrp 822 after
  checking the process manager holds no row for it (`target_absent=1`), and its
  reading is `sig_calls`/`sig_pid`/`sig_num` — the pid the interrupt side
  resolved and handed to `send_signal_to_process_nonblock`, recorded at that
  hop. Setting SIGINT pending on a live process inside a boot gate would be a
  boot hazard: the target could take the signal and die between the injection
  and any attempt to clear it. So the oracle reads the signal as DISPATCHED to
  the right pid under a held lock, where the 2 red mutations above read
  `sig_calls=0`; it does not read a process row's pending mask at all.
* **No timing claim.** No cycle count was taken for the snapshot read, the
  counters or the scope on the input path. `entry_us` measures the oracle's own
  injection, not a benchmark.
* **The oracle does not run in interrupt context.** See the deviations above.
* **The x86 arm does not measure a wait**, and on an entry that still reaches a
  blocking acquisition it reports that fact rather than driving the wedge.
* **The snapshot is not a substitute for the mutex.** The thread side still owns
  the field and still takes the lock; what changed is that the interrupt side
  stopped asking for it. A reader that needs the CURRENT value under mutual
  exclusion — the ioctl paths, `store_foreground_pgrp`'s own check-then-set —
  still takes the mutex.
* **A `tcsetpgrp` in flight can still be raced, and now loses differently.**
  Before, a signal arriving mid-`tcsetpgrp` was dropped. Now it is delivered to
  the outgoing foreground pgrp. That is the POSIX-shaped outcome and it is
  better than a drop, but it is a behaviour change; this round adds 0
  synchronisation between an input interrupt and a `tcsetpgrp` in flight.
* **The PTY's own `foreground_pgid` was read, not surveyed.** It is a different
  lock on a different device, with no interrupt-side caller in this tree.
* **0 of #812, F2 and F5** of the IRQ-context lock census are addressed here.
  Those are the `PROCESS_MANAGER` and UDP-socket findings, and this branch
  changes 0 lines on their paths.
