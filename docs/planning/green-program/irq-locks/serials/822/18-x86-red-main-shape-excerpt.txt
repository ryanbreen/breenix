# x86_64 boot-test gate, 1 boot, on beast. Leg: red
# Extracted from the run directory the gate wrote:
#   serial_user.txt   -- COM1, where the #822 oracle prints
#   serial_kernel.txt -- COM2, the kernel log

## the #822 oracle line (serial_user.txt):
[TTY_IRQ_FG_ORACLE:x86:fg_known=822:target_absent=1:fg_lock_touches=0:fg_blocking_acquires=0:snapshot_reads=0:processed=2:buffered=1:irqs_enabled_before=1:fg_busy_probe=1:entry_us=1294:sig_calls=0:sig_pid=0:sig_num=0:snapshot_agrees=1:restored=1:FAIL:local_hold]

## the boot's own test tally (serial_kernel.txt):
[ INFO] kernel::syscall::handlers: TEST_TALLY: exited=110 nonzero=0 failed=[]
[ INFO] kernel::syscall::handlers: 🏁 TEST RUNNER: All tests passed - you can exit QEMU now 🏁

## occurrences of main's busy-lock announcement, the serial_println! this
## round's F1 repair removed from the interrupt-reachable signal hop
## (grep -c 'foreground_pgrp lock busy' serial_user.txt):
1
TTY0: Cannot send signal 2 - foreground_pgrp lock busy

## the gate verdict (its own stdout, first verdict line and exit):
x86 frame-custody gate run 1: FAIL (set -e abort at docker/qemu/run-x86-boot-tests.sh:930, exit 1)
[?1000l[?1002l[?1003l[?1005l[?1006l[?1015l[?1016l[?2004l[?1l>[?25h
GATE_EXIT=1   (read with `grep -ao 'GATE_EXIT=[0-9]*' <gate log> | tail -1`;
              the runner appends it with no preceding newline, so it shares a
              line with the gate's own serial tail dump in the raw log)
