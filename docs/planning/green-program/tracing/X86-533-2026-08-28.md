# x86_64 tracing leg — #533, the staged boot-test registry

Date: 2026-08-28. Branch `feat/green-tracing`, off `main` @ `54aa16a1`.
All runs on the beast x86 VM (`breenix-x86`), QEMU TCG, one boot per run.

## The state #533 described, measured

`#533` says the x86 boot "reaches marker-only stage advancement without
dispatching the staged boot-test registry" and can emit `[TESTS_COMPLETE:0/0]`.
A full `boot_tests,testing,external_test_bins` boot on `main` @ `54aa16a1`
produced exactly three registry markers across both serial ports:

```
[STAGE:user:ADVANCE]
[TESTS_COMPLETE:0/0]
[BOOT_TESTS:PASS]
```

`[BOOT_TESTS:START]` never appeared; `grep -c BOOT_TESTS serial_kernel.txt` was
`0`. All three lines come from `advance_stage_marker_only`, called from the first
Ring 3 syscall. Nothing in the registry ran, and the boot still printed a
pass-shaped verdict.

## Why the call was unreachable

`test_framework::run_all_tests()` was called from `main.rs` immediately before
`per_cpu::preempt_enable()`, several hundred lines below two hard stops:

1. `kernel_main_continue()` takes the boot sequence's scheduling brake with
   `preempt_disable()` near its top, and the executor joins its subsystem
   kthreads — it needs preemption.
2. Below that, the boot thread enters `test_exec::test_direct_execution()` and
   strands in the disk-backed `get_test_binary()` busy-wait. On the `main`
   baseline the last line `kernel_main_continue` ever printed was
   `=== MULTIPLE CONCURRENT PROCESSES TEST ===`; everything after it in the
   serial comes from userspace and the syscall handlers.

So the dispatch sat past the point the boot thread reaches, behind a brake that
would have deadlocked it anyway.

## What the fix does

* Dispatch from the top of `kernel_main_continue()`, the last x86 point that is
  both preemptible and still on the boot thread.
* Enable interrupts across the dispatch and restore the previous flag. Every
  `kthread_tests::*` case run immediately before ends with
  `arch_disable_interrupts()`, so control arrives with IF clear, and
  `kthread_join`'s `hlt` never wakes.
* Join each subsystem thread before spawning the next. With the cohort spawned in
  parallel the boot deadlocked between `kernel_stack::allocate` on the boot
  thread and `heap_large_alloc` in the Memory kthread. aarch64 keeps the parallel
  cohort.
* Dispatch `ProcessContext` from the same point; `Userspace` stays marker-only on
  both architectures, per #533's own constraint about syscall context.
* `[TESTS_COMPLETE:0/0]` is no longer a pass — it is `[BOOT_TESTS:FAIL:VACUOUS]`.
* A partial tally is no longer a verdict. The first Ring 3 syscall races the
  dispatch on x86; it now reports `[STAGE:user:COMPLETE:c/t]` and leaves the
  verdict to the dispatch. The same race used to push `CURRENT_STAGE` past
  `ProcessContext` and make `advance_to_stage` drop that cohort silently.
* The four tests #567 blocks announce `[TEST:...:DEFERRED:#567]` instead of
  vanishing.

## Results

Beast x86 VM, `boot_tests,testing,external_test_bins,x86_staged_registry`, one boot.
Full marker stream: `serials/x86-r4-registry-markers.txt`.

```
[BOOT_TESTS:START]
[BOOT_TESTS:TOTAL:74]
[BOOT_TESTS:SERIAL_BOOT:1]
[BOOT_TESTS:EARLY_BOOT:70]
[BOOT_TESTS:STAGED:4 tests waiting for later stages]
[STAGE:serial:ADVANCE]      [SUBSYSTEM:process:serial:COMPLETE:1/1]
[STAGE:early:ADVANCE]
  [SUBSYSTEM:memory:early:COMPLETE:23/23]
  [SUBSYSTEM:scheduler:early:COMPLETE:4/4]
  [SUBSYSTEM:interrupts:early:COMPLETE:7/7]
  [SUBSYSTEM:filesystem:early:COMPLETE:4/4]
  [SUBSYSTEM:network:early:COMPLETE:7/7]      (+4 DEFERRED:#567)
  [SUBSYSTEM:ipc:early:COMPLETE:7/7]
  [SUBSYSTEM:process:early:COMPLETE:5/5]
  [SUBSYSTEM:syscall:early:COMPLETE:1/1]
  [SUBSYSTEM:timer:early:COMPLETE:4/4]
  [SUBSYSTEM:logging:early:COMPLETE:3/3]
  [SUBSYSTEM:system:early:COMPLETE:4/4]
[STAGE:user:ADVANCE]        [STAGE:user:COMPLETE:69/74]
```

Seventy of seventy EarlyBoot tests pass, including
`[TEST:process:deferred_fault_ring_overflow_injection:PASS]` — the tracing row's
one functional registry test, running on x86 for the first time. The four tests
#567 blocks announce `[TEST:...:DEFERRED:#567]`. The racing first Ring 3 syscall
correctly publishes stage progress (`[STAGE:user:COMPLETE:69/74]`) rather than a
verdict.

An earlier round of the same run, before the `timer_delay` fix, reported
`[TEST:timer:timer_delay:FAIL:delay too short on x86_64]` against 69 passes —
the first time that test had ever executed on x86. It is fixed here; the run
above shows `timer:early:COMPLETE:4/4`.

## What stops it, and why the feature ships off

After `[SUBSYSTEM:system:early:COMPLETE:4/4]` the boot thread is gone. It is
inside `kthread_join` for that subsystem's thread, the thread exited, the join
never returns, and the scheduler picks the idle thread for the rest of the boot:

```
[DEBUG] kernel::test_framework::executor: System:early: all 4 tests passed
Next thread from queue: 1, cpu: 0
Idle thread 1 is alone, continuing (no switch needed)
   ... for the remainder of the run
```

`[STAGE:early:COMPLETE:...]` never prints, PostScheduler and ProcessContext
never dispatch, and control never returns to `kernel_main_continue` to create
the userspace suite. Filed as **#680**, with
`serials/x86-r4-strand-excerpt.txt`.

A second defect surfaces in the same window: `tty_foreground_pgrp` creates a
user process, and that process's exit makes the x86 userspace runner publish
`TEST RUNNER: All tests passed` with `TEST_TALLY: exited=1` before the real
userspace suite exists — a false-green shape for the gate's own terminal-marker
wait. Filed as **#681**.

Neither is fixable inside this arc. The dispatch therefore sits behind the
off-by-default `x86_staged_registry` feature: with it off, the shipped x86 boot
is what it was on `main`, and **#533 stays open**.

## Bar check

x86_64 tracing is **not green**. #533, #680 and #681 are open against this row.
The registry now demonstrably runs the tracing row's own functional test on x86,
but only with a feature that cannot be turned on, so there is no x86 gate
evidence that exercises it in a shippable configuration. Reported as a gap, not
declared with an asterisk.

## The shipped x86 configuration, after the feature gate

`docker/qemu/run-x86-boot-tests.sh 1` at branch HEAD with default features:
**`x86 frame-custody gate run 1: PASS`**
(`serials/x86-default-gate-pass-20260828.txt`), with
`x86 userspace gate: PASS - exited=104 expected>=104 nonzero=0 allowlist=0`.

The serial still shows `[TESTS_COMPLETE:0/0]` and `[BOOT_TESTS:PASS]` — #533's
symptom, unchanged, because the dispatch is off. One intermediate branch state
did turn that pair into `[BOOT_TESTS:FAIL:VACUOUS]` and reddened this gate; the
vacuity and partial-tally guards now key off whether the registry is dispatched
in the build, so they are live wherever they can mean something (always on
aarch64, on x86 behind the feature) and silent where they would only restate an
open issue as a gate failure.
