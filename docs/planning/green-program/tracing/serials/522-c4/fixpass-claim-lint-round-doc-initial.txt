docs/planning/green-program/tracing/522-C4-2026-09-07.md:162: [universal-claim] 1. **W-1 (major/code)** — `kernel/src/tracing/providers/teardown.rs`, the `workers_ready` wait inside `exit_kick_protocol_gate_test`. `target_complete` was `|_, progress| progress != 0`, treating a worker's own progress counter going non...
    -> unquantified absolute ('every') with no N-of-M count, resolving evidence-log citation, or claim-lint:ok in this paragraph
docs/planning/green-program/tracing/522-C4-2026-09-07.md:162: [unproven-claim] 1. **W-1 (major/code)** — `kernel/src/tracing/providers/teardown.rs`, the `workers_ready` wait inside `exit_kick_protocol_gate_test`. `target_complete` was `|_, progress| progress != 0`, treating a worker's own progress counter going non...
    -> 'proof' with no named mutation/experiment (mutation, revert, redden, falsify, reproduce, regression, --boots, bisect), N-of-M count, or resolving evidence-log citation in this paragraph
docs/planning/green-program/tracing/522-C4-2026-09-07.md:197: [universal-claim] 2. **W-7 (minor/code, test-strength gap)** — `tests/teardown_structure.rs`, the loop asserting each `storm_{a,b,observer}_progress` closure reads only its own counter. `call` was sliced starting at the `let {progress_source} =` anchor it...
    -> unquantified absolute ('always') with no N-of-M count, resolving evidence-log citation, or claim-lint:ok in this paragraph
docs/planning/green-program/tracing/522-C4-2026-09-07.md:208: [universal-claim] + "let {progress_source} =".len()`), so it holds only the closure expression, never the LHS name. The positive assertion, and the `unrelated_counter == counter` case inside the paired `assert_eq!` loop (which was equally vacuous for the ...
    -> unquantified absolute ('never') with no N-of-M count, resolving evidence-log citation, or claim-lint:ok in this paragraph
docs/planning/green-program/tracing/522-C4-2026-09-07.md:232: [universal-claim] **W-1 mutation proof.** Reverted only the `target_complete` closure at the `workers_ready` call site to the pre-fix form (`|_, progress| progress != 0`), leaving every other edit in place, and re-ran `bash scripts/run-structure-tests.sh`...
    -> unquantified absolute ('every') with no N-of-M count, resolving evidence-log citation, or claim-lint:ok in this paragraph
docs/planning/green-program/tracing/522-C4-2026-09-07.md:325: [universal-claim] - This pass closes ONLY W-1 and W-7. Other findings from the same review round are explicitly untouched and remain open for a separate pass: W-13 (a related but distinct attribution-staleness gap — a per-target NoProgress verdict's `targ...
    -> unquantified absolute ('never') with no N-of-M count, resolving evidence-log citation, or claim-lint:ok in this paragraph
docs/planning/green-program/tracing/522-C4-2026-09-07.md:333: [universal-claim] - No x86 gate was run: both `exit_kick_protocol_gate_test` and `exit_kick_worker_window_isolation_test` are `#[cfg(all(feature = "boot_tests", target_arch = "aarch64"))]`, confirmed by direct source read, not merely asserted.
    -> unquantified absolute ('all') with no N-of-M count, resolving evidence-log citation, or claim-lint:ok in this paragraph
docs/planning/green-program/tracing/522-C4-2026-09-07.md:351: [auto-close-keyword]  `python3 scripts/claim-lint.py`, after removing the auto-close-keyword adjacency ("closes #522" -> "#522") | 0 
    -> 'closes #522' immediately in front of an issue reference triggers GitHub's auto-close-on-merge; the convention from here on is a plain '#N', not a close/fix/resolve keyword directly in front of it. Not dischargeable by claim-lint:ok -- rewrite the reference, don't annotate it. A negated claim ('does not resolve #N') is not exempt either: GitHub's own matcher does not parse negation, so the bytes are exactly as dangerous -- break the adjacency instead, e.g. 'resolve issue #N' or lead with the number ('#N is not resolved by this change'); see docs/planning/green-program/claim-linting.md.
docs/planning/green-program/tracing/522-C4-2026-09-07.md:353: [auto-close-keyword] The first check flagged `closes #522` immediately preceding the issue reference in a new source comment as GitHub's auto-close-on-merge phrase; the reference was rewritten to a plain `#522` with no close/fix/resolve keyword in front of i...
    -> 'closes #522' immediately in front of an issue reference triggers GitHub's auto-close-on-merge; the convention from here on is a plain '#N', not a close/fix/resolve keyword directly in front of it. Not dischargeable by claim-lint:ok -- rewrite the reference, don't annotate it. A negated claim ('does not resolve #N') is not exempt either: GitHub's own matcher does not parse negation, so the bytes are exactly as dangerous -- break the adjacency instead, e.g. 'resolve issue #N' or lead with the number ('#N is not resolved by this change'); see docs/planning/green-program/claim-linting.md.

claim-lint: 9 finding(s) across 52 file(s) [changed hunks vs 6432656236ca]. Discharge a legitimate claim with a same-paragraph `claim-lint:ok: <citation>` annotation naming an N-of-M count, a resolving path, an issue, or a review. See docs/planning/green-program/claim-linting.md.
claim-lint: auto-close-keyword finding(s) above cannot be discharged by a claim-lint:ok annotation -- the phrase acts on GitHub at merge/commit time regardless of any annotation. Rewrite the reference as a plain '#N' with no close/fix/resolve keyword directly in front of it.
claim-lint: 302 pre-existing finding(s) outside this branch's changed hunks not reported (--whole-file shows them).
