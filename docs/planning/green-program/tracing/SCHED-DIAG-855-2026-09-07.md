# Issue 855 scheduler sampling follow-up — incomplete

Current evidence is **failed**: no passing unfiltered-window evidence is
accepted in this round. The requested target remains 1500 ms in three boots;
the existing strict gate floor remains 1000 ms. Neither was lowered.

`TRACE_DIAG_SAMPLE` remains 1024. The new aarch64-only
`TRACE_SCHED_DIAG_SAMPLE` is 16384, with a per-CPU relaxed call counter and
`SCHED_DIAG_SAMPLE_DROPPED` call-level accounting. The existing three ring
writes and the x86 no-op stub remain intact. The scheduler also calls the
helper from wake-timer attribution sites; sampling applies to those calls.

## Measurements and stopping decision

The first factor, 8192, produced spans of 921, 895, and 901 ms; strict x3
exited 1. Doubling to 16384 produced 896, 938, and 936 ms; strict x3 exited 1.
Raw serials are under [serials/855](serials/855), named
`aarch64-sched8192-failed-boot-{1,2,3}.txt` and
`aarch64-sched16384-failed-boot-{1,2,3}.txt`. The latter are the current
measurement evidence, not accepted passing evidence. The older `after` and
`final-boot` fixtures remain as failed evidence of the discovery that
SCHED_DIAG occupied 960–1022 of 1024 slots on CPUs 1–3.

A hardware GDB breakpoint at `kernel::capture::emit` on the factor-16384
feature build found the following counts per 1024-slot peer ring:

| CPU | SCHED_DIAG total | CTX_SWITCH_ENTRY + SCHED_QUEUE_STATE | SCHED_RESCHED_IPI_RECV |
| --- | --- | --- | --- |
| 1 | 6 | 772 | 196 |
| 2 | 9 | 722 | 248 |
| 3 | 6 | 784 | 192 |

The [decoded snapshot](serials/855/aarch64-gdb-ring-mix-sched16384.txt) and
[binary dump](serials/855/aarch64-gdb-rings-sched16384.bin) preserve that
probe separately from strict-gate measurements. The snapshot spans are
969, 988, and 878 ms for those peers. The dump uses 16448-byte CPU strides,
1024 `<QHBBI` entries, and a 62500000 Hz counter; the write index is the
64-bit value at offset 16384 of each CPU buffer.

Inference: further SCHED_DIAG-only sampling cannot plausibly bridge the
roughly 600 ms shortfall when that family occupies only 6–9 slots. Tuning
stopped after this first doubling, based on the measured plateau and GDB,
instead of trying another factor without a supporting mechanism. The next
investigation is retention policy for ordinary switch/queue/IPI providers;
this round does not change them or increase the ring size. Issue #855 stays
open.

## Validation

The bare `bash scripts/run-structure-tests.sh` default teardown suite passed
92 tests, exit 0. The explicit `ctx_diag_ring_sample_structure` suite passed
9 tests, exit 0, including the scheduler guard-deletion anti-vacuity leg.
Both strict runs completed their 55-suite structure preflight before booting.
Both feature builds exited 0. Cargo emitted the same pre-existing pinned
Rust `core` future-incompatibility notice recorded in the previous round;
no kernel compilation warning was emitted. This is not a warning-free
build claim. GDB probe exit: 0. The aarch64 production-profile gate exited 0, including its 55/55
structure preflight. The beast x86 boot gate exited 0 at code commit
`504d3667982e95d804163b46bc828540773ca17b`, with 55/55 structure suites
and no compiler warning/error lines. This is a no-regression check:
x86's `trace_sched_diag` remains a no-op; its filtered ring marker does
not measure the aarch64 unfiltered-window target.

Beast used `/root/breenix-855`, `TMPDIR=/root/breenix-855-sched-tmp`, and
`BREENIX_GATE_TMP=/root/breenix-855-sched-tmp/gate`, followed by
`bash docker/qemu/run-x86-boot-tests.sh 1`. Two SSH-origin fetch attempts
exited 128 before the gate ran (missing GitHub host-key trust); fetching
the public HTTPS URL with an explicit remote-tracking refspec succeeded.
No gate criteria changed. The [gate transcript](serials/855/x86-sched16384-gate.txt)
records the fetched SHA and final verdict.

Both `python3 scripts/claim-lint.py` and `python3 scripts/claim-lint.py
--commit-msg <drafted-message-file>` exited 0 for the implementation commit
and the validation-record commit. Binary snapshots are explicitly skipped
by claim-lint's extension filter. The source comparison against `686b959f`
confirmed the three scheduler event calls are byte-identical and the x86
stub is unchanged. Prior context sampling, oracle, and tick ordering code
were not modified in this addition.
