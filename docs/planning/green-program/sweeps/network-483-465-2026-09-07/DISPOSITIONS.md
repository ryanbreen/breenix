# Dispositions: #483, #465

Evidence checked on 2026-09-07. All commands below were run read-only unless explicitly described as future verification commands. No GitHub operation, source edit, commit, build, or VM run was performed. <!-- claim-lint:ok: every command this document cites is a read-only git/rg/bash inspection command; the same read-only session is also recorded in EVIDENCE.md -->

Path keys used throughout this document (each citation expands to an absolute preserved file):

- `V` = `/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/v483`
- `W` = `V/wt`; source paths are relative to this checkout.
- `N` = `V/evidence`.
- `O` = `/Users/wrb/fun/code/breenix/docs/planning/green-program/sweeps/input-gui-aarch64-2026-09-06/evidence`.
- `P` = `/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/astra/PLAN-LAYER-SWEEP.md`.

`git rev-parse HEAD` in `W` returned `891ae034f8c0bb5de0b9ad400cf6524a7e92645c`; `git status --short` returned no entries. `O/MAIN_SHA.txt:1` records the old sweep as `6346f2c5381776a0d64ba471538207a71c1cab40`. The old SHA in `V/EVIDENCE.md:5` omits its final `0`; running `git rev-parse` on that 39-character abbreviation returns the full SHA above. Below, **old** means `6346f2c5`, **current** means `891ae034`.

## #483 — KEEP

Literal acceptance text from the `body` field of `V/issue-483-full.json:1`:

> wait_stress reproducer committed; ordering audit committed; Linux-parity fix committed; 60s wait_stress has 0 stalls; 5x120s Parallels sweep passes; no compositor BlockedOnTimer fallback remains

### Clause-by-clause

1. **wait_stress reproducer committed — MET.** `git show df914fbe --stat` identifies `df914fbe7f0ef27e1ffb97a39eeebca829e0ee56`, includes the constituent title `test(kernel): F32c wait_stress reproducer for waitqueue race`, and lists `userspace/programs/src/wait_stress.rs`. `git merge-base --is-ancestor df914fbe HEAD` exited 0. `git ls-tree HEAD userspace/programs/src/wait_stress.rs` returns tracked blob `8a4d6dd83962331d60a0506c57e4c4fe5dbea63d`. At HEAD, that file has the 60-second default at line 15, waiter/waker loops at lines 56–77, stall detection and failure exit at lines 140–157, and the success marker after sampling at lines 174–180. `userspace/programs/Cargo.toml:334–335` registers the binary. This is an existing committed reproducer, not merely a proposed filename.

2. **ordering audit committed — MET.** The independently run `git show df914fbe --stat` includes the exact constituent title `docs(kernel): F32c waitqueue ordering audit` and `docs/planning/f32c-waitqueue/ordering-audit.md`. The document exists in HEAD as tracked blob `910ba93edc4eb0ab2f75494d76118fea8fde7976`. Its lines 22–28 identify the Linux v6.8 reference sites; lines 34–38 compare enqueue/state serialization, barriers, wake serialization, schedule-state checking, and finish behavior; lines 44–55 give the lost-wake interleaving; lines 64–79 specify the ordering repair. Code shape alone would not establish that an audit was committed. Here a separately named, substantive audit is present, and the commit's constituent-title list corroborates it. These titles are entries in the landed commit message, not a claim that each original constituent commit is separately reachable from HEAD.

3. **Linux-parity fix committed — MET.** `git show --format= df914fbe -- kernel/src/task/waitqueue.rs` shows the committed enqueue/state publication inside `with_waiters`, same-lock wake traversal, finish normalization, and schedule-state gate. The essential ordering remains at HEAD: `kernel/src/task/waitqueue.rs:63–92` encloses insertion and `block_current_for_io_with_timeout(None)` in the same waitqueue critical section; lines 110–130 additionally enclose the checked condition and optional timed publication; lines 225–229 implement that lock scope. `kernel/src/task/scheduler.rs:4134–4143` calls `block_current_for_io_publish`, which publishes `BlockedOnIO` at line 4071 and executes the full `SeqCst` fence at line 4106 before returning. `waitqueue.rs:155–170` wakes under the same lock; lines 137–153 remove the waiter and normalize state; lines 288–307 check for `BlockedOnIO` before scheduling, outside the waitqueue lock. `kernel/src/syscall/graphics.rs:1372–1394` uses prepare, condition recheck, schedule, and finish. This establishes the committed ordering/state-check implementation requested here; it does not certify every Linux waitqueue semantic or runtime gate. The current helper differs from the historical F32e publish-only helper and includes ready-queue removal (`scheduler.rs:4099–4101`), so it should not be described as byte-for-byte historical implementation or exact critical-section-cost parity. <!-- claim-lint:ok: the matched "None" here is Rust's Option::None literal inside `block_current_for_io_with_timeout(None)`, not an empirical universal claim; see kernel/src/task/waitqueue.rs -->

4. **60s wait_stress has 0 stalls — MET, for the preserved old-SHA run.** `O/run2-wait-stress/serial.log:590` reads `WAIT_STRESS_START duration=60s sample=100ms`; line 1539 reaches `WAIT_STRESS_PROGRESS sample=600 entered=2013805 returned=2013804 wakes=17409403 waiters=0`; line 1540 reads `WAIT_STRESS_PASS entered=2013817 returned=2013816 wakes=17409490 waiters=0`. A fresh full-file search for `WAIT_STRESS_PASS|WAIT_STRESS_FAIL|STALL` returned exactly line 1540; `STALL` count is 0. The old sweep command is recorded in `O/../EVIDENCE.md:180`, and `O/run2-wait-stress/stdout.log:507` confirms the 150-second test window. This is the 60-second workload within that window, not an assertion that 150 seconds of stress ran. The same run's preserved screenshot returns `VERDICT=PASS` when checked with `bash scripts/f24-render-verdict.sh O/run2-wait-stress/screenshot.png`. No current-SHA wait_stress receipt was supplied; this MET records the literal historical runtime clause, not a new execution at HEAD.

5. **5x120s Parallels sweep passes — NOT MET.** **No:** runs 1–5 at the old SHA contain only two PASS render verdicts (runs 2 and 5), with run 4 stalled; runs 6–7 attributed to the current SHA both return FAIL. The table below names the independently checked artifacts. Even permitting cross-SHA pooling gives only two PASS screenshots among seven attempts, never five passing runs. A completed service sequence and an empty fatal-marker grep do not establish the rendered-desktop gate. <!-- claim-lint:ok: 2 of 7 preserved screenshot verdicts are PASS, tabulated by run in the table below this section -->

6. **no compositor BlockedOnTimer fallback remains — MET at HEAD.** `rg -n 'BlockedOnTimer' kernel/src/syscall/graphics.rs userspace/programs/src/bwm.rs` returned no matches, exit 1. The actual compositor path was also read: `graphics.rs:1372–1394` waits on `COMPOSITOR_FRAME_WQ` through `BlockedOnIO`; `waitqueue.rs:76` supplies `None` for the timeout. `userspace/programs/src/bwm.rs:1629` calls `graphics::compositor_wait(0, registry_gen)`. This negative is scoped to the compositor fallback, not to unrelated legitimate timed waits elsewhere in the kernel. <!-- claim-lint:ok: the matched "None" here is the literal timeout argument in waitqueue.rs:76, not an empirical universal claim; see kernel/src/task/waitqueue.rs -->

Every screenshot verdict below was recomputed by running `bash scripts/f24-render-verdict.sh <directory>/screenshot.png` from `W`. PASS means that command returned `VERDICT=PASS`, exit 0; FAIL means `VERDICT=FAIL`, exit 1. The serial columns cite `<directory>/serial.log`. The test-window column cites `<directory>/stdout.log`, whose named line records the 120-second wait. These are render verdicts, not assertions that a PASS screenshot alone passes the entire run. <!-- claim-lint:ok: 7/7 rows in the table below this paragraph were recomputed the same way -->

| Run | SHA | Preserved directory | 120s receipt line | Service/progress evidence in serial.log | Screenshot verdict |
|---|---|---|---|---|---|
| 1 | old | `O/run3-test120-1` | 505 | init:391; bsshd:592; boot script:643; bounce:671; heartbeat:2144, `uptime_ms=175669` | FAIL; `distinct=1 dominant=(0, 0, 0)` |
| 2 | old | `O/run3-test120-2` | 80 | init:401; bsshd:602; boot script:653; bounce:679; heartbeat:1986, `uptime_ms=171641` | PASS; `distinct=2002`, coherent region `bbox_frac=0.4424` |
| 3 | old | `O/run3-test120-3` | 80 | init:391; bsshd:592; boot script:643; bounce:670; heartbeat:2030, `uptime_ms=139588` | FAIL; `distinct=1 dominant=(0, 0, 0)` |
| 4 | old | `O/run3-test120-4` | 80 | Last line 331: `[net] ICMP echo reply received from 10.211.55.1 seq=1`; no init/service/heartbeat/FPS lines | FAIL; `distinct=1 dominant=(100, 149, 237)`; also a stalled boot |
| 5 | old | `O/run3-test120-5` | 80 | init:402; bsshd:691; boot script:738; bounce:767; heartbeat:2172, `uptime_ms=201631` | PASS; `distinct=2002`, coherent region `bbox_frac=0.4424` |
| 6 | current, attribution caveat below | `N/run1-test120` | 601 | init:391; bsshd:592; boot script:647; bounce:671; heartbeat:2221, `uptime_ms=214672` | FAIL; `distinct=1 dominant=(0, 0, 0)` |
| 7 | current, attribution caveat below | `N/run2-test120` | 505 | init:391; bsshd:592; boot script:643; bounce:670; heartbeat:2265, `uptime_ms=189631` | FAIL; `distinct=1 dominant=(0, 0, 0)` |

### Verdict rationale

KEEP follows directly from clause 5. The committed reproducer, audits, implementation, historical stress pass, and compositor fallback removal cannot replace the missing five-run acceptance. This conclusion does not depend on adding an FPS threshold to the issue body, assuming the black captures prove a guest rendering defect, or requiring a particular interpretation of cross-SHA pooling.

### If KEEP: exact missing criterion + the command that would supply it

Clause 5 requires a five-run 120-second Parallels sweep in which every run passes its acceptance checks. A straightforward new receipt is five independent `./run.sh --parallels --test 120` runs at one recorded SHA, preferably `891ae034f8c0bb5de0b9ad400cf6524a7e92645c` for this evaluation. Preserve separate `stdout.log`, `serial.log`, `screenshot.png`, exact command/environment, and SHA/build identity before the next invocation overwrites shared `/tmp` artifacts. For each capture run `bash scripts/f24-render-verdict.sh <run-directory>/screenshot.png`; require PASS plus the service/progress evidence. Apply the assigned plan oracle's FPS/AHCI checks as distinguished below. Script exit status alone is insufficient. These are future commands, not commands executed by this read-only evaluation. A fresh `BREENIX_WAIT_STRESS=1 ./run.sh --parallels --test 150` at the same SHA would also remove the historical-only scope of clause 4. <!-- claim-lint:ok: "every run" names the pass bar precisely: 5 of 5 runs, per the P:1029 oracle cited later in this section -->

## #465 — KEEP

Literal contract from the `body` field of `V/issue-465-full.json:1`:

> Audit Breenix waitqueue blocking against Linux prepare_to_wait_event, implement the missing state-check-and-block behavior, validate wait_stress and Parallels gates, and merge only if all required gates pass. <!-- claim-lint:ok: this is a verbatim quotation of issue #465's body, not an assertion made in this document -->

### Clause-by-clause

1. **Audit against Linux prepare_to_wait_event — MET.** `git show df914fbe --stat` includes `docs(kernel): F32e audit Linux waitqueue parity` and `docs/planning/f32e-linux-prepare-to-wait/audit.md`. `git ls-tree HEAD` identifies that file as blob `719219c7a954eefae8527c1027f9758fae0a59e6`; the ancestor check for `df914fbe` exits 0. Its lines 16–21 name the Linux insertion/state/barrier/wake/schedule references, lines 27–29 compare Breenix behavior, and lines 36–46 specify the publication and scheduling split. This directly establishes this issue's own audit clause; it is not inferred solely from #483 or the shape of current code. The Linux citations here are evidence of the committed audit's contents, not a claim that this pass separately fetched and re-audited Linux v6.8.

2. **Implement the missing state-check-and-block behavior — MET.** At HEAD, `kernel/src/task/waitqueue.rs:70–85,110–130` publishes the blocked state while holding the queue lock, via the scheduler helper at `scheduler.rs:4134–4143`, with publication/fence at lines 4071/4106. `waitqueue.rs:288–307` checks current state and skips sleeping when it is no longer `BlockedOnIO`; the scheduling call is outside the queue lock. `waitqueue.rs:137–153` provides finish normalization. `graphics.rs:1383–1394` rechecks the condition after preparation and uses that scheduling helper. `git show --format= df914fbe -- kernel/src/task/waitqueue.rs` supplies the landed implementation, with the present helper evolution noted in #483 clause 3. This is implementation evidence, not runtime acceptance.

3. **Validate wait_stress and Parallels gates, and merge only if all required gates pass — NOT MET.** The shared old-SHA stress run establishes its stress leg: `O/run2-wait-stress/serial.log:590,1539–1540`, with the independent full-file search returning 0 STALL/FAIL matches. Its Parallels leg is not established: old-SHA runs 1 and 3 have FAIL render verdicts, run 4 stalls, and only runs 2 and 5 have PASS render verdicts; current-attributed runs 6 and 7 both have FAIL render verdicts (the exact files/commands are in the table above). `P:1029` explicitly assigns five independent Parallels runs to #465, and its own committed `docs/planning/f32e-linux-prepare-to-wait/exit.md:103–104` records a 5/5 prerequisite before PR/merge. The presence of landed code cannot substitute for that prerequisite. No preserved all-gates-pass receipt, or record establishing that such a receipt preceded landing, was identified.

### Verdict rationale

KEEP follows from #465's own validation/merge condition. Its audit and implementation are present; its required Parallels validation lacks a passing sweep. The shared runs genuinely exercise the relevant waitqueue/compositor system, but they contain unsuccessful acceptance observations. #465 does not inherit a PASS merely because its implementation overlaps #483.

### If KEEP: exact missing criterion + the command that would supply it

Clause 3 lacks an all-required-gates-pass receipt. Run `BREENIX_WAIT_STRESS=1 ./run.sh --parallels --test 150` and five independent `./run.sh --parallels --test 120` invocations on one recorded SHA, with separate receipts and render checks as specified for #483. Require the 60-second stress completion with 0 STALL markers and five accepted Parallels runs, including the assigned `P:1029` oracle. These commands supply prospective runtime evidence only; they cannot prove that acceptance preceded an already historical landing. If that temporal part is required literally, recover the original pre-landing gate receipts and their SHA/timestamps as well. Git ancestry alone does not supply them. <!-- claim-lint:ok: git ancestry cited in this document establishes code presence only, not gate timing; see the landing prerequisite recorded in docs/planning/f32e-linux-prepare-to-wait/exit.md -->

## Cross-issue notes

**Literal AC versus assigned oracle.** Parsing both JSON bodies and searching for `FPS|160|AHCI|fatal|same.*SHA|consecutive` returned no matches. Neither issue literally names FPS ≥160 or an AHCI/fatal-marker checklist. However, the claim in `V/EVIDENCE.md` that these appear only in other issues' plan sections is incorrect: `P:1021–1029` and `P:1133–1141` explicitly index #465 and #483. `O/PLAN-LAYER-SWEEP.md.orig` also has both sections at those same lines, in addition to #447/#452 at lines 939/959. The requested plan's oracle at `P:1029` and `P:1141` is identical:

> `BREENIX_WAIT_STRESS=1 ./run.sh --parallels --test 150` must emit WAIT_STRESS_PASS/no STALL; Five independent `./run.sh --parallels --test 120` runs, each with actual service lifecycle, CPU0 progress, rendered desktop and original FPS ≥160 criterion, no AHCI timeout/fatal marker. A script exit alone does not establish these properties.

Thus the numeric/fatal checklist is plan-level verification methodology, also reflected in the original F32 acceptance tables (`docs/planning/f32c-waitqueue/exit.md:93–101`; `docs/planning/f32e-linux-prepare-to-wait/exit.md:83–90`). It is not additional verbatim issue-body text, nor is it confined to #447/#452. The KEEP decisions already follow from the missing passing sweep without adjudicating whether every transient FPS sample must exceed 160. <!-- claim-lint:ok: 13 of 173 FPS samples in run 1 alone fall below 160, tabulated later in this section -->

For completeness, fresh full-file parsing of `[bwm-fps] ... fps=<number>` in the seven serial logs in the run table yielded min/max/sample-count/count-below-160: run 1 `131/244/173/13`; run 2 `141/270/153/4`; run 3 `183/253/137/0`; run 4 no samples; run 5 `137/261/178/17`; run 6 `185/248/212/0`; run 7 `180/253/186/0`. A fresh `AHCI.*[Tt]imeout|FATAL|PANIC|DATA_ABORT|SOFT_LOCKUP` search returned 0 matching lines for each serial log. Run 4 never reached service operation, so its empty grep is not a passing fatal-free workload. Runs 1 and 3 also report `block_eintr_oracle exited pid=3 code=1` at their respective `serial.log:425`; this limits any blanket characterization of their serial logs as healthy. Runs 6/7 report code 0 at their respective line 427. Heartbeat progress does not independently prove a CPU0-specific progress threshold.

**SHA pooling.** The issue text does not explicitly require the five runs to share a SHA or use the word consecutive. A single-SHA batch is the clearest reproducible sweep; pooling might be defensible with demonstrated relevant behavior and configuration equivalence. It is not demonstrated by this two-file history alone. I reran:

```sh
git log --oneline 6346f2c5381776a0d64ba471538207a71c1cab4..891ae034f8c0bb5de0b9ad400cf6524a7e92645c -- kernel/src/task/waitqueue.rs kernel/src/task/scheduler.rs
```

It returned six entries, in order: `e4ce506b`, `e63edadb`, `504d3667`, `2653a373`, `ce56e44d`, `1c9f0f53`. Restricting the command to `waitqueue.rs` returned no entries; `git diff --quiet 6346f2c5 891ae034 -- kernel/src/task/waitqueue.rs` exited 0. `git rev-list --count 6346f2c5..891ae034` returned 71. The scheduler net diff is 157 insertions/17 deletions, concerning diagnostic sampling/padding and pin-oracle attribution rather than the enqueue/state ordering. But the assertion that the delta is entirely `boot_tests`-only is false: `scheduler.rs:271–272,301–305,318–330` gates sampling/counters on AArch64 alone. Only the special pin-oracle attribution has the additional test-feature gate. Diagnostic overhead can affect timing; unchanged ordering does not establish identical observed behavior. `git diff --name-only 6346f2c5 891ae034 -- kernel/src/arch_impl/aarch64/context_switch.rs` also returns that context-switch file. I therefore do not count these as one demonstrated behavior-equivalent sweep. Even granting pooling, two PASS render verdicts cannot satisfy five passes. <!-- claim-lint:ok: kernel/src/task/scheduler.rs:271-272,301-305,318-330 shows the counter-example lines this correction cites -->

**Artifact attribution and prior-summary corrections.** Old run attribution is backed by `O/MAIN_SHA.txt:1` and the old sweep's command records at `O/../EVIDENCE.md:180,205`. For runs 6/7, `V/EVIDENCE.md:140–141,171–172` supplies the current-SHA/run mapping; their stdout VM names match at `N/run1-test120/stdout.log:531` and `N/run2-test120/stdout.log:435`. Run 6's build output at line 46 names this worktree's kernel directory, and HEAD was independently confirmed. I did not find a separate preserved full-SHA build manifest for these two runs, so their exact built-SHA attribution remains a supplied run-record claim rather than independently authenticated binary identity. Their FAIL screenshots are independently reproducible regardless.

`O/run3-test120-4/NOTE.txt:1` records repeated observations of the 331-line stall; the serial artifact itself ends at that point. `V/EVIDENCE.md:138` attributes it to #906, but I did not independently verify GitHub filing/status and do not infer a waitqueue root cause from this log. The asserted adjacent-issues quotation at old `DISPOSITIONS.md:857` could not be corroborated: a fresh search of `O/../DISPOSITIONS.md` found no `#465`, `#483`, or `#906`. Its 19 per-issue headers and those in old `EVIDENCE.md` indeed omit #465/#483; the archived plan does not omit them.

The window-not-found message exists in both successful and unsuccessful captures (`stdout.log:508` for run 1, `:83` for old runs 2–5, `:604` for run 6, `:508` for run 7). Its presence does not establish the cause of the split. Runs 1/6/7 have identical 3,674-byte PNGs with SHA-256 `17746976ba81ea8955008906765c790b2a957209d85f28c3ba256ea339276bd5`; run 3's black PNG is 5,132 bytes with a different hash. Therefore the prior claim that every FAIL screenshot has identical size/bytes is incorrect. A capture-path problem is a possibility, not a demonstrated explanation or an acceptable substitute for a passing capture. Run 1's actual last heartbeat is `O/run3-test120-1/serial.log:2144` (`uptime_ms=175669`), corroborating the correction to the old summary's `163664` value.
