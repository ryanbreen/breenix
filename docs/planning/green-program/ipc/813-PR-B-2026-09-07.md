# 813 PR B audit and continuation — 2026-09-07

Status: implementation pushed; merge-resolution evidence is in the final Landing subsection. Historical gate results below retain their original revisions. Starting revision was
`4eac269d9fe4fff53fdb3391200635bd4888bc87` on
`ipc/813-pr-b-unix-stream-write-waiters`. This round independently read commits
50e55a2c, a59131d6 and 4eac269d, including their artifacts. The inherited round
text is retained in `serials/813-b/inherited-round-doc.txt`; its statement that
no implementation was delivered became stale when 4eac269d was added.

## Contract audit

| Contract item | Inherited implementation | Audit disposition |
|---|---|---|
| Full-buffer wait | write_unix uses the shared wait_prepared helper, retains buffer and closed guards through publication, drops guards before waiting | Retained; ARM six-arm gate passes below |
| Descriptor mode | sys_write snapshots descriptor O_NONBLOCK; socketpair initializes this flag from SOCK_NONBLOCK | Retained; construction and F_SETFL controls exist |
| Partial progress | Positive copy returns its count before another close check | Retained; no stream atomicity assertion |
| Drain | Read releases receive buffer before waking the opposite endpoint writer queue | Retained |
| POLLOUT/HUP | poll calls has_write_space and retains HUP; public predicate delegates to guarded transmit state | Retained; added direct predicate ratchet |
| Six oracle legs | Fork precedes named listener/connect/accept; six scored arms, both directions, signal variants, byte comparisons, reap | Read independently; six ARM arms pass; 22 valid single-fault mutations reject |
| Close census | Ordinary close/dup2/exec/exit extract ownership, but legacy terminate uses wake_up_deferred and close still wakes readers inline | Replaced Unix notification delivery; original blocker was real |
| Ratchets | Mode, drain, publication, gate arms and deferred close source checks | Kept correct checks; replaced the check that pinned the unsafe legacy route |
| Round evidence | Two docs commits describe a reverted draft; code commit has no accepted runtime evidence | Historical artifacts retained; no inherited pass adopted |

## Close-delivery decision

The plan's objection is real: the inherited deferred waiter delivery calls
isr_unblock_for_io, whose full-buffer thread-context branch acquires the
scheduler. The inherited Unix close also wakes readers before returning its
owned writer notification. Neither establishes the requested PM-free boundary.
Issue 936 remains open for the inherited pipe/FIFO and ISR-buffer concerns.

The candidate returns an owned pair notification for both reader EOF and
writer EPIPE. Legacy terminate and FD-table destruction enqueue the pair via
an intrusive link allocated as part of the pair. Duplicate pending closes
coalesce. Enqueueing takes no scheduler lock, allocates no storage, and uses
no fixed-capacity ISR wake buffer. Existing PM-free reclamation entry points
on both architectures drain at most 64 pairs per pass. Unprocessed pairs
remain owned by the queue. Draining clears queue membership before observing
closed flags; a concurrent later close either coalesces before that observation
or queues another notification. Object and queue locks are dropped before
scheduler delivery. Liveness depends on the existing reclamation cadence.

No Tier-1 or Tier-2 file is edited. No dispatch/interrupt implementation is
changed. The change does not settle the pipe/FIFO portion of issue 936 or
Unix endpoint last-reference ownership.

## Per-arm evidence

| Leg | Acceptance | ARM observed bytes / result | x86 observed bytes / result |
|---|---|---|---|
| Backpressure A/B | Writer queue witness; drain 4096; 4097 request returns 4096; full wire comparison | 139264 / PASS | 139264 / PASS |
| Mode | F_SETFL and SOCK_NONBLOCK full-buffer EAGAIN; clearing flag reaches wait | 139264 / PASS | 139264 / PASS |
| Poll A/B | Full transmit suppresses POLLOUT; one-byte drain restores it; positive write; HUP after close | 131074 / PASS | 131074 / PASS |
| Peer close A/B | Actual close after queue witness; EPIPE; waiter/state cleanup; reap | 0 / PASS | 0 / PASS |
| Signal | Caught EINTR; ignored stays queued; cleanup; recovery write and bytes | 270338 / PASS | 270338 / PASS |
| Partial-space | Both modes and directions return and transmit 4096 of 4097 | 262144 / PASS | 262144 / PASS |

ARM evidence: `serials/813-b/accepted-arm-oracle/boot_1/serial.txt`;
x86 evidence: `serials/813-b/accepted-x86-oracle/boot_1/serial.txt`.
Each architecture reports six passed, zero failed, worker reaped with status 0.

The 4096 boundary is a transfer/count control. It does not claim pipe-style
atomicity for Unix streams.

## Revision-scoped checks

Code commit `69c039a06ef31cdc60790b77211a0dff303c57d3` contains the close repair
and its ratchets. Code commit `40e2925f32800702e3dfd42d31d0fe63760cf65c` adds the
merged 927 prerequisite and matching scorer/structure fixtures. Evidence is
not represented as a single final-HEAD run. The second commit changes kernel
source only inside two x86-only fork helpers; the ARM source-equivalence
comparison is in `serials/813-b/927-arm-source-equivalence.txt`.

| Command | Revision | Result and transcript |
|---|---|---|
| `bash scripts/run-structure-tests.sh` | 69c039a0 | PASS 103/103; `serials/813-b/proof-structure.txt` |
| `bash docker/qemu/run-blocking-io-oracle-gate.sh --arch aarch64 --program unix_stream_blocking_oracle` | 69c039a0 | PASS six arms, one boot, preflight 65/65; `serials/813-b/proof-arm-oracle.txt` |
| `bash docker/qemu/run-aarch64-boot-test-strict.sh 3` | 69c039a0 | PASS 3/3, preflight 65/65; `serials/813-b/proof-arm-strict.txt` |
| `bash docker/qemu/run-x86-boot-tests.sh` on beast | 40e2925f | PASS 1/1, preflight 66/66; `serials/813-b/proof-x86-full.txt` |
| `bash docker/qemu/run-blocking-io-oracle-gate.sh --arch x86_64 --program unix_stream_blocking_oracle` on beast | 40e2925f | PASS six arms, one boot, preflight 66/66; `serials/813-b/proof-x86-oracle.txt` |
| `bash docker/qemu/run-aarch64-prod-profile-boot-test.sh` LAST | 40e2925f | PASS 1/1 after one preflight-only timeout; retry preflight 66/66 at timeout 900; `serials/813-b/proof-arm-prod.txt` |

The saved ARM oracle was also rescored with the new scorer, passing six arms
(`serials/813-b/arm-oracle-rescore-927.txt`). This was not a new boot.
The new Unix fixture rejects seven altered evidence cases, including a fault
appended to benign GDT initialization text (`serials/813-b/unix-scorer-structures.txt`,
10/10 tests). Only two exact initialization substrings are removed before
fault scanning; the Unix arm, byte-count, summary and reap checks remain.

Issue 927 was OPEN at the first two reads, then closed at 21:22:49 UTC during
this round when PR 944 merged. The later state and closure explanation are
in `serials/813-b/issue-927-closed.json`. The x86 six-arm gate passed with that prerequisite present
(`serials/813-b/proof-x86-oracle.txt`). The branch lacked the merged repair,
so the two x86 fork-helper corrections and their ratchets were backported
from ba5a3377, along with the FIFO-coordinated capture fixture from 62988b29.
The fork ratchet failed before the manager repair; afterward the x86 suite
passed 10/10 and capture suite 6/6 (`serials/813-b/927-prerequisite-red.txt`,
`serials/813-b/927-prerequisite-structures.txt`). The x86 oracle is required;
it is no longer described as blocked by 927.

The first production-gate invocation stopped in preflight after the 300-second
capture-drain suite timeout: 65/66 suites passed, and no production kernel
build or guest launch occurred (`serials/813-b/proof-arm-prod-preflight-timeout.txt`,
`serials/813-b/preflight-arm-prod-timeout/`). The enabled fixture had appended
its END record but its shell remained blocked. Standalone traced reproductions
completed; the precise cause is not established. Host-load and reproduction
transcripts are retained. The sole permitted retry uses unchanged source
with `BREENIX_STRUCTURE_SUITE_TIMEOUT_SECS=900`; preflight remains enabled.
The retry passed preflight 66/66 and the production boot 1/1. The captured
host-load snapshot after the first timeout was 86.49 (one-minute average).
The capture-drain suite passed on that retry. The unexplained intermittent
fixture stall is tracked in 957, not attributed to the Unix kernel.

## Mutation sensitivity and retained attempts

| Arm | Single-fault cases | Result |
|---|---|---|
| Backpressure | immediate_eagain, delete_drain, delete_read_wake, swap_writer_queues, full_count | 5 assertion rejections |
| Mode | construction_only, ignore_nonblock_sleep, mode_unconditional_eagain | 2 assertion rejections; ignoring nonblock sleeps and times out after arm start |
| Poll | poll_peer_only, poll_receive_space (receive-data presence via self.has_data(), not receive capacity), reverse_transmit_direction | 3 assertion rejections |
| Peer close | close_readers_only, close_wrong_writers, outer_lock_across_wait, peer_close_fake_success | 3 assertion rejections; retained outer lock times out after arm start |
| Signal | ignore_interruption, ignored_pending_interrupts, leak_registration, unrelated_waiter_list | 4 assertion rejections |
| Partial-space | stream_atomicity, positive_space_eagain, partial_full_count | 3 assertion rejections |

These are 22 valid individual fault boots: 20 explicit rejections and two
bounded liveness failures after the named arm-start marker. Ignoring the
nonblocking flag and sleeping in that control are the same injected defect.
Each case retains its patch, revision, build, launch and serial under
`serials/813-b/mutations/<case>/`; the machine-readable inventory is
`serials/813-b/mutations/results.json`. Cases use starting revision 4eac269d
plus the recorded candidate delta and one fault, not exact final HEAD.
The final reclamation-bracket placement followed these experiments and was
checked with a separate source RED/GREEN test before the accepted ARM gates.
Every mutation was reverted before an acceptance gate. A saved-serial
classification check matched 22/22 cases
(`serials/813-b/mutation-evidence-check.txt`); the two timeout cases require
the arm-start marker, while early explicit-failure cases predate that marker.

One extra threshold-only stream mutation rejected the initial fill rather
than the intended partial-space transfer. Its launcher had already started;
lane-owned QEMU PID 57819 was terminated and the source restored. Its record
`serials/813-b/mutations/stream_atomicity_draft_aborted/` is excluded from the
22 valid cases. A read-wake selector initially matched two locations; the
harness stopped, restored source, and resumed after narrowing the selector.
The first close-boundary GREEN attempt also failed on a trailing-comma
recognizer mismatch; both that failure and the corrected pass are retained.

The latest instruction to run each listed mutation singly expanded the
plan's proposed four-mutation allowance. The optional budget question got no
response; continuation followed the latest explicit mutation instruction.
This round does not claim compliance with the older four-mutation or
25-boot profile proposal. Boot arithmetic for this audit: ARM 22 valid mutation boots + one aborted
draft + one preparatory diagnostic + one accepted oracle + three strict +
one production = 29 launches (28 boot_tests, one production). Beast had
one full regression boot and one Unix oracle boot = two launches. The
production preflight timeout adds zero launches. Inherited draft artifacts
are historical and are not included in this audit-run count.
A preparatory ARM diagnostic ran six unmutated arms and reaped status 0
(`serials/813-b/probe/serial.txt`); it had no official preflight and is not
substituted for the accepted oracle gate.

## Source anchors re-derived at implementation HEAD

The source anchors were re-derived at corrected implementation commit
`3f29bc7bf0d166ef75e5b7c84a1049250d195c29`.
The 301 file-qualified source excerpts across 25 files are retained in
`serials/813-b/landing/merged-tip/source-census.txt`. Earlier censuses remain
historical evidence. The source comparison is repeated after the docs commit.

| Subject | Current source anchor |
|---|---|
| A2 aggregate writev and FIFO no-writer behavior (retained) | `kernel/src/syscall/iovec.rs:26`, `kernel/src/syscall/iovec.rs:54`, `kernel/src/syscall/iovec.rs:85`; `kernel/src/ipc/fifo.rs:62`, `kernel/src/ipc/fifo.rs:69` |
| Shared prepared wait inherited from PR A | `kernel/src/syscall/blocking_io.rs:16`; Unix guarded publication `kernel/src/syscall/blocking_io.rs:115`, `kernel/src/syscall/blocking_io.rs:137`, wait after scope `kernel/src/syscall/blocking_io.rs:141` |
| Descriptor write mode and F_SETFL | `kernel/src/syscall/handlers.rs:464`, `kernel/src/syscall/handlers.rs:522`, `kernel/src/syscall/handlers.rs:4001`; `kernel/src/ipc/fd.rs:592` |
| Direction guard, predicate, copy | `kernel/src/socket/unix.rs:314`, `kernel/src/socket/unix.rs:320`, `kernel/src/socket/unix.rs:328`, `kernel/src/socket/unix.rs:339`, `kernel/src/socket/unix.rs:352` |
| Read unlock and peer-writer wake | `kernel/src/socket/unix.rs:120`, `kernel/src/socket/unix.rs:156`, `kernel/src/socket/unix.rs:159`, `kernel/src/socket/unix.rs:162` |
| POLLOUT, retained HUP, select/epoll reuse | `kernel/src/ipc/poll.rs:267`, `kernel/src/ipc/poll.rs:277`, `kernel/src/ipc/poll.rs:283`, `kernel/src/ipc/poll.rs:329`; `kernel/src/syscall/epoll.rs:350` |
| Named endpoint creation; construction flag to descriptor | `kernel/src/syscall/socket.rs:1692`, `kernel/src/syscall/socket.rs:1904`, `kernel/src/syscall/socket.rs:1909`, `kernel/src/syscall/socket.rs:1915` |
| Oracle fork precedes endpoints | `userspace/programs/src/unix_stream_blocking_oracle.rs:184`, `userspace/programs/src/unix_stream_blocking_oracle.rs:197`, `userspace/programs/src/unix_stream_blocking_oracle.rs:202`, `userspace/programs/src/unix_stream_blocking_oracle.rs:208`, `userspace/programs/src/unix_stream_blocking_oracle.rs:213` |
| Witness, cleanup, construction control, byte comparison | `userspace/programs/src/unix_stream_blocking_oracle.rs:140`, `userspace/programs/src/unix_stream_blocking_oracle.rs:152`, `userspace/programs/src/unix_stream_blocking_oracle.rs:167`, `userspace/programs/src/unix_stream_blocking_oracle.rs:365`, `userspace/programs/src/unix_stream_blocking_oracle.rs:373` |
| Boot-only witness and dispatch | `kernel/src/syscall/mod.rs:31`; `kernel/src/syscall/ioctl.rs:54`; `kernel/src/syscall/blocking_io_oracle.rs:89` |
| Six-arm gate and strict scorer | `docker/qemu/run-blocking-io-oracle-gate.sh:70`, `docker/qemu/run-blocking-io-oracle-gate.sh:75`, `docker/qemu/run-blocking-io-oracle-gate.sh:214`; `scripts/score-blocking-io-oracle.py:18` |
| Owned close, deferred queue, bounded drain | `kernel/src/socket/unix.rs:206`, `kernel/src/socket/unix.rs:237`, `kernel/src/socket/unix.rs:266`, `kernel/src/socket/unix.rs:281` |
| Normal close releases PM; Unix delivery | `kernel/src/syscall/pipe.rs:154`, `kernel/src/syscall/pipe.rs:156`, `kernel/src/syscall/pipe.rs:248` |
| dup2 / cloexec ownership / FD Drop | `kernel/src/ipc/fd.rs:399`, `kernel/src/ipc/fd.rs:576`, `kernel/src/ipc/fd.rs:699`, `kernel/src/ipc/fd.rs:760` |
| Two legacy descriptor-cleanup branches | `kernel/src/process/process.rs:597`, `kernel/src/process/process.rs:680` |
| terminate caller census | `kernel/src/process/manager.rs:1947`; `kernel/src/interrupts/context_switch.rs:1400`; `kernel/src/signal/delivery.rs:239`, `kernel/src/signal/delivery.rs:274` |
| PM-free extracted close and reclamation bracket | `kernel/src/task/process_task.rs:689`, `kernel/src/task/process_task.rs:724`, `kernel/src/task/process_task.rs:1202`, `kernel/src/task/process_task.rs:1222`, `kernel/src/task/process_task.rs:1238` |
| Existing architecture reclamation callers | `kernel/src/interrupts/context_switch.rs:1792`; `kernel/src/arch_impl/aarch64/context_switch.rs:6621` |
| Unsafe inherited deferred-wake fallback, still relevant to 936 | `kernel/src/task/waitqueue.rs:182`; `kernel/src/task/scheduler.rs:6521`, `kernel/src/task/scheduler.rs:6538`, `kernel/src/task/scheduler.rs:6540` |
| 927 fork helpers, both x86-only | `kernel/src/process/manager.rs:2184`, `kernel/src/process/manager.rs:2272`, `kernel/src/process/manager.rs:2280`, `kernel/src/process/manager.rs:2942`, `kernel/src/process/manager.rs:3033`, `kernel/src/process/manager.rs:3041` |

The four changed kernel files pass `rustfmt --check --edition 2021` at
40e2925f (`serials/813-b/rustfmt-check.txt`, exit 0).

## Build and workflow disclosures

ARM builds emitted the pinned-nightly core NEON future-incompatibility
notice tracked under 559. The inspected Cargo report contains only that
warning class; the kernel instruction guard counted zero FP/SIMD instructions.
claim-lint:ok: 559; `serials/813-b/candidate-build.txt` and the accepted ARM gate build/future-incompatibility transcripts.
No warning suppression was added. This is not a warning-free ARM build.
ARM userspace preparation installed 153 binaries
(`serials/813-b/userspace-build.txt`). Beast preparation and the final x86
prerequisite build exited 0 with zero warning/error compile diagnostics
(`serials/813-b/beast-userspace-build.txt`,
`serials/813-b/beast-927-prerequisite-build.txt`).

The configured Git hook unexpectedly exported Beads state to the shared
repository on the first code commit. No bd command was deliberately invoked,
and that shared file was not reverted or otherwise edited. Subsequent Git
operations disable hooks with `-c core.hooksPath=/dev/null`. No other lane's
worktree or QEMU process was modified; only verified owned PIDs were stopped.
No stashes were created or shared stashes cleared.

The x86 Unix build emitted zero warning/error compile diagnostics
(`serials/813-b/proof-x86-oracle.txt`). Its preflight completed 66/66 without retry; its context-restore
suite was the final finisher. The launch then waited for the shared host
QEMU lock; no other lane was interrupted.

The x86 full gate retains its existing single-CPU IRQ/UDP contention skips;
those are visible in `serials/813-b/proof-x86-full.txt` and are not Unix
write-arm acceptance substitutes.

## Not claimed

- A uniform revision across gate evidence, or x86 mutation sensitivity.
- Unix endpoint last-reference semantics across fork/dup-close or global flag sharing.
- Read-mode repair, stream atomicity, SIGPIPE generation, console, or other socket families.
- Independent resolution of 927, the pipe/FIFO and ISR-buffer concerns in 936, or closure of 813.
- Exhaustive legacy deferred-close saturation/fault stress or endpoint last-reference repair.
- A warning-free ARM toolchain build, or resolution of fixture timeout 957.

claim-lint: python3 scripts/claim-lint.py -> exit 1

The first audit tree lint required a source for the FP/SIMD count; the revised
paragraph names the build evidence and toolchain issue.

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr-b-code-commit.txt -> exit 0

This invocation covers 69c039a0, as mapped by
`serials/813-b/code-commit-messages.txt`. No retained commit-message lint
invocation or status identifies inherited implementation commit 4eac269d;
commit-message lint coverage for that commit is not claimed.

The final close-drain placement uses the existing preemption and drain-owner
bracket in process_task. Its additional ratchet failed before relocation and
passed afterward (`serials/813-b/close-bracket-red.txt`,
`serials/813-b/close-bracket-green.txt`). The updated x86 build completed with
exit 0 and 0 warning/error diagnostics
(`serials/813-b/beast-candidate-build-final.txt`). The restored Unix structure
suite passed 9/9 (`serials/813-b/unix-structure-final.txt`).

The 927 prerequisite ratchet failed against the unchanged manager and then
passed after the upstream repair. The manager diff is confined to two
x86-only function bodies (`serials/813-b/927-arm-source-equivalence.txt`).
The shared scorer retains the six Unix counts and passes the saved ARM boot
(`serials/813-b/arm-oracle-rescore-927.txt`); that is a rescore, not another
boot. The new Unix scorer fixture rejects seven altered evidence cases,
including a fault appended to a GDT initialization message
(`serials/813-b/unix-scorer-structures.txt`).

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr-b-927-commit.txt -> exit 1

The prerequisite commit-message draft used an unsupported wording;
its replacement describes the gate sequence without adding an acceptance claim.

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr-b-927-commit.txt -> exit 0

The prerequisite x86 build completed with exit 0 and 0 warning/error compile
diagnostics (`serials/813-b/beast-927-prerequisite-build.txt`).

claim-lint: python3 scripts/claim-lint.py -> exit 1

The last pre-commit tree lint found wording in the uncommitted round doc
(`serials/813-b/claim-lint-927-tree-failure.txt`). The shell did not stop,
and code commit 40e2925f proceeded; that documentation was not in the commit.
The wording is corrected here. This failure is retained rather than
represented as a clean pre-commit tree run.

claim-lint: python3 scripts/claim-lint.py -> exit 1

A later documentation draft triggered two absolute-wording checks; the
source-table label and negative-claim wording were corrected.

claim-lint: python3 scripts/claim-lint.py -> exit 1

The first wording revision still matched the absolute-wording rule; revised again.

claim-lint: python3 scripts/claim-lint.py -> exit 0

Lint scope is changed hunks against the branch merge base, not whole-file
repository coverage. The tool reports 195 pre-existing findings outside that
scope and lists skipped extensions; retained raw transcripts are evidence,
not independently linted prose (`serials/813-b/claim-lint/docs-draft-final.txt`).

The final production run reached bsshd, rejected boot-only futex seams, and
reported zero crash markers (`serials/813-b/proof-arm-prod.txt`). No kernel
build or boot followed it within that prior audit round. Both prior lane-owned QEMU censuses are empty
(`serials/813-b/mac-qemu-cleanup.txt`, `serials/813-b/beast-qemu-cleanup.txt`).

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr-b-docs-commit.txt -> exit 0

claim-lint: python3 scripts/claim-lint.py -> exit 0

The evidence subtree has a scoped `-text` attribute in `.gitattributes`;
Git staging was checked against the original file bytes after disabling
CRLF conversion for these transcripts.

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr-b-docs-commit.txt -> exit 0

## Landing

The requested deferred code findings list is empty. V-1 corrects the Poll
mutation description inline to receive-data presence; V-2 limits the retained
code-message lint invocation to 69c039a0 and discloses missing coverage for
4eac269d. Landing gates and merge disposition will be recorded below.

claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/ipc/813-PR-B-2026-09-07.md -> exit 0

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/prose-commit.txt -> exit 0

Landing disposition: NOT LANDED. After prose correction commit 4108bc94,
`git fetch origin` succeeded. The merge attempt used
`git -c core.hooksPath=/dev/null merge --no-ff --no-commit origin/main`
so lint could precede a merge commit. It stopped with conflicts in
`docker/qemu/run-blocking-io-oracle-gate.sh`,
`scripts/score-blocking-io-oracle.py`, and
`tests/x86_boot_tests_profile_structure.rs`. The docker conflict triggers
the requested stop condition; the merge was aborted without conflict
resolution. Transcript: `serials/813-b/landing/merge-conflict.txt`.

No merged-tip structure run, R182 fixture re-record, ARM strict boot, ARM
Unix oracle gate, or beast x86 boot was attempted in this landing round.
No PR was created or merged, branch deleted, worktree removed, or issue
813 comment posted. The historical arm tally above is not a landing-tip
result. No landing success, merge SHA, or main-branch log status is claimed.

The fetched main revision for this attempt was
`3a8a973b013dbbee09f17c660c0f310ecdf3640e`; the candidate was
`4108bc944d72c3a6691ef298caaaea0db7b2160b`. No QEMU process was
launched by this landing round, so there is no lane-owned guest to stop.

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/blocked-commit.txt -> exit 0


### Merge-resolution continuation (lane i813e)

Main `3a8a973b013dbbee09f17c660c0f310ecdf3640e` was merged into
branch `ipc/813-pr-b-unix-stream-write-waiters` from `3419d2a0`.
Merge commit: `4c93a601a4d3f602823a346ed4f521cdd940a587`.
The old i813d worktree was absent; this lane already had the requested branch.
No other lane was removed or interrupted.

| Conflicted file | Resolution |
|---|---|
| `docker/qemu/run-blocking-io-oracle-gate.sh` | Main startup window and pipe/FIFO path, plus PR B Unix program selection, six arms, installation, result detection and scorer dispatch |
| `scripts/score-blocking-io-oracle.py` | Main pipe/FIFO scoring and exact GDT filtering, plus PR B Unix six-arm counts, summary and worker-reap validation |
| `tests/x86_boot_tests_profile_structure.rs` | Main version without local edits |

`docker/qemu/run-x86-boot-tests-only.sh`, the fork helpers in
`kernel/src/process/manager.rs`, `tests/x86_boot_tests_profile_structure.rs`,
and the FIFO fixture `tests/gate_capture_drain_structure.rs` match main's
versions byte-for-byte. No conflict occurred in the manager, runner, or FIFO
fixture. PR B's Unix kernel, blocking_io and oracle implementation is retained.
This continuation adds no Tier-1 or Tier-2 edits or logging.

The first ARM Unix gate at `4c93a601` stopped before build/guest launch:
66/67 preflight suites passed; the main x86 fixture requires the original
pipe/FIFO scorer command. The merged gate supplied a program option for both
programs. Commit `3f29bc7bf0d166ef75e5b7c84a1049250d195c29` preserves the
main command for pipe/FIFO and adds a separate Unix branch. The unchanged
ratchet was red before and green after that correction (10/10); the Unix
suite also passed 10/10. This was a source mismatch, not a timeout retry.
The code and inherited ratchet remain together in the merged history.

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/merge-message.txt -> exit 0

The preceding two checks cover the merge commit. The merge-wide whitespace
check also reported whitespace in imported main evidence transcripts; those
upstream transcript bytes were retained.

claim-lint: python3 scripts/claim-lint.py -> exit 1

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/fix-message.txt -> exit 0

The correction commit proceeded after the tree check failed on an uncommitted
doc reference to the not-yet-copied source census. That doc was excluded from
the code commit. The census was subsequently copied and the tree check passed.
A clean pre-commit tree check for the correction commit is not claimed.

claim-lint: python3 scripts/claim-lint.py -> exit 0

Merged-tip runtime results follow when collected. Evidence is retained under
`serials/813-b/landing/merged-tip/`. Historical mutations were not repeated;
this continuation injected no mutations.

The corrected ARM Unix attempt at `3f29bc7b` then timed out during the
300-second capture-drain preflight suite: 66/67 completed successfully and
no guest launched. One retry uses `BREENIX_STRUCTURE_SUITE_TIMEOUT_SECS=900`;
preflight is enabled. This repeats the kind of fixture stall tracked in 957;
the cause is not established by the host-load snapshot alone.

The beast full-gate invocation began at `4c93a601`; the lane was fast-forwarded
to `3f29bc7b` during preflight, before the kernel build. That transitional
invocation is retained separately and is not a uniform-revision acceptance
run. A separate full gate starts at the corrected tip.


ARM pipe/FIFO passed 30 arms with matching byte tallies and worker status 0
at `3f29bc7b`, preflight 67/67 (`serials/813-b/landing/merged-tip/arm-pipe.txt`).
ARM strict failed 0/1 at that revision, preflight 67/67
(`serials/813-b/landing/merged-tip/arm-strict.txt`). The serial file
`serials/813-b/landing/merged-tip/mac-artifacts/breenix_aarch64_strict_1/serial.txt`
reports `pm_blocking_acquires=0` and `pm_busy_probe=0`;
the generic test-failure sentence is not evidence of a blocking acquisition.
The predicate in `kernel/src/test_framework/registry.rs:6935` requires
`pm_busy_probe == 1`. The foreground IRQ peer-hold oracle also failed.
These runtime readings do not establish the root cause.

The transitional x86 full run panicked in
`kernel/src/task/softirq_tests.rs:228`, on the assertion that ksoftirqd itself
processed deferred work. Issue 891 remains open with this assertion and
location. This is not classified as an IO/SCSI failure under 871.

A diagnostic-only system-Bash fixture run (`PATH` selecting `/bin/bash` 3.2)
failed 5/6 with exit 101 in the disabled leg. It is not a gate retry or accepted
fixture evidence. Gate invocations keep the normal Homebrew Bash environment;
no fixture source, acceptance predicate, or warning suppression was changed.

The ARM peer-hold follow-up is 959. Its submitted issue body is retained as a
raw transcript in `serials/813-b/landing/merged-tip/tty-followup-body.txt`.

claim-lint: python3 scripts/claim-lint.py -> exit 1

The documentation draft check applied prose rules to the saved issue-body
Markdown transcript. It is retained byte-for-byte as a text transcript;
that does not claim independent prose lint coverage of the submitted issue.


The sole 900-second ARM Unix retry also ended in capture-drain preflight:
66/67 suites passed, no kernel build or guest launch occurred, exit 1.
Both attempts are retained (`serials/813-b/landing/merged-tip/arm-unix-final.txt`
and `serials/813-b/landing/merged-tip/arm-unix-retry.txt`). No third timeout
attempt was made. ARM Unix acceptance at the merged tip is not established.
The separate pipe/FIFO and strict preflights each passed the unchanged fixture;
they are not substitutes for Unix gate preflight. Issue 957 remains open.


#### Merged-tip gate inventory

The corrected implementation revision is
`3f29bc7bf0d166ef75e5b7c84a1049250d195c29`. The following gate transcripts
start with that revision. Subsequent working-tree edits are documentation
and evidence only. The earlier merge-only and transitional attempts remain
separate from this table.

| Gate | Result | Transcript under `serials/813-b/landing/merged-tip/` |
|---|---|---|
| `bash scripts/run-structure-tests.sh` | PASS 105/105 | `structure-final.txt` |
| ARM soft-float `cargo build --release --features boot_tests --target aarch64-breenix-kernel.json -Z build-std=core,alloc -Z build-std-features=compiler-builtins-mem -p kernel --bin kernel-aarch64` | Exit 0; disclosed core notice 559 | `build-final.txt`, `future-incompatibilities.txt`, `no-neon.txt` |
| `bash docker/qemu/run-blocking-io-oracle-gate.sh --arch aarch64 --program unix_stream_blocking_oracle` | BLOCKED: 300-second preflight timeout and sole 900-second retry timeout; 66/67 each; no guest | `arm-unix-final.txt`, `arm-unix-retry.txt` |
| `bash docker/qemu/run-blocking-io-oracle-gate.sh --arch aarch64` | PASS 30 pipe/FIFO arms; preflight 67/67 | `arm-pipe.txt` |
| `bash docker/qemu/run-aarch64-boot-test-strict.sh 1` | FAIL 0/1, TTY peer-hold controls; preflight 67/67; follow-up 959 | `arm-strict.txt` |
| `bash docker/qemu/run-aarch64-boot-test-strict.sh 1` supplementary at `e9d3c168` | BLOCKED: exit 1, capture-drain 300-second preflight timeout, 66/67; no guest | `arm-strict-supplement.txt`, `strict-supplement-preflight/` |
| Beast `bash docker/qemu/run-blocking-io-oracle-gate.sh --arch x86_64 --program unix_stream_blocking_oracle` | PASS six Unix arms; preflight 67/67 | `beast/x86-unix.txt` |
| Beast `bash docker/qemu/run-blocking-io-oracle-gate.sh --arch x86_64` | PASS 30 pipe/FIFO arms; preflight 67/67 | `beast/x86-pipe.txt` |
| Beast `bash docker/qemu/run-x86-boot-tests.sh` | PASS 1/1; preflight 67/67 | `beast/x86-full-final.txt` |

On this continuation, issues 886 and 959 were read directly; snapshots are
`serials/813-b/landing/merged-tip/issue-886-supplement.json` and
`serials/813-b/landing/merged-tip/issue-959-supplement.json`.
The body in `serials/813-b/landing/merged-tip/issue-886-supplement.json`
describes "a peer-CPU kthread another oracle spins up missing a
microsecond-to-low-millisecond scheduling window under host CPU pressure".
It records irq_hold_oracle with `armed=0`, fcntl_pm_contention_oracle with
"the arming rendezvous did not complete", and a preceding tty_irq_fg_oracle
entry-latency failure. It also records a later 20/20 run with start loads 4-8.
These are similar scheduling-window signatures, not the identical TTY
peer-hold predicate failure in 959. The predicate in
`kernel/src/test_framework/registry.rs:6935` is `pm_busy_probe == 1`;
959 records that witness as 0 while its blocking-acquisition counter is 0.
This is evidence supporting possible pre-existing-flake attribution, not
proof of that attribution or a repair. Issue 959 remains open and the original
strict FAIL 0/1 remains in the inventory. A one-line evidence-only cross-reference
was posted at https://github.com/ryanbreen/breenix/issues/959#issuecomment-5577180192.

The single supplementary strict invocation ran at
`e9d3c1682d073d8d553e86dabde9101ed1868267`, with source unchanged from
`3f29bc7b`; the transcript records the revision and kernel SHA-256.
Host load averages were 3.02/3.82/4.57 at invocation start and 2.94/3.96/4.54
at end, below the original failing guest's load-1 of 15.02. The starting and
ending QEMU censuses were empty. Preflight remained enabled and timed out
at 300 seconds in gate_capture_drain_structure (66/67 suites passed), exit 1.
No guest launched, so there is no new guest serial or runtime verdict;
this does not supply the requested low-load strict runtime sample.
The original failing serial remains intact at
`serials/813-b/landing/merged-tip/mac-artifacts/breenix_aarch64_strict_1/serial.txt`.
The new invocation and per-suite logs are retained in
`serials/813-b/landing/merged-tip/arm-strict-supplement.txt` and
`serials/813-b/landing/merged-tip/strict-supplement-preflight/`.
This fixture stall is additional evidence for 957, not a finding that host
load caused the fixture stall. No second supplementary strict invocation
or third ARM Unix timeout attempt was made.

The four PR B kernel files pass rustfmt (`serials/813-b/landing/merged-tip/rustfmt.txt`). No warning
suppression was introduced. The ARM build is not claimed warning-free;
its retained report contains the pinned-nightly core notice tracked in 559.
The instruction guard reports 0 FP/SIMD load/store instructions
(`serials/813-b/landing/merged-tip/no-neon.txt`).

The transitional x86 full artifacts are retained separately in
`transitional-full.tar.gz`. Lane-owned completed images and preflight
executables were removed on beast to recover disk space; their logs and
serials remain. No shared stash, other worktree, or other lane's process was
removed. No production-profile gate or new runtime mutation was run.

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/merge-message.txt -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/fix-message.txt -> exit 0

The last two commands are read-only rechecks of the retained commit messages.


claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-message.txt -> exit 0

claim-lint: python3 scripts/claim-lint.py -> exit 1

The later documentation draft used shortened artifact paths in the instruction-
guard paragraph. Those paths are expanded, and the paragraph states the measured
load/store scope directly.


The final beast full gate's image preparation reports that optional BusyBox
and its coreutils links were not installed because the cross-compiler
prerequisite is absent. This is retained in `beast/x86-full-final.txt`; it is
separate from the Rust compile-diagnostic count. BusyBox coverage is not
claimed by this continuation.


#### Landing limits

Landing remains blocked by the ARM Unix preflight timeout after its sole
permitted retry (957), and the retained ARM strict peer-hold failure (959).
The supplementary strict invocation also stopped in capture-drain preflight
(957), so it supplied no new runtime result. This lane resolved the merge and retained the failures;
it did not merge a PR into main or close 813.

Issue 886 supplies evidence for possible pre-existing peer-scheduling flakiness
as detailed above, but its oracle failures do not establish the cause of 959.
Neither 959 nor 957 is treated as resolved; the original strict failure and
the exhausted Unix preflight attempts retain their recorded outcomes.

Not claimed for this continuation:

- A successful landing or passing results for the full requested gate set.
- ARM Unix runtime acceptance at the merged tip, or an ARM strict pass.
- A uniform revision for the transitional x86 full attempt.
- Root-cause attribution or repair of the failures tracked in 957, 959, or 891.
- New mutation sensitivity, production-profile coverage, or BusyBox coverage.
- A warning-free ARM toolchain build or Unix endpoint last-reference repair.


The uniform-revision final x86 full gate passed 1/1 at `3f29bc7b`, with
67/67 preflight suites. The two x86 oracles passed six Unix and 30 pipe/FIFO
arms. The earlier transitional softirq panic remains retained; this later
pass is not represented as a repair of 891. The final gate retains its
single-CPU contention and census skips, visible in `beast/x86-full-final.txt`.

Both final lane-owned QEMU censuses are empty
(`serials/813-b/landing/merged-tip/mac-qemu-census-final.txt` and
`serials/813-b/landing/merged-tip/beast-qemu-census-final.txt`). No process
was killed by name. The evidence commit changes documentation and retained
artifacts only; source citations are checked again read-only after that commit.


claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-message.txt -> exit 0


The supplementary continuation changes only documentation and evidence, with
no mutations, new PR, merge to main, or issue closure. The earlier code,
ratchet, merge and documentation commits are retained without rewriting them.

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/supplement-docs-message.txt -> exit 0

The preceding checks cover the supplementary documentation draft. The final
pre-commit checks, after recording these results, follow:

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/supplement-docs-message.txt -> exit 0

The evidence commit was amended to include the per-suite `.log` files that
the repository ignore rules excluded from ordinary staging. Checks before
that amendment:

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/supplement-docs-message.txt -> exit 0


#### Slot 2 verification

Read `gh issue view 957 --json state,comments` and
`gh issue view 959 --json state,comments`: both remain OPEN. The new 957
comment concerns another lane's unlanded work at `436313ed` / `ccd61b51`
and its missing-PIL `parallels_capture_structure` failure; it supplies no
landed fix for this branch. The 959 comment retains 886 as evidence only.
After `git fetch origin`, `origin/main` remains `3a8a973b`;
`git log --oneline 4c93a601..origin/main` and the corresponding local-main
check produce no commits. No ARM gate was rerun, and the landing limits
above remain unchanged. No source edits or mutations were made.

- `git status --short` initially produced no output; HEAD and the fetched
  branch ref both equal `cd420e12f62a74185db1f01498900f35998d474f`.
  `git log --oneline origin/main..HEAD` confirms the retained merge, fix,
  and documentation history. The post-test status contains only the new
  Slot 2 transcript before this note is added.
- `bash scripts/run-structure-tests.sh` at `cd420e12` passes 105/105, exit 0,
  with no compile diagnostics. Revision and output are retained in
  `serials/813-b/landing/merged-tip/slot-2-structure.txt`.

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/slot-2-docs-message.txt -> exit 0
