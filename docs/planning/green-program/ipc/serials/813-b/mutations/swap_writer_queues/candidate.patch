diff --git a/docs/planning/green-program/ipc/813-PR-B-2026-09-07.md b/docs/planning/green-program/ipc/813-PR-B-2026-09-07.md
index 94241440..667f0062 100644
--- a/docs/planning/green-program/ipc/813-PR-B-2026-09-07.md
+++ b/docs/planning/green-program/ipc/813-PR-B-2026-09-07.md
@@ -1,149 +1,90 @@
-# 813 PR B: close-delivery scope blocker, 2026-09-07
-
-Status: blocked before a deliverable kernel change. Inspected implementation
-revision: `f1d448b544cf3e8b63183e357e15668f8391c571`, on
-`ipc/813-pr-b-unix-stream-write-waiters`. This revision includes PR 939 (A2).
-The commits in this round contain documentation and inspection evidence only.
-
-## Required boundary and current source
-
-The supplied `PLAN-813.md:44` requires either "safe thread-context extraction"
-or "stop and report the scope conflict" for the legacy close census. Supplied
-`PLAN-813.md:46` explicitly rejects `isr_unblock_for_io()` under PM as a safe
-shortcut and requires leaving the ISR buffer and dispatch paths unchanged.
-Those are plan requirements, not newly inferred kernel restrictions. The plan
-is the sibling scratchpad file at `scratchpad/astra/PLAN-813.md`, outside this
-checkout. Its requested Unix rules, matrix, PR-B scope, and gate sections were
-read against the current implementation.
-
-A2 settled the original writer-notification omission in 919, but expressly
-retained lock-order and saturation concerns in 936. See
-`docs/planning/green-program/ipc/813-A2-2026-09-07.md:127`.
-Issue 936 remains OPEN; its state and body are retained in
-`serials/813-b/issue-936-state.json`. This is not a claim that A2 failed to
-land or that the original notification omission remains.
-
-The inherited delivery route is `kernel/src/task/waitqueue.rs:182`, which
-calls `isr_unblock_for_io` at `kernel/src/task/waitqueue.rs:185`.
-`kernel/src/task/scheduler.rs:6426` handles a full buffer, and
-`kernel/src/task/scheduler.rs:6428` acquires the scheduler in thread context.
-The interrupt branch at `kernel/src/task/scheduler.rs:6435` can reject a wake.
-These are source observations; no saturation failure was reproduced here.
-
-Unix legacy termination closes endpoints at
-`kernel/src/process/process.rs:597` and
-`kernel/src/process/process.rs:679`. `Process::terminate` invokes that cleanup
-at `kernel/src/process/process.rs:474`. The four direct termination call sites
-are `kernel/src/interrupts/context_switch.rs:1400`,
-`kernel/src/process/manager.rs:1947`,
-`kernel/src/signal/delivery.rs:239`, and
-`kernel/src/signal/delivery.rs:274`. The adjacent comment at
-`kernel/src/signal/delivery.rs:242` is not a fifth call.
-
-Exec has an extracted-descriptor owner at `kernel/src/ipc/fd.rs:755`, delivered
-by its Drop at `kernel/src/ipc/fd.rs:759`. Normal exit has extracted cleanup
-with its Unix close at `kernel/src/task/process_task.rs:733`. Those mechanisms
-do not establish a PM-free notification boundary for the four legacy
-termination callers. FD-table Drop also closes Unix directly at
-`kernel/src/ipc/fd.rs:701`.
-
-Consequently, adding immediate writer wakes to the legacy Unix close paths would extend
-scheduler acquisition under PM; copying the inherited deferred route would
-extend the saturation limitation that the supplied plan explicitly rejects.
-No dispatch, interrupt, signal, Tier-1, or Tier-2 file was changed. A scope
-question was sent: may B inherit the disclosed 936 limitation, or must the
-PM-free boundary land first? No answer had arrived when this report was prepared.
-The stopping condition is the supplied plan's boundary requirement.
-
-## Re-derived Unix anchors
-
-These anchors were located in the implementation revision above; plan-time
-line numbers were not copied as proof. `serials/813-b/head-census.txt` retains
-the file-qualified source census.
-
-| Surface | Current source | Observation |
-|---|---|---|
-| Stream transfer | `kernel/src/socket/unix.rs:95` | Full-buffer blocking write still returns EAGAIN; positive space permits a partial count. |
-| Drain | `kernel/src/socket/unix.rs:152` | No dedicated writer queue wake exists. |
-| Close | `kernel/src/socket/unix.rs:230` | Existing peer-reader wake remains synchronous. |
-| Write snapshot | `kernel/src/syscall/handlers.rs:463` | Unix snapshot does not carry descriptor mode. |
-| Write adapter | `kernel/src/syscall/handlers.rs:515` | Calls write through the retained socket guard. |
-| F_SETFL | `kernel/src/syscall/handlers.rs:3985` | Changes descriptor status flags. |
-| Writable readiness | `kernel/src/ipc/poll.rs:277` | Tests peer-open without transmit capacity. |
-| Shared prepared wait | `kernel/src/syscall/blocking_io.rs:16` | A/A2 helper exists for reuse. |
-| Named connection | `kernel/src/syscall/socket.rs:1692` | Constructs the stream pair with false. |
-| Socketpair mode | `kernel/src/syscall/socket.rs:1904` | Construction flag is also installed in descriptor status at `kernel/src/syscall/socket.rs:1909`. |
-
-## Per-arm disposition
-
-No accepted-revision Unix oracle ran. The independent draft used fork before
-named endpoint creation and had six scored arms, but remained incomplete at
-close delivery and was reverted from the tracked implementation. Its source
-is retained locally under `.tmp/pr-b-draft/` for resumption; it is not a
-published implementation or acceptance artifact.
-
-| Matrix leg | Required evidence | This round |
-|---|---|---|
-| Backpressure A/B | Real endpoint writer witness; drain 4096; write 4097 returns 4096; compare wire bytes | Not run |
-| Mode | Descriptor F_SETFL and socketpair construction controls; clear flag reaches wait | Not run |
-| Poll A/B | Full transmit suppresses POLLOUT; drain restores it; preserve HUP | Not run |
-| Peer close A/B | Actual close after queued witness; EPIPE; cleanup; reap | Blocked on close boundary |
-| Signal | Caught EINTR, ignored control, empty queue, cleared blocked flag, recovery write | Not run |
-| Partial-space | Both modes return 4096 of 4097 with matching prefix | Not run |
-
-The 4096 stream boundary is a count control, not a pipe-style atomicity claim.
-No listed runtime mutation was executed. Draft in-memory structure mutations
-covered descriptor consultation, deleted/reversed drain wake, and peer-only or
-receive-based POLLOUT. They are preparatory results on a reverted draft, not
-proof of code at the published HEAD.
-
-## Checks and gate budget
-
-| Check | Result | Evidence |
-|---|---|---|
-| Draft Unix structure suite via scripts/run-structure-tests.sh | 7 tests passed on an uncommitted draft; subsequently reverted | serials/813-b/draft-unix-structure.log |
-| Draft ARM userspace build | Exit 0; no warning/error diagnostics; 153 ELFs installed locally | serials/813-b/draft-userspace-build.log |
-| Accepted structure suites | Not run: no accepted implementation candidate | Not produced |
-| ARM oracle, preflight enabled | Not run | Not produced |
-| ARM strict 3 | Not run | Not produced |
-| Beast x86 boot-tests | Not run | Not produced |
-| Production 1, last | Not run | Not produced |
-| x86 oracle | Not run: 927 is OPEN | serials/813-b/issue-927-state.json |
-
-QEMU boots were not started, so no lane-owned QEMU process required killing.
-No name-based cleanup was used. No preflight was bypassed. No fixtures were
-re-recorded. No hash manifest is retained. Draft checks name their base revision
-and explicitly disclose the uncommitted delta. The requested runtime gate budget is
-unspent; it must run on a completed candidate after the scope conflict resolves.
-
-## Claim lint and remaining work
-
-claim-lint: python3 scripts/claim-lint.py -> exit 1
-
-The first tree run reported eight prose findings; the revised text removes
-unquantified wording and uses explicit not-run dispositions. Issue 936 tracks the boundary
-and saturation concern; 813 remains open for adapter delivery, and 927 tracks
-the x86 oracle prerequisite. No issue was closed or message posted to others.
+# 813 PR B audit and continuation — 2026-09-07
 
-Not claimed:
+Status: audit and validation in progress. Starting revision was
+`4eac269d9fe4fff53fdb3391200635bd4888bc87` on
+`ipc/813-pr-b-unix-stream-write-waiters`. This round independently read commits
+50e55a2c, a59131d6 and 4eac269d, including their artifacts. The inherited round
+text is retained in `serials/813-b/inherited-round-doc.txt`; its statement that
+no implementation was delivered became stale when 4eac269d was added.
 
-- PR-B kernel repair, oracle delivery, accepted structure ratchets, or runtime mutation sensitivity.
-- Any ARM or x86 runtime pass in this round, or resolution of 927 or 936.
-- PM-free legacy termination delivery or lossless wake-buffer saturation.
-- Unix last-reference semantics under fork/dup-close, global descriptor flag sharing, or read-mode repair.
-- Stream atomicity, SIGPIPE generation, console repair, other socket families, or closure of 813.
+## Contract audit
 
-claim-lint: python3 scripts/claim-lint.py -> exit 0
-
-claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr-b-docs-commit.txt -> exit 0
-
-The two issue-state JSON snapshots are skipped by the claim-lint extension
-filter. They retain the actual read-only GitHub responses. The source files
-cited above are unchanged by this documentation-only commit; the source census
-is checked again against the resulting HEAD before handoff.
-
-Evidence follow-up commit:
+| Contract item | Inherited implementation | Audit disposition |
+|---|---|---|
+| Full-buffer wait | write_unix uses the shared wait_prepared helper, retains buffer and closed guards through publication, drops guards before waiting | Retained; runtime evidence pending |
+| Descriptor mode | sys_write snapshots descriptor O_NONBLOCK; socketpair initializes this flag from SOCK_NONBLOCK | Retained; construction and F_SETFL controls exist |
+| Partial progress | Positive copy returns its count before another close check | Retained; no stream atomicity assertion |
+| Drain | Read releases receive buffer before waking the opposite endpoint writer queue | Retained |
+| POLLOUT/HUP | poll calls has_write_space and retains HUP; public predicate delegates to guarded transmit state | Retained; added direct predicate ratchet |
+| Six oracle legs | Fork precedes named listener/connect/accept; six scored arms, both directions, signal variants, byte comparisons, reap | Read independently; runtime and mutation evidence pending |
+| Close census | Ordinary close/dup2/exec/exit extract ownership, but legacy terminate uses wake_up_deferred and close still wakes readers inline | Replaced Unix notification delivery; original blocker was real |
+| Ratchets | Mode, drain, publication, gate arms and deferred close source checks | Kept correct checks; replaced the check that pinned the unsafe legacy route |
+| Round evidence | Two docs commits describe a reverted draft; code commit has no accepted runtime evidence | Historical artifacts retained; no inherited pass adopted |
+
+## Close-delivery decision
+
+The plan's objection is real: the inherited deferred waiter delivery calls
+isr_unblock_for_io, whose full-buffer thread-context branch acquires the
+scheduler. The inherited Unix close also wakes readers before returning its
+owned writer notification. Neither establishes the requested PM-free boundary.
+Issue 936 remains open for the inherited pipe/FIFO and ISR-buffer concerns.
+
+The candidate returns an owned pair notification for both reader EOF and
+writer EPIPE. Legacy terminate and FD-table destruction enqueue the pair via
+an intrusive link allocated as part of the pair. Duplicate pending closes
+coalesce. Enqueueing takes no scheduler lock, allocates no storage, and uses
+no fixed-capacity ISR wake buffer. Existing PM-free reclamation entry points
+on both architectures drain at most 64 pairs per pass. Unprocessed pairs
+remain owned by the queue. Draining clears queue membership before observing
+closed flags; a concurrent later close either coalesces before that observation
+or queues another notification. Object and queue locks are dropped before
+scheduler delivery. Liveness depends on the existing reclamation cadence.
+
+No Tier-1 or Tier-2 file is edited. No dispatch/interrupt implementation is
+changed. The change does not settle the pipe/FIFO portion of issue 936 or
+Unix endpoint last-reference ownership.
+
+## Per-arm evidence
+
+| Leg | Acceptance | Result |
+|---|---|---|
+| Backpressure A/B | Writer queue witness; drain 4096; 4097 request returns 4096; full wire comparison | Pending |
+| Mode | F_SETFL and SOCK_NONBLOCK full-buffer EAGAIN controls; clearing flag reaches wait | Pending |
+| Poll A/B | Full transmit suppresses POLLOUT; one-byte drain restores it; positive write; HUP after close | Pending |
+| Peer close A/B | Actual close after queue witness; EPIPE; waiter/state cleanup; reap | Pending |
+| Signal | Caught EINTR; ignored stays queued; cleanup; recovery write and bytes | Pending |
+| Partial-space | Both modes and directions return and transmit 4096 of 4097 | Pending |
+
+The 4096 boundary is a transfer/count control. It does not claim pipe-style
+atomicity for Unix streams.
+
+## Checks
+
+The default structure command passed 103 tests. The dedicated Unix suite
+passed 9 tests after adding the close-boundary and predicate checks. The
+close-boundary ratchet was first run against unchanged kernel source and
+failed; its first candidate run exposed a trailing-comma mismatch in the
+recognizer, retained as a failed attempt. A subsequent run passed.
+
+The ARM build emitted the pinned-nightly core NEON future-incompatibility
+notice already disclosed under 559. The inspected Cargo report contains only
+that warning class; the kernel instruction check reports zero FP/SIMD
+instructions (559). This is not a warning-free build claim.
+claim-lint:ok: 559; serials/813-b/candidate-build.txt and the inspected Cargo future-incompatibility report. No warning suppression
+was added. The ARM userspace build installed 153 binaries.
+
+Issue 927 was read during this audit and is OPEN. The x86 oracle matrix is
+blocked by 927; no x86 feature pass is claimed.
+
+## Not claimed
+
+- Accepted runtime or mutation results before the pending checks finish.
+- Unix endpoint last-reference semantics across fork/dup-close or global flag sharing.
+- Read-mode repair, stream atomicity, SIGPIPE generation, console, or other socket families.
+- Resolution of 927, the pipe/FIFO and ISR-buffer concerns in 936, or closure of 813.
+- A warning-free ARM toolchain build.
 
-claim-lint: python3 scripts/claim-lint.py -> exit 0
+claim-lint: python3 scripts/claim-lint.py -> exit 1
 
-claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr-b-evidence-commit.txt -> exit 0
+The first audit tree lint required a source for the FP/SIMD count; the revised
+paragraph names the build evidence and toolchain issue.
diff --git a/kernel/src/ipc/fd.rs b/kernel/src/ipc/fd.rs
index 20c25899..d7beb107 100644
--- a/kernel/src/ipc/fd.rs
+++ b/kernel/src/ipc/fd.rs
@@ -699,7 +699,7 @@ impl Drop for FdTable {
                     FdKind::UnixStream(socket) => {
                         // Close the Unix socket endpoint
                         let notifications = socket.lock().close();
-                        notifications.deliver();
+                        notifications.deliver_deferred();
                         log::debug!("FdTable::drop() - closed Unix stream socket fd {}", i);
                     }
                     FdKind::UnixSocket(socket) => {
diff --git a/kernel/src/socket/unix.rs b/kernel/src/socket/unix.rs
index f6efec23..4f920dd4 100644
--- a/kernel/src/socket/unix.rs
+++ b/kernel/src/socket/unix.rs
@@ -32,6 +32,8 @@ pub struct UnixStreamPair {
     closed_a: Mutex<bool>,
     /// Endpoint B closed
     closed_b: Mutex<bool>,
+    /// Intrusive owned close work; no allocation or fixed-capacity wake buffer.
+    pending_close: Mutex<PendingClose>,
 }
 
 impl UnixStreamPair {
@@ -46,6 +48,10 @@ impl UnixStreamPair {
             write_waiters_b: Arc::new(WaitQueueHead::new()),
             closed_a: Mutex::new(false),
             closed_b: Mutex::new(false),
+            pending_close: Mutex::new(PendingClose {
+                queued: false,
+                next: None,
+            }),
         }
     }
 }
@@ -204,41 +210,96 @@ impl UnixStreamSocket {
             UnixEndpoint::B => *self.pair.closed_b.lock() = true,
         }
 
-        // Wake peer's waiters (they'll see EOF)
-        let waiters = match self.endpoint {
-            UnixEndpoint::A => &self.pair.waiters_b,
-            UnixEndpoint::B => &self.pair.waiters_a,
-        };
-
-        let waiter_ids: Vec<u64> = waiters.lock().clone();
-        for thread_id in waiter_ids {
-            crate::task::scheduler::with_scheduler(|sched| {
-                sched.unblock(thread_id);
-            });
+        UnixCloseNotifications {
+            pair: self.pair.clone(),
         }
-        let writers = match self.endpoint {
-            UnixEndpoint::A => self.pair.write_waiters_b.clone(),
-            UnixEndpoint::B => self.pair.write_waiters_a.clone(),
-        };
-        UnixCloseNotifications { writers }
     }
 }
 
-/// Owned peer-writer notification; closed-flag and endpoint guards must drop
-/// before delivery. Caller-owned PM uses the established deferred wake route.
+struct PendingClose {
+    queued: bool,
+    next: Option<Arc<UnixStreamPair>>,
+}
+
+// Queue lock precedes the embedded link lock. Neither is held while delivering
+// wakes, acquiring closed flags, or calling the scheduler. IRQ masking prevents
+// a same-CPU fault/termination publisher from interrupting a queue operation.
+static PENDING_CLOSES: Mutex<Option<Arc<UnixStreamPair>>> = Mutex::new(None);
+
+/// Both peer-reader EOF and peer-writer EPIPE notifications own the pair until
+/// delivery. Callers release the endpoint and PM guards first.
 #[must_use = "deliver Unix close notifications after releasing object guards"]
 pub struct UnixCloseNotifications {
-    writers: Arc<WaitQueueHead>,
+    pair: Arc<UnixStreamPair>,
 }
 
 impl UnixCloseNotifications {
     pub fn deliver(self) {
-        self.writers.wake_up();
+        // FdTable destruction may run under PM or from fault cleanup.
+        if crate::process::process_manager_held_on_current_cpu()
+            || crate::per_cpu::in_interrupt()
+            || crate::tracing::providers::teardown::scheduler_scope_active()
+        {
+            self.deliver_deferred();
+            return;
+        }
+        let closed_a = *self.pair.closed_a.lock();
+        let closed_b = *self.pair.closed_b.lock();
+        for (closed, readers, writers) in [
+            (closed_a, &self.pair.waiters_b, &self.pair.write_waiters_b),
+            (closed_b, &self.pair.waiters_a, &self.pair.write_waiters_a),
+        ] {
+            if closed {
+                let ids = core::mem::take(&mut *readers.lock());
+                for tid in ids {
+                    crate::task::scheduler::with_scheduler(|sched| {
+                        sched.unblock(tid);
+                    });
+                }
+                writers.wake_up();
+            }
+        }
     }
 
-    /// Uses PR A's PM-context mechanism, including issue 936's residual caveat.
+    /// Retain the pair in an intrusive queue for the existing thread-context
+    /// reclamation boundary. Duplicate closes coalesce; no capacity can overflow.
     pub fn deliver_deferred(self) {
-        self.writers.wake_up_deferred();
+        crate::arch_without_interrupts(|| {
+            let mut head = PENDING_CLOSES.lock();
+            let mut pending = self.pair.pending_close.lock();
+            if !pending.queued {
+                pending.next = head.take();
+                pending.queued = true;
+                *head = Some(self.pair.clone());
+            }
+        });
+    }
+}
+
+/// Called from the existing PM-free reclamation entry on both architectures.
+/// A bounded pass leaves remaining pairs owned by the queue for the next pass.
+pub(crate) fn drain_close_notifications() {
+    if crate::process::process_manager_held_on_current_cpu()
+        || crate::per_cpu::in_interrupt()
+        || crate::tracing::providers::teardown::scheduler_scope_active()
+    {
+        return;
+    }
+    for _ in 0..64 {
+        let pair = crate::arch_without_interrupts(|| {
+            let mut head = PENDING_CLOSES.lock();
+            let pair = head.take()?;
+            {
+                let mut pending = pair.pending_close.lock();
+                *head = pending.next.take();
+                pending.queued = false;
+            }
+            Some(pair)
+        });
+        let Some(pair) = pair else {
+            break;
+        };
+        UnixCloseNotifications { pair }.deliver();
     }
 }
 
@@ -290,8 +351,8 @@ impl UnixWriter {
 
     pub(crate) fn queue(&self) -> Arc<WaitQueueHead> {
         match self.endpoint {
-            UnixEndpoint::A => self.pair.write_waiters_a.clone(),
-            UnixEndpoint::B => self.pair.write_waiters_b.clone(),
+            UnixEndpoint::A => self.pair.write_waiters_b.clone(),
+            UnixEndpoint::B => self.pair.write_waiters_a.clone(),
         }
     }
 
diff --git a/kernel/src/task/process_task.rs b/kernel/src/task/process_task.rs
index 605ef47d..1a9d9f74 100644
--- a/kernel/src/task/process_task.rs
+++ b/kernel/src/task/process_task.rs
@@ -1205,6 +1205,7 @@ pub fn reclaim_deferred_process_resources() {
         crate::trace_count!(crate::tracing::providers::teardown::RECLAIM_CONTEXT_VIOLATIONS);
         return;
     }
+    crate::socket::unix::drain_close_notifications();
     #[cfg(feature = "boot_tests")]
     if BOOT_RECLAIM_TEST_OWNER.load(Ordering::Acquire) != 0 {
         return;
diff --git a/tests/unix_stream_blocking_structure.rs b/tests/unix_stream_blocking_structure.rs
index 79e3bac8..f21b452d 100644
--- a/tests/unix_stream_blocking_structure.rs
+++ b/tests/unix_stream_blocking_structure.rs
@@ -14,7 +14,7 @@ fn drain_ok(unix: &str) -> bool {
     body.contains("UnixEndpoint::A=>&self.pair.write_waiters_b")
         && body.contains("UnixEndpoint::B=>&self.pair.write_waiters_a")
         && body.contains("writers.wake_up()")
-        && body.find("drop(rx_buf)") < body.find("writers.wake_up()")
+        && matches!((body.find("drop(rx_buf)"), body.find("writers.wake_up()")), (Some(drop_at), Some(wake_at)) if drop_at < wake_at)
 }
 fn poll_ok(poll: &str) -> bool {
     match_arms(&lex(poll), "FdKind")
@@ -40,6 +40,7 @@ fn mode_drain_and_poll_mutations_are_rejected_singly() {
     let unix = read("kernel/src/socket/unix.rs");
     for changed in [
         unix.replace("writers.wake_up();", ""),
+        unix.replace("drop(rx_buf);", ""),
         unix.replace("&self.pair.write_waiters_b", "&self.pair.write_waiters_a"),
     ] {
         assert!(!drain_ok(&changed));
@@ -51,6 +52,38 @@ fn mode_drain_and_poll_mutations_are_rejected_singly() {
         ));
     }
 }
+fn transmit_predicate_ok(unix: &str) -> bool {
+    let state_impl = unix.split("impl UnixWriteState<'_>").nth(1).unwrap_or("");
+    compact(&function(state_impl, "has_write_space"))
+        == "!*self.peer_closed&&self.buffer.len()<UNIX_SOCKET_BUFFER_SIZE"
+        && compact(&function(
+            unix.split("impl UnixWriteState").next().unwrap(),
+            "has_write_space",
+        )) == "self.writer().state().has_write_space()"
+}
+
+#[test]
+fn transmit_predicate_and_single_mutations() {
+    let unix = read("kernel/src/socket/unix.rs");
+    assert!(transmit_predicate_ok(&unix));
+    for changed in [
+        unix.replace(
+            "!*self.peer_closed && self.buffer.len() < UNIX_SOCKET_BUFFER_SIZE",
+            "!*self.peer_closed",
+        ),
+        unix.replace(
+            "self.buffer.len() < UNIX_SOCKET_BUFFER_SIZE",
+            "self.buffer.len() <= UNIX_SOCKET_BUFFER_SIZE",
+        ),
+        unix.replace(
+            "self.writer().state().has_write_space()",
+            "!self.peer_closed()",
+        ),
+    ] {
+        assert!(!transmit_predicate_ok(&changed));
+    }
+}
+
 #[test]
 fn guarded_state_defines_transmit_space_and_publication() {
     let unix = read("kernel/src/socket/unix.rs");
@@ -92,23 +125,46 @@ fn driver_and_gate_have_six_scored_legs() {
 }
 
 #[test]
-fn close_returns_owned_peer_notifications_and_pm_consumers_defer() {
+fn close_notifications_leave_pm_without_the_isr_buffer() {
     let unix = read("kernel/src/socket/unix.rs");
     let close = compact(&function(&unix, "close"));
-    assert!(close.contains("UnixEndpoint::A=>self.pair.write_waiters_b.clone()"));
-    assert!(close.contains("UnixEndpoint::B=>self.pair.write_waiters_a.clone()"));
-    assert!(close.contains("UnixCloseNotifications{writers}"));
-    assert!(!close.contains("writers.wake_up"));
+    assert!(close.contains("UnixCloseNotifications{pair:self.pair.clone(),}"));
+    assert!(!close.contains("with_scheduler"));
+    assert!(!close.contains("wake_up"));
+    let deferred = compact(&function(&unix, "deliver_deferred"));
+    assert!(deferred.contains("PENDING_CLOSES.lock()"));
+    assert!(!deferred.contains("isr_unblock"));
+    assert!(!deferred.contains("wake_up_deferred"));
+    assert!(!deferred.contains("with_scheduler"));
+    assert!(!deferred.contains("Vec::"));
+    let drain = compact(&function(&unix, "drain_close_notifications"));
+    assert!(drain.contains("head.take()"));
+    assert!(drain.contains("next.take()"));
+    assert!(drain.contains("queued=false"));
+    assert!(drain.contains("UnixCloseNotifications{pair}.deliver()"));
     let process = compact(&lex(&read("kernel/src/process/process.rs")));
     assert_eq!(process.matches("FdKind::UnixStream(socket)=>{letnotifications=socket.lock().close();notifications.deliver_deferred();}").count(), 2);
+    let drop_fd = compact(&lex(&read("kernel/src/ipc/fd.rs")));
+    assert!(drop_fd
+        .contains("letnotifications=socket.lock().close();notifications.deliver_deferred();"));
+    let boundary = compact(&function(
+        &read("kernel/src/task/process_task.rs"),
+        "reclaim_deferred_process_resources",
+    ));
+    assert!(
+        boundary.find("process_manager_held_on_current_cpu()")
+            < boundary.find("drain_close_notifications()")
+    );
+    assert!(
+        boundary.find("scheduler_scope_active()") < boundary.find("drain_close_notifications()")
+    );
     for path in [
-        "kernel/src/ipc/fd.rs",
         "kernel/src/task/process_task.rs",
         "kernel/src/syscall/pipe.rs",
     ] {
-        let text = compact(&lex(&read(path)));
         assert!(
-            text.contains("letnotifications=socket.lock().close();notifications.deliver();"),
+            compact(&lex(&read(path)))
+                .contains("letnotifications=socket.lock().close();notifications.deliver();"),
             "{path}"
         );
     }
diff --git a/userspace/programs/src/unix_stream_blocking_oracle.rs b/userspace/programs/src/unix_stream_blocking_oracle.rs
index 8441441d..e489bf44 100644
--- a/userspace/programs/src/unix_stream_blocking_oracle.rs
+++ b/userspace/programs/src/unix_stream_blocking_oracle.rs
@@ -12,14 +12,7 @@ use libbreenix::{
 const C: usize = 65536;
 const A: usize = 4096;
 const QUERY: u64 = 0xB8130001;
-const ARMS: [&str; 6] = [
-    "backpressure",
-    "mode",
-    "poll",
-    "peer_close",
-    "signal",
-    "partial",
-];
+const ARMS: [&str; 1] = ["backpressure"];
 #[repr(C)]
 #[derive(Clone, Copy, Default)]
 struct Witness {
@@ -399,6 +392,7 @@ fn main() {
     };
     let mut passed = 0;
     for arm in ARMS {
+        println!("[UNIX_WRITE_START:{}:{}]", arch, arm);
         let result = (|| -> TestResult<usize> {
             if arm == "mode" {
                 construction_control()?;
