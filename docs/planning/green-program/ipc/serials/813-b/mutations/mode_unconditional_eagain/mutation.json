{
  "name": "mode_unconditional_eagain",
  "arm": "mode",
  "path": "kernel/src/syscall/blocking_io.rs",
  "before": "if is_nonblocking {\n                return SyscallResult::Err(errno::EAGAIN as u64);",
  "after": "if is_nonblocking || !data.is_empty() {\n                return SyscallResult::Err(errno::EAGAIN as u64);"
}
