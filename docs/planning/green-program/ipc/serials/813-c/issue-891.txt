{"body":"## Signature\n\n`docker/qemu/run-x86-smp-enum-gate.sh 1` on beast (`<x86-build-environment>` remote-environment\ncontainer, `-machine pc,accel=tcg -cpu qemu64 -smp 1`,\n`--features boot_tests,testing,external_test_bins`), 2026-09-06, on a #766\nreview-round branch build:\n\n```\n[ INFO] kernel::task::softirq_tests: SOFTIRQ_TEST: Testing iteration limit and ksoftirqd deferral...\n[ INFO] kernel::task::softirq_tests: SOFTIRQ_TEST: After do_softirq(): 10 iterations (limit=10)\n[ERROR] kernel: KERNEL PANIC: panicked at kernel/src/task/softirq_tests.rs:228:5:\nksoftirqd should have processed deferred softirqs (tid=Some(2))\n```\n\nThe gate exits 1 on the panic. Serial excerpt in-repo:\n`docs/planning/green-program/timekeeping/serials/766/28-x86-smp-gate-ksoftirqd-panic-562-family.txt`.\n\n## Why this is filed separately from #562\n\n#562 is the same assertion -- ksoftirqd did not drain the deferred softirqs the\nself-test raised -- but is scoped to **aarch64** `--features testing`, where it\nreproduces at 5 of 5 boots. This is x86, in the `boot_tests` profile, and it is\nintermittent: the same gate script on the same container reads `SMP_EXIT=0` on\nthe next run of the branch's own final bytes\n(`serials/766/26-x86-boot-tests-gate-green-final.txt`). #803 is the aarch64\nstall *before* the same assert. Neither issue records an x86 occurrence.\n\n## Rate\n\n1 red observed. It is the only x86 occurrence this program has recorded, and no\nsweep was run to estimate a rate.\n\n## What this is not\n\nNot attributed to the branch it was seen on. The failing assertion is an\nearly-boot self-test that runs before that branch's own leg, and the branch's\nleg is withdrawn in the round that observed this. The observation is filed so\nthe signature is on the record and a future x86 red on this assertion is\nattributable rather than unexplained.\n\nRound record: `docs/planning/green-program/timekeeping/766-ROUND-RECORD-2026-09-06.md`\nsection 4.\n","number":891,"state":"OPEN","title":"x86 boot_tests: KERNEL PANIC in the ksoftirqd deferral self-test (softirq_tests.rs:228), 1 red on the smp-enum gate","url":"https://github.com/ryanbreen/breenix/issues/891"}
