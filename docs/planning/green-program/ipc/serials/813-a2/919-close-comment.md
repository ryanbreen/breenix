Re-derived at HEAD 26893a4fb55bb3de33b62e5c522348d231e2a26a: the 4 of 4 Process::terminate callers are kernel/src/interrupts/context_switch.rs:1400, kernel/src/process/manager.rs:1911, and kernel/src/signal/delivery.rs:239 and :274. Process::terminate calls the shared close_all_fds, whose PipeRead/FifoRead arms deliver the owned notification on both architectures (kernel/src/process/process.rs:566, :604, :648, :686). No caller still discards the new writer notification. PipeWrite/FifoWrite discard only the empty writers: None return from close_write.

919 originally requested writer-close wake delivery beyond a safe PM boundary. PR 933 instead delivers through the deferred wake-buffer route while caller-owned PM remains held, with actual scheduling deferred. This closes the original writer-notification omission for 4 of 4 callers; it does not establish general fault-cleanup lock safety. The issue body has already corrected the earlier two-caller count.

The legacy reader-EOF inline scheduler wake and the wake-buffer saturation/fallback concern are now tracked explicitly in 936. The usual deferred enqueue is lock-free, but the thread-context full-buffer fallback can acquire the scheduler and interrupt-context overflow can reject a wake. Closing 919 for its repaired writer-wake scope, with those remaining concerns preserved in 936. Parent 813 remains open. The detailed A2 census and evidence are in docs/planning/green-program/ipc/813-A2-2026-09-07.md.
