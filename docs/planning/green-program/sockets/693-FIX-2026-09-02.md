# #693 fix — a timed-out poll is decided against the peer's publication instant, and the kernel says which

Branch `fix/693-poll-wake-loss`, off `main` @ `3d601400`. Companion to
`693-RCA-2026-09-02.md`, which established what is repaired here.

## 1. What was wrong

#693 records a blocking `poll()` on a connected TCP fd returning `ready=0`,
`revents=0x0000` after its full 5000 ms timeout while a peer process wrote and
exited, and states its undecided fork verbatim:

> **Not** established: whether the peer's `send()` was actually issued and its
> readiness lost, or whether the peer's own `recv` of the token never completed
> so it never wrote at all.
> <!-- claim-lint:ok: verbatim quote of #693's own text; the RCA settled it at
>      368/368 peer branches and 13/13 orderings, 693-RCA-2026-09-02.md §4-§5. -->

The RCA answered it: on 13 of 13 reproductions, and on the specimen #693 itself
preserves, the peer's write landed AFTER the poll's deadline. The poll's zero
was the correct answer for a socket nothing had yet been written to. Two
defects made that undiagnosable:

1. **The verdict decided from inputs that could not separate the two cases.**
   `stage_late` reported `late_lost_wake` — a kernel wake-loss claim — from
   `ready != 1 || revents & POLLIN == 0` alone
   (`userspace/programs/src/poll_tcp_oracle.rs:441` on `85596f62`). The peer's
   publication instant was not among its inputs, and on that arm it was not
   even read: the arm reaped the peer and reported without touching the socket.
2. **The kernel was silent.** A `sys_poll` that times out emitted no record of
   whether readiness had been published inside its own window, and the
   connection kept no such record either — `rx_buffer` holds what arrived, not
   when. The one fact that decides the question lived in the kernel and went
   unstated, which is why the investigation had to reconstruct it from syscall
   ordering across 13 of 13 preserved reproductions.

## 2. The repair

### 2.1 Userspace: the timeout arm recovers the peer's stamps before deciding

`userspace/programs/src/poll_tcp_oracle.rs`. On a timeout return the parent now:
emits the probe (unchanged), reaps the peer and reads its exit code, and then
collects the peer's payload — starting from whatever the probe's *destructive*
non-blocking read already took off the socket. That last point is load-bearing:
on a genuine lost wake the probe's read is the one that succeeds, and the bytes
it takes are the payload whose stamps decide the verdict. The pre-fix probe
discarded them.

With `write_ms` in hand:

| observation | verdict |
|---|---|
| peer exit code is not `sent` | `late_peer_no_publish` FAIL — #693's second candidate, now nameable |
| `write_ms < deadline` AND the probe found bytes on the socket | `late_lost_wake` FAIL |
| `write_ms >= deadline` | `published_after_deadline`: the publication did not enter the window |
| `write_ms < deadline`, probe found an empty socket | `not_readable_at_return`: the send had not reached the socket when the poll gave up |
| either inconclusive shape, Race mode | retry against a doubled poll ceiling (5 s → 10 s → 20 s); on exhaustion `late_peer_overrun` FAIL carrying the measured overrun |
| either inconclusive shape, Forced mode | the arm passed: the classification is correct |

`deadline` is `entry + timeout_ms`, computed from the parent's stamp taken
immediately before the syscall. The kernel starts its countdown at or after
that, so this is a LOWER bound on the kernel's deadline, which is the direction
that keeps the verdict sound.

Raising the ceiling rather than the delay is what makes the retry effective, and
it is free when the trial then succeeds: the poll returns when the peer
publishes, not when the timeout expires.

A `[POLL_TCP_ORACLE:LATE_PUBLISH:…]` line is emitted on each timeout arm,
passing or failing, carrying `anchor`, `entry`, `deadline`, `returned`,
`write_ms` and `late_by_ms`. That is the quantity the RCA had to reconstruct.

Why the verdict needs BOTH facts is §2.4: each stamp on its own lies in a
direction that manufactures #693's error again, and the batteries caught both.

### 2.2 Userspace: a stage 4 that forces the classification on each boot

The classification arm was reached by accident, at whatever rate the peer
happened to overrun by more than five seconds. Stage 4 (`forced`) makes it
happen on purpose: a 150 ms poll against a 500 ms peer delay, so the
publication lands after the deadline by construction. A verdict that cannot
tell "published after my deadline" from "you lost my publication" now fails
deterministically instead of once in twenty-four boots — see mutation 693-U in
§4.

### 2.3 Userspace: the peer is anchored to the parent's own pre-send stamp (#694)

The `entry > token_ms` precondition compared the parent's poll entry to the
instant the PEER was scheduled to read the token. `delay_ms` controls how long
the peer waits AFTER that instant, so widening it did not move the peer's write
relative to the parent's poll entry: the five doublings were five flips of one
coin, and 4 of 24 beast KVM boots lost all five and emitted `late_window_missed`
(#694). The token now carries `anchor`, a stamp the parent takes before it
sends; the peer sleeps to `anchor + delay_ms` and echoes the anchor back. The
precondition becomes `entry <= write_ms`, and each doubling now strictly widens
the room the parent has to get from its `send()` to its `poll()`. The echo check
refuses a payload left over from an earlier attempt.

This lands with #693's repair because #693's repair adds a retry that re-rolls
this precondition: without the anchor the extra trials would have made the
ladder exhaust MORE often on x86 than it does on main.

### 2.4 The two facts, and why one is not enough

The x86 KVM batteries reddened twice on this branch's own bytes, and both reds
were the fix's predicate committing #693's error again one instant to the left.
Both are repaired; both are why the verdict now needs two facts rather than one.

**Boot 20 of the first 25-boot KVM battery — `returned` is not the deadline.**
A 150 ms stage-4 poll entered at 39696 and returned at 40389: 693 ms elapsed,
because the parent did not get the CPU back for 543 ms after its budget expired
at 39846. The peer published at 40093 — 247 ms AFTER the deadline and 296 ms
BEFORE the parent's `returned` stamp. Scored against `returned` that reads as a
lost wake; the poll had in fact already decided, correctly, on an empty buffer,
and the kernel's own check (which compares against `deadline_ns`) stayed silent
on that boot. The comparison moved to `entry + timeout_ms`.

**Boot 14 of the second 25-boot KVM battery — `write_ms` is not the
publication.** A stage-3 peer stamped `write_ms=38488`, 762 ms before its
39250 deadline, and the poll returned 0. But `write_ms` is stamped BEFORE the
peer's `send()`, so it is a lower bound on the publication: this peer was
descheduled between the stamp and the syscall. The kernel read `rx_len=0
publish=none_in_window` at its deadline, and the parent's probe, taken the
instant the poll gave up, agreed: `rescan_ready=0 … nbread_err=EAGAIN`. The
verdict now also requires the probe to have found bytes — kernel state read
directly rather than a stamp that can lie. Under mutation 693-K the same probe
reads `rescan_ready=1 rescan_revents=0x0001 nbread_n=23`, so the two cases
separate on the fact rather than on a threshold, and the RCA named that fact in
advance.

The authoritative detector is the kernel-side marker in §2.5; the userspace
verdict is the corroborating one. On 5 of 5 boots under mutation 693-K they
agree.

### 2.5 Kernel: the publication instant, and a marker at the source

* `kernel/src/net/tcp.rs` — `rx_buffer` becomes private and gains
  `publish_rx()`, the one path by which received bytes enter it and the one
  place `rx_publish_ns` is stamped. The 6 append sites are converted; a new
  append site outside the module does not compile. An empty payload is not a
  publication and does not move the stamp.
* `kernel/src/ipc/poll.rs` — `tcp_rx_publication()` reads `(rx_publish_ns,
  rx_len)` for a connected TCP fd.
* `kernel/src/syscall/handlers.rs` — `sys_poll` keeps `entry_ns` and, on a
  timeout return, calls `poll_report_timeout`, which emits one of two lines per
  polled TCP fd:
  * `[POLL_TCP_READY_LOST]` — bytes were published into this connection
    strictly inside the poll's own window, are STILL buffered, and the poll is
    returning without POLLIN. The last in-loop scan runs at the deadline and
    reads the buffer live through the same connection lock, so it had to have
    seen them: this is a contradiction in kernel state. Gate-failing.
    The "still buffered" clause is what keeps it sound (a publication drained by
    another thread is not a loss), and the strict `< deadline_ns` is the other
    half (bytes landing after the deadline are an on-time timeout).
  * `[POLL_TCP_TIMEOUT]` — the ordinary case, for polls of at least 120 ms,
    carrying the publication's offset from entry or `publish=none_in_window`.

Both use `serial_println!`, not `log::`: on aarch64 the `log::` sink is a second
UART and the aarch64 gates boot QEMU with a single `-serial file:`, so a
`log::error!` marker would be invisible to the gates meant to fail on it.

### 2.6 Gate wiring

`[POLL_TCP_READY_LOST]` is pinned as a failure in 7 gate scripts
(`run-aarch64-service-sequence-gate.sh`, `-boot-test-strict.sh`,
`-boot-test-native.sh`, `-prod-profile-boot-test.sh`, `-full-test.sh`,
`-arma609-arm.sh`, `-refusal-drain-gate.sh`) and in
`scripts/x86-gate-verdict.sh`. Four of those additionally require
`[POLL_TCP_TIMEOUT]` to be present: the absence of a marker a healthy tree does
not emit for months is not by itself evidence that its reporting path is alive,
and the ordinary line from the same function is what turns "no lost-wake marker"
into a reading. `scripts/x86-gate-verdict.sh` gets the failure check only —
whether an x86 profile contains a blocking TCP poll depends on #697.

## 3. Batteries

The 3 batteries below ran at `454ceb8d`, the head this document describes. Two
earlier rounds were run and superseded: their KVM arms are what produced the 2
predicate errors in §2.4, and the numbers here are from bytes carrying both
repairs.

### 3.1 aarch64 — `run-aarch64-service-sequence-gate.sh --boots 25 --profile both`

**49 of 50 boots GREEN, UNATTRIBUTED 0; the gate reports FAILED on 1 boot in the
`690` bucket.** That boot is `max/serial-22`, classified by the gate's own
attribution as "clonevm_exec_test stalled at 'second stage' and never reached
the post-exec rendezvous (#690, pre-existing)". Its poll readings are clean —
`[POLL_TCP_ORACLE:PASS:stages=4:…]`, 2 `[POLL_TCP_TIMEOUT]` lines, 0
`[POLL_TCP_READY_LOST]` — so the red is #690's and it is preserved as
`serials/693-fix/aarch64-693fix-gate-max-boot22-690-serial-20260902.txt`. Two
earlier 50-boot runs of the same gate on this branch, at `af07b8ca` and at
`a8026109`, were 50 of 50 GREEN and PASSED.

Over the 50 boots of this run:

| reading | count |
|---|---|
| `[POLL_TCP_ORACLE:PASS:stages=4` | 100 lines = 2 per boot × 50 (the oracle emits each verdict twice) |
| `[POLL_TCP_ORACLE:FAIL` | 0 |
| serials containing `[POLL_TCP_TIMEOUT]` | 50 of 50 |
| `[POLL_TCP_TIMEOUT]` lines | 100 = 2 per boot (stage 1's 120 ms poll and stage 4's 150 ms poll) |
| `[POLL_TCP_READY_LOST]` lines | 0 |
| `LATE_PUBLISH:stage=forced decided=published_after_deadline` | 100 lines = 2 per boot × 50 |

50 of 50 boots ran the classification arm and decided it the same way, and the
kernel-side reporting path executed twice per boot, so the 0 in the
`READY_LOST` row is a reading rather than an assumption. Gate output:
`serials/693-fix/aarch64-693fix-service-sequence-gate-20260902.txt`.

### 3.2 x86 — 25 sequential TCG boots on beast `breenix-x86`

`-machine pc,accel=tcg -cpu qemu64 -smp 1`, the accelerator the default x86 gate
uses and the one the RCA measured the highest reproduction rate on. Private
image snapshot, verified before use (the branch's oracle strings present,
`tcp_cloexec` absent). **25 of 25 PASS**, `oracle_loaded=1` on 25 of 25,
`ready_lost=0` on 25 of 25.

For comparison, on the pre-fix bytes the RCA's two TCG arms over 55 boots
produced 26 PASS, 12 `late_lost_wake` and 15 `late_window_missed`
(693-RCA-2026-09-02.md §3).

24 of these boots emit 2 `[POLL_TCP_TIMEOUT]` lines and 1 emits 3: on that boot
stage 3's own 5 s poll timed out, was classified `published_after_deadline`, and
was retried against a 10 s ceiling — the Race-mode ladder exercised by the host
rather than by a mutation. Stage 4's `late_by_ms` over the run ranges
238–3427 ms.

### 3.3 x86 — 25 sequential KVM boots on beast `breenix-x86`

`-machine pc,accel=kvm -cpu host -smp 1`, the environment #693 was filed from,
and the arm that reddened on both earlier rounds. **25 of 25 PASS**, with
`oracle_loaded=1 poll_tcp_timeout=2 ready_lost=0` on 25 of 25 and 25 of 25
`LATE_PUBLISH` lines reading `decided=published_after_deadline`. Driver output:
`serials/693-fix/x86-693fix-kvm-driver-output-20260902.txt`.

Per-arch totals for the head this document describes: aarch64 50 boots (49
GREEN, 1 attributed to #690), x86 50 boots (25 TCG + 25 KVM, 50 PASS). The
oracle emitted 0 FAIL verdicts and the kernel 0 `[POLL_TCP_READY_LOST]` lines
over the 100 boots.

## 4. Mutations

Each was applied alone, built, run, and reverted; `git status` is clean at the
head this document describes, so 0 of 3 survive in the branch's bytes. Driver
output and one serial per mutation are preserved next to this document, e.g.
`serials/693-fix/aarch64-693fix-mutK-boot1-serial-20260902.txt`.

### 693-U — main's pre-#693 predicate, restored

Inserts main's decision (`if timed_out { … late_lost_wake … }`) immediately
after the timeout is detected, so the verdict is again made without the peer's
publication instant and without the probe. **5 of 5 aarch64 boots RED**, with
#693's exact signature and the peer having exited cleanly:

```
[POLL_TCP_ORACLE:FAIL:late_lost_wake:ready=0 revents=0x0000 entry=7404 returned=7561
 elapsed_ms=157 delay_ms=500 peer_reaped=true peer_status=0x00000000 peer_code=0]
```

`ready_lost=0` on 5 of 5: the kernel lost nothing, which is the point — the
failure was a mis-attribution, and the kernel marker correctly stays silent.
The same bytes without the mutation are 50 of 50 clean on the oracle (§3.1).

### 693-K — `sys_poll`'s post-wake re-scan deleted

Removes `ready_count = scan_fds(…)` from the blocking loop's tail, so a
publication landing while the poller is parked is never observed. **5 of 5
aarch64 boots**: the kernel marker fires, the probe reads the data on the
socket, and the oracle names the lost wake.

```
[POLL_TCP_READY_LOST] fd=4 timeout_ms=5000 publish_after_entry_us=82211
 before_deadline_us=4917789 rx_len=23 revents=0x0000
[POLL_TCP_ORACLE:LOSTWAKE_PROBE:probe_ms=12085 rescan_ready=1 rescan_revents=0x0001 nbread_n=23]
[POLL_TCP_ORACLE:FAIL:late_lost_wake:stage=late ready=0 revents=0x0000 anchor=7073
 entry=7074 deadline=12074 returned=12083 write_ms=7154 began_before_deadline_ms=4920
 elapsed_ms=5009 timeout=5000 delay_ms=80 readable_at_return=1]
```

Stages 1, 2 and 4 still behave under this mutation, so it is the stage-3
publication that is being lost and not the whole poll path. This is also the
anti-vacuity run for the probe: `rescan_ready=1 … nbread_n=23` here against
`rescan_ready=0 … nbread_err=EAGAIN` on the KVM boot in §2.4.

### 693-M3 — stage 3's peer pushed past the poll's ceiling

`LATE_WRITE_DELAY_MS` 80 → 6000, so stage 3's first attempt cannot contain the
publication inside a 5000 ms poll. This is #693's own scenario made
deterministic. **5 of 5 aarch64 boots PASS**, each after the ceiling doubles:

```
[POLL_TCP_ORACLE:LATE_PUBLISH:stage=late decided=published_after_deadline anchor=6952
 entry=6954 deadline=11954 returned=11958 write_ms=12954 late_by_ms=1000 delay_ms=6000
 timeout=5000]
[POLL_TCP_ORACLE:PASS:stages=4:idle_ms=126:late_ms=6003:park_ms=6000:forced_ms=153:forced_late_by_ms=351]
```

Main's predicate emits `late_lost_wake` on exactly this shape (that is what
693-U demonstrates); the fixed predicate classifies it, retries against a 10 s
ceiling, and passes with a measured parked span of 6000 ms.

## 5. What this does not establish

* It does not close #737, the kernel page fault at `0x8` in TCP teardown. No
  shared mechanism was looked for beyond noting that the signatures do not
  overlap.
* `late_peer_overrun` — the FAIL that ends an exhausted Race-mode ladder — is
  not reached by these batteries. 693-M3 exercises the ladder's first rung and
  its convergence, and one x86 TCG boot exercised it without a mutation; the
  exhaustion arm is exercised by neither. `late_peer_no_publish`,
  `late_token_mismatch` and the payload-collection failures are likewise
  unexercised: they name shapes no battery here produced.
* The `[POLL_TCP_READY_LOST]` marker is sound for the case it names, and it is
  not a detector for each of the ways a wake could be lost. A publication
  consumed by another thread before the poll's deadline is deliberately not
  reported, because reporting it would make a legitimate multi-reader poll look
  defective.
* **The x86 evidence in §3.2 and §3.3 comes from the bespoke driver, not from
  an x86 gate, and the bytes it ran on are not the branch's bytes.** The oracle
  has no x86 launch site on `main` and has none on this branch either: it is
  wired only on aarch64, from `userspace/programs/src/init.rs` under
  `#[cfg(target_arch = "aarch64")]`. Launching it from the x86 `RING3_SMOKE`
  roster — which is what those two batteries needed — forks and reaps a peer
  per retry attempt through the live `complete_wait` path, and
  `docker/qemu/run-x86-boot-tests.sh` pins the resulting count as a frozen
  literal (`removed=6` at `:176`, and the `:548` assertion against
  `PRODUCTION_REAPED_ROWS=4`). The round-2 review measured that gate PASS on
  `main` @ `3d601400` on 2 of 2 runs and FAIL at `eba15887` on 2 of 2 runs, one
  boot each on one host.
  <!-- claim-lint:ok: 2 of 2 and 2 of 2, the round-2 review's four single-boot
       runs of docker/qemu/run-x86-boot-tests.sh, finding B1 -->
  So the launch site is NOT in these bytes. It is preserved as an
  investigation patch that applies to them,
  `serials/693-fix/x86-launch-site.patch` with
  `x86-launch-site-apply.sh` and `x86-launch-site-revert.sh`, which is how the
  §3.2 and §3.3 batteries are reproduced: apply, build, run
  `serials/693-fix/x86-693fix-driver-20260902.sh`, revert. Making that gate
  compatible so the oracle can be gate-visible on x86 is #697, and this branch
  does not do #697.
* `docker/qemu/run-boot-parallel.sh`'s #568 anti-vacuity assertion is likewise
  not re-armed, and its comment at `:181-188` ("not launched on x86") is true
  of these bytes again.
* The 1 red in §3.1 is #690's and is left open. This work did not look at
  `clonevm_exec_test`.
* The RCA's by-catch — `get_monotonic_time()` returning ticks rather than
  milliseconds on x86, where `PIT_HZ = 200` (693-RCA-2026-09-02.md §11) — is
  NOT repaired here. It is a cross-kernel behaviour change: it would move the
  teardown grace epochs in `tracing/providers/teardown.rs`, roughly 12 deadline
  computations in `test_framework/registry.rs`, and the ARP pending TTL, each of
  which currently runs 5× longer than its constant says. That needs its own
  soak and its own issue, and it is not in this fix's causal chain.

## 6. Claim discipline

```
claim-lint: scripts/claim-lint.py                         -> exit 0
```
