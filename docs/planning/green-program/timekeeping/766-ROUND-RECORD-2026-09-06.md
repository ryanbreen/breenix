# #766 round record -- 2026-09-06

Branch `fix/766-x86-timer-wake-dispatch`, based on `origin/main` @ `783a6a53`.
RCA and the fix's reasoning: `766-TIMER-WAKE-DISPATCH-2026-09-06.md` in this
directory. Serials: `serials/766/`.

R157 small-PR mode. Five commits, non-reversing, same day.

---

## 1. What changed

| Commit | File(s) | What |
|---|---|---|
| doc | `766-TIMER-WAKE-DISPATCH-2026-09-06.md` | the traced chain and the producing line |
| fix | `kernel/src/task/scheduler.rs` | `push_back` -> `push_front` at the timer-wake enqueue, plus one counter |
| oracle | `kernel/src/task/timer_wake_oracle.rs`, `kernel/src/task/mod.rs`, `kernel/src/main.rs`, `kernel/src/main_aarch64.rs`, 4 gate scripts | the leg and its gate pins |
| ratchet | `tests/timer_wake_dispatch_structure.rs`, `tests/tty_irq_pm_structure.rs`, 2 re-recorded fixture serials | the structural pin, and the collateral the new gate assertion caused |
| record | this file, `serials/766/` | receipts |

**Tier 1: not touched.** The fix is in `kernel/src/task/scheduler.rs`, which is
neither tier. `kernel/src/interrupts/timer.rs` is read by the RCA and by
`tests/timer_wake_dispatch_structure.rs` (which parses its `TIME_QUANTUM`
literal) and is not modified. Tier 2: `kernel/src/task/scheduler.rs` is on the
Tier-2 list; the defect lives in it (`wake_expired_timers` is where the enqueue
position is decided), and the change adds no logging, no lock, no allocation, no
formatting and no I/O -- one `VecDeque::push_front` in place of one `push_back`,
plus one relaxed atomic increment.

---

## 2. Oracle: red on main, green on the branch

Both readings are from `docker/qemu/run-x86-boot-tests.sh 1` on beast
(`breenix-x86` Incus container, `-machine pc,accel=tcg -cpu qemu64 -smp 1`,
`--features boot_tests,testing,external_test_bins`).

**Red** (the fix reverted at the producing line, everything else identical):

```
[TIMER_WAKE_LATENCY_ORACLE:x86:sleep_ms=10:peers=8:overrun_ms=401:bound_ms=100:quantum_ms=50:round_ms=400:wake_enqueues=1:peers_started=8:peers_spinning=8:backstops=0:setup_ms=510:window_ms=1559:measured=1:FAIL]
```

**Green** (branch):

```
[TIMER_WAKE_LATENCY_ORACLE:x86:sleep_ms=10:peers=8:overrun_ms=49:bound_ms=100:quantum_ms=50:round_ms=400:wake_enqueues=2:peers_started=8:peers_spinning=8:backstops=0:setup_ms=520:window_ms=565:measured=1:PASS]
```

`overrun_ms=401` against `round_ms=400`: the red reading is one full round of
the 8 peers' 50 ms quanta, to within a millisecond, which is what section 1.3 of
the RCA predicts and is the direct confirmation that the queue-position term is
the one the fix removes. The green reading, 49 ms, is inside the mechanism bound
of one quantum plus one tick (55 ms) and 8.2x below the red.

**aarch64 arm**, `docker/qemu/run-aarch64-boot-test-strict.sh` (script default,
20 boots, `-M virt,gic-version=3 -cpu cortex-a72 -smp 4`):

```
[TIMER_WAKE_LATENCY_ORACLE:aarch64:sleep_ms=10:peers=8:overrun_ms=5:bound_ms=100:quantum_ms=10:round_ms=80:wake_enqueues=2:peers_started=8:peers_spinning=8:backstops=0:setup_ms=5:window_ms=30:measured=1:PASS]
```

The aarch64 arm passes on the branch and would have passed before it; it is a
regression guard and the symmetry the two arches are read for, not evidence
about the x86 mechanism (RCA section 1.4 says why).

---

## 3. Mutation table

| # | What was mutated | Command | Exit | Assertion that fired |
|---|---|---|---|---|
| M1 | `kernel/src/task/scheduler.rs`: `self.per_cpu_queues[target].push_front(tid)` -> `push_back(tid)` (the producing line, restored) | `bash scripts/run-structure-tests.sh timer_wake_dispatch_structure` | 101 | ``a_late_timer_wake_is_enqueued_at_the_head``: "`wake_expired_timers` must not enqueue at the tail of a ready queue ... Offending line(s): [\"                        self.per_cpu_queues[target].push_back(tid);\"]" |
| M2 | the same mutation, built and booted | `BREENIX_GATE_TMP=... ./docker/qemu/run-x86-boot-tests.sh 1` on beast | see section 2 | the oracle itself: `overrun_ms=401 > bound_ms=100` -> `FAIL`, and the gate's `TIMER_WAKE_LATENCY_ORACLE_PASS_PATTERN` assertion has no match |
| M3-M6 | 4 in-memory pre-#766 shapes fed to the ratchet's own assertion bodies | the 4 `#[should_panic]` legs of `tests/timer_wake_dispatch_structure.rs` | 0 (each panics as required) | tail enqueue; no enqueue at all; x86 `TIME_QUANTUM` drift; aarch64 `TIME_QUANTUM` drift |
| M7 | a `FAIL` verdict at `overrun_ms=2592` fed to each gate's own pattern | the two anti-vacuity preflights inside `run-x86-boot-tests.sh` and `run-aarch64-boot-test-strict.sh` | 0 (both reject it, and both accept a real measured `PASS`) | "PASS_PATTERN accepts the pre-#766 reading ... so this gate would score green on the round-robin wake latency it exists to catch" |

---

## 4. Gates run

Each result below is from the branch's final bytes unless the row says
otherwise. Section 6 gives the digests.

### x86 (beast, `breenix-x86` Incus container, TCG)

| Gate | Result | Receipt |
|---|---|---|
| build, `--features boot_tests,testing,external_test_bins --bin qemu-uefi` | exit 0, `grep -c '^warning'` = 0 | `/tmp/p766-build.log` on the container |
| `docker/qemu/run-x86-boot-tests.sh 1` | **PASS** (exit 0). Oracle `overrun_ms=45 ... :PASS`; `TEST_TALLY: exited=110 nonzero=0 failed=[]` | `serials/766/01-x86-boot-tests-gate-green.txt` |
| `scripts/check-x86-dispatch-no-alloc.sh` | **PASS**, exit 0: "0 allocating call targets in 3 in-scope symbol(s), 19 edge(s) checked" | `serials/766/08-...txt` |
| `docker/qemu/run-x86-prod-profile-boot-test.sh` | **PASS**: "x86 production profile reached steady state with the teardown census at rest"; `test-only marker '[TIMER_WAKE_LATENCY_ORACLE:': 0` | `serials/766/08-...txt` |
| `docker/qemu/run-x86-smp-enum-gate.sh 1` | **PASS** (1 leg): `[X86_SMP_ENUM:madt_cpus=1:enabled=1:...:online=1:max_cpus=1:src=madt:reason=none]` | `serials/766/08-...txt` |
| `docker/qemu/run-x86-boot-tests.sh 1` with the fix reverted (mutation M2) | **FAIL**, exit 1 at `test "$passed" = true`. Oracle `overrun_ms=401 ... :FAIL` | `serials/766/02-x86-boot-tests-gate-red-mutation.txt` |

No death, no panic, no crash marker in any of the runs above: the boot-test
gate's own `\[BOOT_TESTS:FAIL|KERNEL PANIC|panic!` scan is part of its PASS,
and the production gate's `crash_count` and `FAULT_MARKERS` loops read 0.

### aarch64 (this Mac, QEMU)

| Gate | Result | Receipt |
|---|---|---|
| build, `--features boot_tests`, soft-float target | exit 0; 0 kernel warnings. One rustc notice remains, about the toolchain's own `core v0.0.0` crate; it is not this branch's and not kernel code | -- |
| `scripts/check-kernel-no-neon.sh` | **PASS**: 0 FP/SIMD load/store instructions in kernel `.text` | -- |
| `docker/qemu/run-aarch64-boot-test-strict.sh` (script default: 20 boots) | **PASS 20/20**, 713 s. Oracle on boot 1: `overrun_ms=6 ... :PASS` | `serials/766/03-aarch64-strict-gate-20of20.txt`, `serials/766/04-aarch64-strict-boot1-serial.txt` |
| `docker/qemu/run-aarch64-prod-profile-boot-test.sh` | **PASS**; "Observed timer wake latency oracle marker count: 0" | `serials/766/05-aarch64-prod-gate-pass.txt` |

**One earlier strict run scored 19/20 and is preserved rather than dropped.**
On an intermediate build (the same oracle, before the final comment-only edits)
the strict gate scored 19/20: boot 17 died as
`ended_by=hard_timeout`, `guest_uptime_ms=19358` against the gate's 20 s
ceiling, with "Exec smoke did not complete", while the host's own load average
rose 4.10 -> 9.53 across that boot and the two boots either side of it read the
same rising load. The #766 oracle in that boot PASSED (`overrun_ms=20`,
`backstops=0`), and the boot's guest uptime is the tail of a distribution whose
other 19 boots ran 10.2-13.4 s. Attributed to host-load starvation on a machine
that was concurrently driving an x86 gate over ssh, not to this branch; the
gate passed 20/20 on the next run and 20/20 again on the final bytes.
Serials: `serials/766/06-aarch64-strict-19of20-hosted-out.txt` and
`serials/766/07-aarch64-strict-boot17-hard-timeout.txt`.

### Host-side suites (this Mac)

| Suite | Result |
|---|---|
| `tests/*_structure.rs` + `tests/*_clean.rs`, all 45 files via `scripts/run-structure-tests.sh` | **45/45 PASS** |
| `python3 scripts/test_claim_lint.py` | exit 0 |
| `python3 scripts/claim-lint.py` | exit 0 ("clean, 21 file(s) checked, changed hunks vs 783a6a53668d") |

### `clock_gettime_test`, three boots of the fixed kernel

The test and its allowlist are unchanged by this round. The runner is
`docker/qemu/run-boot-parallel.sh 1`, which is a pure runner -- it boots the
`breenix-uefi.img` already in `target/`, which here is the
`boot_tests,testing,external_test_bins` image the gates above built.

| Boot | Test 3 (sub-millisecond precision) | Test 4 (nanosecond precision) | Tally |
|---|---|---|---|
| cgt1 | `Elapsed: 148010 ns` -- **PASS** | 0/10 millisecond-aligned -- PASS | `exited=110 nonzero=0 failed=[]` |
| cgt2 | `Elapsed: 148921 ns` -- **PASS** | 0/10 -- PASS | `exited=110 nonzero=0 failed=[]` |
| cgt3 | `Elapsed: 164233 ns` -- **PASS** | 0/10 -- PASS | `exited=110 nonzero=0 failed=[]` |

Three further readings of the same leg fell out of the gate runs above:
`Elapsed: 150830 ns` and `Elapsed: 308354 ns` on two green boot-test gate runs,
and `Elapsed: 167017 ns` on the mutation run. So 6 of 6 boots in this round read
Test 3 green, against #631's recorded failing readings of 1.30 s, 1.42 s, 1.61 s
and its recorded rates of 1/70, 2/19, 1/20 and 1/10. **That is not a claim that
#631 is fixed**: 6 boots cannot resolve a 1-in-10-to-1-in-70 event, and the
issue is closed already. It is what this round observed, reported because the
task asked for it.

---

## 5. Checker counts, main vs branch

`scripts/check-critical-path-violations.sh` is run from the repository root and
counts pre-existing logging on the critical-path files. `VIOLATION` lines and
total output lines are both recorded, because the script's exit status is 1 on
main as well and only the count is comparable.

`scripts/check-x86-dispatch-no-alloc.sh` needs a built x86 kernel ELF and so is
run on beast.

| Checker | main @ `783a6a53` | branch |
|---|---|---|
| `check-critical-path-violations.sh`, `VIOLATION` lines | 21 | 21 |
| `check-critical-path-violations.sh`, total output lines | 274 | 274 |
| `check-critical-path-violations.sh`, exit | 1 | 1 |
| `check-x86-dispatch-no-alloc.sh` | -- (needs an ELF) | PASS, exit 0 |

The two `check-critical-path-violations.sh` outputs are identical once the
line-number prefixes are stripped (`diff <(sed 's/^[0-9]*://' main) <(sed
's/^[0-9]*://' branch)` is empty); the raw outputs differ only in the four
`scheduler.rs` line numbers the fix's 52 added lines shift. The count does not
grow.

---

## 6. Provenance

Base: `origin/main` @ `783a6a53` ("Merge pull request #878 ..."). `origin/main`
moved to `93309b6c` while this round ran; the branch is not rebased onto it, and
the checker comparison in section 5 is against `783a6a53`, the base the branch
is measured from.

The x86 results are from a clone at `/root/breenix-p766` inside the
`breenix-x86` Incus container on beast, carrying this branch's kernel and gate
scripts as a patch on `783a6a53`. Digests of the three files that decide those
results, identical on that clone and in this working tree:

```
2154e86dfd894deb8c9d854e484b144df9f615ddd5b5a4f29769fff0c69646f9  kernel/src/task/scheduler.rs
04920c2d6788e1b8d8647936cd77effd31b53025a3cee7f4960b9eb3556f002f  kernel/src/task/timer_wake_oracle.rs
eab37b482ba1a75653fa0f6126243e9513afe30e605927ef604a69bbb1c6f6da  docker/qemu/run-x86-boot-tests.sh
```

The mutation run (M2) is the same tree with one token changed at the producing
line; its evidence file records `4308: self.per_cpu_queues[target].push_back(tid);`
where the green file records `push_front`.

Two x86 readings in this record are from an INTERMEDIATE build rather than the
final bytes, and are labelled as such where they appear: the first green
boot-test gate run (`overrun_ms=49`, `setup_ms=520`) and the mutation run
(`overrun_ms=401`). Between those and the final bytes the only edits to
`kernel/src/task/timer_wake_oracle.rs` were comment text (claim-lint
rewording). The final-bytes green run reads `overrun_ms=45`, `setup_ms=509`.

The beast container was shared with other rounds while this one ran
(`/root/breenix-ftc5` and `/root/breenix-p822` clones were driving their own
x86 gates), which is why several of the timings in
`serials/766/08-...txt` include long `QEMU HOST LOCK: waiting ...` stretches.
The lock is what makes those runs sequential; no run of this round was killed
and none of this round's QEMU processes was killed by name.

---

## 7. What is NOT claimed

* **Not claimed: that #766's own 324-trial distribution is reproduced.** This
  round measures one deterministic leg with a fixed peer count. It does not
  re-run the #693 stage-3 driver, and it does not re-derive p90 2592 ms.
* **Not claimed: that the detection term is removed.** The x86 timer interrupt
  handler still raises `need_resched` only at quantum expiry and still does not
  read the timer heap, so a due deadline can wait up to one quantum before any
  `schedule()` pass sees it. That term is inside the bound this oracle asserts
  and is deliberately left alone (RCA section 2 says why).
* **Not claimed: earliest-deadline order among wakes promoted in one pass.**
  The heap pops earliest-deadline-first and each pop goes to the front, so a
  pass promoting several reverses their order relative to one another.
* **Not claimed: anything about #772.** A dispatched, already-woken reader that
  gives the CPU back without consuming its wake is a different mechanism, filed
  separately, and untouched here.
* **Not claimed: that #631 is fixed.** #631 is closed; its `clock_gettime_test`
  Test 3 failure was attributed to #766's overrun distribution. This round
  reports what that test does on the fixed kernel (section 4) and changes
  neither the test nor its allowlist.
* **Not claimed: a boot-time saving.** The oracle ADDS work to the boot-test
  profile (9 kernel threads created, run and joined). `setup_ms` and
  `window_ms` on the marker are what it costs.
