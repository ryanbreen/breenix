# Issue 963: QMP fixture synchronization

Starting revision: `58af1a7fb63e144586b6eef06e47be178eb175d5`.
Code and validation revision: `4ae11b95a2b8f11b981738c8607e68fd9b3d916f`.
Branch: `testfw/963-qmp-backstop-hang-deterministic`.
The [citation audit](963-evidence/citation-audit.log) records source locations
at the code revision.

Diff scope: tests/gate_qmp_backstop_structure.rs, this document, and its
963-evidence artifacts. No kernel, Tier-1, Tier-2, production shell, or
production-profile change.

## Evidence and design

Read `gh issue view 963 --comments`; the default rendering produced no text,
so also read `gh issue view 963 --json title,body,comments` (no comments):
[issue snapshot](963-evidence/issue-963.json).
The issue reports 14/15 tests and failure of the dump-receipt assertion on
Beast, after a prior preflight timeout and a 900-second retry. This round
does not attribute a precise historical scheduling phase from that report.

PR 962 merged at 2026-09-08T02:07:23Z, merge commit 58af1a7f, as verified
with `gh pr view 962 --json mergedAt,mergeCommit,title`:
[PR snapshot](963-evidence/pr-962.json). Its recipe is
[957-DRAIN-RACE-2026-09-07.md](957-DRAIN-RACE-2026-09-07.md) and
`tests/gate_capture_drain_structure.rs:399`, where the sleep function
advances the producer synchronously at the drain wait boundary.

At the validation revision, `scripts/forensic-capture.sh:125` defines the
QMP exchange; lines 132, 134, and 136 sleep for 0.2, 0.2, and 0.5 seconds.
The stop, status, and dump calls are at lines 158, 164, and 175.
The old fixture's three-second whole-command budget thus included startup,
three socket exchanges, and their sleeps. Expiration before dump receipt
was accepted as a timeout by the library, but correctly rejected by the
fixture's receipt assertion.

The revised fake server records receipt, validates paging, and creates
`dump-received` at `tests/gate_qmp_backstop_structure.rs:253`. The fixture's
shell timeout adapter waits for that file at line 281, without a local
upper deadline, and checks that the capture child is still alive. It starts
the three-second Python subprocess deadline at line 288 only after receipt.
The hung server waits in signal.pause instead of answering the dump request. The adapter requires
timeout status 124, records the budget, kills its own child process group,
and reaps the child. TERM/INT handlers run the same cleanup.

This is a fixture clock boundary: startup is suite-bounded, and the pending
hung response is budget-bounded. It does not measure the shipped GNU timeout
end to end across setup. The actual shell library and actual forensic script
still run, with only the fixture timeout operation adapted. The production
whole-capture timeout remains at `docker/qemu/lib/gate-qmp-backstop.sh:100`,
and the separate decoder timeout remains at line 106. The new
`shipped_capture_deadline_and_mutations` ratchet pins the whole-capture
command, positive caller budget, and kill-after escalation. The receipt
ordering ratchet at `tests/gate_qmp_backstop_structure.rs:767` rejects a
missing receipt wait and a deadline placed before receipt.

## Assertion before / after

The following expressions refer to tests/gate_qmp_backstop_structure.rs.
The receipt assertion is unchanged (old lines 474–477, new lines 508–511):

```rust
assert!(
    hanging.log().contains("RECV dump-guest-memory "),
    "hang must reach dump"
);
```

The missing-socket assertion remains
`out.contains("capture=partial:reason=qmp_socket_missing:")`;
the hung assertion remains
`out.contains("capture=partial:reason=qmp_timeout:")`.
Both legs now also assert `out.lines().count() == 1`.
The invalid-budget and missing-socat reason assertions remain.

Before, the missing/invalid legs asserted
`elapsed.as_secs_f64() < 2.0`; the hung/tool-missing legs asserted
`elapsed.as_secs_f64() < 10.0`; the successful capture sibling asserted
`elapsed.as_secs_f64() < 12.0`. The run helper itself used a 15-second timeout.
Exact old bound assertion text in tests/gate_qmp_backstop_structure.rs:

```rust
assert!(elapsed.as_secs_f64() < 2.0);
assert!(elapsed.as_secs_f64() < 10.0, "timeout took {elapsed:?}");
assert!(elapsed.as_secs_f64() < 12.0);
```

After, setup and these whole-fixture elapsed measurements are covered by the
300-second suite timeout used in these runs; elapsed values are diagnostics,
not scheduler-dependent pass conditions. The hung leg additionally asserts:

```rust
assert_eq!(
    fs::read_to_string(hanging.dir.join("bounded")).unwrap(),
    "3"
);
```

That marker is written only after the acknowledged operation exhausts its
three-second response deadline. No unreceived request can create it.
The numeric whole-fixture ceilings are deliberately not retained or claimed.

The successful sibling still requires a complete capture, undecoded fake
bytes, one report line, three capability negotiations, and stop/dump/SIGTERM
ordering. Before, ordering compared millisecond timestamps; after, it compares
positions in the single server's append log (lines 448–451). This avoids
millisecond ties or wall-clock adjustments changing causal order.

## Wait census at the validation revision

| Site in tests/gate_qmp_backstop_structure.rs | Disposition |
| --- | --- |
| Fake server accept/read and signal.pause, lines 238–256 | Await real protocol input or intentional hang; suite owns server lifetime, fixture Drop kills/reaps its tracked child. |
| New receipt poll, lines 281–283 | File acknowledgement; 10 ms polling cadence, no local expiry; child death fails; suite timeout is the ceiling. |
| Hung response, line 288 | Positive three-second subprocess timeout starts after acknowledgement; expiry required. |
| Success child wait, line 294 | Completion wait, suite-bounded; complete core and causal log assertions retained. |
| Server readiness, lines 340–356 | Replaced 100 × 20 ms startup window with a readiness loop and child-liveness check; 20 ms is cadence only; suite-bounded. |
| Fixture run, lines 363–389 | Removed 15-second wrapper that could cut off receipt setup; suite-bounded. |
| SIGTERM acknowledgement, lines 391–401 | Replaced 100 × 20 ms polling window with Child::wait; status must succeed; suite-bounded. |
| Child cleanup, lines 295–300 and 406–414 | Kill owned process/group and reap; suite-bounded OS wait. |
| Python extraction subprocesses, lines 515–579 | No timed producer; suite-bounded completion. |
| Alloc/bind, alloc/free, namespace refusal helpers, lines 617, 688, 717 | Retained independent timeout 10 with kill-after 1; no delayed producer or assertion racing a readiness window. |
| Decode adapter, line 274 | Executes the real timeout utility with the supplied positive budget; this case uses 8 seconds. |
| Elapsed diagnostics and directory timestamps | Reporting/uniqueness only, not waits or pass deadlines. |

Transitive waits in the real forensic script remain unchanged: QMP pacing
at scripts/forensic-capture.sh:132–151, dump settling at lines 182/188, and
GDB timeout at line 325. Startup through receipt is fixture-synchronized;
success completion and extraction are suite-bounded. Direct single-file
runner use needs an external timeout: the recorded commands use
`timeout 300 bash scripts/run-structure-tests.sh gate_qmp_backstop_structure`.
The full gate preflight supplies its own per-suite timeout.

## Mac experiment

Command for each run:
`timeout 300 bash scripts/run-structure-tests.sh gate_qmp_backstop_structure`.
The [driver](963-evidence/load-driver.py) spawned eight Python CPU loops, checked they were alive before
each load run, and terminated/reaped those eight tracked PIDs in finally.
Lane-local TMPDIR and BREENIX_GATE_TMP were used. Socat was installed;
the post-load normal transcript also prints the socat-present branches.
The ARM build/preflight overlapped part of the experiment, adding work beyond
the eight hogs. The first normal run preceded hog creation; another normal
run after hog cleanup passed 17/17 in 5.673 seconds.

| Run | Tests | Exit | Wall seconds | Transcript |
| --- | --- | --- | --- | --- |
| normal | 17/17 | 0 | 5.628 | [normal](963-evidence/normal.log) |
| load-01 | 17/17 | 0 | 5.715 | [load-01](963-evidence/load-01.log) |
| load-02 | 17/17 | 0 | 5.704 | [load-02](963-evidence/load-02.log) |
| load-03 | 17/17 | 0 | 5.708 | [load-03](963-evidence/load-03.log) |
| load-04 | 17/17 | 0 | 5.695 | [load-04](963-evidence/load-04.log) |
| load-05 | 17/17 | 0 | 5.709 | [load-05](963-evidence/load-05.log) |
| load-06 | 17/17 | 0 | 5.716 | [load-06](963-evidence/load-06.log) |
| load-07 | 17/17 | 0 | 5.689 | [load-07](963-evidence/load-07.log) |
| load-08 | 17/17 | 0 | 5.747 | [load-08](963-evidence/load-08.log) |
| load-09 | 17/17 | 0 | 5.826 | [load-09](963-evidence/load-09.log) |
| load-10 | 17/17 | 0 | 5.854 | [load-10](963-evidence/load-10.log) |
| load-11 | 17/17 | 0 | 5.966 | [load-11](963-evidence/load-11.log) |
| load-12 | 17/17 | 0 | 5.839 | [load-12](963-evidence/load-12.log) |
| load-13 | 17/17 | 0 | 5.749 | [load-13](963-evidence/load-13.log) |
| load-14 | 17/17 | 0 | 5.745 | [load-14](963-evidence/load-14.log) |
| load-15 | 17/17 | 0 | 5.721 | [load-15](963-evidence/load-15.log) |
| load-16 | 17/17 | 0 | 5.775 | [load-16](963-evidence/load-16.log) |
| load-17 | 17/17 | 0 | 5.741 | [load-17](963-evidence/load-17.log) |
| load-18 | 17/17 | 0 | 5.733 | [load-18](963-evidence/load-18.log) |
| load-19 | 17/17 | 0 | 5.738 | [load-19](963-evidence/load-19.log) |
| load-20 | 17/17 | 0 | 5.719 | [load-20](963-evidence/load-20.log) |

Result: 20/20 consecutive load runs, 340/340 test executions. This is a finite
experiment, not exhaustive scheduler coverage. Additional normal evidence:
[normal after load](963-evidence/normal-after-load.log).

## Mutation and gates

Temporary external mutation: remove the real whole-capture timeout from
docker/qemu/lib/gate-qmp-backstop.sh. The new deadline ratchet failed,
exit 101: [mutation red](963-evidence/mutation-red.log). A finally block
restored the production file before the restored 17/17 run, exit 0:
[restored](963-evidence/restored.log). These two logs identify the base HEAD
and working fixture scope. No mutation remained at code commit or gate launch.
Embedded mutations also reject missing escalation, a zero budget, removal
of the receipt wait, deadline-before-receipt, and kill-before-QMP ordering.

The ARM build used the requested fork library and
`--target aarch64-breenix-kernel.json`, after copying the requested ARM ELFs
and ext2 image from /Users/wrb/fun/code/breenix. It passed, exit 0:
[ARM build](963-evidence/arm-build.log). The pinned nightly-2025-06-24 core
future-incompatibility notice is accepted under issues 559 and 945 and
[CRITICAL-PATH-DEBT-PR1-2026-09-06.md](CRITICAL-PATH-DEBT-PR1-2026-09-06.md).
No project-source warning/error diagnostic was observed in this build.

`bash docker/qemu/run-aarch64-boot-test-strict.sh 1` passed its 66/66
structure preflight and 1/1 boot, exit 0, 87.802 seconds:
[ARM gate](963-evidence/arm-gate.log). The transcript names the code revision.
The [preflight details](963-evidence/arm-preflight-details.log) retain suite
output; no compiler warning/error diagnostic was found there.

## Beast x86 consumer result

`bash docker/qemu/run-x86-boot-tests.sh` ran once at the code revision in
/root/breenix-t963, with rust-fork linked to /root/breenix/rust-fork-real,
BREENIX_RUST_FORK_LIBRARY unset, and /root/breenix-t963-tmp for both temporary
variables. The detached setsid/nohup gate was polled to completion.
The missing userspace binaries were built once: 151 installed, exit 0,
[userspace transcript](963-evidence/x86-userspace.log).

Preflight passed 66/66 on attempt 1, with context_restore_structure taking
229 seconds against 300, and gate_qmp_backstop_structure taking 8 seconds
including compilation (17/17 tests; test execution 6.37 seconds):
[preflight details](963-evidence/x86-preflight-details.log).
No structure timeout, 900-second override rerun, or preflight skip occurred.
The kernel build and test-disk creation completed. No compiler warning/error
diagnostic was found in the userspace, gate build, or preflight transcripts.

The boot result is **RED**, exit 1:
[x86 gate](963-evidence/x86-gate.log),
[user serial](963-evidence/x86-serial-user.log),
[kernel serial](963-evidence/x86-serial-kernel.log).
The gate exhausted its 900-iteration poll loop; its boot facts span 1014.119
host seconds, with 647 seconds of QEMU CPU. Host load was 13.12 at boot start
and 0.40 at the end. The failure is the assertion at
`docker/qemu/run-x86-boot-tests.sh:1070`, whose text is `test "$passed" = true`.
The serial's timer-wake oracle reports overrun_ms=25, bound_ms=100,
backstops=3, window_ms=30058, measured=1, and FAIL. This prevents the timer
predicate in the pass conjunction; this round does not establish that it is
the only unsatisfied predicate or attribute its cause to host scheduling.
The userspace loopback receive test emitted PASS.

Follow-up: [issue 965](https://github.com/ryanbreen/breenix/issues/965).
The gate was not retried. No SCSI/IO failure was reported, so this is not
classified as issue 871. The x86 source tree stayed at the code revision
with an empty tracked/untracked status: [status](963-evidence/x86-status.log).
The tracked QEMU PID 1735589 was absent after the gate ended. The Mac process
inventory contained no QEMU process; the eight experiment hogs were reaped.
No name-based process kill or other-lane process cleanup was performed:
[cleanup verification](963-evidence/cleanup.log).

Diff recheck against 58af1a7f: tests/gate_qmp_backstop_structure.rs is the
only non-documentation change. The remaining kernel timer investigation is
tracked in issue 965; a successful x86 boot is not claimed. Issue 963 and
this branch remain available for review; no PR merge or issue closure was
performed.

claim-lint: python3 scripts/claim-lint.py --files .tmp/followup.md -> exit 0

## Claim lint and limits

Before the code-plus-ratchets commit:

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/code-msg -> exit 0

Not claimed: reproduction of the historical failed scheduling phase,
whole-fixture completion within the former 2/10/12-second assertions,
real guest-memory failure capture validation, successful x86 boot validation,
resolution of issue 965, production-profile validation,
exhaustive scheduling coverage, kernel changes, PR merge, or issue closure.

The first documentation lint flagged an absolute in the fake-server description;
the sentence was rewritten to name signal.pause.

claim-lint: python3 scripts/claim-lint.py -> exit 1

Before the documentation/evidence commit:

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-msg -> exit 0

Rustfmt check on the changed Rust file and git diff --check returned exit 0.
The final source citations and artifact links are checked again from the
resulting documentation commit before handoff; runtime results remain scoped
to the code revision above.

The first staged whitespace check flagged final blank lines in the two
concatenated preflight artifacts; those terminal blank lines were removed.
Claim lint checks prose/source, and reports the log/JSON artifacts as skipped
extensions; it does not validate their factual contents.

## Astra fix pass: V-1 and V-2 (2026-09-07)

This appendix corrects the earlier response-deadline claim: at baseline
2d3eb32582c5a36a31ec771d9a50cc305c50bc2f, an early child exit 124 could write
the bounded marker without exhausting the three-second wait. The original
adapter plus the new regression returned 124 after 185.889 ms and wrote the
marker: [initial V-1 red](963-evidence/fixpass/v1-red.log). That is fixture
false-positive evidence, not evidence of a shipped capture deadline defect.

V-1: `tests/gate_qmp_backstop_structure.rs:291` now writes the marker inside
`except subprocess.TimeoutExpired`; a normally returned child status raises
an assertion instead. The regression at
`tests/gate_qmp_backstop_structure.rs:350` runs the extracted adapter against
both an early exit 124 child and a child pending beyond its three-second
budget. It asserts exit status and marker absence/presence, checks the expiry
marker value, and checks a three-second elapsed lower bound for expiry.
There is no host-scheduling upper bound on the early-exit leg. The
[detailed green run](963-evidence/fixpass/fixture-green-details.log) observed
early exit rejected with status 1 at 195.978 ms and no marker, then actual
expiry with status 124 at 3.053 seconds and marker value 3. Owned child process
groups are killed and reaped in the adapter's existing finally block.

V-2: the report validator at `tests/gate_qmp_backstop_structure.rs:306`
requires the QMP_DUMP prefix, requested reason, core and decoded-events
placeholders, nonempty ASCII decimal dump_ms, closing bracket and newline,
and no surrounding content. The missing-socket and hung-dump runtime checks
at `tests/gate_qmp_backstop_structure.rs:565` and
`tests/gate_qmp_backstop_structure.rs:581` use it. Two regression tests at
`tests/gate_qmp_backstop_structure.rs:340` and
`tests/gate_qmp_backstop_structure.rs:345` each accept a valid report and
reject 12 malformed variants: bare reason, missing prefix, missing bracket,
missing core, missing decoded events, empty/nondecimal/negative duration,
wrong reason, duplicate report, leading noise, and trailing noise.

### Fix-pass verification and mutations

The transcripts identify baseline HEAD
2d3eb32582c5a36a31ec771d9a50cc305c50bc2f plus the working fixture patch.
The tested final fixture Git blob is
91db3030b883310e54472ee337fc634478e0f28a. Only
`tests/gate_qmp_backstop_structure.rs` changed outside this round document
and its evidence directory. The existing supervised worker performed this
pass directly; the supplied prompt-fixpass.md was retained without edits.

- `python3 docs/planning/green-program/gates/963-evidence/fixpass/run-mutations.py`
  reran four external mutations, each with expected test exit 101: the
  [old adapter](963-evidence/fixpass/v1-old-adapter.log), old report predicate
  for [missing socket](963-evidence/fixpass/v2-old-missing_socket.log) and
  [hung dump](963-evidence/fixpass/v2-old-hung_dump.log), and
  [removed shipped deadline](963-evidence/fixpass/shipped-deadline-removed.log).
  The script restores source in finally blocks before subsequent green runs.
- `bash scripts/run-structure-tests.sh gate_qmp_backstop_structure` passed
  20/20 tests, exit 0: [fixture green](963-evidence/fixpass/fixture-green.log).
  The `_` filter rerun enabled nocapture for the same 20 tests and passed
  20/20, exit 0: [detailed green](963-evidence/fixpass/fixture-green-details.log).
  Socat was present; success capture and the hung-dump timeout legs ran.
- These suite runs execute the existing kill-before-QMP and kill-before-dump
  mutations, the three shipped deadline mutations (missing escalation,
  budget 0, missing timeout), and the two receipt/deadline ordering
  mutations, together with the new malformed-report variants and adapter
  regression. The full sweep also executes embedded ratchets in its other
  discovered suites.
- `bash scripts/run-structure-tests.sh` passed 66/66 discovered suites on
  attempt 1, exit 0: [sweep](963-evidence/fixpass/structure-sweep.log) and
  [66 suite transcripts](963-evidence/fixpass/structure-details.log).
  No structure timeout override or skip was used. No compiler warning/error
  diagnostic was found in those transcripts.
- `rustfmt --edition 2021 --check tests/gate_qmp_backstop_structure.rs`
  and `git diff --check` returned exit 0.

No kernel or shipped gate change remains after mutation restoration
(`git diff --quiet -- kernel docker scripts` returned exit 0). Therefore the
conditional `bash docker/qemu/run-aarch64-boot-test-strict.sh 1` rerun was not
invoked for this fixture-only pass. No kernel build or cargo test was run.
The earlier ARM and x86 transcripts retain their earlier revision scope.
The known x86 failure remains tracked in issue 965.
[Cleanup inventory](963-evidence/fixpass/cleanup.log) found no QEMU process;
this pass launched 0 QEMU processes and performed no name-based cleanup.

Before the fix-pass commit:

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/fixpass-msg -> exit 0

Not claimed: resolution of issue 965, a new ARM or x86 boot result,
production-profile validation, real guest-memory capture validation,
exhaustive scheduling coverage, PR merge, or issue closure. Claim lint
checks prose/source shape and skips log artifacts; the test transcripts
supply the execution evidence.

The initial appendix lint flagged the budget wording and the process-count
wording; both were rewritten with numeric values before the final check.

claim-lint: python3 scripts/claim-lint.py -> exit 1

The final pre-commit lint runs below include the staged evidence runner.

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/fixpass-msg -> exit 0

Evidence logs have trailing whitespace and terminal blank lines trimmed after
the staged whitespace check flagged two panic transcripts. Test output and
verdicts are otherwise retained. The subsequent staged whitespace check passed.

## Landing (2026-09-08): NOT LANDED

Supplied PROSE findings: []; deferred code findings: []. The round document
passed the requested whole-file check. No inline finding required an edit.

claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/gates/963-QMP-BACKSTOP-2026-09-07.md -> exit 0

`git fetch origin && git merge origin/main` reported Already up to date:
main was 58af1a7fb63e144586b6eef06e47be178eb175d5 and was already an ancestor
of aa6097b1edfa272d071e8c93af9881b728aa3792. No merge commit or conflict was
produced. The gate revision was aa6097b1edfa272d071e8c93af9881b728aa3792.
No scorer requirement grew and no R182 fixture re-record was required.
No source or mutation change was made during this landing attempt.

| Required command | Revision | Result | Transcript |
| --- | --- | --- | --- |
| `bash scripts/run-structure-tests.sh` | aa6097b1 | GREEN, 66/66 suites, exit 0 | [structure](963-evidence/landing/structure.log) |
| `bash docker/qemu/run-aarch64-boot-test-strict.sh 1` | aa6097b1 | GREEN, 66/66 preflight suites, 1/1 boot, exit 0 | [ARM](963-evidence/landing/arm-gate.log) |
| `bash docker/qemu/run-x86-boot-tests.sh` on Beast, one invocation | aa6097b1 | RED, 66/66 preflight suites after internal retry, boot exit 1 | [x86](963-evidence/landing/x86-gate.log) |

The Mac used lane-local TMPDIR/BREENIX_GATE_TMP, the requested fork-library
path, copied ARM ELFs and ext2 image, and the soft-float kernel target.
[ARM build](963-evidence/landing/arm-build.log) exited 0 with the accepted
pinned core future-incompatibility notice. The saved
[structure details](963-evidence/landing/structure-details.log) and
[ARM preflight details](963-evidence/landing/arm-preflight-details.log)
contain no compiler warning/error diagnostic.

Beast was fast-forwarded to the gate revision, with the rust-fork symlink
and lane-local temporary paths; BREENIX_RUST_FORK_LIBRARY was unset.
The missing userspace ELF directory triggered one build, which installed
151 binaries and exited 0: [userspace](963-evidence/landing/x86-userspace.log).
The gate's optional BusyBox build subsequently reported a missing
x86_64-linux-musl-gcc and continued without BusyBox; a clean
aggregate build is not claimed. The Rust kernel build itself completed.

Beast context_restore_structure timed out at 300 seconds with host load
16.15. The runner's built-in 600-second retry passed in 291 seconds:
[x86 preflight details](963-evidence/landing/x86-preflight-details.log).
The planned explicit BREENIX_STRUCTURE_SUITE_TIMEOUT_SECS=900 gate rerun
was not launched because the same invocation subsequently reached a boot
failure, triggering the requested STOP. No preflight skip was used.
The gate waited behind other lanes' QEMU lock owners, then booted with
host load 0.70 at start and 1.76 at end. Boot facts report 1004.494 host
seconds, 585 QEMU CPU seconds, and ended_by=poll_exhausted.

The assertion at docker/qemu/run-x86-boot-tests.sh:1070 returned exit 1.
The timer-wake oracle in [user serial](963-evidence/landing/x86-serial-user.log)
reports overrun_ms=4, bound_ms=100, backstops=3, window_ms=30064,
measured=1, and FAIL. This fails the timer predicate; this attempt does not
establish that it is the only unsatisfied predicate or identify the root
cause. The userspace loopback receive test passed. The
[kernel serial](963-evidence/landing/x86-serial-kernel.log) is retained too.
The existing timer follow-up is issue 965. No SCSI/IO failure classification
was made; issue 871 is not claimed as the cause.

The previous 20/20-under-load table above remains scoped to 4ae11b95 and
17 tests per run; it is not a 20-run experiment at the final 20-test fixture.
No new load experiment was performed in this landing attempt.

The landing sequence stopped before PR creation, merge, branch deletion,
Mac worktree removal, issue 963 closure, or the requested fixed-on-main
comment on PR 964. The branch and Mac worktree remain for follow-up.
A later remote read found main at c93ecf5297f42bf4127849f0b26961d74c25f85c;
the recorded gates do not validate that later revision. The branch has
unlanded commits, so logMainBranchEmpty is false.

Not claimed: successful x86 boot, resolution of issue 965, a 900-second
whole-gate rerun, production-profile validation, real guest-memory capture
validation, exhaustive scheduler coverage, a clean optional BusyBox build,
validation of the later main revision, PR merge, or issue closure.

Before the landing evidence commit:

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/landing-msg -> exit 0

The initial Landing lint flagged the aggregate-build limitation wording;
it was rewritten before the final check.

claim-lint: python3 scripts/claim-lint.py -> exit 1

After evidence capture, Beast's /proc command lines, cwd/exe links, open
file links, and memory maps identified 0 processes using the lane clone
or temporary directory. Both lane paths were then removed under the
requested disk-hygiene rule: [Beast cleanup](963-evidence/landing/beast-cleanup.log).
The failed gate's QEMU PID 1888908 had exited. No name-based kill was used.
The Mac worktree is retained because landing stopped.
The Mac inventory found one QEMU process in the battery5 worktree, not this
lane; it was left untouched: [Mac inventory](963-evidence/landing/mac-cleanup.log).
Saved transcript trailing whitespace was trimmed for git diff --check.

Final pre-commit checks:

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/landing-msg -> exit 0
