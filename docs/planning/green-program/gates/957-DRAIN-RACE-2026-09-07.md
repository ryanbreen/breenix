# Issue 957: deterministic capture-drain fixture

Original code revision: `76d7b725dac238867527b1156313efe8d83fb9c5`. Starting HEAD: `3a8a973b013d`.
[Issue snapshot](957-evidence/issue-957.json). Issue 957 was read with `gh issue view 957 --comments` and JSON fields.
Its current report describes a FIFO fixture stall, not an unmodified 150 ms appender:
the enabled file already contained END while its shell remained blocked. The
specific blocked FIFO operation remains unproved.

Diff scope: host test fixtures, shared preflight, runner, and evidence/docs only;
no kernel code changes, Tier-1 edits, or Tier-2 edits.

## Assertion before and after

At starting HEAD, tests/gate_capture_drain_structure.rs had a stale 150 ms header,
but its oracle used FIFO release/acknowledgement and four legs, including two
300 ms startup delays. It asserted disabled => capture=partial and enabled =>
capture=complete against the same BEGIN/event and remaining event/END bytes.

Rechecked at `fd9b291f`, tests/gate_capture_drain_structure.rs:381 retains that assertion.
The shell sleep function synchronously appends the remaining bytes when the
real library reaches its wait boundary (line 399). Disabled draining reports
before this transition; enabled draining reports after it. Both fixtures are
then advanced to the same complete state and checked by the real classifier
(line 404). No producer process, FIFO, real sleep, or scheduling deadline is
needed. The production drain library is unchanged. This tests ordering and
classification; it does not measure real drain duration or establish FIFO root cause.

## Preflight and ratchet

Re-derived at `fd9b291f`: docker/qemu/lib/gate-structure-preflight.sh:154 defaults
to 300 seconds. Lines 168–185 allow two attempts, only exit 124 triggers the
second, with double the original budget. Other failures remain failures without
retry. Attempts retain separate logs, aggregate logs, wall seconds and host
`uptime` load averages sampled at timeout. Metrics print in sorted suite order
when the workers finish and are available in each metrics file as attempts finish.

scripts/run-structure-tests.sh:45 now invokes the shared preflight when no suite
is specified. Explicit stems still use rustc directly. The seven discovered gate
callers are recorded in [gate-census.txt](957-evidence/gate-census.txt), including
strict ARM at line 364, x86 boot at 155, and x86 production at 967.

The retry behavior test in tests/structure_preflight_parallel_structure.rs uses
a controlled timeout command to prove 7 -> 14 seconds for only alpha, no retry
of bravo, final timeout red, and ordinary failure red without retry. Its original
helper mutation is red: [mutation-red.log](957-evidence/mutation-red.log).
Restored helper: [retry-green.log](957-evidence/retry-green.log), 6/6 tests.
The existing kill-before-drain mutation also passes its rejection assertion.
Mutations were restored before gate runs.

## Sleep census

Source search: `grep -n sleep tests/*_structure.rs`, captured at revision
`bc40e9a3ed1e064371c461f5f781718c58f8ec50`: 87 matching lines in
[sleep-census.log](957-evidence/sleep-census.log). V-1 corrected: the prior
22-line selection was incomplete and included three non-sleep matches. The
replacement includes comments and source-inspection identifiers as well as
executed sleeps; omitted matches did not identify another executed long sleep.

| Location | Starting behavior | Resolution |
| --- | --- | --- |
| tests/gate_capture_drain_structure.rs oracle | Two 300 ms startup sleeps; real settle sleep; FIFO handshake | Synchronous transition; no wall-clock dependence |
| tests/gate_qmp_backstop_structure.rs:255 | 1 second sleep repeated while intentionally hanging dump | signal.pause(), terminated by the existing fixture cleanup; timeout assertion retained |
| tests/teardown_structure.rs:5185 | Literal sleep 2 | Source matching only; no sleep execution |

No executed sleep over 100 ms remains in the discovered structure source census.
claim-lint:ok: 957-evidence/sleep-census.log records the source census; the three rows above distinguish execution from string matching.

Shorter polling remains: 20 ms QMP readiness/lifecycle polling; 10 ms parallel
fixture rendezvous polling; the intentionally hung suite sleeps exactly 100 ms.
Those fixtures retain their existing watchdogs. No load-derived replacement
budget was needed for the removed sleeps; no over-15-minute sleep repair was
identified. context_restore_structure is CPU work, not a wall-clock sleep;
its optimization is not claimed here.

## Eight CPU hogs: twenty consecutive drain runs

[load-proof.py](957-evidence/load-proof.py) starts eight owned `yes` processes,
runs the twenty commands and then the discovered suites, and terminates/waits
only those PIDs in finally. Build and test time are included. Revision is
recorded in [load-results.txt](957-evidence/load-results.txt). Result: 20/20 pass.

| Run | Wall | Exit |
| --- | --- | --- |
| drain-01 | 0.308s | exit=0 |
| drain-02 | 0.312s | exit=0 |
| drain-03 | 0.316s | exit=0 |
| drain-04 | 0.311s | exit=0 |
| drain-05 | 0.319s | exit=0 |
| drain-06 | 0.322s | exit=0 |
| drain-07 | 0.309s | exit=0 |
| drain-08 | 0.303s | exit=0 |
| drain-09 | 0.311s | exit=0 |
| drain-10 | 0.324s | exit=0 |
| drain-11 | 0.316s | exit=0 |
| drain-12 | 0.313s | exit=0 |
| drain-13 | 0.327s | exit=0 |
| drain-14 | 0.330s | exit=0 |
| drain-15 | 0.329s | exit=0 |
| drain-16 | 0.333s | exit=0 |
| drain-17 | 0.325s | exit=0 |
| drain-18 | 0.304s | exit=0 |
| drain-19 | 0.319s | exit=0 |
| drain-20 | 0.311s | exit=0 |

## Discovered suites under the same load

Command: `bash scripts/run-structure-tests.sh`. Result: 66/66, exit 0,
81.722 seconds total. [all-suites.log](957-evidence/all-suites.log) records the
per-suite integer-second measurements, including compilation. The ARM build
also overlapped part of this run; load was not limited to the eight hogs.

| Suite | Attempt | Budget seconds | Wall seconds | Exit |
| --- | --- | --- | --- | --- |
| aarch64_testing_profile_structure | 1 | 300 | 0 | 0 |
| block_request_lifetime_structure | 1 | 300 | 0 | 0 |
| blocking_fd_eagain_structure | 1 | 300 | 0 | 0 |
| capture_bxcap_schema_structure | 1 | 300 | 0 | 0 |
| capture_path_lock_free_structure | 1 | 300 | 0 | 0 |
| context_restore_structure | 1 | 300 | 79 | 0 |
| coreproof_component_h_structure | 1 | 300 | 0 | 0 |
| coreproof_coverage_structure | 1 | 300 | 0 | 0 |
| coreproof_mutation_register_structure | 1 | 300 | 0 | 0 |
| coreproof_sites_structure | 1 | 300 | 0 | 0 |
| critical_path_logging_census_structure | 1 | 300 | 1 | 0 |
| ctx_diag_ring_sample_structure | 1 | 300 | 0 | 0 |
| degenerate_transfer_fd_validation_structure | 1 | 300 | 0 | 0 |
| dispatch_fact_census_structure | 1 | 300 | 0 | 0 |
| dispatch_path_lock_free_structure | 1 | 300 | 0 | 0 |
| dispatch_strand_census_structure | 1 | 300 | 0 | 0 |
| dma_and_log_sink_structure | 1 | 300 | 0 | 0 |
| entry_point_df_structure | 1 | 300 | 0 | 0 |
| exec_lock_order_structure | 1 | 300 | 1 | 0 |
| exit_tally_structure | 1 | 300 | 1 | 0 |
| ext2_disk_size_structure | 1 | 300 | 0 | 0 |
| ext2_lock_structure | 1 | 300 | 0 | 0 |
| fcntl_pm_contention_gate_structure | 1 | 300 | 0 | 0 |
| fork_lock_order_structure | 1 | 300 | 1 | 0 |
| gate_boot_facts_pipefail_structure | 1 | 300 | 1 | 0 |
| gate_boot_facts_structure | 1 | 300 | 1 | 0 |
| gate_capture_drain_structure | 1 | 300 | 1 | 0 |
| gate_qmp_backstop_structure | 1 | 300 | 4 | 0 |
| gate_structure_preflight_wiring_structure | 1 | 300 | 0 | 0 |
| green_program_envelope_structure | 1 | 300 | 0 | 0 |
| launcher_smoke_xhci_evidence_structure | 1 | 300 | 0 | 0 |
| lockup_capture_guard_structure | 1 | 300 | 2 | 0 |
| loopback_pump_structure | 1 | 300 | 4 | 0 |
| masked_binary_load_structure | 1 | 300 | 0 | 0 |
| mmap_floor_structure | 1 | 300 | 0 | 0 |
| net_lock_structure | 1 | 300 | 3 | 0 |
| parallels_capture_structure | 1 | 300 | 2 | 0 |
| parallels_kill_by_name_structure | 1 | 300 | 1 | 0 |
| pipe_fifo_blocking_structure | 1 | 300 | 2 | 0 |
| poll_tcp_gate_wiring_structure | 1 | 300 | 0 | 0 |
| preempt_bracket_structure | 1 | 300 | 0 | 0 |
| qemu_host_lock_structure | 1 | 300 | 0 | 0 |
| qemu_kill_by_name_structure | 1 | 300 | 1 | 0 |
| ring_span_report_site_structure | 1 | 300 | 0 | 0 |
| ring_span_unfiltered_report_site_structure | 1 | 300 | 0 | 0 |
| run_inspector_import_structure | 1 | 300 | 0 | 0 |
| serial_line_atomicity_structure | 1 | 300 | 0 | 0 |
| signal_eintr_predicate_structure | 1 | 300 | 0 | 0 |
| strand_handoff_structure | 1 | 300 | 1 | 0 |
| structure_preflight_parallel_structure | 1 | 300 | 6 | 0 |
| syscall_return_register_structure | 1 | 300 | 1 | 0 |
| teardown_rustfmt_structure | 1 | 300 | 1 | 0 |
| teardown_structure | 1 | 300 | 24 | 0 |
| terminal_edge_capture_structure | 1 | 300 | 0 | 0 |
| timer_wake_dispatch_structure | 1 | 300 | 0 | 0 |
| trace_ring_depth_structure | 1 | 300 | 0 | 0 |
| tracing_provider_gate_structure | 1 | 300 | 0 | 0 |
| ttbr0_shadow_reconciliation_structure | 1 | 300 | 24 | 0 |
| tty_irq_fg_structure | 1 | 300 | 3 | 0 |
| tty_irq_pm_structure | 1 | 300 | 2 | 0 |
| tty_oracle_structure | 1 | 300 | 0 | 0 |
| udp_ports_lock_irq_structure | 1 | 300 | 0 | 0 |
| udp_socket_lock_irq_structure | 1 | 300 | 0 | 0 |
| x86_boot_tests_profile_structure | 1 | 300 | 1 | 0 |
| x86_smp_enum_structure | 1 | 300 | 1 | 0 |
| xhci_wait_irq_order_structure | 1 | 300 | 0 | 0 |

## Consumer gates

ARM strict: `bash docker/qemu/run-aarch64-boot-test-strict.sh 1`, preflight
66/66 and boot 1/1, exit 0. [arm-gate.log](957-evidence/arm-gate.log) begins with
the code revision. Soft-float boot_tests build completed in 25.48 seconds;
[arm-build.log](957-evidence/arm-build.log) contains the pinned core warning
tracked by existing issues 945 and 559. No warning suppression was added.
The coordinator accepts this pinned nightly toolchain notice under the
precedent described below; it does not violate the project-source diagnostic
requirement. The notice remains visible in the transcript.

context_restore_structure measured 79 seconds in the loaded run and 71 seconds
in the ARM consumer preflight. Neither needed a timeout retry.

Beast x86 preflight: 66/66, capture-drain 2 seconds and context-restore
204 seconds, each on attempt 1 with the default 300-second budget. No manual
900-second rerun and no BREENIX_GATE_SKIP_STRUCTURE setting were used.
X86 command: `bash docker/qemu/run-x86-boot-tests.sh`, one run, PASS,
exit 0. [x86-gate.log](957-evidence/x86-gate.log) starts with the code revision
and preserves the preflight, build, shared-lock wait, and final verdict.
The lane waited for another lane's live QEMU lock without changing it.
The x86 build transcript contains no warning/error lines. No SCSI/IO failure
or manual retry occurred. The tracked QEMU PID 1440475 was absent after exit;
the ARM gate likewise left no owned QEMU process. The eight load generators
were terminated and waited by the proof driver's finally block.

No additional sleep repair issue was required. Existing issues 945 and 559
track the pinned toolchain notice; issue 957's precise FIFO stall cause
is still not established by this fixture replacement.

## Claim lint

Initial code lint found one unquantified usage comment; changed it to discovered
suites and reran successfully before the code commit.

Initial code lint (usage-comment finding):

claim-lint: python3 scripts/claim-lint.py -> exit 1

Before code commit:

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/code-msg -> exit 0

Initial document lint (three prose/source-census findings, subsequently corrected):

claim-lint: python3 scripts/claim-lint.py -> exit 1

First documentation commit: the final tree lint found one wording issue, but
the shell sequence incorrectly continued to commit. Corrected in the follow-up
evidence commit; the command sequence now stops on lint failure.

claim-lint: python3 scripts/claim-lint.py -> exit 1
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-msg -> exit 0

The issue JSON and raw .log transcripts are outside claim-lint's extension
scope. The round document, changed source, and load driver are checked.

## Environment note

The inherited hook on the code commit printed an issue export to
/Users/wrb/fun/code/breenix/.beads/issues.jsonl. This was an automatic git hook,
not requested task tracking. The documentation commit disables hooks for that
invocation (`git -c core.hooksPath=/dev/null commit`) to avoid repeating this
cross-worktree side effect. No shared hook configuration was edited.

## Not claimed

- Root cause of the former FIFO shell stall.
- Real-time drain performance or exhaustive scheduler interleavings.
- context_restore_structure optimization.
- Production-profile gates, kernel changes, or a new ARM build in this PIL continuation.
- Resolution of the pinned core NEON warning (existing issues 945 and 559).

Final documentation correction:

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-fix-msg -> exit 0

The .log transcripts were initially omitted by the repository ignore pattern.
The correction explicitly adds the referenced evidence logs.

## Prior landing attempt (historical)

Deferred code findings: [] (no findings supplied).

V-1 evidence-completeness correction replaces the selected census with the
requested grep output; it does not change the race fixture or its oracle.

`git fetch origin` succeeded; `git merge --no-commit --no-ff origin/main`
reported Already up to date. Origin main is `3a8a973b013dbbee09f17c660c0f310ecdf3640e`,
an ancestor of this branch. No merge commit was generated by Git. No incoming
scorer requirements changed, so no R182 fixture re-record was needed.

Explicit lint including the raw grep log flagged quoted source text; the log
is retained byte-for-byte. Prose-only lint is recorded separately.

claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/gates/957-DRAIN-RACE-2026-09-07.md docs/planning/green-program/gates/957-evidence/sleep-census.log -> exit 1
claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/gates/957-DRAIN-RACE-2026-09-07.md -> exit 0
claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/landing-census-msg -> exit 0

Prior landing result: **NOT LANDED** (superseded for the PIL structure blocker below). Validation revision:
`ccd61b512efc97b587f01bb1efe74e89a2127c6f`.

`bash scripts/run-structure-tests.sh` returned exit 1: 65/66 suites passed.
The parallels_capture_structure suite returned 101 with 7 passed and 8 failed;
its Python fixture setup reported ModuleNotFoundError for PIL. See
[landing-structure.log](957-evidence/landing-structure.log) and
[landing-parallels-failure.log](957-evidence/landing-parallels-failure.log).
This was not a timeout, and no timeout-budget retry was made.

The ARM soft-float boot_tests build returned exit 0 and emitted the pinned
core NEON future-incompatibility notice. The prior landing assessment called
this a blocker; that assessment is superseded by the coordinator ruling below. See [landing-arm-build.log](957-evidence/landing-arm-build.log).
`cargo report future-incompatibilities --id 1` identified NEON target-feature
ABI diagnostics in the pinned nightly core library. Existing open issues
945 and 559 track this toolchain notice. No suppression or shared toolchain edit was made.

Neither requested boot ran at the validation revision. The Beast lane clone
was fast-forwarded to that revision, but its x86 userspace ELF files were
missing; its build and boot were not launched after the validation stop.
No PR was created or merged, and no branch or worktree was deleted.
Issue 957 remained open at that stop; the earlier 20/20 load result above remains evidence at its recorded
revision, not a new landing-tip run. No QEMU process was launched this turn.

At that prior stop, landing gates, a PR merge, main ancestry after merge, and
issue completion were not claimed. The new ancestry and structure results
below supersede the corresponding prior limitations.

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/landing-stop-msg -> exit 0


## PIL continuation and current evidence

Code and ratchets are committed together at
`fd9b291f0c9359e9fc0fc068f26e07cf34714cda`. The earlier load and boot transcripts
remain evidence at their recorded `76d7b725` revision; they were not rerun in
this host-tooling continuation. No Tier-1 or Tier-2 file changed.

`scripts/lib/python-with-pil.sh:5` probes, in order: nonempty BREENIX_PYTHON,
PATH-resolved python3, /opt/homebrew/bin/python3, /usr/local/bin/python3, and
/usr/bin/python3. Each executable must successfully import Image, ImageChops,
and ImageDraw from PIL before selection; unsuccessful candidates are skipped.
The helper exports BREENIX_PYTHON or returns an explicit dependency error.
`tests/support/python_with_pil.rs:6` delegates to that same resolver and caches
its result for fixture generation, avoiding a second candidate list.

The import census in [pil-citation-audit.log](957-evidence/pil-citation-audit.log)
finds three shell consumers: scripts/f23-render-verdict.sh,
scripts/f24-render-verdict.sh, and scripts/parallels/capture-display.sh. Those
three source the helper and execute their image operations with BREENIX_PYTHON.
The Rust fixture imports use the helper too. The consumer ratchet recursively
checks shell scripts under scripts/ and docker/qemu/. No image classification
threshold or fixture success criterion was relaxed.

Fresh interpreter probes: on this Mac, /usr/bin/python3 failed to import PIL,
and /opt/homebrew/bin/python3 loaded Pillow 12.3.0; on Beast's breenix-x86
container, /usr/bin/python3 loaded Pillow 10.2.0. Restricted-PATH resolution
selected those respective working interpreters. See
[pil-probe-mac.log](957-evidence/pil-probe-mac.log) and
[pil-probe-beast.log](957-evidence/pil-probe-beast.log). The Beast probe ran the
working-tree helper on stdin; it was an interpreter probe, not a boot gate or
structure suite. The Homebrew path is a candidate, not a universal requirement.

The exact restricted command also required scripts/run-structure-tests.sh:64
to locate rustup via CARGO_HOME (default HOME/.cargo) when rustc is absent from
PATH. This preserves the requested PATH for the test executable and its Python
subprocesses. No package installation, toolchain modification, or preflight
skip was used.

`PATH=/usr/bin:/bin bash scripts/run-structure-tests.sh parallels_capture_structure`
passed 18/18, exit 0, at code tip fd9b291f:
[pil-restricted-tip.log](957-evidence/pil-restricted-tip.log).
The normal-PATH pre-commit full suite passed 66/66, exit 0:
[pil-full.log](957-evidence/pil-full.log), labeled with its base revision and
working-tree changes. Those tested source changes became fd9b291f.

`git fetch origin` succeeded. `git merge origin/main` returned exit 0,
already up to date: origin/main remained
`3a8a973b013dbbee09f17c660c0f310ecdf3640e`, an ancestor of fd9b291f. Git created
no merge commit. [pil-merge.log](957-evidence/pil-merge.log) records both hashes.
After that merge command, `bash scripts/run-structure-tests.sh` passed 66/66,
exit 0, at fd9b291f with the normal PATH:
[pil-merged-full.log](957-evidence/pil-merged-full.log). The transcript records
that revision. Both full runs finished on attempt 1 with the default 300-second
budget; neither needed a timeout-budget rerun.

The bypass-import-probe mutation failed the unusable-override regression with
exit 101: [pil-mutation.log](957-evidence/pil-mutation.log). Restoring the F23
script to unmodified 436313ed source failed the consumer-wiring ratchet with
exit 101: [pil-wiring-red.log](957-evidence/pil-wiring-red.log). Both mutations
were restored before the subsequent full-suite and committed-tip proof runs.
The 18-test restricted run also covers a working override path containing spaces.

V-1 re-verification at fd9b291f: the exact command
`grep -n sleep tests/*_structure.rs` still produces 87 matching lines,
byte-identical to sleep-census.log. The prior ccd61b51 census correction stands.
The citation audit re-reads the drain oracle, preflight retry, QMP hang fixture,
source-only sleep match, runner, helper, and accepted-notice precedent. Earlier
boot/load numbers above retain their historical revision scope.

Accepted notice: the pinned nightly-2025-06-24 core -Z build-std
future-incompatibility notice on aarch64 is a toolchain notice, not a project
source warning or landing blocker. Precedent: issues 559 and 945 and
[CRITICAL-PATH-DEBT-PR1-2026-09-06.md:635](CRITICAL-PATH-DEBT-PR1-2026-09-06.md),
which distinguishes the core-source notice from kernel diagnostics. No
suppression was added. The project-source zero-warning requirement still applies.

Before the PIL code commit:

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pil-code-msg -> exit 0

Before the PIL documentation commit:

Two draft lint runs flagged wording in the not-claimed paragraph and its
lint explanation; revised those sentences and reran.

claim-lint: python3 scripts/claim-lint.py -> exit 1
claim-lint: python3 scripts/claim-lint.py -> exit 1
claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pil-doc-msg -> exit 0

Continuation not claimed: fresh ARM or x86 boot results, production-profile
results, a PR/main merge, issue completion, a repaired Rust nightly, real VM
capture validation, FIFO root cause, or a rerun of the earlier eight-hog load experiment.
No QEMU process was launched by this continuation; other lanes' processes and
worktrees were left untouched. The obsolete t957 worktree was already absent
from the worktree registry, so no worktree removal was needed.
