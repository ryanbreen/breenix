# Critical-path logging debt, PR-0: the census ratchet (#R191)

**Branch:** `gates/chk-pr0-census-ratchet`, from `origin/main`
`783a6a53668d99225e356530a124c081d1fdcbd3` (2026-09-06). No kernel code moved
in this round -- `kernel/src/` is untouched.

## What this round did

Per the drain plan's PR-0 row (`docs/planning/green-program/gates/
CRITICAL-PATH-DEBT-2026-09-06.md`, "§7 Drain plan" → "PR-0"):

1. Added `tests/critical_path_logging_census_structure.rs`, a host-side
   structure suite that pins `scripts/check-critical-path-violations.sh`'s
   current census per `(file, item-path)`, reusing the
   `code_mask`/`item_spans`/`item_path_at`/`census_diff`/`with_synthetic_
   source` machinery `tests/serial_line_atomicity_structure.rs` already
   carries, and the shell-parity discipline `tests/
   capture_path_lock_free_structure.rs` already carries for the capture
   directory's own denylist.
2. Widened the shell script's `PROHIBITED_PATTERNS` with three spellings
   that reach the same locks and escaped the original twelve:
   `serial_print!`, `log_serial_print!`, `log::log!`.
3. Wrote five anti-vacuity mutation tests, each its own `#[test]` asserting
   `Err(...)`.
4. Filed the plan itself at `docs/planning/green-program/gates/
   CRITICAL-PATH-DEBT-2026-09-06.md` (verbatim body, a short provenance
   header) and this round doc.

## The two censuses, and where their numbers came from

`CRITICAL_PATH_LOG_ANCHORS` (48 rows, summing to 135) is not hand-transcribed
from the plan doc's table. It was generated by writing the same `code_mask`/
`item_spans`/`item_path_at` pipeline as a standalone binary, running it
against this branch's checked-out tree, and pasting its output into the
`const`. The generator and its output are not part of this commit (scratch
tooling only); the const in the committed suite is what that pipeline
produces against `783a6a53` today, which this round's own `cargo test`/
`rustc --test` runs (below) re-confirm independently.

The wider census is not a second hand-maintained table -- `wider_anchors()`
appends exactly one row (`kernel/src/arch_impl/aarch64/
exception.rs :: fn sys_write`, count 1) to `CRITICAL_PATH_LOG_ANCHORS` at
test time, so "wider = narrow + the one escaped site" is visible in code
rather than asserted in two tables that could drift.

## Evidence

### Structure suite, standalone

```
$ bash scripts/run-structure-tests.sh critical_path_logging_census_structure
== compiling critical_path_logging_census_structure ==
== running critical_path_logging_census_structure  ==

running 9 tests
test the_shell_and_this_suite_check_the_same_shapes ... ok
test shell_parity_validator_rejects_a_removed_critical_file ... ok
test zero_pin_files_stay_clean ... ok
test zero_pin_validator_rejects_a_synthetic_violation_in_timer_rs ... ok
test critical_path_log_census_is_pinned ... ok
test census_validator_rejects_a_synthetic_violation_at_a_checked_path ... ok
test census_validator_rejects_a_deleted_anchor_row ... ok
test census_validator_rejects_a_decremented_anchor_count ... ok
test critical_path_log_wider_census_is_pinned ... ok

test result: ok. 9 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.14s
```

Also confirmed via `cargo test --test critical_path_logging_census_structure`
(the root crate builds against this worktree's `rust-fork` symlink): same 9
tests, same result, 0.20s, `Finished` in 28.78s including the workspace
build.

### 43 of 43 `tests/*_structure.rs` suites, standalone

Ran each file matching `tests/*_structure.rs` through
`scripts/run-structure-tests.sh <name>` individually (43 files, including
the new one). 43 of 43 exited 0. Per-suite logs are under `/tmp/pr0-struct2-
*.log` on the machine that ran this round (not committed -- scratch
evidence, not repository content).

### `scripts/claim-lint.py`

```
$ python3 scripts/claim-lint.py
claim-lint: clean (2 file(s) checked, changed hunks vs 783a6a53668d).
claim-lint: 3 pre-existing finding(s) outside this branch's changed hunks not reported (--whole-file shows them).
```
(2 files = `scripts/check-critical-path-violations.sh` and `tests/
critical_path_logging_census_structure.rs`, the two files this round edits
outside of docs.)

```
$ python3 scripts/claim-lint.py --files docs/planning/green-program/gates/CRITICAL-PATH-DEBT-2026-09-06.md
claim-lint: clean (1 file(s) checked, whole files).
```

### `scripts/claim-lint.py`, re-run after this round's own fixes (F1-F3)

A review of the round found three minor findings; this round addresses 3
of 3: F1 added a doc-comment paragraph to `tests/critical_path_logging_
census_structure.rs` disclosing that the pin's validator is a full exact
match (a DECREASE at an existing anchor fails the run too, not only an
increase); F2 corrected an invented symbol name
(`CRITICAL_PATH_LOG_WIDER_ANCHORS`, which does not exist in the suite) in
this doc's "two censuses" section to reference the real `wider_anchors()`
function it was already describing correctly in the next sentence; F3 is
this subsection.

With F1 and F2 on disk, and this subsection drafted up to the point above
this paragraph, the diff-mode check still reports clean against the same
four files as the first run of this round (the base commit is unchanged,
so a rerun after review-round edits still diffs against `783a6a53668d`):

```
$ python3 scripts/claim-lint.py
claim-lint: clean (4 file(s) checked, changed hunks vs 783a6a53668d).
claim-lint: 3 pre-existing finding(s) outside this branch's changed hunks not reported (--whole-file shows them).
```

`--whole-file` confirms those 3 pre-existing findings (lines 28, 159, 212,
each a `NEVER`/`all` with no `claim-lint:ok`) are unchanged from the run
recorded above -- each one sits in `scripts/check-critical-path-
violations.sh`'s original comments carried over from before this branch;
0 of 3 were introduced or touched by this round or this fix.

The finding this subsection answers observed that a doc cannot quote a
linter run over its own not-yet-final text. That is still true here: the
invocation quoted above predates the words in this paragraph. What
actually gates this round's push is `python3 scripts/claim-lint.py` run
once more, on the tree as committed, immediately before `git push`, per
CLAUDE.md's requirement that command exit 0 first -- not a run captured
inside the doc it is checking.

### `scripts/test_claim_lint.py`

Exit 0. The tool's own regression corpus (miss/flag cases, the two
verified-true `FLAGGED` cases that need `claim-lint:ok` and the two `clean`
cases) is unchanged by this round; ran to confirm the linter itself was not
altered.

### `scripts/check-critical-path-violations.sh`, before and after the pattern widening

Before (original `PROHIBITED_PATTERNS`, twelve spellings):

```
$ bash scripts/check-critical-path-violations.sh; echo "EXIT=$?"
... (274 stdout lines, 9 `VIOLATION in <file>` headers) ...
EXIT=1
```

After (fifteen spellings -- the twelve plus `serial_print!`,
`log_serial_print!`, `log::log!`):

```
$ bash scripts/check-critical-path-violations.sh; echo "EXIT=$?"
... (275 stdout lines) ...
EXIT=1
```

`diff` of the two runs' stdout is exactly one line:

```
131a132
> 1985:        crate::serial_print!("{}", byte as char);
```

Exit status is unchanged (1 before, 1 after -- the script was already red on
`main`, per the plan doc's own header). The `+1` line count (274 → 275, not
274 → 274+3) is expected: `serial_print!` is the only one of the three new
spellings with a live site in a checked file today (`log_serial_print!` and
`log::log!` match 0 lines in the checked set), and `serial_print!` itself is
not a substring of any already-reported line, so it adds exactly the one new
row and does not trigger the report-inflation multiplication the plan's §1
describes for the original twelve.

### On-disk mutation

```
$ sha256sum kernel/src/arch_impl/aarch64/context.rs
e4464418b95255797589ec020cc6db182c41b0998b7f6a4d323c0b9fb41f1768  kernel/src/arch_impl/aarch64/context.rs
```

Appended one `serial_println!` inside a new function to the end of
`kernel/src/arch_impl/aarch64/context.rs` (one of the three `ZERO_PIN_FILES`
and a file `CRITICAL_PATH_LOG_ANCHORS` carries no row for). Reran the
structure suite:

```
test result: FAILED. 2 passed; 7 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.14s
```

7 of 9 tests failed -- every test whose validator sees the real tree
(`critical_path_log_census_is_pinned`, `critical_path_log_wider_census_is_
pinned`, `zero_pin_files_stay_clean`) and every mutation test whose expected
`Err(...)` no longer matches once a second, unplanned `+` row is mixed in
(`census_validator_rejects_a_deleted_anchor_row`,
`census_validator_rejects_a_decremented_anchor_count`,
`census_validator_rejects_a_synthetic_violation_at_a_checked_path`,
`zero_pin_validator_rejects_a_synthetic_violation_in_timer_rs`). The two
that kept passing (`the_shell_and_this_suite_check_the_same_shapes`,
`shell_parity_validator_rejects_a_removed_critical_file`) check the shell
script's array text, which this mutation never touched.

Reverted with `cp` from a pre-mutation backup and re-hashed:

```
$ sha256sum kernel/src/arch_impl/aarch64/context.rs
e4464418b95255797589ec020cc6db182c41b0998b7f6a4d323c0b9fb41f1768  kernel/src/arch_impl/aarch64/context.rs
```

Identical hash, and `git status --short` shows no diff on that file. Reran
the suite: 9 passed, 0 failed.

## What this round deliberately did not do

The plan doc's own PR-0 section says the suite should be "named in
`docker/qemu/run-x86-boot-tests.sh` and `docker/qemu/run-aarch64-boot-test-
strict.sh` beside the existing structure-suite calls." Checked the tree at
this commit for that wiring before adding to it: neither script invokes
`scripts/run-structure-tests.sh` or `cargo test --test <structure-suite>`
today, and a repo-wide search finds no gate script anywhere under
`docker/qemu/` that calls `run-structure-tests.sh`. `scripts/run-structure-
tests.sh`'s own header already says as much ("It does not run in any gate
... Wiring it into a gate is a separate change with its own review"). So the
plan's "beside the existing calls" premise does not hold against this tree,
and this round's actual DELIVER/PROOF list (from the dispatching brief) asks
only for `scripts/run-structure-tests.sh critical_path_logging_census_
structure` green plus the full suite battery -- not for gate wiring.

Separately, `docker/qemu/run-aarch64-boot-test-strict.sh` carries its own
documented hazard for exactly this kind of addition: its
`require_boot_tests_kernel` comment states that "ANY `cargo test` in the
same session" silently rebuilds and hardlinks a kernel binary without the
`boot_tests` feature over the one that gate expects, and that an earlier
acceptance battery that ran the structural suites and then this gate scored
0/6 against a production kernel that was never asked to emit the gate's
markers. A same-session `cargo test --test critical_path_logging_census_
structure` call added naively into that script would risk exactly that
failure mode. `scripts/run-structure-tests.sh` sidesteps it (it compiles via
`rustc --test` directly, not `cargo test`), so wiring this suite into that
gate -- if wanted -- should go through that script, sequenced before any
`boot_tests` kernel build in the same session, as its own deliberate change
with its own review, per that script's header. Left undone here rather than
done unreviewed.

## Files touched

- `tests/critical_path_logging_census_structure.rs` (new)
- `scripts/check-critical-path-violations.sh` (`PROHIBITED_PATTERNS`
  widened by three entries, plus the comment explaining them)
- `docs/planning/green-program/gates/CRITICAL-PATH-DEBT-2026-09-06.md` (new,
  the plan filed verbatim with a provenance header)
- `docs/planning/green-program/gates/CRITICAL-PATH-DEBT-PR0-ROUND-2026-09-06.md`
  (this file)

## Landing re-smoke

Landed at `eae95309e745433cd17056a2ce05ca8843f4e718` from a fresh worktree
(`git worktree add ... origin/gates/chk-pr0-census-ratchet`). `git merge
--no-ff origin/main` reported "Already up to date." -- `origin/main` at
`783a6a53668d99225e356530a124c081d1fdcbd3` is already an ancestor of this
branch's HEAD (`git log --oneline HEAD..origin/main` at landing time: 0
commits -- the branch was built directly on that commit), so no merge
commit was created and no anchor table needed re-deriving. `kernel/` is
untouched by this diff (`git diff --stat origin/main..HEAD`: 4 of 4
changed files are under `docs/`, `scripts/`, or `tests/` -- 0 under
`kernel/`).

### `scripts/claim-lint.py`

```
$ python3 scripts/claim-lint.py
claim-lint: clean (4 file(s) checked, changed hunks vs 783a6a53668d).
claim-lint: 3 pre-existing finding(s) outside this branch's changed hunks not reported (--whole-file shows them).
```
Exit 0. Same "4 files, 3 pre-existing findings" shape the round's own
re-run recorded above.

### 43 of 43 `tests/*_structure.rs` suites, standalone

Ran every file matching `tests/*_structure.rs` (43 files, alphabetical,
including `critical_path_logging_census_structure`) through
`scripts/run-structure-tests.sh <name>` individually. 43 of 43 exited 0,
689 individual `#[test]` cases passed, 0 failed. Per-suite summary in
`/tmp/structure_suite_summary.log` on the landing machine (scratch
evidence, not repository content); `critical_path_logging_census_structure`
itself: 9 passed, 0 failed, 0.15s, matching the round's own run above.

### `scripts/test_claim_lint.py`

```
$ python3 scripts/test_claim_lint.py
...
Ran 72 tests in 1.722s

OK
```
Exit 0. Same historical-corpus catch rates (9/12 in-context, 15/18
isolated-quote) and verified-true corpus shape as before this round; this
round does not touch the linter.

### `scripts/check-critical-path-violations.sh`

```
$ bash scripts/check-critical-path-violations.sh; echo "EXIT=$?"
... (275 stdout lines, 9 `VIOLATION in <file>` headers) ...
EXIT=1
```
275 lines, 9 headers, exit 1 -- identical to the round's own "after"
measurement above. The script is unchanged since that measurement; this is
an independent re-run at the landing commit, not a copy of that number.

### aarch64 strict boot gate, one run at the script's default iteration count

Built the `boot_tests` kernel first (`cargo build --release --features
boot_tests --target aarch64-breenix-kernel.json -Z build-std=core,alloc -Z
build-std-features=compiler-builtins-mem -p kernel --bin kernel-aarch64`),
then ran `docker/qemu/run-aarch64-boot-test-strict.sh` (default 20
iterations) with a private `BREENIX_GATE_TMP` pointed at a directory under
this landing worktree's own scratchpad, so its per-boot output could not
collide with the concurrent gate another workflow had running on this host
at the same time (`docker/qemu/lib/qemu-host-lock.sh` serialized the two
across QEMU launches; both ran to completion). No `cargo test` ran in this
same shell session before this build -- all 43 of 43 structure-suite runs
above went through `scripts/run-structure-tests.sh`'s direct `rustc --test`
path, not `cargo test` -- so the `require_boot_tests_kernel` hazard this
round's own "What this round deliberately did not do" section describes
(a same-session `cargo test` silently relinking a non-`boot_tests` kernel
over this gate's expected binary) does not apply here.

```
=========================================
RESULTS
=========================================
Total iterations: 20
Successes: 20
Failures: 0
Success rate: 100%
Duration: 529s

=========================================
PASS: 20/20 boots succeeded
=========================================
```

### claim-lint on this section

This subsection's own first draft tripped the linter 3 of 3 times on its
own text before this paragraph existed (two unquantified-absolute findings
in the merge paragraph above, one in the aarch64-gate paragraph); each was
reworded to name an explicit count rather than suppressed, and a rerun
after each edit is what brought the finding total down to 0. The doc
quoted above still predates the words in this paragraph, so what actually
gates this round's push is the run below, on the tree as committed,
immediately before `git push`:

```
$ python3 scripts/claim-lint.py
claim-lint: clean (4 file(s) checked, changed hunks vs 783a6a53668d).
claim-lint: 3 pre-existing finding(s) outside this branch's changed hunks not reported (--whole-file shows them).
```
Exit 0.
