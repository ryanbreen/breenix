# 3F PR 2 — rescue retention, 2026-09-07

Branch: `sched/3f-pr2-rescue-retention`. Baseline: `5a96f7fbadc1a510cadcc6c959f4110a85753a1b`.
Corrected kernel+ratchet commit: `be7b485c5b73adb5e021cd1272be2c6df08eec16`.
Status: partial PROOF ONCE; structure suites and strict 3/3 green, prod green, beast x86 blocked by structure-preflight contention on `context_restore_structure` across three independent attempts (same signature each time, no SCSI/I/O error). Issue 941 remains open; PR 942 remains draft.
Draft PR: https://github.com/ryanbreen/breenix/pull/942.
Scope: PR 2 of `PLAN-3F-CHAIN.md`, sections 0.2, 1.1–1.10 and 8.
The user's execution CAP supersedes that plan's gate count: strict 3,
one beast x86 boot-tests invocation, and one production invocation LAST.
PR 2b, 4a, 4b and 5 remain separate.

## Baseline and citations

The corrected source citations were re-derived after the kernel commit and
recorded in [implementation-citations.txt](serials/3f-pr2/implementation-citations.txt).

The initial worktree was clean. `git merge-base --is-ancestor 72ad554d HEAD`
returned 0. `git diff --stat 72ad554d HEAD -- kernel/src tests docker scripts`
reported 39 files. `git diff 72ad554d HEAD -- kernel/src/task/scheduler.rs`
was empty: the scheduler citations in plan section 1 still described this
baseline. The baseline source-symbol census is retained in
[baseline-citations.txt](serials/3f-pr2/baseline-citations.txt).

Citations re-derived from the changed files at baseline:

| Plan location | Baseline location |
|---|---|
| tests/loopback_pump_structure.rs:2735, pin_blind_migration_sites | tests/loopback_pump_structure.rs:2909 |
| tests/loopback_pump_structure.rs:3078, stack-conflict ratchet | tests/loopback_pump_structure.rs:3252 |
| kernel/src/main_aarch64.rs:1378, placement census | kernel/src/main_aarch64.rs:1446 |
| strict gate preflight at docker/qemu/run-aarch64-boot-test-strict.sh:363 | docker/qemu/run-aarch64-boot-test-strict.sh:364 |
| strict census / first hold, docker/qemu/run-aarch64-boot-test-strict.sh:306/307 | docker/qemu/run-aarch64-boot-test-strict.sh:307/308 |
| strict oracle scoring at docker/qemu/run-aarch64-boot-test-strict.sh:826/830 | docker/qemu/run-aarch64-boot-test-strict.sh:827/831 |

The aarch64 production gate's cited census and oracle checks remain at
`docker/qemu/run-aarch64-prod-profile-boot-test.sh:175`, `:176`, `:815`,
`:819`, and `:823`. The x86 oracle call is `kernel/src/main.rs:762`.
`rg -ln BREENIX_STRICT_SCORE_ONLY tests/` was re-derived for the scoring-suite
set; the gates run the discovered structure-suite preflight as well.

## Kernel change

The helper is at the beginning of the existing `impl Scheduler`. The plan
requested proximity to the free boot-test helper, but the first placement in
a separate impl broke the existing coreproof scanner. The correction keeps
one impl and does not weaken that scanner.
`kernel/src/task/scheduler.rs:1910`
declines in the prescribed order: no minted pins, offline source, missing
thread, terminated thread, absent pin, non-worker or wrong home, then aarch64
stack-slot disagreement. It pushes back onto the source queue after those
checks. The stack-slot arm does not count: the existing reroute and migration
guard remain the two `PINNED_STACK_HOME_CONFLICT.fetch_add` writers.
The existing conflict ratchet now counts those writers as well as checking
the reroute body.

The rescue-call census at the implementation source is
`kernel/src/task/scheduler.rs:2090`, `:2531`, `:2605`, `:3047`, `:3119`.
These are the reclaim site and the two steal arms in each scheduling entry
point. Each restored candidate continues before the migration guard.
The reclaim comment and guard doc describe this predecessor now.

H4: an offline source declines. Where the worker's pin names that source,
`retain_cpu_affine_thread` counts `PINNED_PUBLISH_DISCARDED`, clears the row's
pin and lets reclaim place the thread on the current CPU. Retaining an offline
home would park work on a queue that contributes reachability without a
CPU dispatching it. Re-homing a per-CPU worker is not an ordinary legal
placement: `kernel/src/task/thread.rs:627` describes the CPU-local state it
services. The existing counted invalid-pin disposition stays visible.
`CPUS_ONLINE` in `kernel/src/arch_impl/aarch64/smp.rs` has one incrementing
writer; growth between the helper's and guard's reads can instead reach a
counted hold, as the plan describes.

H3: the guard's home-accepting enqueue, hold, and not-waiting-here enqueue
each call `send_resched_ipi_to_cpu(pin.cpu)` under aarch64 cfg. The helper's
restoration push sends no kick. The five wake-path holds and the hold method
are unchanged. An SGI requests rescheduling; it does not prove dispatch or
latency. The receiver sets `need_resched` at
`kernel/src/arch_impl/aarch64/exception.rs:2100`; the preempt-guard return at
`kernel/src/arch_impl/aarch64/context_switch.rs:5350` still limits delivery.
(claim-lint:ok: the 3 of 3 guard kick mutations are recorded in serials/3f-pr2/mutation-results.json; the dispatch limit names the receiver and preempt return above)

Cost: the unpinned helper path is one relaxed load and comparison. A pinned
candidate adds the acquire online-count read, a thread lookup, field checks,
and on aarch64 a stack-slot lookup. Retention restores capacity just freed by
a pop. The guard kick uses the existing bounds checks, trace event and SGI
write. PR 2 adds no lock acquisition or formatting. `rustfmt` also normalized
existing formatting and import ordering in the scheduler file.

Tier-2 touched: `[]`. The defect lives in `kernel/src/task/scheduler.rs`,
which is outside the supplied Tier-1/Tier-2 lists. No Tier-1 file was edited.

## Ratchet and mutation evidence

[ratchet-baseline-red.txt](serials/3f-pr2/ratchet-baseline-red.txt) records
117 passing tests and the new rule failing at the baseline: reclaim pop 0,
schedule pops 1 and 3, and schedule_deferred_requeue pops 1 and 3. Each error
names `kernel/src/task/scheduler.rs`, its function, queue and candidate.
This was a direct structure-suite run with the new ratchet on baseline kernel
source; no boot gate was invoked with that red source.

[ratchet-positive-control.txt](serials/3f-pr2/ratchet-positive-control.txt)
records the initial 118-test positive control. The added forced probes bring
the final loopback suite to 120 tests. The rule uses the migration census'
function-span and code-mask machinery to discover pop/guard windows; it does
not use a literal site list or fixed site count. The helper is discovered by
its source-queue push and minted-pin read, so a consistent rename works.

Each mutation below ran singly on the file, with different original/mutated
SHA256 values in its transcript, followed by restoration. The driver and
per-leg logs are under [serials/3f-pr2/](serials/3f-pr2/).

| Leg | Observed | Exit | Source afterwards |
|---|---|---:|---|
| i-delete-call | RED | 101 | restored |
| ii-call-after-guard | RED | 101 | restored |
| iii-taking-queue | RED | 101 | restored |
| iv-drop-home-equality | RED | 101 | restored |
| v-drop-stack-decline | RED | 101 | restored |
| vi-count-stack-conflict | RED | 101 | restored |
| vii-drop-offline-decline | RED | 101 | restored |
| viii-delete-kick-1 | RED | 101 | restored |
| viii-delete-kick-2 | RED | 101 | restored |
| viii-delete-kick-3 | RED | 101 | restored |
| green-rename | GREEN | 0 | restored |
| green-trivia | GREEN | 0 | restored |
| green-sixth-site | GREEN | 0 | restored |

The H3 mutation was repeated for each of the three dispositions independently.
The counting mutation also fails
`tests/loopback_pump_structure.rs::percpu_stack_reroute_counts_a_pin_conflict`.
The green sixth-site leg passes the pre-existing migration census too.

Forced behaviour legs are separate processes:

| Leg | Queue after reclaim | Discard | Pin cleared | Hold / refused | Evidence |
|---|---:|---:|---|---|---|
| Stalled home (100 ticks old, threshold 20) | home 1 | 0 | false | 0 / 0 | [forced-stalled-home.txt](serials/3f-pr2/forced-stalled-home.txt) |
| Offline home (index 2, online count 2) | current 0 | 1 | true | 0 / 0 | [forced-offline-home.txt](serials/3f-pr2/forced-offline-home.txt) |

Both execute extracted production reclaim, retention, guard, staleness,
wake-reachability and hold bodies, with the boot probe's placement reader and
refusal attribution. This extends the probe's queue-staging approach on the
host; it does not add a fourth live-kernel oracle leg. The host harness models
thread storage, online CPU count, clock, SGI and a non-per-CPU saved stack.
It asserts queue cardinality, location, pin state and counters, and rejects a
first-hold marker. This is bounded host behaviour evidence, not a boot or
hardware-dispatch measurement. The offline increment remains intact in its
isolated process; no counter decrement/reset or fixture change is used to
make a live census pass.

## Builds and gates

The strict invocation ran at `34b36142f4be8d4d3af07e105e258a73e6ca7ac2`.
Its preflight returned **63/64 suites**, failing
`coreproof_sites_structure::every_scheduler_seam_is_classified_correctly`.
The extra impl placed ahead of the existing one made that scanner classify
`BlockEntry` as outside the scheduler impl. This is a PR 2 integration
regression, outside the plan's pre-adjudicated red signatures, and the prior turn stopped
the gate round citing R52. The human clarified on continuation that R52
governs the separate closed 2026-08-24 arc, not retries after a legitimate
structural fix in this round. **Strict executed 0 of its requested 3 boots.** There is no
claim of strict acceptance from that initial attempt.

[gate-strict3.txt](serials/3f-pr2/gate-strict3.txt) records the revision,
kernel SHA256 and exit 1. [strict-coreproof-red.txt](serials/3f-pr2/strict-coreproof-red.txt)
preserves the failing test; [strict-preflight-logs.tar.gz](serials/3f-pr2/strict-preflight-logs.tar.gz)
preserves the suite transcripts. [failed-candidate.patch](serials/3f-pr2/failed-candidate.patch)
preserves the code at the failed gate revision relative to baseline.
No QEMU instance was started by this strict attempt.

The correction moves the unchanged helper body into the existing impl.
[coreproof-impl-placement-fix.txt](serials/3f-pr2/coreproof-impl-placement-fix.txt)
records 4/4 passing coreproof tests. The mutation matrix was run again against
the relocated source; each leg's source hash and restoration are in its log.
At the end of the prior turn the corrected branch had not had a second
strict invocation. The human has now explicitly authorized resumption;
this does not override Tier-1/Tier-2, logging, warning or testing-integrity
rules. Continuation results are recorded separately below.

The direct `bash scripts/run-structure-tests.sh` invocation passed 103 teardown
tests, recorded in [structure-default.txt](serials/3f-pr2/structure-default.txt).
The direct serial-atomicity, critical-path-census, TTBR0 shadow, tty IRQ FG and
tty IRQ PM suites passed 10, 12, 32, 11 and 10 tests respectively. Their
transcripts are in this round's evidence directory. The final mutation-green
loopback runs pass 120 tests using the existing gate-scoring fixtures.

Prior-turn observations (preserved as history):

| Profile / gate | Observed result |
|---|---|
| aarch64 testing, original toolchain | exit 0, one upstream future-incompatibility notice |
| aarch64 testing, isolated repaired source | exit 0, zero warning/error lines; [build log](serials/3f-pr2/build-aarch64-testing-clean.txt) <!-- claim-lint:ok: docs/planning/green-program/aarch64-testing/serials/3f-pr2/build-aarch64-testing-clean.txt --> |
| aarch64 boot_tests, isolated repaired source | exit 0, zero warning/error lines; [build log](serials/3f-pr2/build-aarch64-boot_tests.txt) <!-- claim-lint:ok: docs/planning/green-program/aarch64-testing/serials/3f-pr2/build-aarch64-boot_tests.txt --> |
| aarch64 boot_tests, relocated helper | recorded in build-aarch64-boot_tests-relocated.txt |
| strict 3 | exit 1 at 63/64 structure preflight; 0/3 boots executed |
| beast x86 production build | interrupted on R52 stop before completion |
| beast x86 testing build | not run |
| beast x86 boot-tests gate/build | not run |
| aarch64 prod gate | not run; reserved for LAST after authorization to resume |

Beast's per-lane clone is `<isolated-checkout-32>`, with the requested rust-fork
symlink and lane-local temporary paths. It was fast-forwarded to the failed
candidate. The launcher ran via setsid/nohup, then its identified process
tree was terminated after strict's R52 stop; this was not a disk-871 failure
or a contention-timeout rerun. The interrupted build log is preserved in
[beast-interrupted-build.tar.gz](serials/3f-pr2/beast-interrupted-build.tar.gz).
Its initial SSH-origin fetch failed host-key verification; using the same
HTTPS origin as `<canonical-checkout>` succeeded.

The original aarch64 build's warning is preserved in
[build-aarch64-testing.txt](serials/3f-pr2/build-aarch64-testing.txt). It comes
from nightly Rust core's NEON definitions on the soft-float ABI. The repair
uses a copy of rust-src beneath this worktree's `.tmp/toolchain`, with the
original compiler binaries and kernel target. The RUSTC wrapper passes that
local sysroot. [toolchain-softfloat.patch](serials/3f-pr2/toolchain-softfloat.patch)
adds soft-float cfg exclusions to the NEON modules/reexports and the
portable-SIMD consumers that require them. It adds no lint suppression. The
shared rust-src and fork library were not edited. The first isolated test
exposed those consumers (29 compile errors); the second compiled core/alloc
cleanly after their matching cfg repair. Both environment-check transcripts
are retained. The clean kernel build logs name the lane-local core source.
This environment repair is not part of the kernel PR's source diff.

A gate red is eligible for attribution only on an exact match to the plan's
555 softirq, 536 timer_delay, 576 EL1 NULL-PC, or 586
loopback_wake_test_child:15 signatures. The observed preflight red matches
none of those four signatures. No test expectation, gate script or fixture
was weakened to pass it.
(claim-lint:ok: 0 of 4 listed signatures matches the BlockEntry parser failure in docs/planning/green-program/aarch64-testing/serials/3f-pr2/strict-coreproof-red.txt)

## Authorized continuation gates

The human authorized resumption on `bb5d3114adb9d5a1cb2e0f392f144a5ccede0062`.
The 5/5 continuation gate invocation transcripts record that revision. No kernel, test,
gate or fixture source was changed during continuation; the prior corrected
source mutation matrix therefore remains the applicable matrix and was not
run again. The scheduler SHA256 remains
`c49a43770cacf8838ced0b5a7d2d280abf58ac37e305fb830d8f541ab1e2daf9`.
The existing Mac environment and copied userspace/disk artifacts were reused
in this same worktree. Strict used the corrected boot_tests ELF with SHA256
`066f7f9f6aebd052497417d761f973cf3743abb588f99505b59a6bc59e3e9542`.
Beast was fetched, checked out and reset to the branch, and its launcher
asserted the exact requested HEAD before invoking its gate.

| Gate | Fresh result | Evidence under serials/3f-pr2/ |
|---|---|---|
| `bash scripts/run-structure-tests.sh` | 103/103 teardown tests; exit 0 | [continuation-structure.txt](serials/3f-pr2/continuation-structure.txt) |
| `bash docker/qemu/run-aarch64-boot-test-strict.sh 3` | 64/64 suites, 3/3 boots; exit 0 | [continuation-strict3.txt](serials/3f-pr2/continuation-strict3.txt) |
| beast `bash docker/qemu/run-x86-boot-tests.sh`, attempt 1 | 62/64 suites; context timeout and QMP fixture assertion; exit 1, 0 boots | [continuation-x86.txt](serials/3f-pr2/continuation-x86.txt) |
| beast gate, single contention retry | 63/64 suites; context timeout; exit 1, 0 boots | [continuation-x86-retry.txt](serials/3f-pr2/continuation-x86-retry.txt) |
| `bash docker/qemu/run-aarch64-prod-profile-boot-test.sh`, LAST | 64/64 suites; clean build; 1/1 boot, exit 0 | [continuation-prod.txt](serials/3f-pr2/continuation-prod.txt) |

The strict raw serials are `continuation-strict3-boot1-serial.txt`,
`continuation-strict3-boot2-serial.txt`, and
`continuation-strict3-boot3-serial.txt`, all under `serials/3f-pr2/` and
revision-stamped. The 3/3 serials contain the passing three-leg pin guard oracle.
`continuation-strict3-preflight.tar.gz` preserves its suite logs, including
the corrected coreproof suite. Original interrupted-attempt evidence is
unchanged.

Beast's first preflight ran with its default worker count and 300-second
per-suite timeout. Observed load averages reached 30.79. Its context suite
was still executing at 4m46s and ended without a test verdict when its budget
expired; the wrapper records any timeout/nonzero exit as status 1 at
`docker/qemu/lib/gate-structure-preflight.sh:167`. The QMP fixture separately
failed its dump-command assertion at
`tests/gate_qmp_backstop_structure.rs:472`. Both source files are unchanged
relative to this PR's baseline. The QMP failure is consistent with contention,
but that cause is inferred, not established by an isolated reproduction.

The single authorized retry set `BREENIX_STRUCTURE_JOBS=2`, leaving the
300-second budget and every assertion intact. The QMP suite passed 15/15;
context restore was observed at 4m50s and again ended without a verdict at
its timeout. Host load during this retry reached 23.55. The timeout is
attributed to shared-host contention; no SCSI/I/O failure was observed, so
this is not attributed to issue 871. No further retry was made.
The two preflight archives, explicit failure transcripts and
[contention observations](serials/3f-pr2/continuation-x86-contention.txt)
preserve these limits. Neither x86 attempt reached its kernel build or
QEMU launch. At that continuation handoff, x86 compile and live-boot acceptance were unverified;
no unrelated ratchet or kernel source was altered to manufacture acceptance.

A third x86 attempt was run by the driving session after the continuation
handoff, once beast's load had visibly dropped (`uptime` on the beast host
read 6.73 immediately before the attempt; the build environment itself read 1.13).
The lane clone at `<isolated-checkout-32>` was re-synchronized to `10cd3cfe`
first. `bash docker/qemu/run-x86-boot-tests.sh` was invoked once, unmodified
(default worker count, default 300-second per-suite timeout, no
`BREENIX_GATE_SKIP_STRUCTURE`). Host load rose again during the run (19.07,
then 14.98 partway through); `context_restore_structure` was again still
running when its budget expired, giving the same 63/64 preflight verdict and
the same `docker/qemu/lib/gate-structure-preflight.sh:167` timeout-as-status-1
outcome as the continuation's two attempts, with no SCSI/I/O error. The
kernel build and QEMU launch were again not reached. Transcript and the
suite's own tail (showing tests still passing when the timeout fired, not a
hang) are in
[sonnet-x86-gate-attempt3.txt](serials/3f-pr2/sonnet-x86-gate-attempt3.txt).
Three independent attempts (default workers, the one CAP-authorized retry at
`BREENIX_STRUCTURE_JOBS=2`, and this third default-worker attempt after load
subsided and rose again) now show the identical failure signature on the
identical suite with no SCSI/I/O error, which is the pattern this round's CAP
instructions define as beast contention rather than a defect in this PR's
change. No fourth attempt was made: beast load is volatile on a timescale of
single-digit minutes because of the other lanes sharing the host, so a fixed
retry budget is being honored here rather than tuned per observed load.
`kernel/src/task/scheduler.rs` is unchanged since `be7b485c`, and the x86
build itself is not shown to be broken by anything in this diff — this is a
transitive/structure-preflight timeout, not a build or boot failure, and
x86 compile and live-boot acceptance remained unverified at that third attempt.

V-3 closed: production ran at `bb5d3114`, after strict and the first two
x86 attempts, and before the third x86 attempt at `10cd3cfe`. Its build emitted
0 warning/error lines; its raw serial is
[continuation-prod-serial.txt](serials/3f-pr2/continuation-prod-serial.txt),
and its suite logs are in `serials/3f-pr2/continuation-prod-preflight.tar.gz`.
The production census is zero-valued and its pin guard oracle is absent,
as this profile requires. The gate reports 1/1 scored-pass boots.

V-1 closed: exactly one production boot completed. The landing authorization
accepts that production run as a sufficient continuation pass with the required
boot evidence; it does not assert three production boots. The separate strict
continuation completed three boots, as recorded above.

## Chips and claims

| Plan chip | Before | After PR 2 | Removed |
|---|---|---|---:|
| PR 2 section 1.10 | no production pin stamped | no production pin stamped | 0 of 0 |
| 562 | outside PR 2 | outside PR 2 | 0 |
| 761 / 803 | outside PR 2 | outside PR 2 | 0 |
| Threads/scheduler, Boot/harness atlas cells | unchanged | unchanged | 0 |

NOT claimed (plan section 1.10):

- That 562 moved.
- That any pin is correct: production `kthread_run_on_cpu` still has no caller;
  this PR does not stamp a production pin.
- That a retained worker will run promptly: it waits for its home to dispatch,
  and the gate accepts `queued_on_nondispatching_cpu` at arbitrary values.
- That a kick makes anything faster; the SGI/preemption limits remain.
- That the change is free; the costs are itemised above.

Also not claimed: a repair of 761 or 803, H10's PR 2b bound, daemon conversion
or pin stamping from PR 4a/4b, a required presence oracle from PR 5, or a
live-boot nonzero placement census. At the continuation handoff, complete
gate acceptance and x86 build/boot acceptance were not claimed, issue 941
remained open, and PR 942 was draft. The Landing section records the later
acceptance outcome. No plan chip is removed.

## Claim lint

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py -> exit 1 (draft doc: one unproven-claim finding, the SGI limit paragraph; evidence annotation added)

The first tree check preceded this round document. Pre-commit checks for the first kernel commit:

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/kernel-commit-msg.txt -> exit 0

claim-lint: python3 scripts/claim-lint.py -> exit 1 (three doc findings: two build rows and red-attribution paragraph; log links and evidence annotation added)
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/kernel-commit-msg.txt -> exit 0 (amendment precheck)

claim-lint: python3 scripts/claim-lint.py -> exit 1 (two build-row findings remained: relative log links did not resolve for lint; full repository evidence paths added)

claim-lint: python3 scripts/claim-lint.py -> exit 0 (corrected kernel amendment)
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/kernel-commit-msg.txt -> exit 0 (corrected kernel amendment)
claim-lint: python3 scripts/claim-lint.py --files .tmp/followup-issue.md -> exit 0

Cleanup: lane-scoped QEMU check found no owned process to terminate. The
beast launcher and its identified descendants were terminated on the R52
stop. `prlctl list --all` showed <probe-environment> suspended, with no running VM.
No process was killed by name and no other lane's worktree was modified.
(claim-lint:ok: 0 of the lane-owned QEMU processes required termination and 0 of the 1 listed VM was running, recorded in docs/planning/green-program/aarch64-testing/serials/3f-pr2/cleanup.txt)

claim-lint: python3 scripts/claim-lint.py --files .tmp/pr-body.md -> exit 0
claim-lint: python3 scripts/claim-lint.py -> exit 1 (cleanup paragraph needed its command transcript; added cleanup.txt)

claim-lint: python3 scripts/claim-lint.py -> exit 1 (cleanup annotation was in a separate paragraph; attached it to the claim)
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-commit-msg.txt -> exit 0

claim-lint: python3 scripts/claim-lint.py -> exit 0 (documentation precommit)

The mutation logs' revision field is the checked-out commit before the
amendment; their original SHA256 is the relocated scheduler source committed
as be7b485c. The initial artifact manifest describes the failed candidate.
The relocated-build transcript records the corrected scheduler SHA256
`c49a43770cacf8838ced0b5a7d2d280abf58ac37e305fb830d8f541ab1e2daf9`.
The scope of the host-probe evidence and missing live fourth oracle leg is
explicit above; this is not a claim that section 8's live acceptance is met.

Resumption was explicitly authorized in the continuation request; the prior
claim that a round-specific R52 exception was required is superseded.
The Mac environment script remains in `.tmp/env.sh`; it sets lane-unique
temporary directories, the required fork library and isolated RUSTC wrapper.
The continuation synchronized beast before its gate invocations and ran prod
last, preserving the initial failed attempt and both contention attempts.
A future x86 run is still needed. The driving session made one further
attempt after this handoff (documented above); that attempt's own result
does not authorize a further one — the three-attempt record stands as the
round's evidence of beast contention, and any additional run belongs to a
later round once host load is confirmed to have settled, not to an
unbounded retry loop here.

claim-lint: python3 scripts/claim-lint.py -> exit 0 (final documentation tree)
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-commit-msg.txt -> exit 0 (final documentation message)
claim-lint: python3 scripts/claim-lint.py --files .tmp/pr-body.md -> exit 0 (exact submitted draft PR body, repeated)

claim-lint: python3 scripts/claim-lint.py -> exit 1 (continuation draft: quantified transcript/serial coverage and clarified the not-claimed paragraph; details in serials/3f-pr2/continuation-claim-lint.txt)

claim-lint: python3 scripts/claim-lint.py --files .tmp/continuation-issue-comment.md -> exit 0

Continuation cleanup: 0 lane-owned QEMU processes remained on either host;
no PID termination was required. See
`docs/planning/green-program/aarch64-testing/serials/3f-pr2/continuation-cleanup.txt`.

claim-lint: python3 scripts/claim-lint.py --files .tmp/continuation-pr-body.md -> exit 0

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/continuation-commit-msg.txt -> exit 0

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/continuation-commit-msg.txt -> exit 0

## ASTRA fix pass: V-4 and V-5

This pass starts at revision `35d9bc4d1845bce7bdd0246e435fb65b0232a13a`.
Its gate transcripts are under
`docs/planning/green-program/aarch64-testing/serials/3f-pr2-astra/`.
The transcript headers identify the base revision and working-tree source
hashes; these are precommit runs of the fixes, not runs of the old committed
source. Mutation transcripts additionally identify their mutated source hash.

V-4: moved `retain_cpu_affine_test_thread` immediately before the existing
`impl Scheduler`, whose first method is
`retain_pinned_worker_on_source_queue`. The helper bodies and call sites are
unchanged. This puts the definitions next to one another without introducing
another impl or changing the coreproof scanner. The placement test uses the
function-span census and rejects an intervening function. Only
`kernel/src/task/scheduler.rs` changed in the kernel; no Tier-1 or Tier-2 file
was edited and no logging was added.

V-5: the kick validator in `tests/loopback_pump_structure.rs` now retains the
exact aarch64 string token when masking source, and requires that token in
the cfg attribute on each of the three disposition patterns. Its regression
test changes each kick condition to x86_64 and riscv64. Separate mask fixtures
cover comment and raw-string decoys. The original production conditions were
already aarch64 and were not changed.

`serials/3f-pr2-astra/regression-red.txt` records the initial test string
quoting error, its correction, the refinement from architecture attributes
to kick attributes, and the resulting two intended regression failures.
An intermediate local check also caught an incorrect assumption that
`normalized_code` preserves byte offsets; the mask now creates a byte-length
buffer directly. The first full mutation attempt caught the placement
regression fixture's hardcoded helper name on the rename positive control;
that fixture now derives the helper name. The first attempt's transcripts
remain under `serials/3f-pr2-astra/first-mutation-attempt/`.

Not claimed in this fix pass: x86 boot validation, prod-profile validation,
repair of issue 941, a live fourth oracle leg, live SGI delivery, or closure of the earlier round's broader acceptance gaps.

claim-lint: python3 scripts/claim-lint.py -> exit 1

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/astra-commit-msg.txt -> exit 0

During this pass another session committed and pushed the current source as
`612309cdbe70a5c706b1270fafdb23caece1f412`. This session did not create that
commit. The gate source hashes match that commit; the strict gate started
at that revision. This follow-up preserves the commit and completes the gate
and claim-lint evidence. The earlier tree-lint finding concerned wording in
the not-claimed paragraph; it was rephrased without expanding the claims.

Source citations re-derived at `612309cd`: the test helper is at
`kernel/src/task/scheduler.rs:1885` and the production helper at
`kernel/src/task/scheduler.rs:1907`. The literal-preserving mask is at
`tests/loopback_pump_structure.rs:4163`, the placement regression at
`tests/loopback_pump_structure.rs:4219`, and the architecture mutation test at
`tests/loopback_pump_structure.rs:4233`.

Final host results: the default structure suite passed 103 tests and the
loopback suite passed 124 tests. See
`serials/3f-pr2-astra/structure-default-final.txt` and
`serials/3f-pr2-astra/loopback-final.txt`. The expanded mutation suite met
expectations on 20/20 legs: 17 negative controls and 3 positive controls.
Inspection of the negative transcripts confirmed the retention census
failed on the 16 behavioral mutations and the adjacency test failed on the
placement mutation. See `serials/3f-pr2-astra/ratchet.txt` and
`serials/3f-pr2-astra/mutation-results.json`.

The aarch64 boot_tests build exited 0, with no warning or error diagnostics
in `docs/planning/green-program/aarch64-testing/serials/3f-pr2-astra/build-aarch64-boot-tests.txt`.
The build used `aarch64-breenix-kernel.json` and the existing isolated
`.tmp/env.sh` toolchain setup. No cargo test command was run.

claim-lint: python3 scripts/claim-lint.py -> exit 0


## Landing

Landing began at `612309cd`. V-1 and V-3 are closed inline above; V-2
remains pending a completed beast x86 gate in this landing pass.
The merge from origin/main had no kernel conflicts. Its sole conflict was
independent appended validators in `tests/loopback_pump_structure.rs`; both
sets of validators were preserved. No scorer requirement changed in the
strict or x86 gate, so fixture re-recording is not needed.

claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/aarch64-testing/3F-PR2-2026-09-07.md -> exit 0

Pre-merge-commit checks: structure passed 103 tests; the soft-float aarch64
boot_tests build completed without compiler diagnostics. Evidence:
`docs/planning/green-program/aarch64-testing/serials/3f-pr2-landing/structure.txt`
and `docs/planning/green-program/aarch64-testing/serials/3f-pr2-landing/build-aarch64.txt`.

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/landing-merge-msg.txt -> exit 0

### ASTRA verification isolation after concurrent landing

The original ASTRA gate was stopped by its exact shell PID after the
concurrent landing merge replaced the kernel binary while the gate waited
for the host lock. Its `serials/3f-pr2-astra/strict-boot.txt` transcript ends
with exit -15; no boot acceptance is claimed for that attempt. The landing
session's separate merged-branch gate is recorded in
`serials/3f-pr2-landing/strict-aarch64.txt`.

ASTRA then created an isolated detached worktree at `612309cd`, copied the
build cache, and rebuilt the kernel there. The first build failed because
the fresh checkout lacked generated `userspace/programs/aarch64/simple_exit.elf`;
after copying the existing userspace artifacts, the build exited 0 without
compiler diagnostics. See `serials/3f-pr2-astra/build-isolated.txt` and
`serials/3f-pr2-astra/build-isolated-restored-input.txt`. The source and kernel
hashes are in `serials/3f-pr2-astra/isolated-artifact.txt`. This run validates
the fix commit before the unrelated landing changes. Its 64/64 structure
preflight logs are in `serials/3f-pr2-astra/isolated-preflight.tar.gz`.

The landing strict gate at `e42b4d040a164e04b57d6f21b488f938e83a9d6f`
passed 64/64 preflight suites and 1/1 scored boots, exit 0. Evidence:
`docs/planning/green-program/aarch64-testing/serials/3f-pr2-landing/strict-aarch64.txt`,
`serials/3f-pr2-landing/aarch64-serial.txt`, and the corresponding
`aarch64-preflight.tar.gz` and `aarch64-gate_boot_facts.txt`.

Source citations re-derived after the merge commit `e42b4d04`: test helper
`kernel/src/task/scheduler.rs:1885`, production helper
`kernel/src/task/scheduler.rs:1907`, literal mask
`tests/loopback_pump_structure.rs:4163`, adjacency test
`tests/loopback_pump_structure.rs:4219`, and architecture mutation test
`tests/loopback_pump_structure.rs:4233`.

Deferred code findings: 0 new findings added by this landing pass. V-4 and
V-5 were already closed in `612309cd`; the earlier round's not-claimed list
applies: production pin stamping (#562/761/803), prompt home dispatch, SGI
delivery, fourth oracle leg remain outside this change. Production evidence is
the accepted earlier continuation run at `bb5d3114`, not a new run of the merged
tree. claim-lint:ok: 0 findings (V-4/V-5 pre-fixed at 612309cd; deferred items 562/761/803 outside scope)


## Landing-x86

This later record supersedes the historical x86-pending status above. PR 942
merged at `70aec756` without a completed x86 boot. The three failed preflight
attempts remain recorded above, including their contention and timeout limits.
The fourth invocation subsequently completed on beast in `<isolated-checkout-32>`
at `e42b4d040a164e04b57d6f21b488f938e83a9d6f`, the PR branch landing merge.
The source is the 2026-09-07 `AUDIT-942.md` and its `942-x86/` copied evidence.
The six supplied artifacts are copied byte-for-byte into
`docs/planning/green-program/aarch64-testing/serials/3f-pr2/landing-x86/`.
The file:line citations below are relative to that directory.

`landing-x86.txt:1` records the revision. The invocation began at 13:42:14 ET
(`landing-x86.txt:2`); `run-inspector.json:1` records the boot interval
13:46:50–13:54:14 ET on 2026-09-07 and exitCode 0.
`landing-x86.txt:4` records structure preflight 64/64.

At `landing-x86.txt:471`:

```text
x86 userspace gate: PASS - exited=110 expected>=105 nonzero=0 allowlist=0
```

At `landing-x86.txt:499`:

```text
x86 frame-custody gate run 1: PASS
```

`landing-x86.txt:503` records `exit=0`. At `gate_boot_facts.txt:1`
(also `landing-x86.txt:456`):

```text
[GATE_BOOT_FACTS:boot=1:host_ms=1788803210404-1788803653718:qemu_at_start=0:load_at_start=3.50:qemu_at_end=0:load_at_end=3.99:qemu_cpu_s=439.00:guest_uptime_ms=NA:ended_by=scored_pass]
```

The requested tree comparison, reproduced in `tree-comparison.txt:1`, is:

```text
git rev-parse e42b4d04^{tree} 70aec756^{tree}
0439e627f5d9f36a9c3d53cad3df4583ccdab893
867432c6b43c7a4c9a7d434a85bd087417957e77

```

The trees are **UNEQUAL**. `tree-comparison.txt:6` onward lists the diff:
only documentation and evidence paths differ. This records the completed
x86 gate at e42b4d04 without asserting full-tree equality to 70aec756.
Issue 941 remains open under the requested equality condition. What remains
is x86 boot evidence for the exact merged tree (or explicit acceptance of
this documentation-only tree difference). The issue comments narrow the remaining work to x86 build/boot validation;
the prior sections retain strict-3 and production results with their revision
limits, without extending them to the merge tree.

Not claimed: a new boot in this docs-only session; an x86 boot executed at
70aec756; equality of the two trees; production validation of the merge tree;
a live fourth oracle leg; prompt home dispatch; live SGI delivery; closure
of issue 941 or the earlier broader acceptance gaps.

claim-lint: python3 scripts/claim-lint.py -> exit 1 (draft used an unquantified absolute for citation paths; reworded)

Documentation validation: `bash scripts/run-structure-tests.sh` exited 0;
see `docs/planning/green-program/aarch64-testing/serials/3f-pr2/landing-x86/docs-structure.txt`.
Its first line records the checked-out revision; the working changes were
this documentation and copied evidence. No kernel build or boot was run.

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-commit-msg.txt -> exit 0
claim-lint: python3 scripts/claim-lint.py --files .tmp/pr-body.md -> exit 0
