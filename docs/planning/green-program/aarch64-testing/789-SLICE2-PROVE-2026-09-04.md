# #789 slice 2 — gate pass, 2026-09-04

This is a gate-running pass over `origin/fix/789-sched-lock-irq-shape` at the
commit it stood at when this pass started. It does not change the fix; it
reproduces the branch's own anti-vacuity legs from a fresh checkout, then runs
gates the branch's own EVIDENCE doc (`789-SLICE2-2026-09-04.md`) had not yet
run: aarch64 strict x10, aarch64 production-profile x10, x86 production-profile
x3 on beast, and an in-situ merge cross-check against the parked
`origin/fix/562-761-aarch64-testing-profile` branch. Each number below sits
beside the command that produced it.

## Branch setup and the main merge

```
git fetch origin
git checkout -B wf789-slice2-local origin/fix/789-sched-lock-irq-shape
```

HEAD after checkout: `fc954257cd30e52f1713ecb0f9c8682883b568fe`.

```
git merge-base HEAD origin/main
```

returned `d6b7a186e37b67ee53f9c233442cdc54565874df` — `origin/main`'s own tip
at the time of this pass. `origin/main` is therefore already an ancestor of
this branch (the branch's earlier rounds already carried main's tip forward),
so:

```
git merge origin/main --no-edit
```

<!-- claim-lint:ok: verbatim git-status output for #789, HEAD unchanged at fc954257 -->
printed `Already up to date.` and created no new commit. `git status` after
the merge attempt: `Your branch is up to date with
'origin/fix/789-sched-lock-irq-shape'.` / `nothing to commit, working tree
clean`. This step pushes 0 commits: HEAD is unchanged at `fc954257`, and
`origin/fix/789-sched-lock-irq-shape` already points at the same commit.

**Worktree note.** A different local worktree on this machine already had
`fix/789-sched-lock-irq-shape` checked out (stale, at `52702669`, five commits
behind `origin`'s `fc954257`), so this pass worked from a differently-named
local branch (`wf789-slice2-local`) tracking the same remote ref rather than
reusing that literal branch name. No content difference results — `git
log --oneline -1` on `wf789-slice2-local` and on `origin/fix/789-sched-lock-irq-shape`
both read `fc954257`.

## Anti-vacuity pair, re-run at HEAD

Both legs from `789-SLICE2-2026-09-04.md`'s "Anti-vacuity" section, re-run
independently in this pass rather than read from the earlier artifacts.

### Pre-fix leg (`origin/main`'s scheduler.rs, ratcheted by this branch's test)

```
STAGE=/tmp/breenix-789-prefix-e11-13
rm -rf "$STAGE" && mkdir -p "$STAGE/tests"
cp -R kernel "$STAGE/"
cp -R tests/shared_coreproof "$STAGE/tests/"
cp tests/teardown_structure.rs "$STAGE/tests/"
git show d6b7a186:kernel/src/task/scheduler.rs > "$STAGE/kernel/src/task/scheduler.rs"
CARGO_MANIFEST_DIR="$STAGE" rustc --edition=2021 --test "$STAGE/tests/teardown_structure.rs" -o /tmp/td-789-prefix-e11-13
```

Compile: exit 0, 0 lines matching `^(warning|error)`.

```
/tmp/td-789-prefix-e11-13 scheduler_lock_acquisitions_are_irq_safe_by_shape --nocapture
```

exit 101. Census line: `29 acquisition sites in 28 functions; 18 admitted by a
local mask shape, 4 by caller derivation, 7 unadmitted`. The 7 unadmitted
identities, byte-for-byte the same set the branch's EVIDENCE doc recorded:

```
kernel/src/task/scheduler.rs::init
kernel/src/task/scheduler.rs::init_with_current
kernel/src/task/scheduler.rs::is_current_idle_thread
kernel/src/task/scheduler.rs::lock_scheduler
kernel/src/task/scheduler.rs::try_liveness_snapshot
kernel/src/task/scheduler.rs::try_lock_scheduler
kernel/src/task/scheduler.rs::try_schedule
```

`test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 82 filtered
out`. Full output at
`/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/slice2-789/antivacuity/ratchet-prefix-e11-13.txt`
(a session scratch path, not committed; the in-repo artifact this reproduces
is `docs/planning/green-program/aarch64-testing/789-slice2/confirm/ratchet-prefix-2026-09-04.txt`,
whose own tail records the identical `exit=101` and census numbers).

### Post-fix leg (this branch's own scheduler.rs)

```
scripts/run-structure-tests.sh teardown_structure scheduler_lock
```

exit 0. Census line: `28 acquisition sites in 27 functions; 22 admitted by a
local mask shape, 6 by caller derivation, 0 unadmitted`. `test
scheduler_lock_acquisitions_are_irq_safe_by_shape ... ok` /
`test scheduler_lock_irq_shape_ratchet_is_not_vacuous ... ok` /
`test result: ok. 2 passed; 0 failed; 0 ignored; 0 measured; 81 filtered out`.
Full output at
`/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/slice2-789/antivacuity/ratchet-postfix-e11-13.txt`
(session scratch path; reproduces the in-repo
`docs/planning/green-program/aarch64-testing/789-slice2/confirm/ratchet-postfix-2026-09-04.txt`
byte-for-byte on the numbers).

Both legs' numbers match the branch's already-committed EVIDENCE doc exactly.
This pass is an independent re-run at the same HEAD, not a first measurement.

## aarch64 strict boot gate — 10/10

Build (feature profile the strict gate itself requires — see
`docker/qemu/run-aarch64-boot-test-strict.sh`'s `require_boot_tests_kernel`):

```
./userspace/programs/build.sh --arch aarch64
```

exit 0, 148 binaries installed (needed because the `boot_tests` feature
compiles `kernel/src/test_framework/registry.rs`, which embeds userspace ELFs
via `include_bytes!`).

```
./scripts/create_ext2_disk.sh --arch aarch64
```

exit 0, 256M image at `target/ext2-aarch64.img`.

```
CARGO_FUTURE_INCOMPAT_REPORT_FREQUENCY=never cargo build --release --features boot_tests --target aarch64-breenix-kernel.json -Z build-std=core,alloc -Z build-std-features=compiler-builtins-mem -p kernel --bin kernel-aarch64
```

exit 0. `grep -cE '^(warning|error)'` over the captured output: 0.

```
./scripts/check-kernel-no-neon.sh target/aarch64-breenix-kernel/release/kernel-aarch64
```

`PASS: 0 FP/SIMD load/store instructions in kernel .text (allowlisted &
suppressed: 0)`, exit 0.

Host load immediately before the gate: `pgrep -fl qemu-system-aarch64 | wc -l`
→ 0.

```
docker/qemu/run-aarch64-boot-test-strict.sh 10
```

`RESULTS: Total iterations: 10 / Successes: 10 / Failures: 0 / Success rate:
100%`, `PASS: 10/10 boots succeeded`, exit 0. Zero reds — nothing to classify
against the known buckets (#555 softirq, #536 timer_delay, #576 EL1 NULL-PC,
#586 starved wake-loss, host-load window miss).

Per-boot serials (each copied from the gate's own
`/tmp/breenix_aarch64_strict_<N>/serial.txt`):
`/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/slice2-789/prove/strict/boot-1.txt`
through `boot-10.txt` (10 files, 772–839 lines each, `wc -l` over the
directory: 7949 total lines across the 10 files).

## aarch64 production-profile boot gate — 10/10

Same ext2 image and userspace build as above (the production profile ships no
test features but still boots `/sbin/init` from the same ext2 root). The gate
script builds its own kernel internally with no `--features` flag — that
omission is the point (see the script's own header): this is the profile
`scripts/parallels/build-efi.sh` ships, not a differently-configured test
build.

Host load before boot 1: `pgrep -fl qemu-system-aarch64 | wc -l` → 0. Checked
again before each of the other 9 boots (9 of 9 checks): 0 each time.

```
docker/qemu/run-aarch64-prod-profile-boot-test.sh
```

run 10 times sequentially: 10/10 runs printed `PASS: production profile
reached bsshd with the futex oracle seam absent`, exit 0 each time. Reds: 0 of
10.

Per-boot serials (copied from the gate's own
`/tmp/breenix_aarch64_prod_profile/serial.txt` after each run, before the next
overwrites it):
`/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/slice2-789/prove/prod/boot-1.txt`
through `boot-10.txt` (10 files, 394–446 lines each; `grep -liE
"KERNEL PANIC|panic!|DATA_ABORT|INSTRUCTION_ABORT|Unhandled sync
exception|soft lockup detected"` over all 10 files: no matches).

## x86 production profile on beast — 3/3

Scratch clone, built from `/root/breenix` (which fetched the branch from
GitHub first, since the container's `/root/breenix` had not tracked it):

```
ssh beast sudo -n incus exec breenix-x86 -- git -C /root/breenix fetch origin fix/789-sched-lock-irq-shape
ssh beast sudo -n incus exec breenix-x86 -- git -C /root/breenix branch fix/789-sched-lock-irq-shape origin/fix/789-sched-lock-irq-shape
ssh beast sudo -n incus exec breenix-x86 -- git clone /root/breenix /root/breenix-slice2
ssh beast sudo -n incus exec breenix-x86 -- git -C /root/breenix-slice2 fetch origin
ssh beast sudo -n incus exec breenix-x86 -- git -C /root/breenix-slice2 checkout -B fix/789-sched-lock-irq-shape origin/fix/789-sched-lock-irq-shape
```

`git -C /root/breenix-slice2 rev-parse HEAD` → `fc954257cd30e52f1713ecb0f9c8682883b568fe`
— matches the branch HEAD used for the other 4 legs of this pass (strict,
aarch64 production-profile, the anti-vacuity pair, and the parked-branch
cross-check's `origin/fix/789` side).

```
ssh beast sudo -n incus exec breenix-x86 -- ln -s /root/breenix/rust-fork-real /root/breenix-slice2/rust-fork
```

(the repo's `.gitignore` carries `/rust-fork`, and the container's forked std
lives at `/root/breenix/rust-fork-real`).

Build (the exact command specified for this pass; `incus exec` runs without a
login shell, so `cargo` is invoked by its full `/root/.cargo/bin/cargo` path
with an explicit `--env PATH=...` rather than relying on shell PATH lookup):

```
cargo build --release --features testing,external_test_bins --bin qemu-uefi
```

exit 0, `Finished` in 3m 00s. `grep -E "^(warning|error)"` over the full
captured build output: **0 lines** — the grep itself exits 1 (no match), and
the file it was redirected to,
`/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/slice2-789/prove/x86prod/warnings-errors.txt`,
is empty (0 bytes). Full build output (123 lines) at
`/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/slice2-789/prove/x86prod/build-output.log`.

x86_64 userspace, needed by `run-x86-prod-profile-boot-test.sh`'s own
preflight (it checks the ELF artifacts exist and errors otherwise; it does not
build them):

```
userspace/programs/build.sh --arch x86_64
```

exit 0, 148 binaries installed.

Do **NOT** run `docker/qemu/run-x86-boot-tests.sh`: it is RED on main for the
#787 regression and cannot attribute anything here.

Host load before each of the 3 boots: `ssh beast sudo -n incus exec
breenix-x86 -- pgrep -fc qemu-system-x86_64` → 1, 2, 1 respectively (another
lane's QEMU; the container is shared with two other lanes per the task, and 1
and 2 are both ≤ 3).

```
docker/qemu/run-x86-prod-profile-boot-test.sh
```

run 3 times sequentially (the script builds its own zero-feature kernel and
its own ext2 image internally each time): 3/3 runs printed `PASS: x86
production profile reached steady state with the teardown census at rest`,
exit 0 each time. Reds: 0 of 3.

Per-boot serials
(`/root/breenix-slice2`'s own `serial_kernel.txt`/`serial_user.txt` at
`/tmp/breenix_x86_prod_profile/`, copied out after each run): boot 1
(2127/160 lines), boot 2 (2404/160 lines), boot 3 (2301/160 lines), at
`/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/slice2-789/prove/x86prod/boot-{1,2,3}-serial_{kernel,user}.txt`.
Gate stdout for each run at `gate-run-{1,2,3}.log` in the same directory.

## Parked-branch cross-check — in-situ evidence for slice 3, not a closure of #789

A second worktree at
`/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/slice2-789/parked-xcheck`,
created from `origin/fix/562-761-aarch64-testing-profile` (tip
`d277213dc1b1a2c9a276ba17e0acedc1d1a5f95e` per `git log --oneline -1` there —
the same commit `git worktree add` reported preparing).

```
git merge --no-ff origin/fix/789-sched-lock-irq-shape --no-edit
```

produced 6 conflicted files: `.gitattributes`, `kernel/src/per_cpu_aarch64.rs`,
`kernel/src/process/manager.rs`, `kernel/src/syscall/clone.rs`,
`kernel/src/task/scheduler.rs`, `kernel/src/task/thread.rs`. 6/6 resolved by
hand, additively — every conflict was two branches each adding new,
non-overlapping content (a struct field, a new function, a `.gitattributes`
entry) that `git`'s 3-way diff mis-aligned because the two additions sat next
to identical boilerplate lines (both new functions independently start by
reading the same per-CPU thread pointer and null-checking it, and both new
`Thread` fields sit at the end of the struct next to the same preceding
field). No functional content from either side was dropped:

- `.gitattributes`: kept both branches' additive gitattributes blocks.
- `kernel/src/per_cpu_aarch64.rs`: kept both `current_thread_id_lock_free`
  (parked branch, #609) and `note_wait_loop_park` (this branch's ancestor,
  #772) as two complete, separate functions.
- `kernel/src/task/thread.rs`: kept both the `cpu_affinity`/`CpuPin` addition
  (parked branch) and the `wait_loop_iters: AtomicU64` addition (this
  branch's ancestor) as two struct fields, and combined both in the `Clone`
  impl and in 8/8 `Thread` constructors. One reconstruction slip — a missing
  closing `}` for `impl CpuPin` swallowed by the same
  boilerplate-alignment problem — was caught by the very next step (the build
  failing with `error: this file contains an unclosed delimiter`) and fixed
  before proceeding; the fixed file is what the merge commit carries.
- `kernel/src/process/manager.rs`, `kernel/src/syscall/clone.rs`: same
  `cpu_affinity`/`wait_loop_iters` field pair in `Thread` struct-literal
  construction, 4 sites and 1 site respectively.
- `kernel/src/task/scheduler.rs`: one conflict, inside
  `is_current_idle_thread`. The parked branch's side was the OLD, unmasked
  shape (`try_lock_scheduler()` called with no interrupt mask — the exact
  #790/R153 defect this slice's fix repairs); `origin/fix/789`'s side was the
  masked, fixed shape (`without_interrupts(|| { ... })`). Kept the fixed side
  whole, since showing whether the fix survives contact with the parked
  branch is the point of this cross-check.

`grep -n is_current_idle_thread kernel/src/task/idle_sleep.rs`'s only caller
(1 of 1) of the changed function, `idle_identity_must_not_sleep` in
`kernel/src/task/idle_sleep.rs:158` (`match
crate::task::scheduler::is_current_idle_thread() { Some(true) => ...,
Some(false) => ..., None => ... }`), needed 0 adaptation: the function's
signature (`() -> Option<bool>`) is unchanged by the fix — only its internal
masking changed — so the existing three-armed `match` still compiles as-is.

Merge commit: `d047330af17fc79c1b16ea0864ac353f7ec1675e` (`git -C
.../parked-xcheck log --oneline -1`), on a **detached HEAD in the scratch
worktree only** — not pushed, not fetched by any remote-tracking branch.

Build (identical command to the two aarch64 production builds above, run in
the parked-xcheck worktree after symlinking `rust-fork` and building aarch64
userspace + ext2 the same way):

```
CARGO_FUTURE_INCOMPAT_REPORT_FREQUENCY=never cargo build --release --target aarch64-breenix-kernel.json -Z build-std=core,alloc -Z build-std-features=compiler-builtins-mem -p kernel --bin kernel-aarch64
```

exit 0, 0 lines matching `^(warning|error)`.
`scripts/check-kernel-no-neon.sh`: `PASS: 0 FP/SIMD load/store instructions`,
exit 0.

Host load, checked before each of the 10 boots (10 of 10 checks): `pgrep -fl
qemu-system-aarch64 | wc -l` → 0.

```
docker/qemu/run-aarch64-prod-profile-boot-test.sh
```

run 10 times sequentially against the merged kernel. **10/10**: `PASS:
production profile reached bsshd with the futex oracle seam absent`, exit 0
each time. `grep -lc "bsshd: listening"` over all 10 saved serials: all 10
match. Against the RCA's own recorded baseline for the unmodified parked
branch — **3/10** reached `bsshd: listening` — this is a 10/10-vs-3/10
result, not a partial improvement.

Per-boot serials at
`/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/slice2-789/prove/parked-xcheck/boot-1.txt`
through `boot-10.txt` (414–452 lines each).

**This result is in-situ evidence for slice 3, not a closure of #789.** #789
tracks the parked branch's nine `idle_sleep.rs` call sites, which are not on
`origin/main` and are unaffected by anything this pass ran; the cross-check
merge exists only in a throwaway detached-HEAD worktree, was not pushed, and
shows that this slice's scheduler-lock fix does not regress the parked
branch's own boot path when combined with it — not that slice 3's distinct
work is done or even started.

The parked-xcheck worktree is removed after this document is written and
committed (`git worktree remove --force`), per this pass's own instructions.

## Summary

| Gate | Result |
|---|---|
| Anti-vacuity, pre-fix leg | exit 101, 7/7 unadmitted identities match |
| Anti-vacuity, post-fix leg | exit 0, 2/2 passed |
| aarch64 strict x10 | 10/10, 0 reds |
| aarch64 production-profile x10 | 10/10, 0 reds |
| x86 production-profile x3 (beast) | 3/3, 0 reds, build warnings/errors output empty |
| Parked-branch cross-check x10 | 10/10 (baseline 3/10) — in-situ evidence, not closure |

No red in any of the five executed gates required classification against a
known bucket (#555, #536, #576, #586, host-load) — there were 0 reds to
classify. **Unattributed reds: 0 of 5 gates.**

## Claim-lint

```
claim-lint: python3 scripts/claim-lint.py -> exit 0
```

First pass: 16 of 16 findings were this document's own new paragraphs,
triggered by ordinary English words the tool's word-list catches
context-free (quoting the words themselves here would re-trigger it, so see
the tool's own `UNIVERSAL_WORDS`/`PROVEN_RE` definitions in
`scripts/claim-lint.py`). Each was reworded to an explicit N-of-M count or a
same-paragraph `claim-lint:ok:` citation; 0 of 16 needed a change to a factual
claim. Re-run after: clean, 16 files checked against changed hunks vs
`d6b7a186e37b`.
