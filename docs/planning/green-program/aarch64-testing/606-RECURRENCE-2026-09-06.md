Battery SHA `5f68fedd`, gate `docker/qemu/run-aarch64-service-sequence-gate.sh` max profile boot 14/25.
Preserved serial: `serials/606-recurrence-2026-09-06/max-boot14-serial.txt`, sha256 `cf23f62eb7e4832b3109ec9e3cd417cbb7e1c974cd2304bac8fb38aea6ff857f`.

**Verdict**: #606 (reopen)

**Confidence**: medium. Boot 14 reproduces #606's explicitly named gate detector and its spawn/one-child-exit/missing-verdict sequence. However, the complete battery log records `qemu_status=137` (SIGKILL), and this gate does not check QEMU liveness during polling. A timestamped observation establishing that QEMU remained alive until the 45-second deadline would raise confidence. This is attribution to the documented reopen detector, not proof of the original spinning-parent mechanism or continuous guest progress.

**Evidence**

Read the serial's 667 lines, the complete gate and oracle sources, and the title, body, and comments of #598 (0 comments), #826 (4), #586 (2), and #606 (1) -- 7 of 7 comments across the four issues. Both requested issue searches were run and each returned title was skimmed; #789's body and its three comments were additionally read because its silence signature was potentially relevant.

Provenance: this detached worktree actually resolves to `45daec35bae5d9851bd6b62075ecb9bdc921c2f1`. Both inspected source files are byte-identical to their `5f68fedd` versions, verified with `git show`; all source line references below therefore apply to the artifact's commit. The preserved serial is byte-identical to the original gate output `/tmp/breenix_aarch64_service_sequence_gate_20260907T001651Z-23882/max/serial-14.txt`; SHA-256: `cf23f62eb7e4832b3109ec9e3cd417cbb7e1c974cd2304bac8fb38aea6ff857f`.

Serial references below are to `scratchpad/battery/5f68fedd/aarch64-service-seq-serials/max-boot14-UNATTRIBUTED.txt`.

```text
430: [TEST:network:loopback_recv_wake_when_idle:PASS]
625: [TESTS_COMPLETE:114/114]
626: [BOOT_TESTS:PASS]
633: [TESTS_COMPLETE:114/114]
634: [BOOT_TESTS:PASS]
652: [spawn] path='/bin/block_eintr_oracle'
654: manager.create_process_with_argv [ARM64]: ENTRY - name='block_eintr_oracle', elf_size=304752, argc=1
655: manager.create_process_with_argv [ARM64]: Generated PID 91
```

The last ten lines, verbatim after the added line-number labels:

```text
658: manager.create_process_with_argv [ARM64]: ELF loaded, entry=0x4000f938
659: manager.create_process_with_argv [ARM64]: Allocating user stack
660: manager.create_process_with_argv [ARM64]: User stack will be at 0xfffffeff0000-0xffffff000000
661: manager.create_process_with_argv [ARM64]: argc/argv set up on stack, SP=0xfffffefffed0
662: manager.create_process_with_argv [ARM64]: SUCCESS - returning PID 91
663: [spawn] Created child PID 91 for parent PID 1
664: [spawn] Success: child PID 91 scheduled
665: F123456789SC[syscall] exit(0) pid=92 name=block_eintr_oracle_child_92
666: [TTBR0_ASID_CENSUS:untagged=0:tagged=2199:kernel=3221:cleared=5390]
667: [STRAND_INJECT_ORACLE:aarch64:legA_exercised=1:legA_recovered=1:legB_exercised=1:legB_recovered=1:stranded=0]
```

Thus init successfully spawns and schedules oracle PID **91**, and exactly **one** oracle child exit is observed: PID **92**, line **665**. There is no second oracle-child exit and no oracle-parent exit anywhere in lines 1–667. The literal `[BLOCK_EINTR_ORACLE:` occurs in **0 of 667** lines, PASS or FAIL. The serial cannot establish whether the second fork was reached, whether its child was created, or which read the parent reached.

The full-file scan contains no abort, fatal-register, PC-alignment, kernel-panic, soft-lockup, unhandled-sync-exception, or actual FAIL verdict marker. Line 379's `[exception] Breakpoint (BRK #0) at 0xffff00004042e1d4` belongs to the deliberate breakpoint test, which passes at line 380; it is not a crashing boot. Expected device/probe failures and failure-injection test names likewise do not indicate a fatal fault. The scheduler census at line 298 has `stranded=0`, `running_shape=0`, `ready_shape=0`, `nonprogress=0`, and `queued_on_nondispatching_cpu=0`; line 667 also reports `stranded=0`. These are observed samples, not a continuous 45-second census.

**Gate decision and timeout**

In `docker/qemu/run-aarch64-service-sequence-gate.sh`, the deciding arm is exactly:

```text
778:     if ! grep -qF "[BLOCK_EINTR_ORACLE:" "$serial_file" 2>/dev/null; then
779:         CLASS_BUCKET="UNATTRIBUTED"
780:         CLASS_REASON="oracle marker absent"
781:         return
782:     fi
```

An isolated, read-only execution of the source's classifier functions against this file returns `UNATTRIBUTED: oracle marker absent`.

Ordering matters. Before that arm, the classifier considers, in order:

1. #596 runtime-oracle FAIL (466–470).
2. EL1 data-abort field signatures: #612, #622-as-UNATTRIBUTED, #641, or unrecognized/disagreeing records (486–532).
3. Block-oracle FAIL → bucket 575 (533–537); poll-oracle FAIL and lost TCP readiness → UNATTRIBUTED (544–559); service EIO and silent spawn → 575 (560–570).
4. Instruction-abort fields → #576, #626, #635, or UNATTRIBUTED (585–636); remaining DATA_ABORT (637–642).
5. CLONE_EXEC (647–652), #690 second-stage rendezvous stall (677–693), STRAND census/injection/mutation failures (695–712), and aggregate BOOT_TEST_FAIL (716–723).
6. PC-alignment records, including #625, and kernel panic → UNATTRIBUTED (728–768); #609 early-boot stall (773–777).

0 of 6 apply here. In particular, the oracle's process ENTRY and successful return at serial lines 654 and 662–664 exclude the silent-spawn shape. The missing-block-oracle arm precedes the later poll-marker/timeout and #596-arming anti-vacuity checks (786–809), P5B completion checks (810–843), remaining census/device requirements and GREEN (844–886), and the final last-line fallback (889–891). P5B is therefore a later bucket, not an earlier attributed failure. Any UNATTRIBUTED count fails the profile at line 1340.

`BOOT_TIMEOUT=45` is line 20. After starting QEMU, lines 1136–1137 set `boot_end="timeout"` and `boot_start=$SECONDS`. Lines 1138–1149 measure elapsed host-shell seconds and sleep for at most two seconds or the remaining budget. Lines 1151–1184 switch to `boot_end="early"` and kill on block-oracle FAIL, poll-oracle FAIL, lost TCP readiness, instruction abort, #596 oracle FAIL, data abort, or successful service-sequence completion. 0 of 7 conditions appears in this serial. Other fatal/test-failure markers were also absent, although not every such marker is itself an early-exit predicate in this loop.

At lines 1140–1142 or 1186–1189, reaching the time bound kills QEMU without changing `boot_end` from `timeout`. Lines 1193–1196 then wait and record its status; line 1288 reports the end reason and elapsed seconds. There is **no process-liveness check** inside the poll loop.

The actual battery log, `scratchpad/battery/5f68fedd/4-service-sequence-gate.log`, confirms:

```text
25: Per-boot timeout: 45s
28: Profile max: running 25 sequential boots
70:   Boot 14/25: futex handoff oracle marker missing or failed
71:   Boot 14/25: UNATTRIBUTED — oracle marker absent [timeout, 45s, ctx596_divergence=0, ret_dispatch_refusals=0, resume_pc_refusals=0, percpu_stack_aliens=0, cpu_identity_splits=0, ret_stage_refusals=0, lr_nontext=0]
267:   /tmp/breenix_aarch64_service_sequence_gate_20260907T001651Z-23882/max/serial-14.txt: UNATTRIBUTED — oracle marker absent (qemu_status=137) [timeout, 45s]
```

Line 69 also reports QEMU PID 68853 as `Killed: 9`. The original census independently records `14 / UNATTRIBUTED / timeout / 45 / oracle marker absent (qemu_status=137)`. The missing-futex diagnostic is a post-wait check at script lines 1202–1205, not an early-exit trigger. The battery actually selected `both` (log line 23); this is boot 14 of its max leg.

**Confirmed:** the gate exhausted its configured 45-second host budget, at the script's integer-second precision, without a marker-triggered early kill. **Not confirmed:** that QEMU itself ran alive for the full 45 seconds. SIGKILL timing and sender are not recorded; an earlier external kill would produce the same timeout result because the loop does not check liveness. The script's own `kill` calls request SIGTERM, not SIGKILL. The lone heartbeat is serial line 653, `uptime_ms=3256`; there are no subsequent heartbeats. Consequently, continuous guest liveness cannot honestly be inferred from `[timeout, 45s]` alone.

**Candidate decisions**

- **#598: no.** Its documented failure requires `[BLOCK_EINTR_ORACLE:FAIL:sig_handler_never_ran:flag=0]` followed by the parent's `exit(1)`. Both are absent from the serial's 667 lines. One child exit at line 665 is shared evidence, but the required FAIL-and-parent-exit suffix does not occur. A matching FAIL would have hit script lines 533–537 and the early-kill arm at 1151–1155. [Issue #598](https://github.com/ryanbreen/breenix/issues/598).
- **#826: no field-exact attribution.** It concerns the strict gate's missing `[EXEC_SMOKE:TARGET_OK]`, originally an 18-second poll/20-second host bound, with measured ongoing heartbeat progress. This specimen is the service-sequence gate's missing block-oracle verdict under its 45-second bound. Lines 625–634 show 114 of 114 boot tests passed and lines 652–665 place it in init's oracle. Qualification to the task's suggested discriminator: #826's full body also reports `[BOOT_TESTS:PASS]` twice and one specimen just past a block-oracle child exit. Completed boot tests alone therefore do **not** exclude #826. Its different detector/budget and ongoing-progress evidence are the relevant distinctions; boot 14 does not establish that starvation mechanism. [Issue #826](https://github.com/ryanbreen/breenix/issues/826).
- **#586: no.** The exact test passes at serial line **430**, quoted above. Its required `[TEST:network:loopback_recv_wake_when_idle:FAIL:...]` does not appear. [Issue #586](https://github.com/ryanbreen/breenix/issues/586).
- **#606: yes to its named detector and preserved recurrence trigger; not a proven reproduction of the original runtime detail set.** Same gate, successful oracle spawn, exactly one observed child exit, 0 oracle verdicts, clean observed strand counters, no guest crash/fault/FAIL marker, and `UNATTRIBUTED — oracle marker absent [timeout, 45s]`. Its original body additionally reports continuing one-second heartbeats and thousands of same-thread idle-context save/restore pairs. Boot 14 has neither that heartbeat sequence nor those traces. The second child's state and the parent's spinning/waiting mechanism remain unobserved, and SIGKILL prevents an unconditional liveness claim. [Issue #606](https://github.com/ryanbreen/breenix/issues/606).

The oracle source explains why the missing verdict is distinct from #598. Each `run_race()` forks **one** child, which sleeps 120 ms then exits 0 (43, 126–134). The parent reads `/bin/bwm` and `/bin/bsshd` to EOF using 32 KiB chunks (35–45, 89–117, 136–155). There are two such stages: default-ignore SIGCHLD, then an installed SIGCHLD handler and reset flag (160–205). Stage 2 checks the flag without waiting for the child (207–209). Only `main()` emits the final PASS or FAIL and exits (214–228); `emit()` duplicates the marker (120–124). The 30-second read-probe bound is checked after the read returns, so it cannot force a marker from a permanently blocked read.

#606 was closed after E2's **0 UNATTRIBUTED / 200 boots** at `f96ea36c`, refuting the filed 4/50 rate, explicitly without identifying a named repair. Its closing comment quotes exactly the same three-line `grep` / `CLASS_BUCKET` / `CLASS_REASON="oracle marker absent"` arm as script lines 778–780. Its reopen trigger, verbatim:

> **Reopen trigger, stated so it is not lost:** the wedge's signature still scores `UNATTRIBUTED` and still fails the gate with no tolerance. A single recurrence in any future battery reopens this issue with its serial preserved. This close does not weaken the detector.

[#606 closing comment](https://github.com/ryanbreen/breenix/issues/606#issuecomment-5390402294).

Boot 14 satisfies that operational detector trigger and its serial is preserved, which is why the attribution is **#606 (reopen)** rather than inventing a new issue from an already named detector. The qualified confidence preserves the distinction between that routing decision and demonstrating a live 45-second wedge. If uninterrupted guest/QEMU survival is required as a strict prerequisite, this artifact alone cannot certify it.

The searches surfaced no stronger supported attribution. #789 resembles the output silence, but its documented production-profile branch defect was tied to a specific scheduler-lock producer, and its comments record that producer's slice-2 repair already merged; these serial bytes cannot establish its recurrence. Other surfaced titles concern different faults, missing markers, stages, architectures, or tooling. No repository file or GitHub issue was edited, commented on, or reopened.
