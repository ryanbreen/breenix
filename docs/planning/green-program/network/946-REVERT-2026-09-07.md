# Revert of 946 — 2026-09-07

Reverts merge 62924b4739870b344a78cd16cf4099c774ea48e9 with `git revert -m 1 62924b47`. Original structure/x86 tested revision: 8abe02f99cbbe06cd860abc817e42532b597f161. Resumed ARM64 tested revision: dce7a93dcd49833455cb58ae870d930382f2a64d (`git rev-parse HEAD` before the resumed runs). The intervening commit changes only documentation/evidence; executable source is identical. The final evidence commit also changes no executable source.

Reason 1: at the reverted revision, `kernel/src/test_framework/registry.rs:2633–2648` grants 50/100 ms from counter-time without a tick-clock input or measured-starvation boolean. The caller at `:3072` requires a pending runnable reader but not the `starved` computed at `:3056`. A nonstarved initial dispatch failure can therefore recover in an unjustified extension and pass. This violates R220 condition (a): extend only when the guest is measurably not executing; dispatch must remain a failing verdict. These are historical citations to 62924b47, not the restored file's line numbers.

Reason 2: the committed proof set did not execute strict 3, a real starved recovery receipt, the starved-loop boots, the three forced real-boot legs, the R182 fixture re-record, or production 1. `docs/planning/green-program/network/586-PR3-2026-09-07.md:1,6–7,71–73` explicitly labels an incomplete draft and lists missing work; `docs/planning/586-pr3/round-doc.md:84–90` limits the harness evidence to shell/host checks; `docs/planning/net/586-pr3/round-doc.md:31–37` records only strict 1 with no recovered starvation. These documents were removed by the revert and remain available at 62924b47.

Audit: `/private/tmp/claude-501/-Users-wrb-fun-code-breenix/d69ffb9d-4539-4cf3-8a3d-a872ff7c830b/scratchpad/audits0907/AUDIT-946.md`.

No conflicts occurred. The service-sequence gate and `tests/gate_structure_preflight_wiring_structure.rs` are byte-identical to 5be3ac04, preserving 949; its documentation is retained too. The revert removes PR3's registry/executor changes, starvation script, strict ELF marker-list addition, host fixture, structure additions, three document locations and roadmap update.

Status: revert acceptance GREEN; ready for the authorized merge of PR 950. Required structure, ARM64 build, strict 1 on cortex-a72 and max, service-sequence 2+2, and x86 boot-test proofs are GREEN at the revisions recorded below. The original direct structure command passed 103/103 tests (exit 0); it runs the default teardown suite, not the whole discovered roster. Boot gate preflights run the roster separately. The first beast x86 preflight failed 1/64 suites: `gate_qmp_backstop_structure::missing_socket_and_hung_dump_are_partial_and_bounded` timed out before its expected dump command (14/15 fixture tests passed). This was an assertion failure, not the 300-second suite timeout. A single retry with `BREENIX_STRUCTURE_SUITE_TIMEOUT_SECS=900` passed 64/64 suites and one scored boot, with no test changes or structure bypass. Both attempts remain in the receipts.

Toolchain-warning disposition: the ARM64 builds exit 0 with the pre-existing Cargo future-incompatibility summary for `core v0.0.0` from pinned `nightly-2025-06-24` and `-Z build-std`. This is about the toolchain's own core source, not repository code. The driving session explicitly accepted this exact notice as non-blocking under the zero-warnings policy, citing the landed precedent at [CRITICAL-PATH-DEBT-PR1-2026-09-06.md:635](../gates/CRITICAL-PATH-DEBT-PR1-2026-09-06.md#L635): "1 unrelated nightly toolchain notice appears" and "it is not gated on by the round doc's own check (§6.6) and is orthogonal to this PR's diff." The full initial report is `serials/946-revert/toolchain-warning.txt.gz`; the resumed build is `serials/946-revert/build-resumed.txt`. No repository compilation warning or error appears in that resumed build transcript. The accepted notice does not claim a literally warning-free Cargo transcript.

The Mac build uses `--target aarch64-breenix-kernel.json`, `--features boot_tests`, `-Z build-std=core,alloc`, `-Z build-std-features=compiler-builtins-mem`, `-p kernel --bin kernel-aarch64`. `BREENIX_RUST_FORK_LIBRARY=<rust-fork-checkout>/library`; userspace aarch64 ELFs and ext2 image were copied from `<local-checkout>`. Build and boot scratch paths are this worktree's `.tmp` and `.gate-tmp`; service output is explicitly lane-local. On beast the isolated clone is `<isolated-checkout-149>`, linked to `<rust-fork-checkout>`, with the fork-library variable unset and both scratch variables set to `<isolated-checkout-150>`. No subagents were spawned, so no astra brief was dispatched.

PR3 will be redone against the contract, using the audit gap list as its checklist: production starvation eligibility and dispatch counterexample, terminal verdict precedence, finite accounting and grant units, production-guard mutation ratchet, clarified always-extend experiment semantics, strict 3, real starved recovery and starved-loop boots, three real-boot mutation legs, both R182 recordings, production 1, service-sequence 25+25, and external R220 battery-roster disposition. Redo remains pending under issue 586; this revert does not close or retitle it.

Not claimed: corrected PR3 policy; starvation recovery; forced mutation acceptance; strict 3; production 1; R182 re-record; service-sequence 25+25; battery-roster deletion; resolution of issue 586; a warning-free initial ARM64 build.


| Gate | Revision | Result | Receipt |
| --- | --- | --- | --- |
| `bash scripts/run-structure-tests.sh` | 8abe02f99cbbe06cd860abc817e42532b597f161 | GREEN, exit 0, 103/103 | `serials/946-revert/structure.txt` |
| ARM64 boot_tests build, resumed | dce7a93dcd49833455cb58ae870d930382f2a64d | GREEN, exit 0; accepted toolchain notice disclosed above | `serials/946-revert/build-resumed.txt` |
| `bash docker/qemu/run-aarch64-boot-test-strict.sh 1` (cortex-a72) | dce7a93dcd49833455cb58ae870d930382f2a64d | GREEN, exit 0, 1/1 boots, 64/64 preflight suites | `serials/946-revert/strict1.txt`; `serials/946-revert/strict1/` |
| Strict 1, max profile adaptation | dce7a93dcd49833455cb58ae870d930382f2a64d | GREEN, exit 0, 1/1 boots, 64/64 preflight suites | `serials/946-revert/strict1-max.txt`; `serials/946-revert/strict1-max/` |
| Service-sequence `--boots 2 --profile both`, max | dce7a93dcd49833455cb58ae870d930382f2a64d | GREEN, 2/2 boots; enclosing gate exit 0 | `serials/946-revert/service-sequence.txt`; `serials/946-revert/service-sequence/max/` |
| Service-sequence `--boots 2 --profile both`, cortex-a72 | dce7a93dcd49833455cb58ae870d930382f2a64d | GREEN, 2/2 boots; enclosing gate exit 0 | `serials/946-revert/service-sequence.txt`; `serials/946-revert/service-sequence/cortex-a72/` |
| `bash docker/qemu/run-x86-boot-tests.sh`, first invocation | 8abe02f99cbbe06cd860abc817e42532b597f161 | exit 1, preflight assertion failure; no boot | `serials/946-revert/x86/x86.log` |
| same x86 command, single retry, suite timeout 900 | 8abe02f99cbbe06cd860abc817e42532b597f161 | GREEN, exit 0, 64/64 preflight suites, one scored boot PASS; no build warning/error lines | `serials/946-revert/x86/x86-retry.log` |

The stock strict script hardcodes cortex-a72. To cover strict on max too, a temporary adjacent copy changed only `-cpu cortex-a72` to `-cpu max`; `serials/946-revert/strict-max-profile.diff` records that single-line substitution. Its command and revision appear in `strict1-max.txt`. The temporary script was removed after execution, and the committed gate is unchanged. Both strict invocations ran the full 64-suite structure preflight without a bypass.

The x86 gate terminated its QEMU after scoring. A subsequent process command found no QEMU command referencing the isolated clone. Serial files are unmodified; `serials/946-revert/revision.txt` and the enclosing gate transcripts bind the captured sidecars to the tested revision. No name-based process termination was used. No structure bypass was set. The ARM64 ELF hash and copied ext2 image hash are recorded in `serials/946-revert/inputs.txt` and match the resumed build. The resumed gates terminated their own QEMU children; a lane-scoped process check after the runs found no QEMU referencing this worktree. Other lanes were untouched.

The initial local `git revert` committed directly without invoking the temporary commit-msg hook. Before pushing, that local commit was recreated with Git's generated revert message and identical staged inverse patch after explicit tree/message lint checks; 8abe02f9 is the resulting pushed revert. No shared branch history was rewritten.

Issue 586 was read to verify its open state and title. After this acceptance-evidence commit, the authorized sequence is to mark PR 950 ready, merge it, verify mergedAt and ancestry, delete the remote feature branch, and comment on issue 586 with the two revert reasons and the pending redo checklist. Worktree removal belongs to the outer driver. The merge receipt is reported in the session handoff rather than pre-claimed by this pre-merge commit.

Literal claim checks before the revert commit:

```text
claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/revert-message.txt -> exit 0
claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg <local-checkout> -> exit 0
```

Literal checks before the evidence commit and draft PR:

```text
claim-lint: python3 scripts/claim-lint.py -> exit 1
Initial lint included generated mutation-source copies from preflight scratch directories; removed those disposable copies, keeping suite transcripts and status receipts.
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-message.txt -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr-body.txt -> exit 0
claim-lint: python3 scripts/claim-lint.py -> exit 0
```

Literal checks before the final acceptance-evidence commit:

```text
claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-final-message.txt -> exit 0
```
