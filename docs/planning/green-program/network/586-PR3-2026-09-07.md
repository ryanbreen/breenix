# #586 PR3 execution — incomplete draft

Branch: `net/586-pr3-guest-execution-budget`.
Starting commit: `c12ff1409b7e1e1f276ca4c6d5cd97f02973b69b`.

This round has not satisfied the PR3 acceptance contract. No boot evidence has
been collected in this round. The source edits are a draft, not a landing claim.

## Draft implementation

`kernel/src/test_framework/registry.rs` measures independent 200 ms windows
using the existing tick, counter and context-switch measurements. A pending
reader observed Ready or Running can receive an extension only when the PR2
starvation predicate holds. The cap is four extensions. The existing wake stamp
and three-byte receive checks remain the success criteria. The final marker
prints the extension count. Its recovered-window verdict semantics are pending
clarification; the current draft retains PR2's terminal-window verdict selection.

The strict kernel profile guard requires the budget marker. The new
`docker/qemu/run-aarch64-starved-loop.sh` delegates boots to the strict gate,
which uses `qemu-host-lock.sh` and `gate-boot-facts.sh`. It uses CPU hogs rather
than competing QEMU lock holders: the latter serialize on an exclusive lock.
It retains a unique run directory and refuses a strict-green run that contains
no recovered idle wake test with a starved verdict and positive extension count.
This script has only received shell syntax validation, not a live boot run.

## Contract questions

1. The requested always-extend mutation must supposedly make a permanent
   connection-waiter wake suppression PASS. The reader remains blocked after
   this mutation, however, and a bounded sleep extension does not wake it.
   Making that boot pass would require a different temporary defect or a
   weakened success criterion. The actual R220 ledger entry requires the
   always-extend ratchet to redden; PLAN-586 additionally requires the
   wake-defect boot to turn green. Clarification was requested before selecting
   different mutation semantics.
2. The grammar says a reader that ran receives `dispatched`, while closing
   condition 3 requires a recovered PASS to retain `starved`. Clarification
   was requested on retaining the starvation reason after recovery.
3. The build emits the pinned Rust toolchain's existing `core` soft-float/NEON
   future-incompatibility notice, also recorded by PR2. The repository requires
   zero warnings. An exception for this existing toolchain notice was requested;
   no warning suppression or shared toolchain modification was performed.
   claim-lint:ok: AGENTS.md, Code Quality section, requires zero warnings;
   the observed notice is preserved in serials/586-pr3/build.txt.

## Evidence collected

Evidence directory: `serials/586-pr3/`.

- `build-initial.txt`: build failed because the prescribed ELF copy placed
  binaries at the worktree root, while the kernel includes them from
  `userspace/programs/aarch64/`. Creating that directory and copying the supplied
  binaries there resolved that error.
- `build.txt`: aarch64 `boot_tests` build completed; Cargo printed the `core`
  future-incompatibility warning. This is not a zero-warning build claim.
- `structure-loopback.txt`: loopback structure suite passed 126 tests after
  fixing a draft validator's missing enclosing braces.
- `always-extend-ratchet.txt`: removing the measured-starvation conjunct from
  the source extension guard made the focused structure test fail (exit 101).
  The original source was restored in a Python `finally` block. This is a
  host-side ratchet mutation, not the requested real-boot mutation evidence.

- `structure-suites.txt`: the full structure preflight reported 64/64 suites
  passing. The transient source mutation was performed while that preflight
  was running, so this is not a final immutable-source acceptance run.
- `restored-ratchet.txt`: after restoration, the focused budget tests passed.
- `claim-lint.txt`: `python3 scripts/claim-lint.py` returned exit 0. No
  commit-message lint was run because no commit was made.

Strict 3, the real starved recovery, the three real-boot mutation legs, fixture
re-recording and production 1 remain unrun. No issue closure or deletion of the
battery's #586 attribution is justified by this draft.
