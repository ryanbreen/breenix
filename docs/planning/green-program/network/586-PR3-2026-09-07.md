# 586 PR 3 redo — acceptance blocked

Status: **blocked**. The required recovered-starvation receipt and extension-deleted starvation red were not reproduced. R232 resolves the permanent-wake control specification: the added delayed-wake comparison is red under the measured guard and green under unconditional extensions, with the ratchet red. The extra 64-hog guest also exposed a separate UDP oracle failure, tracked as 955. Named proofs were executed; failed or unexercised acceptance outcomes are not called complete.

Branch: `net/586-pr3-guest-execution-budget-v2`.
Base: `9f79cb71969fe9b9832b1ff921e148a76170a850`.
Code: `81379ef979928659d2b7ca2efe1ea798efe4d1c2`.
Preflight corrections: `290152b19545388a099056988714d132cbea4ff7`.
This continues the prior worktree attempt; its preserved `redo-*` receipts are historical worktree checks, not new guest proofs. PR 946 history was not recreated or deleted. The authoritative contract is PLAN-586 Q3/Q4/PR 3, ledger R220 as amended by R232, and AUDIT-946; PR 943 supplies the classifier and measured constants.

Source citations re-derived at HEAD 87ccec10 (kernel and ratchet lines unchanged from 290152b1): `kernel/src/test_framework/registry.rs:2616` (classifier), `kernel/src/test_framework/registry.rs:2622` (guard), `kernel/src/test_framework/registry.rs:3011` (window loop), `kernel/src/test_framework/registry.rs:3163` (terminal verdict), `kernel/src/test_framework/registry.rs:3262` (emitter), `kernel/src/test_framework/registry.rs:3289` (missing-stamp FAIL); `tests/loopback_pump_structure.rs:4846` (ratchet), `tests/loopback_pump_structure.rs:4880` (production-helper cases); `docker/qemu/run-aarch64-boot-test-strict.sh:415`, `docker/qemu/run-aarch64-boot-test-strict.sh:423`, `docker/qemu/run-aarch64-boot-test-strict.sh:549`, `docker/qemu/run-aarch64-boot-test-strict.sh:553` (prefix, fields and scorer). No kernel/source diff remained after mutation restoration.

## Audit checklist

| Requirement | Implementation and evidence |
| --- | --- |
| Measured-starvation eligibility | `kernel/src/test_framework/registry.rs` uses the PR 943 classifier: context delta below 30 OR counter/tick ratio above 4. Each extension requires a pending, observed runnable reader, starvation in the window just spent, and remaining capacity. No counter-only allocation rule. |
| Initial dispatch refusal | Production-helper host test rejects tick=200, counter=200, ctx=30. The deadline wake stamp is latched before refusal, so a later stamp cannot erase failure. |
| Low-switch starvation | Production-helper test accepts ctx=29 at equal tick/counter clocks. There is no opposing minimum-switch refusal. |
| Terminal dispatch classification | Remembered starvation labels only a completed three-byte recovery. A missing wake stamp uses the terminal measured window and reader state. Dispatch and wake FAIL markers are directly fatal to the strict scorer. |
| Bounded, counted extensions | At most four grants of 200 ms; `extensions` counts grants, `extension_ms` counts milliseconds, `extension_bound_ms=1000` bounds total requested sleep. This does not bound host wall time. Early setup failures precede budget measurement. |
| Production guard ratchet | `tests/loopback_pump_structure.rs` pins the production guard and call wiring and executes extracted production helper bodies. The real always-extend source mutation makes the guard-only test fail with exit 101. |
| Strict marker fields | `docker/qemu/run-aarch64-boot-test-strict.sh` checks the new prefix and field fragments in the ELF and requires the structured budget line for both variants in the serial. Field fragments are separate from the bracketed prefix census. |
| Starved-loop harness | `docker/qemu/run-aarch64-starved-loop.sh` provides cycle/hog knobs, retains cycle serials and strict gate output, prints revision-bearing STARVED_LOOP_FACTS, and delegates QEMU ownership, host lock and GATE_BOOT_FACTS to the strict gate. Hogs are stopped by owned PID. |
| R182 fixtures | Exactly `tests/fixtures/udp-socket-lock-aarch64-serial.txt` and `docs/planning/green-program/aarch64-testing/serials/slice3e/02-prod-boot1-serial.txt`. Final strict fixture copied byte-for-byte from `serials/586-pr3/strict/boot-1-serial.txt`; production fixture from `serials/586-pr3/prod/serial.txt`, both recorded at 290152b1. `r182-provenance.json` pins hashes and byte equality; `r182-score-only.txt` records both replays returning 0. The earlier worktree recording used to unblock initial preflight remains preserved under `fixture-recording/`, with its revision, ELF hash and patch; it is not the final fixture source. |
| Wake-defect forced leg | Real boot: permanent early return from `wake_connection_waiters` for local port 54511. `serials/586-pr3/wake-defect/serial.txt` records `verdict=wake`, `extensions=0`, and loopback FAIL. Restored-source strict score-only rejects it specifically as a loopback wake failure. |
| Extension-deleted forced leg | Kernel built with extension loop and eligibility helper removed; source restored before the contention gate. Three real boots with 14 host hogs each passed, with zero extensions and no starved-red receipt. Harness exit 1 reflects absence of recovered starvation, not a guest failure. The continuation added 3/3 strict passes with 28 hogs and no starved red (details below). Required starved-red outcome remains unmet; see `serials/586-pr3/extension-deleted/cycle-1/serial.txt`, `cycle-2/serial.txt`, `cycle-3/serial.txt` and `loop.txt`. |
| Always-extend forced leg | Real boot with the same permanent wake suppression and unconditional pending-reader grants: `serials/586-pr3/always-extend/serial.txt` records `extensions=4`, `extension_ms=800`, `verdict=wake`, and FAIL. Guard-only ratchet exit 101. R232 retains this red-under-both-policies leg and uses the delayed-wake comparison below for the green control. |
| Real recovered starvation | The 10-cycle, 14-hog loop completed 10/10 guest passes, zero recoveries, harness exit 1 (`serials/586-pr3/recovery-loop.txt`). The 64-hog attempt stopped before QEMU: context_restore_structure hit its 300-second preflight limit (63/64 suites completed; log-to-status birth interval 300.0087 s). The single authorized retry with BREENIX_STRUCTURE_SUITE_TIMEOUT_SECS=900 passed 64/64 suites and booted, but the strict gate failed on the UDP-lock oracle (armed=0, masked_in_hold=1) and ended at hard timeout. Its loopback tests passed with zero extensions. Across the 10 baseline recovery boots plus this stronger real boot, no qualifying recovery was observed. `serials/586-pr3/recovery-heavy/context-timeout.txt` and `timeout-provenance.txt` preserve the timeout. The continuation added 3/3 strict passes with 28 hogs, again zero extensions and no recovery (details below). No qualifying receipt is claimed. |
| Strict 3 | First invocation stopped at 62/64 preflight suites; no boots. Two source-shape problems corrected together with their pins. Corrected-source rerun passed 3/3 at 290152b1 with 64/64 structure suites, exit 0 (`serials/586-pr3/strict-3.txt` and `strict/boot-1-serial.txt` through `boot-3-serial.txt`). |
| Service sequence 2+2 | PASS: max 2/2 and cortex-a72 2/2 at 290152b1, exit 0. `serials/586-pr3/service-2-both.txt` records the combined verdict; `service/max/serial-1.txt`, `serial-2.txt` and the corresponding `service/cortex-a72/` files preserve the boots. The service script uses its own timestamp/PID-unique output directory; that original path is recorded in `service/provenance.txt`. This round does not claim the plan's eventual 25+25 closing population. |
| Beast x86 | PASS at 290152b1 after the preflight-shape correction. Both serial streams and full transcript are in `serials/586-pr3/beast/`; `gate.txt` records exit 0. Registry compiles on x86; the registry loopback tests remain deferred on that architecture. The initial preflight-only failure is preserved in `serials/586-pr3/beast-preflight-red.txt`. No timeout override or SCSI/IO retry was needed. |
| Production profile last | PASS at 290152b1, exit 0, after the stronger contention attempt and x86 proof. 64/64 preflight suites; warning-free production build with the recorded local core patch. `serials/586-pr3/prod-last.txt` and `prod/serial.txt` preserve the gate and full serial. That was the prior attempt’s last boot. R232 continuation rebuilt boot_tests for the additional controls and contention attempts; its production gate is recorded separately below. |
| R220 roster disposition | Not changed or certified: this branch has not landed. |

The requested default `bash scripts/run-structure-tests.sh` already ran in the prior attempt: `serials/586-pr3/redo-structure-default.txt` records 103 passed and exit 0. Its suite also runs inside the corrected gates’ 64-suite preflights; no cargo test path was used.

## Build warning and preflight corrections

The prior attempt stopped after a future-incompatibility warning in the pinned Rust library. The warning's producer is stdarch NEON target-feature code under the required aarch64 soft-float ABI. Merely exporting BREENIX_RUST_FORK_LIBRARY does not select the core source for this kernel's build-std invocation.

A private copy of the pinned toolchain library lives under this lane's `.tmp/rust-src/library`. `serials/586-pr3/local-core-softfloat.patch` excludes the incompatible NEON modules and dependent portable-SIMD conversions/import from soft-float builds, without a warning suppression or shared-toolchain edit. The initial incomplete local patch failed to compile; `local-core-first-build.txt` preserves that attempt. The completed patch built the kernel without warnings/errors (`continue-build.txt`, `committed-build.txt` and each mutation's `build.txt`). The kernel target JSON and ABI were unchanged. The durable toolchain fix remains tracked by existing issues 945 and 559; neither is closed by this private build adaptation. This toolchain adaptation limits reproducibility to the recorded source patch and environment; it is not a claim that the stock pinned library builds warning-free.

Reproduce the private source preparation in a fresh lane before the build:

```sh
mkdir -p .tmp/rust-src
cp -R "$(rustc --print sysroot)/lib/rustlib/src/rust/library" .tmp/rust-src/library
patch -d .tmp/rust-src/library -p1 < docs/planning/green-program/network/serials/586-pr3/local-core-softfloat.patch
```

`serials/586-pr3/toolchain.json` records the compiler identity and original/patched file hashes.

Build command:

```sh
export TMPDIR="$PWD/.tmp" BREENIX_GATE_TMP="$PWD/.gate-tmp"
export BREENIX_RUST_FORK_LIBRARY=/Users/wrb/fun/code/breenix-parallels/rust-fork/library
export __CARGO_TESTS_ONLY_SRC_ROOT="$PWD/.tmp/rust-src/library"
cargo build --release --features boot_tests --target aarch64-breenix-kernel.json -Z build-std=core,alloc -Z build-std-features=compiler-builtins-mem -p kernel --bin kernel-aarch64
```

Userspace aarch64 ELFs and ext2 image were copied from the requested main checkout in the previous attempt. No cargo test was invoked. Registry rustfmt changed a tracing wrapper's expression arm into an equivalent block, so its exact-body ratchet needed the same brace change. The strict profile census requires bracketed prefixes, so new field fragments were moved to a separate loop. Both originally failing suites passed after correction (`preflight-fixes.txt`); the full loopback suite passed 132/132 (`continue-loopback.txt`). No Tier-1/Tier-2 file was edited.

## Starvation attempt measurements

The following when-idle fields are extracted from each row’s serial in `serials/586-pr3/starvation-attempts.json`; no marker bytes were edited.

| Leg | Cycle | Hogs | Tick ms | Counter ms | Context switches | Extensions | Verdict | Test |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| extension-deleted | 1 | 14 | 156 | 202 | 81 | 0 | ok | PASS |
| extension-deleted | 2 | 14 | 109 | 200 | 233 | 0 | ok | PASS |
| extension-deleted | 3 | 14 | 72 | 227 | 39 | 0 | ok | PASS |
| recovery | 1 | 14 | 142 | 200 | 251 | 0 | ok | PASS |
| recovery | 2 | 14 | 167 | 213 | 90 | 0 | ok | PASS |
| recovery | 3 | 14 | 122 | 206 | 188 | 0 | ok | PASS |
| recovery | 4 | 14 | 136 | 200 | 217 | 0 | ok | PASS |
| recovery | 5 | 14 | 138 | 208 | 65 | 0 | ok | PASS |
| recovery | 6 | 14 | 112 | 216 | 87 | 0 | ok | PASS |
| recovery | 7 | 14 | 109 | 210 | 59 | 0 | ok | PASS |
| recovery | 8 | 14 | 140 | 204 | 63 | 0 | ok | PASS |
| recovery | 9 | 14 | 95 | 202 | 59 | 0 | ok | PASS |
| recovery | 10 | 14 | 99 | 202 | 151 | 0 | ok | PASS |

The stronger real boot is preserved at `serials/586-pr3/recovery-heavy-retry/cycle-1/serial.txt`. Its when-idle window had tick=223 ms, counter=296 ms, ctx=52, extensions=0, verdict=ok, and test PASS. This is not the required recovered-starvation receipt. The first 64-hog attempt never booted; its preflight timeout is not counted as a guest failure. The retry’s UDP record has `armed=0` and `masked_in_hold=1`; the generic interruptibility FAIL text does not diagnose an IRQ-masking defect. Issue 955 tracks that separate failure.

## R232 continuation contention

At 0776f442, the extension-deleted image was rebuilt without warnings and tested with 28 host hogs for 3 cycles. Each gate passed 64/64 structure suites and its strict boot: 3/3 gate exits were 0, but none produced the required starved FAIL. The starvation harness returned 1 because recovered=0. The when-idle samples (ticks/counter/context switches) were 151/209/84, 176/225/63, and 161/204/54; each had extensions=0, verdict=ok, and test PASS. Source was restored before each gate’s preflight. `serials/586-pr3/r232-extension-deleted/` preserves the mutation patch, warning-free build, ELF hash, gate output, full serials, and sidecars. `r232-attempts.json` extracts the marker with its source filename. No timeout override was needed in these 3 cycles.

The restored production policy was then rebuilt at 0776f442 and run for 3 cycles with 28 hogs. Each passed 64/64 structure suites and its strict boot, with 0 recovered-starvation receipts and harness exit 1. The when-idle samples (ticks/counter/context switches) were 163/208/76, 161/221/41, and 158/204/143; each had extensions=0, verdict=ok, and test PASS. `serials/586-pr3/r232-recovery/` preserves the warning-free build, empty mutation patch, ELF hash, full serials, gate output, and host-facts sidecars. No timeout override was needed in these 3 cycles either.

Exact remaining blocker: this host did not produce a genuine when-idle verdict=starved with extensions>0 and PASS, nor a starved FAIL with extensions deleted. Across both attempts, restored-policy recovery runs comprise 10 boots at 14 hogs, 1 boot at 64 hogs (the separate UDP-oracle by-catch), and 3 boots at 28 hogs. Extension-deleted runs comprise 3 boots at 14 hogs and 3 at 28 hogs. 0/20 of these boots supplies its required starvation outcome. The preflight-only 64-hog timeout is not counted as a guest boot. R232’s delayed control is satisfied and issue 955 is not treated as a PR 3 blocker.

## Mutation interpretation and blocker

R232 (also recorded in issue 954 comments) amends the anti-vacuity control to a bounded delayed wake. The continuation adds two real recording boots. Both suppress `wake_connection_waiters` for local port 54511 and schedule a separate test kthread to deliver the reader wake after 600 ms. The deadline stamp and reader state are retained before joining the injection thread; the original success checks remain intact. No injection remains in production source.

- `serials/586-pr3/delayed-guard/serial.txt`: counter=226 ms, ticks=174 ms, ctx=90, extensions=0, verdict=wake, loopback FAIL. `score.txt` records strict score-only exit 1 for that FAIL.
- `serials/586-pr3/delayed-always/serial.txt`: unconditional pending-reader grants, extensions=3, extension_ms=600, loopback PASS. `score.txt` records strict score-only PASS. The printed starved verdict is produced by the mutation and is not a recovered-starvation acceptance receipt.
- `serials/586-pr3/delayed-always/guard-only.txt`: production guard ratchet exit 101, a runtime assertion failure without compile warnings/errors. `r232-restored-structure.txt` records the restored loopback suite passing 132/132.

These recording boots ran at df9b819e with the source patches and ELF hashes preserved in each directory. Builds were warning-free. An accidental duplicate delayed-always recorder launch hit a disk-image lock and truncated the shared serial. `delayed-always-launch-collision/` retains that invalid attempt; its verified orphan QEMU PID 27995 was terminated with SIGTERM before the valid repeat. The invalid attempt is excluded from acceptance counts. Evidence: `docs/planning/green-program/network/serials/586-pr3/delayed-always-launch-collision/qemu.txt`.

Continuation commit 0776f442 records the R232 mutation recipes and adds contemporaneous revision headers to each nested contention-gate transcript. Production kernel source and ratchets are unchanged from 290152b1. The existing permanent-suppression boots remain red under both policies, as R232 requires.

Mutation application/restoration is recorded in `serials/586-pr3/mutate.py` and `mutation-leg.sh`. Direct recording boots use `raw-boot.py`, retain full serials and SHA-256 provenance, and terminate only their owned QEMU process group. They are not represented as full boot gates. Source was restored before strict score-only calls and before gate preflight. Top-level gate transcripts begin with their revision. For nested contention cycles, archived `gate.txt` prepends the enclosing driver’s recorded revision and cycle as an explicitly archival provenance header; `gate-raw.txt` preserves the original gate output byte-for-byte. The enclosing `run.txt` carries the contemporaneous revision-bearing STARVED_LOOP_FACTS. Mutation-image runs additionally name the recorded patch and ELF hash.

The inherited `core.hooksPath` points to the shared checkout’s `.beads/hooks`. The two code commits printed a Beads export from that hook; no Beads task-tracking command was issued. Subsequent documentation commits use a per-command `core.hooksPath=/dev/null` override to prevent repeating that side effect. The shared hook/config was not edited.

## Claim lint

claim-lint: python3 scripts/claim-lint.py -> exit 0

Before code commit 81379ef9; receipt `serials/586-pr3/continue-code-claim.txt`.

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/code-commit.txt -> exit 0

Receipt `serials/586-pr3/continue-code-message-claim.txt`.

claim-lint: python3 scripts/claim-lint.py -> exit 0

Before correction commit 290152b1; receipt `serials/586-pr3/preflight-tree-claim.txt`.

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/preflight-commit.txt -> exit 0

Receipt `serials/586-pr3/preflight-message-claim.txt`.

claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/network/586-PR3-2026-09-07.md -> exit 1

Draft check: an abbreviated evidence path did not resolve. The path was corrected.

claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/network/586-PR3-2026-09-07.md -> exit 0

R182 fixture replay verification after the final production boot: both score-only commands exited 0. The dependent suites passed 132/132 (loopback_pump_structure), 32/32 (ttbr0_shadow_reconciliation_structure), 11/11 (tty_irq_fg_structure), and 10/10 (tty_irq_pm_structure), each via scripts/run-structure-tests.sh. Their revision-bearing transcripts are the `serials/586-pr3/r182-*_structure.txt` files.

Raw serial CR/LF bytes are retained. Staging initially normalized 27 receipts; a scoped serial-directory .gitattributes rule disables text conversion, and the evidence commit was amended before push after index byte equality was verified. The generic git diff whitespace check flags carriage returns in the re-recorded fixtures; those bytes were not normalized. Source and round-document whitespace checks passed separately.

Scoped final process inspection found no QEMU command belonging to this worktree; `serials/586-pr3/local-cleanup.json` records the check. The beast check is `serials/586-pr3/beast/cleanup.txt`. Gate traps stopped their owned QEMU and hog PIDs. No process-name kill was used.

claim-lint: python3 scripts/claim-lint.py -> exit 0

Final documentation tree check; receipt `serials/586-pr3/final-tree-claim.txt`.

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/docs-commit.txt -> exit 0

Receipt `serials/586-pr3/final-message-claim.txt`.

## Follow-up tracking

Issue [955](https://github.com/ryanbreen/breenix/issues/955) tracks the UDP oracle’s armed=0 failure in the 64-hog boot. Issue [954](https://github.com/ryanbreen/breenix/issues/954) records R232, which resolves the control specification; its delayed-wake outcomes are recorded above. Existing issue 586 remains open for the missing recovery and extension-deleted reproduction evidence and the broader closing contract. Existing issues 945 and 559 track the durable toolchain fix. No issue was closed.

## Not claimed

- Acceptance completion, merge, issue closure or battery roster removal.
- A green permanently suppressed-wake control.
- Real recovered starvation or an extension-deleted starvation red: neither was observed in the recorded attempts.
- The eventual 25+25 service population, PR 4 x86 EOF repair, or diagnosis of the original issue artifact's reschedule flag.
- A warning-free stock pinned Rust library, or changes to shared toolchains.

## R232 continuation claim lint

claim-lint: python3 scripts/claim-lint.py -> exit 0

Before 0776f442; receipt `serials/586-pr3/r232-code-tree-claim.txt`.

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/r232-code-commit.txt -> exit 0

Receipt `serials/586-pr3/r232-code-message-claim.txt`.

claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/network/586-PR3-2026-09-07.md -> exit 1

Continuation draft needed a resolving path for the duplicate-launch record; corrected before commit.

claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/network/586-PR3-2026-09-07.md -> exit 0

Continuation draft after resolving the collision receipt path.

claim-lint: python3 scripts/claim-lint.py --files docs/planning/green-program/network/586-PR3-2026-09-07.md -> exit 1

The combined-attempt paragraph needed an explicit denominator; changed to 0/20.

## Continuation production and handoff

The final kernel gate was `bash docker/qemu/run-aarch64-prod-profile-boot-test.sh` at 0776f442: exit 0, 64/64 preflight suites, warning-free production build, and ended_by=scored_pass. `serials/586-pr3/r232-prod-last.txt` records the revision and exit; `serials/586-pr3/r232-prod/serial.txt` retains the full serial. No kernel build or guest boot followed. The existing R182 recordings at 290152b1 remain the two fixture sources; this continuation did not change the production kernel or strict scorer.

`serials/586-pr3/r232-cleanup.json` records zero QEMU commands owned by this worktree after the gate. Contention traps reaped their owned hogs. The only manual termination in this continuation was the verified duplicate-launch QEMU PID documented above; no process-name kill was used. Full preflight work directories, executables, and disks remain in lane scratch; committed contention receipts contain full serials, gate transcripts, and host sidecars.

Source citations re-derived from HEAD 87ccec10: `kernel/src/test_framework/registry.rs:2616` classifier, `kernel/src/test_framework/registry.rs:2622` guard, `kernel/src/test_framework/registry.rs:3011` window, `kernel/src/test_framework/registry.rs:3163` terminal verdict; `tests/loopback_pump_structure.rs:4871` guard ratchet; `docker/qemu/run-aarch64-starved-loop.sh:40` nested revision header. The continuation changes no Tier-1 or Tier-2 file.

claim-lint: python3 scripts/claim-lint.py -> exit 0

Continuation documentation tree; receipt `serials/586-pr3/r232-docs-tree-claim.txt`.

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/r232-docs-commit.txt -> exit 0

Receipt `serials/586-pr3/r232-docs-message-claim.txt`.

## Coordinator ruling R233 — verify-only slot

Coordinator ruling R233, as scoped by the coordinator's verify-only instruction for PR 956: treat 87ccec10 as the finished 586 PR 3 redo implementation and restrict this follow-up to structure verification and documentation. Completion of this slot does not certify acceptance completion: the genuine recovered-starvation receipt and extension-deleted starvation red remain outstanding under issue 586. The R232 delayed-wake control remains historical evidence; this slot runs no boot gate, applies no mutation, and changes no code.

NOT-CLAIMED: No genuine verdict=starved extensions>0 PASS receipt was obtained.

Verification at 87ccec1020c21049485d7a8a521d5efbf1d14fa6: the requested branch log was inspected and initial git status was clean. `bash scripts/run-structure-tests.sh` exited 0: 103/103 teardown_structure tests passed, with no compile warnings or errors. This is the script's default suite, not a fresh full structure preflight or boot gate. The revision-bearing transcript is lane-local at `.tmp/r233-structure.txt`; it is not a committed guest receipt. Source line citations above were re-derived from this revision; this documentation-only follow-up leaves their source bytes unchanged.

claim-lint: python3 scripts/claim-lint.py -> exit 0

claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/r233-docs-commit.txt -> exit 0
