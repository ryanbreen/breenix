# 586 PR 4 — audited x86 EOF dispatch probe

Branch: `net/586-pr4-x86-eof-arm-dispatch`; existing PR: 948.
Audited starting HEAD: `d095c8569d8a34b02b2f7adc4fba0c4f12d76f7e`.
Code and ratchet revision: `96fa978116f7472b9ea0729f830e0b1711af3a9d`.
Evidence paths below are relative to this document's directory.

## Audit of the inherited commits

The diff from base `62924b4739870b344a78cd16cf4099c774ea48e9` to the
starting HEAD contains two commits, `a60a1a77` (userspace plus ratchet) and
`d095c856` (documentation plus captures). The audit read PR 4 and Q5 in the
provided PLAN-586.md before changing source.

Kept: the exit-15 arm calls the existing probe, prints measured gap, sample
count, window, and `verdict=eof_timeout`, then exits 15. The 4000 ms EOF
bound remains. The arm-scoped validator and its delete-mutation remain.

Corrected: the validator accepted the expected format string anywhere in the
arm, including a comment accompanying an incorrect printed message. Revision
`96fa9781` binds the literal to the executable print and adds the comment
mutation. The probe's comment now states that clock-read time contributes to
the measured gap; it does not identify the cause of the preceding blocked read.

Replaced acceptance narrative: the inherited strict gate first failed, the
ten parallel boots ran next, and only then did a strict rerun pass. That is not
the requested strict-PASS-then-ten-boots order. Those captures also lacked an
exact tested revision in their gate transcripts. The old files remain as
historical artifacts, not this audit's acceptance evidence. The new captures
are under `serials/586-pr4/audit/`.

## Current source and grammar

Citations were re-derived at the code revision above, with file blob IDs and
line text in `serials/586-pr4/audit/source-citations.txt`. Documentation-only
commits do not change those source blobs.

- `userspace/programs/src/loopback_wake_test.rs:96`: existing clock-sampling probe.
- `userspace/programs/src/loopback_wake_test.rs:225`: EOF timeout arm;
  call at line 228, measured print at lines 229–232, exit 15 at line 233.
- `tests/loopback_pump_structure.rs:899`: validator;
  baseline at line 2021, delete-mutation at line 2029,
  comment-mutation at line 2046.
- `kernel/src/test_framework/executor.rs:705` and line 710: x86 deferral under 567.
- `kernel/src/task/scheduler.rs:6593`: idle accessor, returning false on x86.

The format literal in `userspace/programs/src/loopback_wake_test.rs:230` is:

```text
LOOPBACK_WAKE_TEST: reader_eof_dispatch_probe max_gap_ms={} samples={} window_ms={} verdict=eof_timeout
```

PLAN-586 specifies bracket grammar for the separate kernel budget marker; it
does not supply a separate x86 EOF literal. This implementation follows the
userspace data-probe field order and appends the already-decided EOF verdict.
It does not label a subsequent sample as a diagnosis of earlier starvation or a
scheduler defect. No Tier-1 or Tier-2 file is modified.

Q5's historical rates were checked against the live issue comments:
[2/5 at c79124b8](https://github.com/ryanbreen/breenix/issues/586#issuecomment-5322508885)
and [4/70 control versus 4/35 branch](https://github.com/ryanbreen/breenix/issues/586#issuecomment-5377794087).
These are historical reports, not measurements at this HEAD. The x86
measurement is the userspace probe, not the aarch64 placement accessor.

## Mutation and structure checks

`serials/586-pr4/audit/audit-delete-mutation.txt` records removal of the EOF
probe assignment from actual source: the focused validator fails with exit 101.
`serials/586-pr4/audit/audit-comment-mutation.txt` restores the inherited
validator while retaining the new comment-only mutation test: exit 101.
Both edits were restored before the final structure run and Beast gates.
These are structural controls, not forced-timeout guest execution.

The Mac ran `bash scripts/run-structure-tests.sh <suite>` for the 64 discovered
`tests/*_structure.rs` files at `96fa9781`: 64/64 suites passed, 994 tests.
The loopback suite passed 138 tests. Individual revision-tagged transcripts
are in `serials/586-pr4/audit/mac-structure/`; the suite tally is
`serials/586-pr4/audit/mac-structure-driver.txt`. No Mac kernel build or boot
was run. Compiler diagnostic scans found 0 warnings and 0 errors in these
64 suite transcripts.

## Beast: acceptance blocked by 960

The lane clone is `/root/breenix-n586p4` in `breenix-x86` on Beast.
`TMPDIR` and `BREENIX_GATE_TMP` were `/root/breenix-n586p4-tmp`;
`BREENIX_RUST_FORK_LIBRARY` was unset. The detached driver ran revision
`96fa978116f7472b9ea0729f830e0b1711af3a9d`. The userspace build exited 0
and installed 151 binaries. At this HEAD the script installs x86 ELFs directly
in `userspace/programs/`, not its `x86_64/` subdirectory. Source and installed
ELF hashes are in `serials/586-pr4/audit/source-and-elf-sha256.txt`.

The single `bash docker/qemu/run-x86-boot-tests.sh` attempt passed its 64/64
structure preflight, built the kernel, and booted. No structure timeout or
900-second preflight rerun occurred. The gate exited 1; it is not a PASS.
Its transcript is `serials/586-pr4/audit/strict.txt`, with the tested revision
at line 1 and exit status at line 995. The driver stopped on that failure,
so neither five-boot parallel invocation ran. Ten-boot tally: **not run**,
not 0/10 failures and not inherited from the previous commits.

Marker substring at `serials/586-pr4/audit/strict/serial_user.txt:126`
(the original line also contains context-switch output):

```text
[TIMER_WAKE_LATENCY_ORACLE:x86:sleep_ms=10:peers=8:overrun_ms=40:bound_ms=100:quantum_ms=50:round_ms=400:wake_enqueues=1:peers_started=8:peers_spinning=8:backstops=3:setup_ms=653:window_ms=30056:measured=1:FAIL]
```

This matches the timer-oracle failure signature reported by open
[issue 960](https://github.com/ryanbreen/breenix/issues/960). A shared root cause
has not been established. The strict disk-preparation transcript also records
`Error: x86_64-linux-musl-gcc not found in PATH` at
`serials/586-pr4/audit/strict.txt:338`, followed by BusyBox warnings at lines
343–344. Image creation continued. The Rust compiler diagnostic scan counts
0 warnings and 0 errors; it does not erase those image-preparation diagnostics.
Counts are in `serials/586-pr4/audit/compile-diagnostics.txt`.
No SCSI/IO failure was observed; this is not attributed to 871.

After the explicit timer FAIL was observed, the operator verified QEMU PID
1623509's executable and both lane-specific image/serial paths, then sent
SIGTERM to that PID. `serials/586-pr4/audit/operator-stop.txt` records the
command line and reason. The gate records `ended_by=qemu_exited_early` at
`serials/586-pr4/audit/strict.txt:575` and fails its assertion at script line
1070 (transcript line 587). This was an operator stop after a failure marker,
not a natural timeout or a passing strict run. No gate was retried.

The loopback result itself was PASS at
`serials/586-pr4/audit/strict/serial_user.txt:1064`. The userspace tally at
`serials/586-pr4/audit/strict/serial_kernel.txt:17802` was
`TEST_TALLY: exited=110 nonzero=0 failed=[]`.
Across this one strict boot's two serials, the exact reader-exit-15 marker
occurs 0 times and the new EOF dispatch line occurs 0 times. There are no new
EOF dispatch lines to quote. `serials/586-pr4/audit/marker-tally.txt` records
those counts, relevant marker substrings with source filenames, and serial
hashes. The EOF arm remains unexercised in this guest run; PLAN-586's additional
positive-observation condition is also unmet.

Follow-up is tracked by existing issue 960: diagnose the timer-oracle failure
and image-build prerequisite, then obtain the required strict PASS before the
two five-boot batches. PR 948 stays open and 586 is not closed.

## Claim checks and limits

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr4-audit-code-message.txt -> exit 0

The preceding two commands ran before code commit `96fa9781`; outputs are
`serials/586-pr4/audit/audit-code-tree-lint.txt` and
`serials/586-pr4/audit/audit-code-msg-lint.txt`.

claim-lint: python3 scripts/claim-lint.py -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg .tmp/pr4-audit-docs-message.txt -> exit 0

These two commands ran before the documentation commit; outputs are
`serials/586-pr4/audit/audit-docs-tree-lint.txt` and
`serials/586-pr4/audit/audit-docs-msg-lint.txt`. Changed-hunk lint separately
reports 14 pre-existing findings outside this branch's changes.

Not claimed: resolution of 586; a diagnosis of its historical CPU-0 artifact;
absence of a dispatch defect; aarch64 boot coverage; necessity of the x86
loopback pump; a new starvation/dispatch classification threshold; or a merge
of PR 948. The LAND slot owns PR-body editing and merge.
