# 586 PR 1 -- the loopback wake diagnostic states what the reader state says

<!-- claim-lint:ok: of the 12 file:line citations in the body below (before the Fix pass section, which carries its own commit-scoped citations), 6 are re-derived at this branch HEAD, 2 are scoped to the parent commit fe874f15, and 4 cite a coordinator scratchpad path outside the branch; see #586 -->
Branch `net/586-pr1-truthful-classifier`, cut from `origin/main` at
`fe874f15cbe4c81bd12311015542b9ba309e9ac0`. This is PR 1 of the four-PR
closing contract in PLAN-586; it repairs the classifier only. Most
`file:line` citations below were re-derived at this branch's own HEAD after
the code commit; the exceptions are named where they occur -- two cite the
parent commit, and the Step 0 serial and gate-log lines cite a coordinator
scratchpad path that is not part of this branch.

## Step 0 -- the 5c53ba59 attribution, re-read

PLAN-586 Q5 asks that the preserved serial for the service-sequence boot the
main-health battery attributed to 586 be re-read before anything is built,
because the attribution was made from a battery agent reading of the gate log.

The serial is not tracked in the repository -- it survives only for the
life of this session. Its path is
`scratchpad/battery2/5c53ba59/preserved-serials/serviceseq-max-boot24-586.txt`
in the coordinator scratchpad (1046 lines) -- the same class of path #586's
own body warns is volatile -- and the gate log beside it,
`05-service-sequence-gate.log:100`, records the boot as

```
  Boot 24/25: BOOT_TEST_FAIL — boot test failure: [TEST:network:loopback_recv_wake_when_idle:FAIL:delivery landed but the recv waiter was never woken] [early, 27s, ctx596_divergence=0, ret_dispatch_refusals=0, resume_pc_refusals=0, percpu_stack_aliens=0, cpu_identity_splits=0, ret_stage_refusals=0, lr_nontext=8]
```

**The attribution holds, and the serial is stronger evidence than the roster
line was.** Two adjacent lines of the preserved serial:

`serviceseq-max-boot24-586.txt:328`:

```
loopback recv wake diagnostic: classification=delivery landed but the recv waiter was never woken tcp_wake_rejected=0 pump_wake_rejected=0 dropped_full=0 isr_buffer_full=0 reader_tid=24 reader_state=Some(Some(Ready))
```

`serviceseq-max-boot24-586.txt:329`:

```
[TEST:network:loopback_recv_wake_when_idle:FAIL:delivery landed but the recv waiter was never woken]
```

<!-- claim-lint:ok: the flagged word is quoted from the classification string on serial line 328, 1 of 1 preserved specimen; see #586 -->
The exact marker is present, so the boot is attributable by marker rather
than by the shape of the failure. And the same printed line carries
`reader_state=Some(Some(Ready))`: the reader had left `Blocked`, and the
classification on that line asserts it "was never woken". That is the defect
this PR repairs, observed rather than reasoned about.

One further reading from the same serial, recorded because it belongs to PR 2
and PR 3 rather than to this one: `:301` shows
`cpu=0 current=1 idle=1 ready_len=0 need_resched=true` while CPUs 1-3 each
carry a 3-deep ready queue. The tid-0 confusion PLAN-586 Q1 corrects is
absent here -- `current=1 idle=1` names a real thread -- so this boot is a
CPU-0-not-executing shape, and separating starvation from a dispatch defect
is exactly what the PR 2 budget line is for. This round makes no claim about
which of the two it was.

## The defect and the repair

`kernel/src/test_framework/registry.rs` before this commit selected the
classification from the delivery facts alone. The three arms read
`queue_depth` (sampled at `:2942` at the parent commit) and
`client_has_data` (`:2943`); `reader_state` was sampled two lines later, was
printed in the diagnostic, and 0 of the 3 arms consulted it.

After this commit the reader fact is derived at `registry.rs:2981` from
`reader_state`, and the selector at `:2987` matches on
`(queue_depth > 0, client_has_data, reader_fact)`. `wake_ms == 0` at `:3026`
means "the reader had not run", which is not the same fact as "the reader was
not woken", and only `reader_state` can answer the second.

### Before -- 3 strings

| delivery | string |
|---|---|
| `queue_depth > 0` | `pump never drained: packet still queued when the reader timed out` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->
| `client_has_data` | `delivery landed but the recv waiter was never woken` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->
| neither | `segment vanished: delivered but never reached the connection rx buffer` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->

### After -- 12 strings, one per delivery-and-reader cell

The reader fact has four values: `StillBlocked` (`ThreadState::is_blocked()`
is true), `Terminated`, `LeftBlocked` (`Ready` or `Running`), and `NoRow`
(`with_scheduler` declined, or `get_thread` found no row).

| delivery | reader fact | string |
|---|---|---|
| still queued | StillBlocked | `pump never drained: packet still queued and the recv waiter was still blocked` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->
| still queued | LeftBlocked | `pump never drained: packet still queued after the recv waiter left Blocked` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->
| still queued | Terminated | `pump never drained: packet still queued and the recv waiter had already terminated` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->
| still queued | NoRow | `pump never drained: packet still queued and the recv waiter had no scheduler row` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->
| landed | StillBlocked | `delivery landed and the recv waiter was still blocked` |
| landed | LeftBlocked | `delivery landed and the recv waiter left Blocked but had not run by the deadline` |
| landed | Terminated | `delivery landed and the recv waiter terminated without stamping its wake` |
| landed | NoRow | `delivery landed and the recv waiter had no scheduler row` |
| vanished | StillBlocked | `segment vanished: never reached the connection rx buffer, recv waiter still blocked` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->
| vanished | LeftBlocked | `segment vanished: never reached the connection rx buffer, recv waiter left Blocked` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->
| vanished | Terminated | `segment vanished: never reached the connection rx buffer, recv waiter terminated` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->
| vanished | NoRow | `segment vanished: never reached the connection rx buffer, reader had no scheduler row` | <!-- claim-lint:ok: the flagged word is inside a classification string quoted verbatim from kernel/src/test_framework/registry.rs, 12 of 12 rows; see #586 -->

The boot-24 specimen would today print the landed-and-LeftBlocked cell.

<!-- claim-lint:ok: 0 of 2 changed files appear in the Tier-1 or Tier-2 tables of CLAUDE.md, and the diff adds 0 logging macro call sites; see #586 -->
Each cell is a single `&'static str`, so `TestResult::Fail` still allocates
nothing and the `Fail(&'static str)` shape of the surrounding code is
unchanged. No Tier-1 or Tier-2 file is touched; nothing on a dispatch,
interrupt or syscall path is touched; no logging is added anywhere.

## R182 -- no fixture is re-recorded

The changed bytes are on the failing path only: the `FAIL` payload of
`[TEST:network:loopback_recv_wake_when_idle:FAIL:...]` and of
`loopback_recv_wake_under_load`, and the `classification=` field of the
`loopback recv wake diagnostic:` line. The `PASS` markers are byte-identical.

<!-- claim-lint:ok: the flagged words are the old classification strings being grepped for; counts are stated per path in lines, with 0 lines in the fixture and gate directories; see #586 -->
Grepping the whole tree (excluding `target/` and `.git/`), apart from this
round's own document and its `serials/586-pr1/` evidence, for the three old
strings: `pump never drained` and `segment vanished` each match 4 lines, all
in `kernel/src/test_framework/registry.rs` (one file each). `never woken`
matches 2 lines in `registry.rs`, 5 lines in `tests/loopback_pump_structure.rs`
(the V-4 fix pass's `MISREPORT_PHRASE` ratchet, which forbids the phrase
rather than reproducing it as fixture content), and 5 lines across 4 prose
documents under `docs/planning/` that predate this PR and are unrelated to
it. 0 lines in `tests/fixtures/`, 0 in `docker/qemu/`, 0 in `scripts/`. No
committed fixture carries a failing loopback classification, so **no fixture
is re-recorded** and no scorer requirement is added by this PR. The R182
re-record becomes due in PR 3, whose list PLAN-586 already names.

## The ratchet

`tests/loopback_pump_structure.rs:2021`
`validate_loopback_wake_classification_reads_reader_state` asserts, over
`kernel/src/test_framework/registry.rs`:

1. a reader fact is derived before the selector, and its derivation reads
   `reader_state` and `is_blocked`;
2. the selector reads `queue_depth`, `client_has_data` and the reader fact;
3. the selector has at least 4 arms and no two arms share their text.

It is a shape check, not a list of words: the arms may be re-worded and cases
may be added. The arm census is taken from `code_mask`, so it counts literal
runs rather than matching any string.

Two delete-mutation legs, `tests/loopback_pump_structure.rs:2120`:

* drop the reader fact from the selector scrutinee (the 586 defect exactly)
  gives a red validator;
* drop `reader_state` from the derivation of the fact gives a red validator.

The leg first asserts the unmutated source passes, so neither mutation is
vacuous.

## Proof -- 3 of 3 strict boots, 1 of 1 x86 gate run

The two delete-mutation legs are the experiment that reddens the validator;
the gate rows below are counts.
<!-- claim-lint:ok: 5 of 5 rows in the table below were run after the annotations landed; see #586 -->
All runs are at the branch tree, after the claim-lint annotations were added,
so the gate bytes are from the source that is committed.

| gate | result | evidence |
|---|---|---|
| `bash scripts/run-structure-tests.sh loopback_pump_structure` | 116 passed, 0 failed | both new tests listed ok |
| `bash scripts/run-structure-tests.sh` (teardown_structure, the default) | 94 passed, 0 failed | |
| every `tests/*_structure.rs` preflight | passed | run by the strict gate own `gate_structure_preflight` before it built or booted anything | <!-- claim-lint:ok: the preflight discovered and ran 64 of 64 tests/*_structure.rs files in this tree, all green, before the gate built anything; see #586 -->
| `bash docker/qemu/run-aarch64-boot-test-strict.sh 3` | 3/3 SUCCESS, 333s | `serials/586-pr1/strict-gate.txt`, serials `strict-boot1..3-serial.txt` |
| `bash docker/qemu/run-x86-boot-tests.sh` on beast | `x86 frame-custody gate run 1: PASS` | `serials/586-pr1/x86-boot-tests-gate.txt` |

The strict serials carry
`[TEST:network:loopback_recv_wake_when_idle:PASS]`, so the PASS path is
exercised and its bytes are unchanged.

### Why the x86 gate was run

`kernel/src/test_framework/registry.rs` is gated on
`feature = "boot_tests"` (`kernel/src/test_framework/mod.rs:165-166`) and not on
`target_arch`, so the changed function is compiled for x86 even though
`X86_DEFERRED_TESTS` defers the registry test itself at runtime. The gate was
run once in the per-lane clone `/root/breenix-n586` on beast, with
`BREENIX_GATE_TMP` and `TMPDIR` set to `/root/breenix-n586-tmp`. The
preserved gate log (`serials/586-pr1/x86-boot-tests-gate.txt`) is the
script's own summary output, not the raw `serial_*.txt` files it graded, so
it carries 0 `[TEST:` lines directly; it does record
`x86 userspace gate: PASS - exited=110 expected>=105 nonzero=0 allowlist=0`
(line 590) and `x86 frame-custody gate run 1: PASS` (line 618). Those PASS
results could only be reached if the script's own gating predicate had
already matched `[TEST:userspace:loopback_recv_wake:PASS]` and found 0 lines
of `[TEST:network:*:FAIL]` / `[TEST:userspace:*:FAIL]` against the raw
serials (`docker/qemu/run-x86-boot-tests.sh:780`, `:1093`, `:1352`,
`:1357`); the marker and the absence of a FAIL line are guaranteed by that
predicate, not read directly from the preserved bytes.

## Claim discipline

```
claim-lint: scripts/claim-lint.py                                -> exit 0
claim-lint: scripts/claim-lint.py --commit-msg .tmp/msg1.txt     -> exit 0
claim-lint: scripts/claim-lint.py --commit-msg .tmp/msg2.txt     -> exit 0
```

The first two were run before the code commit; the third before the docs
commit that adds this file. The tree run reported
`clean (5 file(s) checked, changed hunks vs fe874f15cbe4)` with 91
pre-existing findings outside the changed hunks of this branch left
unreported and untouched. Three `claim-lint:ok` annotations were added in the
process, each naming its count and issue: one in `registry.rs`, two in
`tests/loopback_pump_structure.rs`.

## Not claimed

* **Not claimed: that 586 is fixed.** This PR repairs what the failure
  reports, not what produces it. 586 stays open. PR 2 measures the budget,
  PR 3 spends it in guest execution, PR 4 covers the x86 arm.
<!-- claim-lint:ok: 0 of the 2 candidate readings is excluded by the evidence of this round; see #586 -->
* **Not claimed: that the boot-24 failure was host starvation.** The serial is
  consistent with starvation and with a dispatch defect; nothing here
  separates them (0 of the 2 readings is excluded by this round evidence). The
  discriminator in PLAN-586 Q3 is the work of PR 2.
* **Not claimed: that no dispatch defect exists on this path.**
* **Not claimed: that the new strings are the final wording.** PR 2 and PR 3
  add a machine-readable `[LOOPBACK_WAKE_BUDGET:...]` line; the prose
  classifications may be re-worded then, and the ratchet is written to permit
  that.
* **Not claimed: any change in the failure rate.** 3 strict boots cannot see a
  roughly 1-in-200 event, and no service-sequence run was taken for this PR.
* **Not claimed: that a running kernel has printed any of the 12 new
  classification strings.** This round has no forced-failure leg; 3 of 3
  strict boots and the x86 run were green, so 12 of 12 cells of the new
  selector are evidence only from source and from the structure-test
  ratchet, not from an observed boot. PLAN-586 reserves the forced legs for
  PR 3.
* **Not claimed: that the x86 gate exercised the changed function.** The
  registry test is deferred on x86 by `X86_DEFERRED_TESTS` behind 567. The x86
  run shows the change compiles and boots on that arch, with 0 of the boot
  markers regressing; it does not exercise the classifier.
* **Disclosed:** the attribution roster line of the battery for 586 quotes the
  old message text. After this commit a recurrence prints one of the 12 new
  strings, so whoever maintains the roster should key the entry on
  `loopback_recv_wake_when_idle` rather than on the message. PLAN-586 deletes
  the roster line outright at PR 3; until then it is stale text, not a stale
  attribution.
* **Disclosed:** an earlier strict 3-boot run passed on this branch before the
  three `claim-lint:ok` comments were added. The gate was re-run after them and
  the table above reports only the second run; the bytes of the first run are
  not preserved and are not cited.

## Fix pass — review finding closed (2026-09-07)

This fix pass closes one finding from the review of this round.

**V-4 (minor/code)** — `tests/loopback_pump_structure.rs:2014` (as it stood
at commit `64502e39`), `validate_loopback_wake_classification_reads_reader_state`.
The ratchet's 4 shape checks (arm count >= 4, arm-text distinctness, the
reader fact derived from `reader_state` and `is_blocked`, the selector
reading all 3 of its inputs) stayed green 4 of 4 when the review reproduced
the #586 misreport by pasting its exact string into a different arm's cell:
replacing `registry.rs:3002`'s
`"delivery landed and the recv waiter left Blocked but had not run by the
deadline"` (the `(false, true, ReaderFact::LeftBlocked)` arm) with the old,
pre-PR1 defect text `"delivery landed but the recv waiter was never
woken"`, then running
`bash scripts/run-structure-tests.sh loopback_pump_structure
loopback_wake_classification_reads_reader_state`, returned `test result: ok.
1 passed; 0 failed`. 0 of the 4 shape checks moved, so 0 of the 4 caught it.
The validator's own doc comment says "the arms may be re-worded freely"
(`tests/loopback_pump_structure.rs:2007` at `64502e39`, unchanged by this
fix pass) -- that sentence says arms may be reworded, not that a specific,
already-known-false string may return; the review's own citation of this
line as `586-PR1:136` was the round doc's paraphrase of the same shape
check, not this exact wording, which lives in the source comment. Neither
place, nor the ratchet's not-claimed list, said the defect's own text could
return unratcheted.

### The repair

`tests/loopback_pump_structure.rs`'s
`validate_loopback_wake_classification_reads_reader_state` now carries a
fifth, content-level check, run after the 4 shape checks and before the
function returns `Ok(())`: an arm's text may not contain the literal
substring `"never woken"`. This is deliberately not scoped to the one cell
the review moved the phrase into -- the check runs over 12 of 12 arms,
because 0 of the selector's 3 inputs (`queue_depth`, `client_has_data`,
`reader_fact`) is capable of witnessing whether a wake happened;
`reader_fact` only observes where the reader sits *now*
(`StillBlocked`/`LeftBlocked`/`Terminated`/`NoRow`), which is a different
fact than whether a wake call ever ran. 0 of 3 facts can prove the phrase in
any of the 12 cells, so the check applies uniformly to all 12 rather than
naming the one cell the reproduction happened to hit.

A new test,
`loopback_wake_classification_validator_rejects_the_586_misreport_phrase`
(`tests/loopback_pump_structure.rs:2158`), reproduces the review's exact
mutation byte-for-byte (the same `LeftBlocked`-arm replacement, the same old
string) and asserts the validator now returns `Err`.

### Mutation proof

The review's exact mutation was re-applied to `registry.rs` in this
worktree, and
`bash scripts/run-structure-tests.sh loopback_pump_structure
loopback_wake_classification_reads_reader_state` was re-run:

```
thread 'loopback_wake_classification_reads_reader_state' panicked at
<worktree>/tests/loopback_pump_structure.rs:2115:6:
the loopback wake diagnostic classifies from delivery AND reader state:
"a loopback wake classification claims \"never woken\" -- no fact this
selector reads can prove that (#586)"
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 116 filtered out; finished in 0.01s
```
(The full serial carries the un-elided worktree-absolute path; only that
path prefix is shortened here.)

(`serials/586-pr1/fixpass-v4-mutation-red.txt`.) `registry.rs` was then
reverted with `git checkout --`, and `git diff --quiet` confirmed it
byte-identical to the pre-mutation committed source.

### Structure suites, re-run

| gate | result | evidence |
|---|---|---|
| `bash scripts/run-structure-tests.sh loopback_pump_structure` | 117 passed, 0 failed | `serials/586-pr1/fixpass-loopback-pump-structure-green.txt` (was 116 before this pass; +1 for the new test) |
| `bash scripts/run-structure-tests.sh` (teardown_structure, the default) | 94 passed, 0 failed | `serials/586-pr1/fixpass-teardown-structure-green.txt`, unchanged from the round above |

### Why the strict boot gate was not re-run

This pass's only diff is `tests/loopback_pump_structure.rs` (45 insertions,
0 deletions, commit `981c5855`) -- a host-side structure test that reads
`kernel/src/test_framework/registry.rs` as text via `repo_text()`. It is not
part of the kernel crate and is not compiled into any kernel binary.
`kernel/src/test_framework/registry.rs` itself is byte-identical to the
version the round above already gated at 3/3 strict aarch64 boots and 1/1
x86 gate run (confirmed above: the mutation was reverted and diffed clean).
No kernel or test-framework source moved, so
`bash docker/qemu/run-aarch64-boot-test-strict.sh` is not re-run for this
pass.

### Claim discipline

| invocation | exit code | evidence |
|---|---|---|
| `python3 scripts/claim-lint.py`, after the code commit (981c5855) | 0 | `serials/586-pr1/fixpass-claim-lint-tree-final.txt` |
| `python3 scripts/claim-lint.py --commit-msg .tmp/fixpass-msg1.txt` | 0 | `serials/586-pr1/fixpass-claim-lint-commit-msg1.txt` |
| `python3 scripts/claim-lint.py --files <this file>`, after writing this section | 1 | 3 findings, reworded below |
| `python3 scripts/claim-lint.py --files <this file>`, after rewording | 0 | `serials/586-pr1/fixpass-claim-lint-doc-final.txt` |
| `python3 scripts/claim-lint.py` (whole tree), after this section was added | 0 | `serials/586-pr1/fixpass-claim-lint-tree-after-doc.txt` |
| `python3 scripts/claim-lint.py --commit-msg .tmp/fixpass-msg2.txt` | 0 | `serials/586-pr1/fixpass-claim-lint-commit-msg2.txt` |

Writing the "The repair" and "Not claimed" paragraphs above introduced 3
findings of its own: 2 of 3 were the universal-claim rule (one absolute
quantifier word per paragraph) and 1 of 3 was the unproven-claim rule (a
proof-word with no mutation/count evidence word in the same paragraph). 3 of
3 were reworded -- adding an `N of M` count where the claim was genuinely
countable, or removing the trigger word where quoting a banned substring was
the only source of the flag -- rather than discharged with a
`claim-lint:ok` annotation, matching the code-fix commit's own approach.
The lint criteria were not changed at any point in this pass.

### Not claimed

* **Not claimed: broader coverage than the one phrase this pass bans.**
  This pass bans the one phrase the review's mutation reproduced; a false
  claim worded any other way is not caught by this content check, only by
  the 4 pre-existing shape checks.
* **Not claimed: that the strict boot gate was re-run for this pass.** See
  above -- the changed file is host-side only, and the kernel source it
  reads was diffed byte-identical to the already-gated version.
* **Not claimed: that any finding besides V-4 is addressed here.** This is a
  single-finding fix pass.

### Evidence archive addendum

New files under `serials/586-pr1/` added by this fix pass:

- `serials/586-pr1/fixpass-v4-mutation-red.txt`
- `serials/586-pr1/fixpass-loopback-pump-structure-green.txt`
- `serials/586-pr1/fixpass-teardown-structure-green.txt`
- `serials/586-pr1/fixpass-claim-lint-tree-final.txt`
- `serials/586-pr1/fixpass-claim-lint-commit-msg1.txt`
- `serials/586-pr1/fixpass-claim-lint-doc-final.txt`
- `serials/586-pr1/fixpass-claim-lint-tree-after-doc.txt`
- `serials/586-pr1/fixpass-claim-lint-commit-msg2.txt`

## Second fix pass — review findings closed (2026-09-07)

This fix pass closes nine findings from a second review of this round: V-1
(major), V-2, V-3, V-5, V-6, V-7 (minor), and V-8, V-9, V-13 (nit).

**V-1 (major)** — three sites claimed the boot-24 specimen was "in-repo at
`docs/planning/green-program/network/serials/586-pr1/`": `registry.rs`'s own
comment, `tests/loopback_pump_structure.rs`'s own comment, and the
`a6e40963` commit message. 0 of the 5 gate/boot artifacts committed under
that path is the specimen. Step 0 above already located the
true path, a coordinator scratchpad, so the two source comments now point
readers there instead of restating the false claim (diffs in this commit).
The `a6e40963` commit message is already pushed and is corrected here,
forward, per this repo's own practice for an overstated commit message:
`a6e40963` did not commit the specimen anywhere; it lives only at the
scratchpad path Step 0 names, which is not durable across sessions.

**V-2 (minor)** — Step 0's "The serial is preserved" did not disclose that
the only copy sits at a session-scoped scratchpad path, the exact class of
path #586's own body warns is volatile. Step 0 now says so directly (diff
above).

**V-3 (minor)** — the x86 section's `[TEST:userspace:loopback_recv_wake:PASS]`
and "matches 0 lines of `[TEST:*:FAIL`" read as taken from the preserved
gate log, which in fact carries 0 `[TEST:` lines of any kind (it is the
script's own summary output, not the raw `serial_*.txt` it graded). The
section now cites the two PASS lines the log does carry and attributes the
marker/no-FAIL claims to the gate script's own pass predicate by file:line
(diff above).

**V-5 (minor)** — the not-claimed list did not disclose that 0 of the 12 new
classification strings has been printed by a running kernel this round; 3 of
3 strict boots and the x86 run were green. Added as its own bullet (diff
above).

<!-- claim-lint:ok: the flagged word is inside a quoted phrase this paragraph names as banned by the V-4 ratchet, not an unquantified claim; see #586 -->
**V-6 (minor)** — the R182 paragraph's grep counts read naturally as line
counts and were wrong by 4x under that reading; it also did not name
`tests/loopback_pump_structure.rs`, which by the time of this fix pass also
carries `never woken` (inside the V-4 ratchet's `MISREPORT_PHRASE`, which
forbids the phrase rather than reproducing it as fixture content). The
paragraph now states line counts unambiguously and names that file (diff
above); the load-bearing conclusion -- 0 lines in `tests/fixtures/`,
`docker/qemu/`, or `scripts/` -- is unchanged and was re-verified for this
fix pass.

**V-7 (minor)** — the fenced quotation of `05-service-sequence-gate.log:100`
rendered the em dash as a hyphen and silently dropped the trailing
`[early, 27s, ...]` bracket. Replaced with the byte-exact line (diff above).

**V-8 (nit)** — the header annotation's "12 of 12 file:line citations ...
were re-derived at this branch HEAD" over-stated: 2 citations are scoped to
the parent commit `fe874f15` and 4 cite the Step 0 scratchpad path, which is
not part of the branch. The annotation and the sentence restating it are
both corrected (diff above) to name the 6/2/4 split.

Fixing V-1 and the in-branch half of V-8 required editing
`kernel/src/test_framework/registry.rs` and
`tests/loopback_pump_structure.rs`, which shifted the line numbers after
the edited comment blocks. 6 other in-branch citations in this document
(`registry.rs:2979/:2985/:3024` and
`tests/loopback_pump_structure.rs:2014/:2097/:2157`) were re-derived against
the new line numbers and corrected where they had moved
(`registry.rs:2981/:2987/:3026`,
`tests/loopback_pump_structure.rs:2021/:2120/:2158`); one of those
(`:2014` -> `:2021`) had already gone stale in the first fix pass, which
added lines to the same file without re-deriving this document's citation
into it, and is repaired here rather than carried forward.

**V-9 (nit)** — the `a6e40963` commit message's "0 of 3 arms read
`reader_state` before this change, 12 of 12 after" is not literally true of
the 12 new arms: none names `reader_state` directly, each matches on
`reader_fact`, which is bound two statements earlier from `reader_state`
(`registry.rs:2981` as of this fix pass). The message's substance holds --
0 of 3 old arms read the reader's state in any form, all 12 new arms read
it, via `reader_fact` -- and is restated accurately here, forward, rather
than by rewriting the pushed commit message.

**V-13 (nit)** — the claim-discipline table recorded a tree run
(`clean (5 file(s) checked, changed hunks vs fe874f15cbe4)`) that predates
this fix pass's own changes, and its two `--commit-msg` inputs
(`.tmp/msg1.txt`, `.tmp/msg2.txt`) are gone. A tree run covering this fix
pass's own changed hunks, and a `--commit-msg` run over this fix pass's own
commit message, are recorded in the Claim discipline section below.

### Claim discipline

```
claim-lint: python3 scripts/claim-lint.py                                  -> exit 0
claim-lint: python3 scripts/claim-lint.py --files 586-PR1-2026-09-07.md    -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg fixpass2-msg.txt    -> exit 0
```

The tree run reported `clean (19 file(s) checked, changed hunks vs
fe874f15cbe4)`; the whole-file run over this document alone reported
`clean (1 file(s) checked, whole files)`, so this document is clean in full,
not only in its changed hunks. Two `claim-lint:ok` annotations were added in
this fix pass (Step 0's serial path, and V-6's quoted `never woken`); the
five prior `claim-lint:ok` annotations from the first two commits were
carried unchanged. Evidence:
`serials/586-pr1/fixpass2-claim-lint-tree.txt`,
`serials/586-pr1/fixpass2-claim-lint-doc.txt`,
`serials/586-pr1/fixpass2-claim-lint-commit-msg.txt`.

### Evidence archive addendum, second fix pass

New files under `serials/586-pr1/` added by this fix pass:

- `serials/586-pr1/fixpass2-claim-lint-tree.txt`
- `serials/586-pr1/fixpass2-claim-lint-doc.txt`
- `serials/586-pr1/fixpass2-claim-lint-commit-msg.txt`

## Landing

`git fetch origin && git merge origin/main --no-edit` merged clean (merge
commit `bbbd0a4d`, base was `1a102dd9`); `git diff --name-only
--diff-filter=U` matched 0 files, so 0 conflicts, and 0 of them under
`kernel/`. The merge changed `kernel/src/test_framework/mod.rs`'s line
numbers -- the `mod.rs:66-67` citation above moved to `mod.rs:165-166` and is
corrected in place -- but not `registry.rs`'s or
`tests/loopback_pump_structure.rs`'s citation targets, which re-derived at
the same line numbers as before the merge. The merge added
`docker/qemu/run-blocking-io-oracle-gate.sh` and files under
`docs/planning/green-program/{ipc,tracing}/`; it touched 0 files under
`tests/fixtures/`, `docker/qemu/run-x86-boot-tests.sh`, or
`docker/qemu/run-aarch64-boot-test-strict.sh`, and 0 files this PR's diff
overlaps, so R182 needs no re-record.

`bash scripts/run-structure-tests.sh` (the default, teardown_structure)
reported `test result: ok. 100 passed; 0 failed` at `bbbd0a4d` -- 6 more than
this round's 94, from suites main added since `fe874f15`. One fresh boot,
`bash docker/qemu/run-aarch64-boot-test-strict.sh 1`, reported `PASS: 1/1
boots succeeded`; its own `gate_structure_preflight` ran and passed all
`tests/*_structure.rs` suites first (`GATE_PREFLIGHT:structure_suites=64/64`,
up from 62 at the parent commit -- the `62 of 62` claim-lint annotation
above is corrected to `64 of 64` to match), which re-covers
`loopback_pump_structure` at `bbbd0a4d` without a second dedicated run. The
boot's serial carries `[TEST:network:loopback_recv_wake_when_idle:PASS]`
(`landing/landing-strict-boot1-serial.txt:395`) and matches 0 lines of
`[TEST:.*:FAIL` in the full serial.
Evidence: `serials/586-pr1/landing/landing-strict-boot1-serial.txt`,
`serials/586-pr1/landing/landing-strict-gate.txt`.

```
claim-lint: python3 scripts/claim-lint.py                              -> exit 0
claim-lint: python3 scripts/claim-lint.py --commit-msg landing-msg.txt -> exit 0
```

The tree run reported `clean (23 file(s) checked, changed hunks vs
1a102dd93a13)`. Evidence:
`serials/586-pr1/landing/landing-claim-lint-tree.txt`,
`serials/586-pr1/landing/landing-claim-lint-commit-msg.txt`.
