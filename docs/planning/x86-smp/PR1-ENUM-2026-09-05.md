# x86 SMP PR-1: the CPU count comes from the firmware, not from a constant

Branch `x86/smp-pr1-madt-enum`, based on `origin/main` at `39169922`
("Merge pull request #848 from ryanbreen/sched/562-slice3e-pin-guard").
Tracking issue #814; the reporting defect this addresses is #629. The census
and staged plan this round executes row 1 of is
`docs/planning/x86-smp/X86-SMP-CENSUS-PLAN-2026-09-05.md`, whose section 8
records where PR-1 left that document behind.

---

## 1. What existed before this round

x86 had no processor enumeration. `Scheduler::online_cpu_count()` answered with
the compile-time `MAX_CPUS` (`kernel/src/task/scheduler.rs`), which is 1 on this
arch, so the answer was right at `-smp 1` by construction and would be wrong the
moment a second processor existed. aarch64 has answered the same question from
`smp::cpus_online()` since `25afe0e1`.

The bootloader was already handing the kernel the RSDP —
`bootloader_api::BootInfo::rsdp_addr` — and the kernel was not reading it: a
repo-wide `grep -rn "rsdp" kernel/src` returned one hit, an unrelated field of
the aarch64 Parallels `HardwareConfig`.

## 2. What changed

| file | what |
|---|---|
| `kernel/src/arch_impl/x86_64/acpi.rs` (new, 431 lines) | RSDP validation, RSDT/XSDT walk, MADT (`APIC`) match, per-entry decode of type-0 Local APIC and type-9 Local x2APIC structures. Allocation-free; each walk carries a constant bound as well as the table's own declared length |
| `kernel/src/arch_impl/x86_64/smp.rs` (new, 260 lines) | `cpus_online()`, `cpus_present()`, `madt_cpu_count()`, `madt_enabled_count()`, `madt_x2apic_count()`, `bsp_apic_id()`, `enumerated_cpu_count()`, `apic_id_of()`, `enumeration_source()`, and `init()` — which reads the MADT, publishes it into atomics, and emits the one marker |
| `kernel/src/arch_impl/x86_64/mod.rs` | `pub mod acpi; pub mod smp;` |
| `kernel/src/main.rs` | reads `boot_info.rsdp_addr` and calls `smp::init` after `memory::init` |
| `kernel/src/task/scheduler.rs` | `online_cpu_count()` gains an x86 arm reading `smp::cpus_online()` clamped by `MAX_CPUS`; the aarch64 arm is untouched |
| `docker/qemu/run-x86-smp-enum-gate.sh` (new) | the three-leg gate |
| `tests/x86_smp_enum_structure.rs` (new) | the ratchet |

### Where the reads are safe

`main.rs`'s `BOOTLOADER_CONFIG` sets `mappings.physical_memory =
Some(Mapping::Dynamic)`, and the pinned bootloader maps `[0, max_phys_addr)` at
the reported offset, with `max_phys_addr` raised to at least 4 GiB so that
firmware and MMIO regions below 4 GiB are reachable. `build_master_kernel_pml4`
copies the bootloader's PML4 entries into the table the kernel installs — its
lower-half loop names "physical memory offset" as one of the mappings it exists
to preserve — so the window survives the CR3 switch inside `memory::init`. On
the boots measured here that offset was `0x28000000000`, a lower-half address.

`acpi.rs` relies on one part of that window: physical addresses below 4 GiB. A
table address at or above that ceiling is refused with a named reason rather
than dereferenced, since establishing that a higher address is mapped would
mean reading the memory map, which this module does not do. Reads are one byte
at a time and composed into 32- and 64-bit fields, because ACPI table fields are
not necessarily naturally aligned and a wide unaligned `read_volatile` is
undefined behaviour.

## 3. The marker and what each field means

```
[X86_SMP_ENUM:madt_cpus=N:enabled=M:x2apic=K:bsp_apic_id=B:cpuid_logical=L:present=P:online=1:max_cpus=1:src=madt:reason=none]
```

| field | source | meaning |
|---|---|---|
| `madt_cpus` | MADT type-0 + type-9 entries | what the firmware reports. This is the field that tracks `-smp` |
| `enabled` | the same entries' flags bit 0 | of those, the ones flagged enabled |
| `x2apic` | type-9 entries | of those, the ones that arrived as x2APIC structures. QEMU's `pc` machine emits type-0 at these APIC ids, so this reads 0 on the measured legs |
| `bsp_apic_id` | CPUID leaf 0xB EDX when the leaf is implemented, else leaf 1 EBX[31:24] | the APIC id of the processor emitting the line |
| `cpuid_logical` | CPUID leaf 1 EBX[23:16] | a cross-check, not a count anything derives from — see below |
| `present` | `madt_cpus` clamped into `[1, MAX_CPUS]` | what this kernel's per-CPU state can address. 1, until `MAX_CPUS` moves |
| `online` | `cpus_online()` | processors that have entered the scheduler. 1: no AP is started |
| `max_cpus` | `crate::task::scheduler::MAX_CPUS` | the clamp itself, printed so the two numbers can be read together |
| `src` | `madt` or `cpuid_fallback` | which path answered |
| `reason` | the placeholder `none`, or a refusal token (claim-lint:ok: field quoted from `kernel/src/arch_impl/x86_64/smp.rs`) | why the MADT path did not answer |

`cpuid_logical` earns its place by disagreeing: the `-smp 1` leg measured
`cpuid_logical=0` while the `-smp 2` and `-smp 4` legs measured 2 and 4. QEMU's
`qemu64` model leaves CPUID leaf 1 EBX[23:16] at 0 for a single-vCPU guest, so a
kernel that derived its count from that field alone would read a count of 0
processors on the `-smp 1` profile the existing x86 gates run. The MADT is the source for that
reason, and CPUID is printed beside it.

The nine refusal tokens `reason=` can carry are
`no_rsdp_from_bootloader`, `above_read_ceiling`, `rsdp_signature`,
`rsdp_checksum`, `no_root_table`, `root_table_header`, `no_madt_in_root_table`,
`madt_header` and `madt_entry_length`. None of them was exercised by a boot in
this round: `src=madt:reason=none` on 3 of 3 legs.

## 4. The gate

`docker/qemu/run-x86-smp-enum-gate.sh` boots one boot_tests image per `-smp`
leg (1, 2 and 4 by default) under `-machine pc -accel tcg,thread=multi -cpu
qemu64`, and scores each leg on:

- exactly one `[X86_SMP_ENUM:...]` line in the boot's serial;
- that line matching `madt_cpus=<leg>:enabled=<leg>:...:present=1:online=1:max_cpus=1:src=madt:reason=none` (claim-lint:ok: pattern quoted from `docker/qemu/run-x86-smp-enum-gate.sh`);
- three existing boot_tests pass markers, identical on each leg:
  `[TEST:process:frame_custody_refusal_gate:PASS]`,
  `[TEST:process:x86_retire_cohort:PASS]`, and the test runner's terminal
  verdict line (claim-lint:ok: the 3 marker strings are quoted from
  `docker/qemu/run-x86-boot-tests.sh`, which pins the same 3 in its own poll
  condition).

Three legs rather than one is the anti-vacuity term: a hardcoded `madt_cpus=1`
leaves a `-smp 1` leg green, and only the other two legs redden on it (measured
below, mutation M1).

The gate is unwired. No other gate calls it, it changes no existing gate's QEMU
line, and `tests/green_program_envelope_structure.rs`'s `X86_GATE_SCRIPTS` — the
two-script list its `-smp 1` ratchet loops over — does not include it.

## 5. Evidence

Beast (`breenix-x86` Incus VM), clone `/root/breenix-smp1`, head `e2717b88`,
`BREENIX_GATE_TMP=/root/breenix-smp1-tmp`. Transcripts are under
`docs/planning/x86-smp/serials/pr1/`; per-leg serial files stay in the container
at `/root/breenix-smp1-tmp/breenix_x86_smp_enum_<leg>/`.

### 5.1 The three legs

The three marker lines, quoted from the gate's own transcript, and the verdict
each leg scored:

```
=== leg -smp 1 ===
  leg -smp 1: marker [X86_SMP_ENUM:madt_cpus=1:enabled=1:x2apic=0:bsp_apic_id=0:cpuid_logical=0:present=1:online=1:max_cpus=1:src=madt:reason=none]
  leg -smp 1: PASS
=== leg -smp 2 ===
  leg -smp 2: marker [X86_SMP_ENUM:madt_cpus=2:enabled=2:x2apic=0:bsp_apic_id=0:cpuid_logical=2:present=1:online=1:max_cpus=1:src=madt:reason=none]
  leg -smp 2: PASS
=== leg -smp 4 ===
  leg -smp 4: marker [X86_SMP_ENUM:madt_cpus=4:enabled=4:x2apic=0:bsp_apic_id=0:cpuid_logical=4:present=1:online=1:max_cpus=1:src=madt:reason=none]
  leg -smp 4: PASS
```

Booted image sha256 `0c03a63319b613e3d92863a2847687896e95b28f0b7b457a02edf5c729396e59`
on 3 of 3 legs — one image, three `-smp` values. Transcript:
`serials/pr1/final-gate-3legs.txt`; per-leg excerpts, with the marker in its
boot context and the three pass markers quoted:
`serials/pr1/leg-serial-excerpts.txt`.

Counted in those serials, per leg: 1 `[X86_SMP_ENUM:` line in
`serial_kernel.txt` and 0 in `serial_user.txt`; 1
`[TEST:process:frame_custody_refusal_gate:PASS]` and 1
`[TEST:process:x86_retire_cohort:PASS]` in `serial_user.txt`; 1 terminal
test-runner pass line in `serial_kernel.txt`. The marker lands between the
memory subsystem's own boot output and the `KLAYOUT:` lines, which is where
`kernel_main` calls it — after `memory::init`.

The same three legs were also run once at `35337848` (the head before the
claim-lint rewordings, booted image `537a61c7…`), with the same three marker
lines and the same three PASS verdicts.

### 5.2 What happened to the extra processors

The kernel does not act on them. It sends no INIT/SIPI and has no AP entry
point, so on
the `-smp 2` and `-smp 4` legs the application processors are started by OVMF
during firmware initialisation and parked there; the kernel schedules on the
boot processor alone. The observable consequences on those legs:

- `online=1` in the marker, on 3 of 3 legs;
- the three boot_tests pass markers present on 3 of 3 legs, so the userspace
  test surface behaved as it does at `-smp 1`;
- `bsp_apic_id=0` on 3 of 3 legs — the emitting processor is APIC id 0 on each.

### 5.3 The other gates, at this head

| gate | result |
|---|---|
| `cargo build --release --features testing,external_test_bins --bin qemu-uefi` | exit 0, and `grep -E "^(warning\|error)"` over the build log printed no line |
| `docker/qemu/run-x86-boot-tests.sh 1` | PASS — "x86 frame-custody gate run 1: PASS", exit 0, with "x86 userspace gate: PASS - exited=110 expected>=105 nonzero=0 allowlist=0" (`serials/pr1/x86-boot-tests-x1.txt`) |
| `docker/qemu/run-x86-prod-profile-boot-test.sh` | PASS — "x86 production profile reached steady state with the teardown census at rest", exit 0, and the marker present once in the zero-feature profile's serial: it is not test-only (`serials/pr1/x86-prod-profile.txt`) |
| aarch64 `cargo build … --target aarch64-breenix-kernel.json … --bin kernel-aarch64` | exit 0 |
| `scripts/check-kernel-no-neon.sh` | PASS, 0 FP/SIMD load/store instructions |
| `docker/qemu/run-aarch64-boot-test-strict.sh 1` | PASS 1/1 (`serials/pr1/aarch64-strict-x1.txt`) |
| the 34 `tests/*_structure.rs` suites | 34 of 34 green, 618 test cases |
| `python3 scripts/test_claim_lint.py` | 72 tests, OK, exit 0 |
| `python3 scripts/claim-lint.py` | exit 0 |
| `scripts/check-x86-dispatch-no-alloc.sh` | PASS — "0 allocating call targets in 3 in-scope symbol(s), 19 edge(s) checked", run by the boot-tests gate against the linked kernel |
| `scripts/check-critical-path-violations.sh` | exit 1, with the identical violation set it reports on `origin/main` — see §7 |

### 5.4 aarch64 is untouched, and how that was checked

The two new modules live under `kernel/src/arch_impl/x86_64/`, and
`kernel/src/arch_impl/mod.rs` gates `pub mod x86_64;` behind
`#[cfg(target_arch = "x86_64")]`, so neither file is parsed in an aarch64
build. `kernel/src/main.rs` is the x86 binary. That leaves one shared file,
`kernel/src/task/scheduler.rs`, whose aarch64 arm this PR does not touch.

A byte comparison was attempted and found the aarch64 build is not
reproducible on its own: two builds of identical source differ in 5 bytes,
which are the `BREENIX_BUILD_ID` timestamp `kernel/build.rs` stamps into
`.rodata`. So the comparison was taken per section instead
(`serials/pr1/aarch64-section-comparison.txt`):

- `.text`, `.data` and `.init_array` are byte-identical between the branch and
  its base — including at the branch head, after the rewordings;
- `.rodata` differs in 107 bytes: 5 are that build id, and the rest sit on a
  24-byte stride, which is `sizeof(core::panic::Location)`. The differing field
  is the line number, each one +11 — the number of lines this PR adds above
  those call sites in `scheduler.rs`;
- `.strtab`, which is not loaded, is 365 bytes shorter: 746 of 4058 symbols
  differ in LLVM's `.llvm.<n>` internal-linkage suffix, whose value moves with
  the crate's contents, while each symbol's Rust hash `17h…E` is the same on
  both sides.

The same +11 shift is visible in a second, independent place:
`scripts/check-critical-path-violations.sh` reports the identical set of sites
on both sides, with the `scheduler.rs` line numbers moved from 5746/5882/5886/5890
to 5757/5893/5897/5901.

## 6. Oracle and mutations

### 6.1 Oracle: red on main, green here

The red arm runs `origin/main`'s kernel bytes with this branch's gate script:
the three kernel files this PR edits are checked out from `origin/main` and the
two new modules removed, which leaves `git diff origin/main -- kernel/` empty.
The image it booted is sha256 `de02b2b9…`, against `0c03a633…` for the green
arm.

```
=== leg -smp 2 ===                                     [red arm, main's kernel]
  leg -smp 2: FAIL (expected exactly 1 [X86_SMP_ENUM:] line, found 0)
  leg -smp 2: FAIL (no marker matching madt_cpus=2:enabled=2:...:online=1)
    found:
x86 SMP enumeration gate: FAIL (1 of 1 leg(s) red)     -> exit 1
```

The red is the marker's absence and not a broken boot: the gate's three
pass-marker checks printed no failure on that arm, and the leg's own serial
carries 1 of each of the three, with 0 `[X86_SMP_ENUM:` lines in either serial
file. The green arm is §5.1 above: the same gate, `madt_cpus` 1, 2 and 4 across
the three legs. Transcript: `serials/pr1/oracle-main-smp2.txt`.

### 6.2 Mutations

| # | mutation | leg | command | exit | assertion that fired |
|---|---|---|---|---|---|
| M1 | `census.processor_entries` replaced by the literal `1` in `smp::init` | `-smp 1` and `-smp 2` boots | `docker/qemu/run-x86-smp-enum-gate.sh 1 2` | 1 | leg 1 PASS, leg 2 FAIL: "no marker matching madt_cpus=2:enabled=2:...:online=1", the line found being `madt_cpus=1:enabled=2:…` |
| M2 | the `log::info!` marker emission deleted from `smp.rs` | ratchet | `rustc --test tests/x86_smp_enum_structure.rs && ./x86_smp_enum_structure` | 101 | `the_enumeration_marker_has_exactly_one_emission_site`: "expected exactly one [X86_SMP_ENUM:] emission site under kernel/src, found 0" |
| M3 | the `MAX_MADT_ENTRIES` cap removed from `census_of_madt`'s entry-walk loop | ratchet | same | 101 | `every_walk_in_the_madt_reader_is_bounded_by_a_constant`: "the MADT entry walk must be capped by a constant iteration bound, not only by the table's own declared length: while cursor + 2 <= end {" |

The ratchet legs record their baseline either side: exit 0 unmutated, 101 under
each mutation, exit 0 again after the tree is restored
(`serials/pr1/ratchet-mutations.txt`).

M1 is also the gate's anti-vacuity argument, run rather than asserted: under
that mutation the `-smp 1` leg still passes, and the marker the `-smp 2` leg
prints carries the hardcoded `madt_cpus=1` next to the true `enabled=2` it
still computes. A single-leg gate would have scored the mutated kernel green.

M1's boot-level red and M2's ratchet red are different failures of the same
claim: M1 leaves the marker in place and makes its count a lie, which only a
multi-leg gate catches; M2 removes the marker, which the source ratchet catches
without a boot and which the oracle's red arm also shows at boot level, since a
kernel with no enumeration emits no marker either.

## 7. What is NOT claimed

- **No processor is started.** This PR sends no INIT/SIPI, writes no LAPIC or
  IOAPIC register, and adds no AP trampoline or entry symbol. On `-smp 2` and
  `-smp 4` the extra processors stay parked in firmware.
- **`MAX_CPUS` on x86 is still 1**, and `tests/x86_smp_enum_structure.rs` pins
  that. `cpus_present()` is therefore 1 on each leg regardless of `madt_cpus`,
  and `online_cpu_count()` still answers 1.
- **Placement behaviour is unchanged.** `online_cpu_count()` reads an atomic
  instead of a constant, and the atomic's value is the same 1 the constant was.
- **#629 is not resolved by this PR.** Its "suggested first move" is what
  landed; what stays open under it is that the count is capped by `MAX_CPUS = 1`
  and that the census-widen oracle's x86 arm still emits its
  `uniprocessor_no_dispatching_peer` SKIP, because there is still no peer CPU to
  force-place a thread onto.
- **The MADT reader is not exercised beyond QEMU's `pc` machine.** 3 of 3 legs
  took the `src=madt` path with `reason=none`; 0 of 9 refusal tokens were
  produced by a boot in this round, and the x2APIC (type-9) decode path counted
  0 entries on each leg. Those paths are covered by source shape and by
  construction, not by a boot.
- **The gate is not wired into any other gate**, and this round did not widen
  `X86_GATE_SCRIPTS` or the `-smp 1` ratchet that loops over it.
- **`scripts/check-critical-path-violations.sh` still exits 1**, as it does on
  `origin/main`, on the same set of sites. This round neither introduced nor
  repaired those; the only difference between the two runs is `scheduler.rs`
  line numbers moving by the 11 lines this PR adds.

## 8. Claim discipline

```
claim-lint: scripts/claim-lint.py                                              -> exit 0
claim-lint: scripts/claim-lint.py --files docs/planning/x86-smp/X86-SMP-CENSUS-PLAN-2026-09-05.md -> exit 0
claim-lint: scripts/claim-lint.py --files docs/planning/x86-smp/PR1-ENUM-2026-09-05.md            -> exit 0
claim-lint: scripts/claim-lint.py --commit-msg /tmp/msg1.txt                   -> exit 0
claim-lint: scripts/claim-lint.py --commit-msg /tmp/msg2.txt                   -> exit 0
claim-lint: scripts/claim-lint.py --commit-msg /tmp/msg3.txt                   -> exit 0
claim-lint: scripts/claim-lint.py --commit-msg /tmp/msg4.txt                   -> exit 0
claim-lint: scripts/claim-lint.py --commit-msg /tmp/msg5.txt                   -> exit 0
claim-lint: scripts/claim-lint.py --files /tmp/pr-body.md                      -> exit 0
```

The round opened PR #857.

The first pass over the branch's own new files reported 25 findings, closed by
rewording in `e2717b88`; that commit's message records the counts.
