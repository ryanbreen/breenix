# F20a Decisions

## Decisions

- 2026-04-17: Treat the user's message as the frozen factory prompt because no existing `prompt.md`, `plan.md`, or `decisions.md` bundle was present in or near the repository.
- 2026-04-17: Discarded the first attempted Parallels sweep before counting it because the host CPU sampler selected pre-existing Parallels helper processes ahead of the active `prl_vm_app` VM process and I manually terminated the run. The counted five-run sweep must sample `prl_vm_app` directly to measure the Breenix VM process.
- 2026-04-17: Counted sweep run 1 failed after `/sbin/init` started (`boot_script_completed=0`, `timer_tick_count=11`, host CPU avg `105.2%`). The broad helper routing through `CpuOps::halt_with_interrupts()` was outside the idle-loop scope and is being reverted so blocking syscall waits keep their prior behavior.
- 2026-04-17: The rerun still stalled after `/sbin/init` with the generic syscall wait path restored, so the `idle_loop_arm64()` `dsb sy; wfi` conversion is also being reverted. The next attempt scopes the helper only to the secondary CPU bring-up idle loops in `smp.rs`.
- 2026-04-17: Secondary-only validation booted but host CPU stayed high (`host_cpu_avg=595.0%`, `host_cpu_peak=697.4%`), proving the sustained spin is in the scheduler idle path, not the raw secondary bring-up loop. Reintroduce the Linux barrier in `idle_loop_arm64()` as one inline `daifclr; dsb sy; wfi` sequence to avoid the split-call lost-wake window that stalled init.
- 2026-04-17: `daifclr; dsb sy; wfi` lowered host CPU but still stalled init. Move the barrier before interrupt unmasking (`dsb sy; daifclr; wfi`) so the final enable-and-wait adjacency matches the prior booting sequence while still draining operations before idle.
- 2026-04-17: `dsb sy; daifclr; wfi` also stalled init. Test `daifclr; isb; wfi` as a narrower DAIF synchronization fix before concluding that the wake path needs a deeper change.
- 2026-04-17: `daifclr; isb; wfi` lowered host CPU (`avg=105.9%`, `peak=107.0%`) but still stalled init (`boot_script_completed=0`, `timer_tick_count=13`). Next test keeps the Linux-style `dsb sy; wfi` but uses the kernel's normal IRQ/FIQ unmask (`daifclr #3`) instead of clearing all DAIF bits with `#0xf`.
- 2026-04-17: `daifclr #3; dsb sy; wfi` also stalled init (`boot_script_completed=0`, `timer_tick_count=11`, `avg=105.6%`). The raw secondary `arch_cpu_idle()` path still gets early timer ticks, so the next test removes the older per-iteration scheduler-idle timer rearm from commit `200c72c38` and calls `arch_cpu_idle()` directly after IRQ/FIQ unmask.
- 2026-04-17: Removing the scheduler-idle timer rearm restored the original host CPU pin (`avg=796.8%`, `peak=800.0%`) and still stalled init. Re-arm affects HVF WFI behavior, but removal is not viable. Restore it and test a full Linux-style `dsb sy; wfi; isb` helper with a compiler memory barrier.
- 2026-04-17: `dsb sy; wfi; isb` with a compiler memory barrier still stalled init (`boot_script_completed=0`, `timer_tick_count=11`, `avg=106.0%`). Reuse the older `df282146` insight only in scheduler idle: after idle-loop `rearm_timer()`, force a GICD_CTLR MMIO read VM exit so HVF observes the rearmed vtimer before WFI.
- 2026-04-17: The idle-loop GICD_CTLR VM-exit read restored the CPU pin (`avg=789.1%`) and did not boot. Test one narrower secondary-only scheduler idle fix: keep CPU0's existing booting idle instruction sequence, but use the Linux-style helper for CPUs 1-7.
- 2026-04-17: Required sweep stopped on run 1. The secondary-only scheduler idle candidate was not stable (`boot_script_completed=0`, `timer_tick_count=11`, `host_cpu_avg=795.7%`, `host_cpu_peak=800.0%`). Reverted all kernel changes and marked VERDICT FAIL.
