[package]
name = "libicon"
version = "0.1.0"
edition = "2021"
description = "Breenix animated icon library — physics-based interactive icons"

[lib]
crate-type = ["rlib"]

[dependencies]
libgfx = { path = "../libgfx" }

[workspace]
