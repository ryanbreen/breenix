[package]
name = "breenish-js"
version = "0.1.0"
edition = "2021"
description = "ECMAScript engine for Breenish shell"
license = "MIT"

[workspace]

[lib]
crate-type = ["rlib"]

[features]
default = ["std"]
std = []

[dependencies]
